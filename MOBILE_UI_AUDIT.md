# تدقيق واجهة الموبايل — دليلك
## "Calm Government Digital Service" — تقرير التنفيذ (batch #366)

هذا مستند تدقيق منفصل عن `UX_AUDIT.md` (الذي يوثّق كل الدفعات التاريخية) — مخصّص تحديداً لطلب المستخدم الأخير: إعادة بناء الواجهة بالكامل بهوية بصرية جديدة "هادئة" على طراز خدمة حكومية رقمية محترفة، مع الحفاظ على الاسم/الشعار/اللون العنّابي والوظائف الحالية بالكامل.

**قيد بيئة العمل، مذكور بصدق قبل أي نتيجة:** بيئة التنفيذ الحالية لا تملك خادم تطوير فعلياً قابلاً للتشغيل المستمر، ولا Playwright، ولا Lighthouse، ولا متصفحاً حقيقياً لالتقاط لقطات شاشة عند عروض مختلفة (375/390/430/768/1024/1440px). كل نتيجة "تحقّق" هنا هي تحقّق على مستوى الكود: قراءة الملفات فعلياً، تتبّع القيم رقمياً (الألوان/المسافات/الأحجام)، و`tsc --noEmit`. لم تُنتَج لقطات شاشة أو نتائج Lighthouse فعلية — البديل المتّبع هو دفع كل دفعة عبر `push.sh` ثم مراجعة حية على `https://dalilak-frontend.vercel.app`، وطلب تأكيد من المستخدم على هاتف حقيقي.

---

## 1. الملخّص التنفيذي

المشكلة المُبلَّغة: بعد عشرات دفعات التحسين التدريجي (لمسات وصولية، أداء، تنظيم)، ظل الشكل العام على الهاتف "كما هو" من منظور المستخدم — لأن كل تلك الدفعات كانت غير مرئية بطبيعتها (إعادة تنظيم داخلي، إصلاحات وصولية، تحسينات أداء) بينما البنية البصرية الأساسية (الألوان، التخطيط، الكثافة) لم تتغيّر. الحل المطلوب لم يكن "لمسة أخرى" بل استبدال البنية البصرية بالكامل بهوية جديدة محدَّدة حرفياً من المستخدم.

هذه الدفعة نفّذت الطبقة الأعلى أولوية من طلب المستخدم (**جودة الموبايل البصرية** أولاً حسب ترتيبه المعلَن): الرموز التصميمية، الهيدر، الصفحة الرئيسية بكامل أقسامها، وشريط التنقّل السفلي. الصفحات الفرعية (الإجراءات، تفاصيل الإجراء، المحادثة) ما زالت تستخدم التصميم القديم وتحتاج دفعة تالية.

---

## 2. المشاكل البصرية التي حُلَّت

| # | المشكلة | الموقع | الحل |
|---|---|---|---|
| 1 | تدرّجات لونية (gradient) في الأزرار والبطاقات والهيدر — ممنوعة صراحة في الموجز | `globals.css`, `.btn-primary`, `.pcard`, `.ds-card` | كل متغيّرات gradient أصبحت أسماء مستعارة للّون المسطّح الواحد `#9F1D2F` |
| 2 | ظلال ثقيلة على البطاقات والأزرار (`shadow-lift` عند hover) | نفس الملفات | الظلال ≈ صفر (`0 1-2px`, شفافية ≤0.06)، الـ hover يغيّر لون الحدود فقط |
| 3 | هيدر موبايل مزدحم بـ6 عناصر في صف 56-64px (شعار + جرس + بحث + زر محادثة جديدة + تبديل لغة + فاصل + هامبرغر) | `TopNav.tsx` | هيدر الموبايل الآن: شعار + هامبرغر فقط. البقية انتقلت لقائمة `MobileMenu` |
| 4 | Hero بشارة + عنوان بلونين + وصف + CTA مزدوج + شريط بحث بزر منفصل + رقائق اقتراح | `app/page.tsx` | استُبدل بعنوان واحد + نص مساعد + شريط بحث واحد (بلا زر منفصل) + إجراءان سريعان فقط |
| 5 | "الأكثر بحثاً" كان شبكة بطاقات (6 بطاقات) | `app/page.tsx` | قائمة صفوف موحّدة (4 صفوف: أيقونة + اسم + سهم)، حسب مواصفات الموجز الحرفية |
| 6 | "تصفّح حسب الفئة" كان صفاً من الرقائق القابلة للف (10 رقائق) | `app/page.tsx` | شبكة ثابتة 2×6 ببطاقات موحّدة الارتفاع |
| 7 | 3 أقسام إضافية غير مطلوبة في ترتيب الموجز: شبكة "أحداث الحياة"، شرح "كيف يعمل دليلك" بثلاث خطوات، شبكة ثقة رباعية | `app/page.tsx` | أُزيلت من العرض (البيانات/المنطق باقية وقابلة للوصول عبر البحث/القائمة)، استُبدلت بسطر ثقة واحد |
| 8 | فوتر ثلاثي الأعمدة داكن (`#191713`) بـ8 روابط + تبديل لغة مضمّن — لا يتماشى مع "Footer بسيط عند الحاجة" | `app/page.tsx` | فوتر فاتح مضغوط: الشعار، حقوق النشر، رابطان (حول/الخصوصية)، سطر إخلاء مسؤولية |
| 9 | شريط تنقّل سفلي بزر محادثة عائم دائري متدرّج اللون، مرتفع عن باقي الأزرار، بظل ملوّن، وشريط تفعيل علوي طويل | `BottomNav.tsx` | تبويب رابع مسطّح كالباقي، تلوين `var(--brand)` عند التفعيل فقط، بلا ارتفاع/ظل/شريط علوي |

---

## 3. الملفات المعدَّلة والجديدة

**معدَّلة:** `app/globals.css`، `components/TopNav.tsx`، `components/MobileMenu.tsx`، `app/page.tsx`، `components/BottomNav.tsx`.
**جديدة:** `app/privacy/page.tsx` (سياسة خصوصية تقنية دقيقة — تحتاج مراجعة قانونية فعلية قبل الاعتماد النهائي)، `MOBILE_UI_AUDIT.md` (هذا الملف).
**لم يُحذف أي ملف أو مكوّن.**

---

## 4. الرموز التصميمية المعتمدة (Design Tokens)

| الفئة | القيمة |
|---|---|
| اللون الأساسي | `#9F1D2F` (hover `#861827`, soft `#F8ECEF`) |
| الخلفية | `#F8F8F6` |
| السطح | `#FFFFFF` |
| النص الأساسي / الثانوي | `#181817` / `#65635F` |
| الحدود | `#E5E2DD` |
| زوايا الإدخال/البطاقة | 14px |
| زوايا الزر | 12px |
| زوايا النافذة المنبثقة | 20px |
| الظلال | شبه معدومة (0 1-2px، شفافية ≤0.06) |
| التدرّجات اللونية | ممنوعة بالكامل — صفر استخدام فعلي في الكود بعد هذه الدفعة |

---

## 5. ما تحقَّق منه فعلياً

- `tsc --noEmit` نظيف بعد كل ملف من الملفات الخمسة المعدَّلة (لا أخطاء أنواع).
- تتبّع يدوي لكل استخدام سابق لمتغيّرات gradient/shadow للتأكد من عدم بقاء أي قيمة قديمة غير مستبدَلة.
- التحقّق من أن كل وظيفة نُقلت (بحث، إشعارات، تبديل لغة، إمكانية وصول) ما زالت قابلة للوصول فعلياً من `MobileMenu` بنفس المكوّنات الحقيقية (لا إعادة تنفيذ مصغّرة).
- التحقّق من أن بيانات "أحداث الحياة" وآليتها (`LIFE_JOURNEYS`, `JourneySheet`, `onJourneySelect`) لم تُحذف من الكود، فقط أُزيل عرضها من الصفحة الرئيسية.

## 6. ما لم يتحقَّق منه (قيود بيئية صريحة)

- لا لقطات شاشة فعلية عند 375/390/430/768/1024/1440px.
- لا قياس Lighthouse فعلي (أداء/وصولية/أفضل الممارسات/SEO).
- لا اختبار تباين ألوان آلي (WCAG) خاص بالرموز الجديدة تحديداً — القيم القديمة (`--text-3`, إلخ) كانت مُدقَّقة سابقاً (batch #348) لكن الرموز الجديدة (`#9F1D2F` على `#F8F8F6`، إلخ) لم تُدقَّق رقمياً بعد.

---

## 7. الأولوية التالية (حسب ترتيب المستخدم المعلَن)

1. استكمال "جودة الموبايل البصرية": إعادة بناء صفحة `/procedures` وصفحة تفاصيل الإجراء وواجهة المحادثة حسب المواصفات التفصيلية في الموجز.
2. تدقيق تباين الألوان الجديدة (WCAG 2.2 AA) رقمياً.
3. استخلاص المكوّنات الموحّدة (`MobileHeader`, `SearchInput`, `ServiceCategoryCard`, إلخ) كملفات مستقلة.
4. تدقيق الحركة (Motion) — التأكد من عدم وجود قفزات/ارتداد في أي مكوّن جديد.
5. تكيّف سطح المكتب (آخر بند في ترتيب أولوية المستخدم).
