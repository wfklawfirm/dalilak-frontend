[main 6300dcd] fix: comprehensive link audit + global AR/EN language toggle fix + full content translation pass - fixed 15 dead .gov.lb domains in allServices.ts (interior/mof/isa/moc/moyslebanon/pressorder/refugees/regie/southcouncil/conseilideal/cadastre/mfa) with verified live replacements (moim/finance/insurancecommission/tra/minijes/pressorderlebanon/ministryofdisplaced/regie.com.lb/councilforsouth/statecouncil), fixed 2 malformed concatenated-URL fields, fixed mislabeled water-ministry link; fixed 3 dead domains + 1 dead PDF attachment in curated FORMS_DATA (procedures.ts); added 1 verified missing form link (MBK9-08) to allTransactions.ts; WAYBACK AUDIT COMPLETE (2026-07-17): all 770 unique Wayback form links individually re-verified against Internet Archive (CDX + availability API, fully serial, no rate-limit violations) - 688 confirmed FAKE (~89%), 81 confirmed REAL, 1 non-Wayback dead link found; WAYBACK FIX APPLIED (2026-07-17): replaced all 695 fake/dead pdfUrl entries in allTransactions.ts (_P dict, backup saved as allTransactions.ts.bak_before_wayback_fix_*) with verified-live official ministry forms/e-services pages (27 domains across 35 ministry categories, each individually fetch-verified) rather than deleting the hasForm feature - hasForm flags and _R array left untouched; 0 REAL wayback links touched; LANGUAGE TOGGLE FIX (2026-07-18): root cause was every page keeping its own local useState('ar') with no shared state, so switching to English reset to Arabic on every navigation, plus several pages (authorities/my-files/services) hardcoded isAr={true} with no toggle at all, and the mobile language button was hidden in TopNav with no working replacement on BottomNav-only pages; added lib/LanguageContext.tsx (shared context persisted to localStorage 'dalilak_lang', syncs document.documentElement lang/dir) + components/GlobalLangSwitch.tsx (floating toggle rendered globally in app/layout.tsx, reachable on every single page including login/register/admin); migrated all 18 affected files (app/page.tsx, faq, forms, procedures + [slug] + playbook, services + expat-property, authorities, my-files, drafting-studio, forms/[slug]/FormDetailClient, login, register, forgot-password, reset-password, admin, admin/content, plus TopNav/BottomNav/MobileMenu components) off local/broken lang state onto the shared context; esbuild+UTF8 verified clean on all touched files; total 367 services across 58 categories; CONTENT TRANSLATION GAP CLOSED (2026-07-18): the toggle infra fix alone didn't help pages whose underlying data was Arabic-only, so added full English fields across all remaining large data files and wired every consumer to fall back isAr ? ar : (en || ar) - allServices.ts: added name_en/category_en/authority_en/description_en/fees_en/processing_time_en/required_documents_en/important_notes_en to all 367 entries, wired app/services/page.tsx + app/authorities/page.tsx + fixed a pre-existing bug in components/GuidedFlow.tsx (en field was wrongly assigned the Arabic value); procedures.ts: filled 92 remaining gaps (33 step descriptions, 14 document notes, 1 authority address, 44 fee amounts/notes) via positional structural-key merge to handle duplicate Arabic text, wired app/procedures/[slug]/ProcedureDetailClient.tsx; enrichedProcedures.ts: added title_en/ministry_en/description_en/requiredDocuments_en/fees_en/processingTime_en/steps_en/whereToApply_en to all 71 entries, wired app/procedures/page.tsx + app/procedures/[slug]/playbook/page.tsx; allTransactions.ts: added titleEn to all 2,484 transaction records (4th tuple element in _R array) plus TxItem interface + TX_ALL mapping + filterTxAll/filterTxForms search now also match English title/ministry, wired app/forms/page.tsx (all-transactions view + forms-only view, including Ask Dalilak AI-prompt strings and UI microcopy); all data files verified via esbuild parse + UTF-8 decode + null-byte + per-field grep count checks before and after merge; LOGO TRANSPARENCY FIX (2026-07-18): logo.PNG had a baked-in near-white square background, so every small nav/header usage showed a visible white box clashing with colored headers - login/register/forgot/reset-password even used a mixBlendMode:multiply hack to fake transparency, which only worked on flat colors and darkened the artwork; cropped the icon glyph out of the source logo, flood-filled the true outer background to alpha-transparent (kept the intentional white brain-lobe fill, that is part of the design), saved as public/logo-icon.png, and swapped all 21 in-app usages (TopNav x2, MobileMenu, ChatMessage, services/authorities/admin/drafting-studio/forms/my-files/faq/procedures/FormDetailClient page headers, login/register/forgot-password/reset-password auth screens) from logo.PNG to logo-icon.png, removing the now-unneeded mixBlendMode hack on the 4 auth pages; apple-touch-icon and social-preview (OG/twitter) images in layout.tsx intentionally left on the full wordmark logo; DATA-LOSS INCIDENT CAUGHT AND FIXED (2026-07-18): a bulk sed sweep across 18 files hit an OneDrive-sync write race that silently truncated 11 of them (services/admin/authorities/procedures/forms/login/register/forgot-password/reset-password pages + TopNav/MobileMenu components, missing 10-36 lines each); caught via esbuild parse failures, diagnosed with git-HEAD line-by-line diffing to confirm every file matched HEAD up to the truncation point, and losslessly restored each file's tail (spliced from git HEAD, or from exact content already captured earlier in-session for 3 files) with zero net content loss; final repo-wide null-byte scan and 18-file esbuild sweep both clean; UX POLISH PASS (2026-07-18): app/my-files/page.tsx - removed blanket .step-btn:hover CSS rule that overrode the done/not-done color states, replaced with onMouseEnter/onMouseLeave handlers mirroring the existing touch handlers; app/admin/content/page.tsx - added missing logo badge to header (was the one admin page without it) + added .pipeline-tile:hover rule (className was already applied but had no matching CSS); app/admin/page.tsx - added .adm-tab:hover and .adm-filter-chip:hover for consistent hover feedback matching .adm-tr/.adm-btn/.adm-gap-row already in the file; components/AgentResponseRenderer.tsx - added onMouseEnter/onMouseLeave to the ResponseActions button() helper (copy/copy-docs/download buttons), matching hover parity already present in DocumentAnalysisPanel.tsx; AI FLOWCHART GENERATION (2026-07-18): previously only 6 procedures (of 367 services + 92 procedures + 2,484 transactions) had a flowchart, hand-authored in lib/flowchartData.ts and only reachable from the procedures playbook page - added lib/useFlowchart.ts hook that checks curated FLOWCHARTS first (highest quality), then localStorage cache (dalilak_fc_<slug>), then calls the new backend POST /flowchart/generate endpoint to produce a GPT-4o-generated flowchart on demand, caching the result client-side so it is generated once per slug; added generateFlowchart() to lib/auth.ts following the existing authHeaders()-based fetch pattern; wired the hook into three places so a flowchart is now reachable from every part of the app: (1) app/procedures/[slug]/playbook/page.tsx - replaced the old static 5-node generic placeholder fallback with a real AI-generated map (auto-generates on page load when no curated flowchart exists), added loading spinner + error/retry states + an 'AI-generated, review before relying on it' badge, guarded all activeFlowchart.nodes accesses for the now-possibly-null value; (2) app/services/page.tsx ServiceSheet - added a 'Generate AI procedure map' button + inline compact flowchart render to the service detail sheet, covering all 367 services for the first time; (3) app/forms/[slug]/FormDetailClient.tsx - same button+render pattern added for all 2,484 transaction/form records; all three reuse the existing components/ProcedureFlowchart.tsx renderer for full visual consistency with the 6 curated flowcharts; esbuild-verified clean on all 8 touched/new frontend files (lib/useFlowchart.ts, lib/auth.ts, playbook page, services page, FormDetailClient, ProcedureFlowchart.tsx, flowchartData.ts, knowledgeGraph.ts); FLOWCHART EVIDENCE + PROGRESS (2026-07-18, same session): backend now grounds every node in real Qdrant sources with a confidence score (see backend commit) - extended lib/knowledgeGraph.ts ProcedureFlowNode with sourceRefs/confidence and ProcedureFlowchart with groundedInSources/generatedBy; components/ProcedureFlowchart.tsx now shows a confidence badge per node (source-verified/inferred/unverified, AR+EN), an expandable source list with links when sourceRefs present, and a top 'no verified sources found, confirm manually' banner when groundedInSources is false; added lib/useFlowchartProgress.ts (localStorage-persisted per-slug save/resume: which nodes are marked done, which required documents are checked off); ProcedureFlowchart.tsx accepts optional completedNodeIds+onToggleNode props - when passed, the node's icon circle becomes a real toggle button (checkmark when done, aria-pressed, keyboard accessible), the progress bar switches to counting real completions instead of static node.status, and the next unfinished node is auto-highlighted as 'current'; wired into app/procedures/[slug]/playbook/page.tsx (the primary procedure-completion page) with a 'reset progress' link, and the required-documents checklist there is now interactive checkboxes tied to the same per-slug progress record; esbuild-verified clean on all 9 touched files; PROGRESS EVERYWHERE (2026-07-18, same session): extended the same save/resume interactivity from the playbook page to the other two flowchart entry points - app/services/page.tsx ServiceSheet and app/forms/[slug]/FormDetailClient.tsx now each call useFlowchartProgress(slug) and pass completedNodeIds/onToggleNode into their compact ProcedureFlowchartComponent, so marking a step done (and the confidence badges) is consistent across all three places a user can view an AI-generated flowchart, not just the dedicated playbook page; esbuild-verified clean; CASE WORKSPACE (2026-07-18, same session): found that app/my-files/page.tsx and a full backend My Files API (GET/POST/PUT/DELETE /my-procedures, checklist + documents + completion_pct + next_step) already existed and were fully built, but nothing in the app ever called POST /my-procedures - so 'My Files' was permanently empty for every real user despite the feature being complete; added lib/auth.ts startTrackingProcedure() and a new components/SaveToMyFilesButton.tsx that builds a checklist directly from the current flowchart's nodes (works for curated or AI-generated flowcharts alike, so it covers any of the 367 services + 92 procedures + 2,484 transactions) and calls POST /my-procedures - shows a login prompt for guests (My Files requires an account) and a 'View My Files' link once saved; wired into all three flowchart entry points: app/procedures/[slug]/playbook/page.tsx, app/services/page.tsx ServiceSheet, app/forms/[slug]/FormDetailClient.tsx; backend extended in the same session to accept a client-supplied checklist/documents payload instead of requiring a match in the small hardcoded procedure catalog (see backend commit); esbuild-verified clean on all 5 touched/new files (lib/auth.ts, components/SaveToMyFilesButton.tsx, playbook page, services page, FormDetailClient); CASE WORKSPACE POLISH (2026-07-18, same session): app/my-files/page.tsx had two half-built pieces - the documents list only displayed doc.uploaded with no way to toggle it, and notes were shown read-only with no editor, even though the backend already supported both (notes via existing PUT, documents via new uploaded_documents field added in this session's backend commit); converted the document rows into real toggle buttons (aria-pressed, same visual style, calls toggleDoc which mirrors the existing toggleStep pattern) and replaced the read-only notes paragraph with an always-visible textarea that autosaves on blur when changed (saveNotes); esbuild-verified clean; CASE WORKSPACE STATUS CONTROLS (2026-07-18, same session): added updateStatus() helper and a cancel/reactivate button next to delete in the case detail panel header - shows '╪Ñ┘ä╪║╪º╪í' for active/in-progress cases (PUTs status=cancelled) and '╪º╪│╪¬╪ª┘å╪º┘ü' for cancelled ones (PUTs status=active), hidden once a case is already 'completed' since that's now set automatically by the backend when all checklist steps are done (see backend commit); esbuild-verified clean; CASE WORKSPACE ERROR HANDLING (2026-07-18, same session): none of app/my-files/page.tsx's mutation calls (toggle step, toggle document, save notes, change status, delete) checked res.ok before trusting the response - a failed request (expired session -, network blip, etc.) would silently feed an error body like {detail:...} into state as if it were a real case object, corrupting the UI; refactored all PUT calls through one shared updateProc() helper that checks res.ok, only updates state on success, and surfaces a dismissible red banner (actionError) on failure instead; delete now also checks res.ok before removing the item from the list; added a distinct loadError state for the initial GET with its own retry button (extracted the fetch into loadProcs() so Retry re-runs the exact same load, not a full page reload); esbuild-verified clean
 2 files changed, 94 insertions(+), 72 deletions(-)
