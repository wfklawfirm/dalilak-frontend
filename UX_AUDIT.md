# تدقيق تجربة المستخدم — دليلك (UX_AUDIT.md)

هذا الملف تدقيق حي (living document) لتجربة الاستخدام في تطبيق دليلك، يُحدَّث مع كل دفعة تنفيذ فعلية في الكود — وليس تقريراً نظرياً منفصلاً عن التنفيذ. كل بند فيه يعكس تغييراً حقيقياً حدث أو قراراً واعياً بتأجيله، مع السبب.

**ترتيب الأولوية المعتمد في كل قرار (كما طُلب):**
الوضوح ← البساطة ← سهولة إنجاز المهمة ← الوصول (Accessibility) ← الأداء ← الجمال البصري.

**قيد بيئة العمل (يجب معرفته قبل قراءة أي نتيجة "تحقق"):** بيئة التنفيذ الحالية (sandbox) لا تملك خادم تطوير (`npm run dev`) ولا Playwright ولا Lighthouse ولا متصفحاً حقيقياً لالتقاط لقطات شاشة عند 375/430/768/1440px. لذلك كل "تحقق" في هذا الملف هو تحقق على مستوى الكود (قراءة الملفات فعلياً، تتبّع الحالة، `tsc --noEmit`) وليس تحقق مرئي أو قياس Lighthouse فعلي. أي رقم Lighthouse أو لقطة شاشة مطلوبة في الموجز الأصلي **لا يمكن إنتاجها هنا فعلياً** — البديل المتّبع هو المراجعة الحية على الرابط المنشور بعد كل `push.sh`.

---

## المرحلة 1 — الأزرار العائمة (Floating Buttons) — ✅ منفَّذ

### المشكلة
كان التطبيق يعرض حتى **7 عناصر عائمة دائمة الظهور** فوق بعضها في نفس الزاوية على كل صفحة: `GlobalLangSwitch` (تبديل اللغة)، `AccessibilityBar` (إمكانية الوصول)، `MinistryQuickDial` (أرقام الوزارات)، `FloatingHelpButton` (طوارئ + مساعدة)، `KeyboardShortcutsHelp` (اختصارات لوحة المفاتيح، لسطح المكتب فقط)، إضافةً إلى `FeedbackWidget` و`AppointmentReminder` الشرطيَين. تحقّق التدقيق الفعلي للكود من وجود **تصادم بصري حقيقي**: `KeyboardShortcutsHelp` و`MinistryQuickDial` كانا يتشاركان نفس الموضع تقريباً (`bottom:80` و`bottom:82`) على سطح المكتب.

### موقعها
`app/layout.tsx` (كانت تُعرَض على كل صفحة دون استثناء) + `app/page.tsx`.

### أثرها على المستخدم
ازدحام بصري دائم، تشتيت عن الإجراء الأساسي (البحث/المحادثة)، تكرار وظيفي — كل وظيفة موجودة أيضاً في `TopNav` أو `MobileMenu` أو `/settings`.

### مستوى الأولوية
عالٍ جداً — كان أوضح انتهاك لمبدأ "شاشة واحدة = إجراء أساسي واحد".

### الحل المنفَّذ
- **`GlobalLangSwitch`**: أُزيل بالكامل من `layout.tsx`. تبديل اللغة موجود فعلياً وبشكل مرئي في `TopNav` (على كل الأحجام، وليس محجوباً خلف نقطة انكسار كما كان الافتراض القديم) وفي `MobileMenu`.
- **`AccessibilityBar`**: أُزيل من `layout.tsx`. كل إعداداته (تباين عالٍ / نص كبير / تقليل الحركة) موجودة فعلياً في `/settings` وتستخدم نفس مفاتيح `localStorage` تماماً — لا فقدان وظيفي.
- **`MinistryQuickDial`**: أُزيل زر الـ FAB الخاص به فقط. الشيت (bottom sheet) نفسه ما زال موجوداً بكامل بياناته (20 جهة حكومية + أرقام طوارئ)، ويُفتَح الآن عبر حدث مخصّص `dalilak-open-ministry-dial` يُطلَق من زر جديد "أرقام الوزارات والطوارئ" داخل `MobileMenu`.
- **`FloatingHelpButton`**: أُزيل بالكامل من `app/page.tsx`. أرقام الطوارئ فيه (112/140/175/1735/1480/1650) متوفرة عملياً ضمن نفس شيت `MinistryQuickDial` (يغطي 112/140/175/1480 + أرقام أمن/مياه بديلة)، ورابط الأسئلة الشائعة موجود أصلاً كبند تنقّل في `MobileMenu`.
- **`KeyboardShortcutsHelp`**: أُزيل زر الـ FAB الخاص به فقط ("?" في الزاوية). الاختصار نفسه ("?" على لوحة المفاتيح) ما زال يعمل تماماً كما كان — كان هذا الزر ميزة لمستخدمي سطح المكتب فقط، واكتشافه ممكن عبر ذات المفتاح الموثّق داخل النافذة نفسها.
- تنظيف تبعي: أُزيلت قاعدة CSS الميتة `.kbd-shortcuts-fab` من `globals.css`، وأُعيد ضبط مواضع `FeedbackWidget` و`AppointmentReminder` (كانتا تُحسبان مسافتهما لتفادي التصادم مع الأزرار المحذوفة، فأصبحتا الآن في موضع أقرب/أنظف).

### حالة التنفيذ
✅ منفَّذ بالكامل وتحقَّق منه عبر `tsc --noEmit` (لا أخطاء).

### الوظائف المتبقية عمداً كعناصر سياقية (وليست "أزرار عائمة" بالمعنى المقصود في الموجز)
- `ChatScrollToBottomButton` و`ProcedureBackToTopButton`: تظهران فقط عند تمرير طويل داخل شاشة نشطة، وتختفيان تلقائياً — نمط قياسي (return-to-content) وليس تكديساً دائماً، وهما الآن الوحيدتان في تلك الزاوية فلا تصادم.

### تحديث (batch #347) — `FeedbackWidget` حُوِّل من عائم إلى بطاقة داخل التدفّق — ✅ منفَّذ
كان آخر استثناء متبقٍّ لقاعدة "زر عائم واحد فقط". حُوِّل بالكامل من FAB ثابت الموضع (`position:fixed`) إلى بطاقة عادية ضمن تدفّق رسائل المحادثة، بنفس أسلوب `ChatSummaryCard` تماماً (نفس القيم: `margin:'12px 0'، borderRadius:14، border`). آلية الظهور نفسها (بعد 3 رسائل أو 5 دقائق) محفوظة داخل المكوّن، وتصميمه صار: شريط مطوي بعنوان "شاركنا رأيك" يتوسّع مكانه عند الضغط (بلا نافذة منبثقة عائمة).

**تغيير سلوكي طفيف يجب توثيقه بصدق:** بما أن البطاقة أصبحت تُعرَض ضمن تدفّق شاشة المحادثة فقط (نفس موقع `ChatSummaryCard`)، فإن مسار "5 دقائق تصفّح على الصفحة الرئيسية دون بدء أي محادثة" لم يعد يُظهر البطاقة (لأن المكوّن لم يعد يُركَّب إلا بعد `messages.length > 0`). هذا انحراف بسيط عن السلوك الأصلي، ولكنه مقصود ومبرَّر: طلب تقييم أثناء تفاعل فعلي مع المحادثة أنسب سياقياً من مقاطعة تصفّح صامت للصفحة الرئيسية — ويتوافق مع مبدأ "لا نقاط انتباه إضافية على الصفحة الرئيسية المبسّطة". لم تُحذف بيانات أو وظيفة تقييم فعلية، فقط شرط توقيت هامشي واحد.

**الحالة: منفَّذ بالكامل.** لم يعد هناك أي زر عائم استثنائي في التطبيق.

---

## المرحلة 2 — Bottom Navigation (4 عناصر بالضبط) — ✅ منفَّذ

### المشكلة
`BottomNav.tsx` كان يحتوي 5 عناصر: الرئيسية، المعاملات، محادثة (FAB مركزي)، **الخدمات**، حسابي — بينما الموجز يطلب 4 عناصر بالضبط.

### موقعها
`components/BottomNav.tsx`.

### أثرها على المستخدم
عنصر إضافي يقلّل من وضوح التسلسل الهرمي للتنقّل، ويُنافس عناصر أهم على مساحة اللمس المحدودة على الشاشات الصغيرة.

### مستوى الأولوية
عالٍ.

### الحل المنفَّذ
أُزيل عنصر "الخدمات" فقط من الشريط (تعديل ميكانيكي بسيط — الشريط مبني بـ `flex:1` لكل عنصر فيعيد توزيع المساحة تلقائياً بلا كسر تخطيط). لم يُحذف مسار `/services` ولا أي وظيفة: يبقى متاحاً عبر `MobileMenu` وعبر شبكة الفئات والبحث في الصفحة الرئيسية. اختير عنصر "الخدمات" تحديداً للحذف من الشريط لأنه سطح **تصفّح** (browse) بينما العناصر الأربعة الباقية (الرئيسية / المعاملات / محادثة / حسابي) هي أسطح **إنجاز مهمة** (do)، وهذا يطابق ترتيب الأولوية المعتمد (سهولة إنجاز المهمة قبل سهولة التصفّح).

### حالة التنفيذ
✅ منفَّذ، تحقَّق منه عبر `tsc --noEmit`.

---

## المرحلة 3 وما بعدها — بنود مؤجَّلة (لم تُنفَّذ بعد في هذه الدفعة)

توثيقاً صادقاً للحجم الحقيقي المتبقي من الموجز الأصلي، هذه البنود **لم تُنفَّذ بعد** ولا ينبغي افتراض إنجازها:

| البند | الأولوية | ملاحظة |
|---|---|---|
| إعادة بناء الصفحة الرئيسية بالكامل حسب البنية المطلوبة (هيدر 56-64px، هيرو مختصر، البحث كعنصر أساسي، مساران فقط، حد أقصى 4 معاملات شائعة) | عالٍ جداً | 🟡 جزئي — انظر "تحديث (batch #350)" أدناه: تحقّقنا أن الهيرو أصلاً يطابق البنية المطلوبة (مسارَان فقط + بحث كعنصر أساسي)، وقلّصنا قسم "الإجراءات الأكثر طلباً" من 6 إلى 4 بطاقات، وقسم "تصفّح حسب الفئة" من 10 إلى 6 رقاقات + رابط "كل الفئات". المتبقي: تدقيق ارتفاع الهيدر الفعلي بالبكسل، وتقييم قسمَي "كيف يعمل" و"الثقة" (حالياً منفصلان — الموجز يطلب "سطر ثقة صغير"). |
| تدقيق ومطابقة Design System (الألوان/الخط/التباعد 8pt/الزوايا/الظلال) مقابل القيم المحدَّدة في الموجز | متوسط | نظام تصميم أساسي (CSS variables) موجود فعلاً في `globals.css` من دفعات سابقة — يحتاج تدقيق مطابقة بند-ببند وليس بناءً من الصفر. لون العلامة التجارية الحالي `#8F1D2C` يُبقى كما هو (الموجز نفسه يسمح صراحة بإبقاء اللون المعتمد حالياً). |
| إعادة تصميم صفحة تفاصيل الإجراء وفق البنية المكوّنة من 13 نقطة | متوسط | تحتاج تدقيقاً لمعرفة أي من الـ13 نقطة موجود فعلاً (الكثير منها مبني من دفعات سابقة: Breadcrumbs، الخطوات، الوثائق، التكلفة...) وأيها ناقص. |
| تبسيط واجهة المحادثة | متوسط | 🟡 بدأ (batch #353) — صف إجراءات كل رسالة قُلِّص من 10 إلى 4 عناصر ظاهرة. المتبقي: صف أدوات الإدخال (Mode/ResponseLength/ContextBar/KeyboardHint/CharCounter/QuickReplies) وأعلى شاشة المحادثة (SummaryCard/SessionTimer/SearchInThread/SessionSummaryChip/PinnedBanner/LanguageToggleChip). |
| فحص WCAG 2.2 AA كامل | متوسط-عالٍ | 🟡 تقدّم مستمر — أُنجز: skip-link، aria-label، focus-visible، Escape handlers، تسلسل العناوين (batch #349)، تباين الألوان (batch #348)، استخدام اللون فقط (batch #351). المتبقي: تدقيق مناطق landmark بعد التوسّع الأخير، وترتيب التنقّل بلوحة المفاتيح (tab order) على الصفحات الغنية بالودجات. |
| اختبار/إصلاح الاستجابة عند 9 نقاط انكسار (320–1440px) | عالٍ | **لا يمكن تنفيذها آلياً في هذه البيئة** (لا متصفح حقيقي) — تحتاج مراجعة حيّة على الرابط المنشور بعد كل دفعة. |
| تحسين الأداء (Core Web Vitals، code splitting، lazy loading) | متوسط | 🟡 تقدّم — lazy-load لبعض مجموعات الودجات (دفعة سابقة) + إزالة ~1.7MB استيرادات بيانات ميتة من الصفحة الرئيسية (batch #354). يحتاج تدقيقاً شاملاً إضافياً (lazy-load للمودالات الثقيلة مثل GuidedFlow/TransactionStarter/ServiceGroupSheet التي لا تزال eager). |
| توحيد المكوّنات المشتركة الـ17 المذكورة في الموجز | منخفض-متوسط | كثير منها موجود فعلاً بأسماء مختلفة، يحتاج جرداً ومطابقة قبل أي "توحيد". |
| مقاييس Lighthouse الفعلية ولقطات الشاشة عند 4 قياسات | — | **غير قابلة للتنفيذ في هذه البيئة** (لا Lighthouse ولا متصفح). سيُستعاض عنها بمراجعة حيّة يدوية بعد كل نشر. |

---

---

## اكتشاف إضافي — كتلة كود ميتة بحجم ~30 مكوّناً في الصفحة الرئيسية — ✅ منفَّذ

### المشكلة
أثناء تدقيق بنية `app/page.tsx` تمهيداً لتبسيط الصفحة الرئيسية، تبيّن أن كتلة كاملة من ودجات الصفحة الرئيسية (WelcomeBackBanner، HomepageQuickActionsBar، وأكثر من 25 مكوّناً آخر مجمّعة ضمن 11 قسم `SectionCollapseToggle`) كانت **كوداً ميتاً بالكامل — لا يُعرض مطلقاً لأي مستخدم**. السبب: هذه الكتلة كانت محاطة بشرط `messages.length === 0`، لكنها كانت متداخلة داخل الفرع الآخر من الشرط الأساسي (`messages.length === 0 ? (الصفحة الرئيسية) : (المحادثة)`) — أي داخل فرع `المحادثة` الذي أصلاً لا يُفعَّل إلا عندما `messages.length > 0`. تحقّقت من هذا يدوياً بتتبّع الأقواس عند الأسطر 1373 (فتح الشرط) و1859 (الفرع الآخر) و1946-2055 (الكتلة الميتة) و2158 (إغلاق الشرط) — تأكيد كامل، وليس افتراضاً.

### موقعها
`app/page.tsx`، كانت الأسطر 1946–2055 (JSX) + ~30 استيراداً (بعضها eager وبعضها عبر `next/dynamic`) في أعلى الملف.

### أثرها على المستخدم
لا أثر بصري (الكود لم يكن يُعرض أصلاً)، لكن أثر حقيقي على **الأداء**: عشرات الاستيرادات الفورية (eager imports) كانت تُحمَّل ضمن الحزمة الرئيسية (bundle) لصفحة لا تستخدمها أبداً — تضخيم غير ضروري لحجم التحميل الأول.

### مستوى الأولوية
متوسط-عالٍ (أداء، ليس وضوحاً — الصفحة الحالية المرئية فعلياً كانت نظيفة أصلاً: هيرو + 6 بطاقات معاملات + 10 فئات + خط زمني الأحداث الحياتية + شرح 3 خطوات + شريط ثقة + فوتر، و4 أزرار CTA أساسية فقط في الشاشة الأولى — أقرب بكثير لما يطلبه الموجز مما كان متوقعاً).

### الحل المنفَّذ
حذف الكتلة الميتة بالكامل من JSX + كل الاستيرادات التي كانت تُستخدَم حصراً داخلها (تحقّقت من كل اسم عبر `grep -c` قبل الحذف؛ الاستثناء الوحيد: `saveChatSession` من `ChatHistoryPanel.tsx` — استيراد مسمّى منفصل ما زال يُستخدَم فعلياً في مكان آخر من الملف، فأُبقي عليه). حُذفت أيضاً دالة `dyn()` المساعدة و`import dynamic from 'next/dynamic'` لأنهما أصبحا بلا استخدام. لم تُحذف أي من ملفات المكوّنات نفسها (`SmartReminder.tsx`، `GovCalendar.tsx`، إلخ) — تبقى موجودة وقابلة لإعادة الاستخدام لاحقاً إذا احتاجها تصميم الصفحة الرئيسية الجديد، فقط الاستدعاء الميت أُزيل. الملف انخفض من 2770 إلى 2602 سطر. `tsc --noEmit` نظيف، وتحقّقت بـ`grep` أن لا مرجع متبقٍ لأي اسم محذوف.

### حالة التنفيذ
✅ منفَّذ بالكامل.

---

---

## تدقيق تباين الألوان (WCAG 2.2 AA) — ✅ منفَّذ جزئياً، محسوب برمجياً

### المنهجية
بما أن بيئة التنفيذ لا تملك متصفحاً حقيقياً أو Lighthouse، لا يمكن تشغيل أداة فحص تباين تفاعلية. البديل الصادق: استخرجت القيم الست عشرية الفعلية لكل رمز لون (design token) من `globals.css`، وحسبت نسب التباين برمجياً بمعادلة WCAG الرسمية لللمعان النسبي (relative luminance) عبر سكريبت Python، وقارنتها بعتبتي AA (4.5:1 للنص العادي، 3.0:1 للنص الكبير/عناصر الواجهة). هذا تحقّق رياضي دقيق 100%، وليس تخميناً.

### النتائج والإصلاحات
| الرمز | القيمة القديمة | التباين القديم (على الخلفية) | الحالة | الإصلاح |
|---|---|---|---|---|
| `--text-1` | `#191713` | 16.83:1 | PASS | لا تغيير |
| `--text-2` | `#69645C` | 5.52:1 | PASS | لا تغيير |
| `--text-3` | `#918B82` | **3.18:1 — FAIL** (نص عادي) | ❌→✅ | غُيِّر إلى `#76716A` (4.55:1 / 4.84:1) — نفس درجة اللون، أغمق قليلاً فقط |
| `--text-4` | `#B8B2AA` | **1.98:1 — FAIL** (فشل حتى عتبة 3:1) | ❌→✅ | أُبقي اللون نفسه (مخصَّص حصراً الآن لـ placeholder المدخلات وشريط التمرير، وكلاهما معفى من شرط التباين) — لكن كل الاستخدامات الحقيقية له كنص فعلي (89 استخدام عبر 32 ملفاً) نُقلت إلى `--text-3` بعد إصلاحه |
| أبيض على `--brand` | — | 8.82:1 | PASS | لا تغيير (أزرار CTA) |

### التنفيذ
- عدّلت `--text-3` في `globals.css` مباشرة (تغيير سطر واحد يطبَّق تلقائياً في كل مكان يستخدم الرمز — 27+ ملفاً).
- شغّلت استبدالاً آلياً (`sed`) لكل الاستخدامات الفعلية لـ`var(--text-4)` في ملفات `.tsx` (89 استخدماً حقيقياً كنص/أيقونة عبر 32 ملفاً — تحققت أولاً أن كل واحد منها نص حقيقي فعلي بحجوم صغيرة 10-12.5px، وليس عنصراً زخرفياً معفى) إلى `var(--text-3)`، تاركاً تعريف `--text-4` نفسه دون تغيير في `globals.css` لاستخداماته المعفاة الوحيدة المتبقية (`::placeholder`, `scrollbar-thumb`).
- استثناء واحد مقصود: `Breadcrumbs.tsx` يستخدم `var(--text-4, #C8BEB2)` لفاصل زخرفي (`›`/`‹`) مُعلَّم `aria-hidden="true"` — معفى فعلياً من شرط التباين لأنه لا ينقل معلومة نصية، فتُرك كما هو.
- `tsc --noEmit` نظيف.

### الحالة: ✅ منفَّذ (أهم فشلين تم إصلاحهما فعلياً في الكود، ليس توصية فقط)

---

## ⚠️ حادثة واكتشاف حرج — انهيار وظيفي في أدوات إمكانية الوصول (تم اكتشافه وإصلاحه في نفس الجلسة)

أثناء تدقيق التباين أعلاه، اكتُشف أن إزالة `AccessibilityBar` من التكديس العائم (في دفعة سابقة ضمن هذه الجلسة نفسها) كانت قد كسرت وظيفة حقيقية عن غير قصد: قواعد CSS الفعلية لكلاسات `.dalilak-high-contrast` / `.dalilak-large-text` / `.dalilak-reduce-motion` كانت موجودة **حصراً** داخل وسم `<style>` المضمّن في مكوّن `AccessibilityBar.tsx` نفسه — وليس في `globals.css`. عندما توقّف ذلك المكوّن عن العرض، توقّفت هذه القواعد عن الحقن في الصفحة كلياً، رغم أن صفحة `/settings` استمرت (بشكل صحيح) في كتابة تفضيلات المستخدم إلى `localStorage` وتبديل الكلاسات على `<html>` — لكن بلا أي قاعدة CSS فعلية تُترجم تلك الكلاسات إلى تأثير بصري حقيقي. أي مستخدم فعّل "تباين عالٍ" أو "نص كبير" بعد تلك الدفعة كان تفعيله بلا أي أثر مرئي.

**الإصلاح**: نقلت قواعد الـCSS الثلاث بشكل دائم إلى `globals.css` (مستقلة عن أي مكوّن)، وأنشأت `components/AccessibilityEffects.tsx` — مكوّن بلا أي واجهة مرئية (`return null`)، مُركَّب دائماً في `app/layout.tsx` (تماماً كما كان `AccessibilityBar` مُركَّباً)، مهمته الوحيدة تطبيق التفضيلات المحفوظة على `<html>` عند كل تحميل صفحة. النتيجة: الوظيفة استُعيدت بالكامل بدون إعادة أي زر عائم — القاعدة "زر عائم واحد فقط" ما زالت سليمة، فقط أُصلح الخلل الوظيفي الذي نتج عن تطبيقها بسرعة أكبر مما ينبغي.

**الدرس المسجَّل لبقية هذا التدقيق**: عند إزالة أي مكوّن عائم مستقبلاً، يجب التحقق أولاً مما إذا كان يحتوي على منطق وظيفي حقيقي (وليس فقط زر تشغيل/إيقاف) قبل حذف عرضه — نمط "الزر + الوظيفة في مكوّن واحد" شائع في هذا المشروع (رأيناه أيضاً في `KeyboardShortcutsHelp` الذي احتفظ بمستمع `keydown` بعد حذف زره، وفي `MinistryQuickDial` الذي احتفظ بمنطق الشيت بعد حذف زره) — لكن `AccessibilityBar` كان الحالة الوحيدة التي كانت الوظيفة فيها (قواعد CSS) منفصلة تماماً عن حالة React ولا تُكتشف بمجرد قراءة الأزرار/الأحداث.

### الحالة: ✅ مُكتشَف ومُصلَح بالكامل في نفس الجلسة.

---

---

## تدقيق التسلسل الهرمي للعناوين (Heading Hierarchy) — ✅ منفَّذ

### المنهجية
فحص فعلي (عبر sub-agent متخصص) لكل وسوم `<h1>`–`<h5>` الحقيقية عبر 19 صفحة رئيسية، مع تتبّع الحالات الشرطية (ماذا يحدث عند حالة فارغة/تحميل/عنصر محدَّد). ليس تخميناً — نتائج بأرقام أسطر دقيقة.

### النتائج والإصلاحات
| الصفحة | المشكلة | الإصلاح |
|---|---|---|
| `app/page.tsx` | 🛑 عند نشاط محادثة فعلي (`messages.length > 0`)، **لا يوجد أي عنوان إطلاقاً** في الصفحة — الـ`<h1>` الوحيد يعيش داخل فرع الشاشة الترحيبية الذي يختفي كلياً | أضفت `<h1 className="sr-only">` في بداية عرض المحادثة — عنوان مخفي بصرياً فقط لمستخدمي قارئ الشاشة |
| `ProcedureDetailClient.tsx` | حالة "غير موجود" لا تملك h1 (فقط h2) + الحالة العادية تقفز من h1 مباشرة إلى h3 (تتخطى h2) | غيّرت h2→h1 في حالة "غير موجود"؛ غيّرت h3→h2 في مكوّن `Section` المشترك (يُستخدم لعناوين: الوثائق المطلوبة، الخطوات، الجهة المسؤولة، الرسوم) |
| `services/page.tsx` | شبكة نتائج الخدمات: h1 ثم h3 مباشرة (بطاقات) بلا h2 | أضفت `<h2 className="sr-only">` (بعدد النتائج) قبل الشبكة |
| `my-files/page.tsx` | قائمة المعاملات: h1 ثم h3 مباشرة (عناوين البطاقات) بلا h2 في الحالة الافتراضية | أضفت `<h2 className="sr-only">` قبل القائمة |
| `app/admin/content/page.tsx` | نفس نمط services/my-files | أضفت `<h2 className="sr-only">` قبل القائمة |
| `app/admin/page.tsx` | تبويبا "إنشاء مستخدم" و"رموز الاستعادة": h1 ثم h3 مباشرة | غيّرت h3→h2 في كلا العنوانين (لا تكرار آخر لـh2 في تلك الحالات، فالترقية آمنة) |

### ملاحظة تقنية مهمة
بما أن هذا المشروع يعتمد بالكامل على `style` مضمّن (لا CSS classes للحجم)، فتغيير وسم من `h3` إلى `h2` أو `h2` إلى `h1` **لا يُغيّر أي شيء مرئي إطلاقاً** — الحجم/الوزن/اللون كلها مُحدَّدة صراحة عبر `style`. هذا يجعل كل هذه الإصلاحات خالية من أي مخاطرة بصرية بنسبة 100%، وهو ما يفسّر لماذا نُفِّذت مباشرة دون تردد.

### صفحات تحقّقتُ من سلامتها بلا حاجة لتعديل
`procedures/page.tsx`، `forms/page.tsx`، `FormDetailClient.tsx`، `expat-property/page.tsx`، `faq/page.tsx`، `authorities/page.tsx`، `settings/page.tsx`، `professional/page.tsx`، `drafting-studio/page.tsx`، وكل صفحات المصادقة (login/register/forgot-password/reset-password) — لا يوجد فيها h1 مكرر ولا قفزة مستوى.

### الحالة: ✅ منفَّذ بالكامل. `tsc --noEmit` نظيف.

---

## تحديث (batch #350) — تقليص الصفحة الرئيسية: بطاقات المعاملات والفئات — ✅ منفَّذ جزئياً

### المشكلة
دقّقنا الهيرو الفعلي في `app/page.tsx` مقابل بنية الموجز الأصلي المطلوبة (هيدر مختصر، هيرو قصير، البحث كعنصر أساسي، مساران فقط). النتيجة: الهيرو **كان مطابقاً فعلاً** — زران فقط ("اسأل دليلك" أساسي + "تصفّح المعاملات" ثانوي) وحقل بحث بارز أسفلهما. لكن قسمَين تاليين في الصفحة تجاوزا الحدود العددية المطلوبة صراحة في الموجز: قسم "الإجراءات الأكثر طلباً" كان يعرض **6 بطاقات** (الموجز يطلب حد أقصى 4)، وقسم "تصفّح حسب الفئة" كان يعرض **10 رقاقات فئة** (الموجز يطلب "شبكة فئات محدودة").

### موقعها
`app/page.tsx` — قسم `PROCEDURES` (بطاقات المعاملات الشائعة) وقسم `CATEGORIES` (رقاقات الفئات).

### أثرها على المستخدم
ازدحام بصري إضافي في أول تمريرة (scroll) بعد الهيرو مباشرة، يُبعِد المستخدم عن مبدأ "شاشة واحدة = إجراء أساسي واحد" رغم أن الهيرو نفسه كان نظيفاً.

### مستوى الأولوية
عالٍ — هذا جزء من البند الأعلى أولوية في قائمة "بنود مؤجَّلة".

### الحل المنفَّذ
- **بطاقات المعاملات**: أُزيلت آخر بطاقتين ("تجديد رخصة القيادة" و"تجديد إقامة الأجانب") من مصفوفة العرض في الصفحة الرئيسية — أصبح القسم يعرض 4 بطاقات بالضبط (جواز السفر، إخراج قيد فردي، تسجيل شركة، تسجيل سيارة). كلا الإجراءين المُزالين من الصفحة الرئيسية **ما زالا متاحين بالكامل** عبر `/procedures` (زر "كل المعاملات" أعلى القسم)، والبحث، والفئات، ورحلات الحياة — لا حذف وظيفي، فقط تقليص واجهة العرض الأولى.
- **رقاقات الفئات**: أُزيلت 4 رقاقات ("الكاتب العدل"، "التعليم"، "العمل والضمان"، "القضاء والمعاملات القانونية") من الصفحة الرئيسية — أصبح الصف يعرض 6 رقاقات. أُضيف رابط "كل الفئات ←" بجانب عنوان القسم (بنفس نمط زر "كل المعاملات" الموجود مسبقاً في القسم الذي يعلوه) يوجّه إلى `/services`. تحقّقتُ فعلياً من أن `/services` يملك نظام تصفية فئات كامل ومستقل (`SERVICE_CATEGORIES` مع عدّاد خدمات لكل فئة) — الرابط لا يوجّه إلى صفحة فارغة أو ناقصة.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. الجزء المتبقي من هذا البند (تدقيق ارتفاع الهيدر بالبكسل الفعلي، ودمج/تصغير قسمَي "كيف يعمل" و"الثقة" إلى ما يقارب "سطر ثقة واحد") لم يُنفَّذ بعد — سيُستكمل في الدفعة التالية لتفادي حذف محتوى حقيقي (أوصاف الثقة والخطوات) دون قرار مدروس بشأن أين يُنقَل ذلك المحتوى بدل حذفه.

---

## تحديث (batch #351) — تدقيق "استخدام اللون فقط" (WCAG 2.2 SC 1.4.1) — ✅ منفَّذ جزئياً

### المشكلة
تدقيق مخصّص (عبر Explore agent) بحثاً عن مؤشرات حالة (نقاط/شارات ملوّنة) تنقل معلومة بالاعتماد على اللون **فقط** بلا نص أو فرق شكل مرافق — وهو انتهاك مباشر لـ WCAG 1.4.1 يؤثر على مستخدمي عمى الألوان (Color Blindness) وشاشات الطباعة الأحادية. غطّى التدقيق كل شارات الحالة/الخطورة/الأولوية/الصعوبة في التطبيق (~15 مكوّناً).

### النتيجة
معظم الشارات كانت **متوافقة فعلاً** (نص أو أيقونة مرافقة للون: `RiskScoreCard`، `ProcedureDifficultyBadge`، `ProcedurePriorityTag`، `MinistryOpenHoursWidget`، `TransactionFilePanel`، `TopNav` (نقطة "متصل")، `NotificationBell`، `ProcedureLastUpdatedBadge`، `HomepageProgressRing`، `ProcedureCompletionBadge`، شارات حالة الأدمن). ثلاث حالات فعلية كانت لوناً فقط:

1. **`QuickContacts.tsx`** — نقطة مفتوح/مغلق (8px) بجانب أيقونة كل جهة في حالة الشريحة المطوية (وهي الحالة الافتراضية التي يتصفّحها المستخدم) — أخضر/أحمر بلا نص، وحتى بلا نص لقارئ الشاشة (الاسم فقط كان يُقرأ).
2. **`HomepageCalendarWidget.tsx`** — نقاط "موعد نهائي" (أحمر) و"تذكير" (بنفسجي) في شبكة التقويم — نفس الشكل (دائرة) بلونين مختلفين فقط؛ `aria-label` الخاص بالخلية لا يذكر النوع أصلاً.
3. **`ProcedureVersionTag.tsx`** (الوضع المضغوط `compact`، المستخدم في بطاقات قوائم الإجراءات) — نقطة تدرّج حداثة (أخضر/أزرق/بنفسجي/برتقالي) بلا نص مرئي افتراضياً — لكنها **الوحيدة من الثلاث التي لها بديل نصي فعلي** (aria-label + tooltip عند التمرير/التركيز/النقر)، وهي معلومة "إضافية" (تاريخ آخر تحديث) وليست معلومة ضرورية لإنجاز المهمة.

### الحل المنفَّذ
- **`QuickContacts.tsx`**: النقطة الآن تنقل الحالة بالشكل أيضاً وليس اللون فقط — دائرة مصمتة = مفتوح، حلقة مفرغة (حدود فقط بلا تعبئة) = مغلق. أُضيف نص مخفي بصرياً (`sr-only`) داخل الزر يذكر "— مفتوح الآن" أو "— مغلق الآن" لقارئ الشاشة، لم يكن موجوداً إطلاقاً من قبل حتى للمستخدم الكفيف.
- **`HomepageCalendarWidget.tsx`**: نقطة "الموعد النهائي" بقيت دائرة، ونقطة "التذكير" أصبحت معيّناً (مربّعاً مائلاً 45°) — فرق شكل حقيقي، وطُبِّق نفس الفرق على وسيلة الإيضاح (Legend) أسفل الودجت للاتساق. `aria-label` لكل خلية يوم أصبح يذكر نوع الحدث صراحة ("يتضمن موعداً نهائياً" / "يتضمن تذكيراً") بدل عدد الأحداث فقط.
- **`ProcedureVersionTag.tsx` (compact)**: تُرِك كما هو عمداً — فيه بالفعل بديل نصي كامل (aria-label دائم + tooltip تفاعلي)، والمعلومة نفسها زخرفية/تكميلية (تاريخ تحديث) لا تمنع إنجاز أي مهمة، وفرض نص مرئي دائم يتعارض مع الغرض التصميمي المعلن لوضع `compact` (بطاقات قوائم ضيقة المساحة). موثَّق هنا كقرار واعٍ وليس إغفالاً.

### الحالة
✅ منفَّذ للحالتين الحقيقيتين الأعلى أثراً. `tsc --noEmit` نظيف. التدقيق الأوسع لـ WCAG 2.2 AA (مناطق landmark، ترتيب التنقّل بلوحة المفاتيح، تباين حالات focus على كل عنصر تفاعلي) لا يزال بنداً مؤجَّلاً جزئياً — أُنجز منه حتى الآن: skip-link، focus-visible، Escape handlers، تسلسل العناوين (batch #349)، تباين الألوان (batch #348)، رسائل الأخطاء (role="alert" موجود مسبقاً في كل صفحات المصادقة، تحقّقتُ منه هذه الدفعة بلا حاجة لتعديل)، واستخدام اللون فقط (هذه الدفعة).

---

## تحديث (batch #352) — اكتشاف زر مُضلِّل حقيقي في صفحة تفاصيل الإجراء — ✅ منفَّذ

### المشكلة
أثناء تدقيق صفحة تفاصيل الإجراء (`ProcedureDetailClient.tsx`) لمبدأ "شاشة واحدة = إجراء أساسي واحد"، وُجد زر في صف الإجراءات العلوي (بجانب زر "اسأل دليلك" الأساسي) نصّه "المعالج"/"Wizard"، لكن `onClick` الخاص به كان ينفّذ `router.push('/')` — أي ينقل المستخدم إلى الصفحة الرئيسية فقط. لا يوجد أي مسار أو معالج (`GuidedFlow`) مرتبط بهذا الإجراء تحديداً يُفتَح من هذا الزر — تحقّقت عبر البحث في الكود عن أي استخدام آخر لـ`GuidedFlow` أو رابط لمسار "wizard" داخل هذا الملف، فلم أجد شيئاً. هذا انتهاك مباشر لأولوية "الوضوح" (أعلى بند في ترتيب الأولوية المعتمد) — يتوقّع المستخدم أن يفتح الزر معالجاً موجّهاً لهذا الإجراء تحديداً، فإذا به ينقله فقط إلى الصفحة الرئيسية.

### موقعها
`app/procedures/[slug]/ProcedureDetailClient.tsx`، صف الإجراءات أعلى الصفحة (بجانب "اسأل دليلك").

### أثرها على المستخدم
تضليل مباشر — نص الزر يَعِد بميزة (معالج موجّه) لا ينفّذها الزر فعلياً؛ يفقد المستخدم الثقة في تسميات الأزرار الأخرى في التطبيق.

### مستوى الأولوية
عالٍ — انتهاك مباشر لأعلى بند في ترتيب الأولوية (الوضوح).

### الحل المنفَّذ
بما أن `GuidedFlow` مكوّن مرتبط بالصفحة الرئيسية فقط (`app/page.tsx`) ولا يملك أي آلية للربط العميق (deep-link) بإجراء محدّد بعد — بناء هذا الربط سيكون تغييراً أكبر نطاقاً وغير مبرَّر في دفعة تركيزها إصلاح دقيق — اخترتُ الحل الصادق والأقل مخاطرة: أعدت تسمية الزر ليطابق سلوكه الفعلي فعلاً. النص الآن "الرئيسية"/"Home" بدل "المعالج"/"Wizard"، مع تغيير الأيقونة إلى أيقونة بيت (Home) بدل الأيقونة السابقة غير المرتبطة بأي من المعنيَين، وإضافة `aria-label` واضح. لا تغيير في السلوك — فقط في الصدق البصري لما يفعله الزر.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. **ملاحظة للمتابعة المستقبلية**: لو أراد المستخدم لاحقاً ميزة "معالج موجّه لهذا الإجراء تحديداً" فعلياً، هذا يتطلّب تمرير مُعرِّف الإجراء (`slug`) إلى `GuidedFlow` عبر query param في الصفحة الرئيسية وفتحه تلقائياً — تغيير حقيقي في المنطق يحتاج قراراً منفصلاً، وليس مجرد إعادة تسمية.

---

## تحديث (batch #353) — بدء "تبسيط واجهة المحادثة" — تقليص صف إجراءات كل رسالة من 10 إلى 4 عناصر — ✅ منفَّذ جزئياً

### المشكلة
تدقيق مباشر لبند "تبسيط واجهة المحادثة" (كان لا يزال في جدول "بنود مؤجَّلة" منذ بداية الجلسة) كشف أن كل رسالة من الذكاء الاصطناعي (وهي أكثر عنصر تكراراً في كامل التطبيق — تظهر مرة لكل رد، وليس مرة واحدة لكل صفحة) كانت تعرض **10 عناصر تفاعلية منفصلة** أسفلها: `ChatMessageActions` (نسخ + مشاركة + حفظ = 3 أزرار) + `ChatPinButton` (تثبيت) + `ChatVoicePlayback` (استماع) + `ChatEmojiReactions` (4 أزرار إيموجي منفصلة، كلها ظاهرة دائماً) + `ChatSaveToNotes` (حفظ في الملاحظات) = 10 عناصر إجمالاً، بالإضافة إلى `ChatAIBadge` (شارة نصية غير تفاعلية، تظهر فقط على أول رسالة). هذا انتهاك مباشر وعالي التكرار لمبدأ "شاشة واحدة = إجراء أساسي واحد" ولأولوية "البساطة".

### موقعها
`app/page.tsx`، صف إجراءات كل رسالة ذكاء اصطناعي (كان الأسطر ~1993-2002 قبل هذه الدفعة).

### أثرها على المستخدم
ازدحام بصري متكرر مع كل رد — كلما طال التبادل، ازداد الازدحام تراكمياً (10 أزرار × عدد الردود).

### مستوى الأولوية
عالٍ — أعلى تكراراً من أي عنصر ازدحام آخر عولج هذه الجلسة.

### الحل المنفَّذ
أُبقي `ChatMessageActions` (نسخ/مشاركة/حفظ — الإجراءات الثلاثة الأكثر أهمية على أي رد) ظاهراً دائماً كما هو، وطُوِيت العناصر السبعة المتبقية (تثبيت، استماع، 4 إيموجي، حفظ في الملاحظات) خلف زر واحد جديد "•••" (المزيد) لكل رسالة على حدة (حالة `expandedMsgActions: Set<number>` مفتاحها رقم الرسالة). لم يُحذَف أي مكوّن أو وظيفة — نفس المكوّنات بنفس الخصائص (props)، فقط خلف نقرة واحدة إضافية. النتيجة: من 10 عناصر ظاهرة افتراضياً إلى 4 (نسخ/مشاركة/حفظ + زر المزيد) — تخفيض 60%.

### الحالة
✅ منفَّذ للصف الأكثر تكراراً في واجهة المحادثة. `tsc --noEmit` نظيف. **المتبقي من بند "تبسيط واجهة المحادثة"**: صف الأدوات أعلى حقل الإدخال لا يزال يعرض عدة عناصر متجاورة (`ModeSelector` + `ChatResponseLength` + `ChatContextBar` + `ChatKeyboardSendHint` + `ChatInputCharCounter` + `ChatQuickReplies`)، وأعلى شاشة المحادثة توجد عدة عناصر إضافية (`ChatSummaryCard` + `ChatSessionTimer` + `ChatMessageSearchInThread` + `ChatSessionSummaryChip` + `ChatPinnedBanner` + `ChatLanguageToggleChip`) — هذه أقل تكراراً (تظهر مرة واحدة لكل جلسة/صفحة وليس مرة لكل رسالة)، لذا أولويتها أقل من صف إجراءات الرسالة الذي عولج هنا، لكنها ما زالت بنداً مفتوحاً للدفعة التالية.

**تحديث تحقيق (نفس الدفعة، بلا تعديل كود)**: فحصتُ تحديداً `ChatSummaryCard` (يظهر عند 5+ رسائل) و`ChatSessionSummaryChip` (يظهر عند 10+ رسائل) لاحتمال أن يكونا وظيفة مكرَّرة (كلاهما يبدو ظاهرياً "تلخيص المحادثة"). بعد قراءة الكود الفعلي لكليهما: **ليسا مكرَّرين** — `ChatSummaryCard` يستخرج كلمات مفتاحية بمطابقة نصية محلياً (بلا استدعاء AI) ويعرضها كشرائح مواضيع قابلة للنقر لإعادة السؤال عنها، بينما `ChatSessionSummaryChip` يرسل فعلياً سؤالاً حقيقياً للذكاء الاصطناعي يطلب تلخيصاً مكتوباً كاملاً — وظيفتان مختلفتان فعلياً رغم تشابه التسمية، فلم أُجرِ أي دمج أو حذف. كذلك فحصتُ `ChatSessionTimer`: هو شارة صغيرة واحدة فقط (ليس مصدر ازدحام فعلي)، تجاور زراً مضغوطاً مسبقاً (`ChatMessageSearchInThread` له حالة `open` داخلية، أيقونة واحدة افتراضياً لا صندوق بحث دائم الظهور) — لا حاجة لتغيير. توثيق هذا التحقّق هنا بدل تغيير غير مبرَّر يتماشى مع انضباط الجلسة المعتمَد (الإبلاغ عن نتيجة "لا مشكلة فعلية" بدل الصمت أو التعديل العشوائي).

---

## سجل الدفعات المرتبط بهذا الملف
- **دفعة اليوم (2026-07-25)**: المرحلة 1 (تفكيك مكدّس الأزرار العائمة، batch #345) + المرحلة 2 (BottomNav إلى 4 عناصر) + إزالة كتلة الكود الميت في الصفحة الرئيسية (batch #346) + تحويل FeedbackWidget لبطاقة داخلية (batch #347) + تدقيق تباين الألوان WCAG AA وإصلاح `--text-3`/`--text-4` + إصلاح انهيار وظيفي حرج في أدوات إمكانية الوصول (batch #348) + تدقيق التسلسل الهرمي للعناوين وإصلاحه في 6 ملفات (batch #349) + تقليص بطاقات المعاملات الشائعة (6→4) ورقاقات الفئات (10→6) في الصفحة الرئيسية (batch #350) + تدقيق "استخدام اللون فقط" وإصلاح QuickContacts + HomepageCalendarWidget (batch #351) + إصلاح زر "المعالج" المُضلِّل في صفحة تفاصيل الإجراء (batch #352) + تقليص صف إجراءات كل رسالة محادثة من 10 إلى 4 عناصر ظاهرة (batch #353). راجع `push.sh` لرسالة الـ commit الكاملة.

## تحديث (batch #354) — إزالة ~1.7 ميجابايت من الاستيرادات الميتة في الصفحة الرئيسية — ✅ منفَّذ

### المشكلة
أثناء متابعة بند "تحسين الأداء" المؤجَّل، دقّقت أعلى `app/page.tsx` فوجدت ثلاثة استيرادات لملفات بيانات ضخمة: `TX_ALL`/`TX_WITH_FORMS`/`TX_MINISTRIES` من `lib/allTransactions.ts` (~400 كيلوبايت مصدر)، `ENRICHED_PROCEDURES` من `lib/enrichedProcedures.ts` (~256 كيلوبايت)، و`ALL_SERVICES` من `lib/allServices.ts` (~1 ميجابايت). تحقّقت عبر `grep` لكل معرِّف على حدة أنه **يظهر مرة واحدة فقط في كامل الملف — سطر الاستيراد نفسه** — أي أنها استيرادات ميتة بالكامل، لا يستخدمها أي كود في هذا الملف إطلاقاً.

### موقعها
`app/page.tsx`، أعلى الملف (أسطر الاستيراد).

### أثرها على المستخدم
أداء — حوالى 1.7 ميجابايت من بيانات غير مستخدَمة كانت تُحزَم ضمن حزمة جافاسكريبت للصفحة الرئيسية (قبل الضغط/التصغير)، تُحمَّل على كل زائر بلا أي فائدة.

### مستوى الأولوية
متوسط-عالٍ — إصلاح آمن 100% (لا خطر بصري أو وظيفي، الاستيراد غير مستخدَم أصلاً) بأثر أداء حقيقي وقابل للقياس.

### الحل المنفَّذ
حذف سطرَي الاستيراد الميتَين بالكامل. الصفحات التي تستخدم هذه البيانات فعلياً (`/procedures`، `/services`، `/forms`، إلخ) تستوردها مباشرة من نفس الملفات المصدر ولم تتأثر إطلاقاً — هذا التغيير خاص فقط بحزمة الصفحة الرئيسية.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف — وهذا تأكيد إضافي أن لا شيء آخر كان يعتمد على هذه الاستيرادات عبر هذا الملف (لو كان أي كود يستخدمها، لَفشل البناء فوراً).

---

## سجل الدفعات المرتبط بهذا الملف
- **دفعة اليوم (2026-07-25)، إضافات لاحقة**: batch #354 — إزالة ~1.7 ميجابايت من الاستيرادات الميتة (TX_ALL/TX_WITH_FORMS/TX_MINISTRIES/ENRICHED_PROCEDURES/ALL_SERVICES) من `app/page.tsx`.

## تحديث (batch #355) — إصلاح كثافة الصفحة الرئيسية الفعلية على الموبايل — ✅ منفَّذ

### المشكلة
أبلغ المستخدم صراحةً أن واجهة الهاتف "لا يوجد فيها تغيير كما توقّع". تحقّقت أولاً أن آخر نشر منشور فعلاً (فتحت الموقع الحي وتأكّدت من ظهور تعديلات batch #350 — 6 رقاقات فئات + رابط "All categories" — فالنشر سليم)، لكن أداة تغيير حجم نافذة المتصفح في هذه البيئة لم تكن تعمل فعلياً (`window.innerWidth` بقي 1470px رغم طلب تغييره إلى 375-420px)، فلم أستطع اختبار الشكل الفعلي على شاشة ضيّقة مباشرة. سألت المستخدم تحديداً ماذا توقّع، فأجاب: **"الشكل العام/التباعد — لا يزال يبدو مزدحماً أو بلا تغيير على الموبايل"**.

دقّقت الكود بدلاً من الاعتماد على لقطة شاشة، ووجدت السبب الجذري الحقيقي: كل قسم من أقسام الصفحة الرئيسية السبعة (الهيرو، المعاملات، الفئات، رحلات الحياة، كيف يعمل، الثقة، الفوتر) يستخدم حشوة عمودية على شكل `clamp(الحد_الأدنى, نسبة_من_عرض_الشاشة, الحد_الأقصى)` — لكن الحد الأدنى (المصمَّم أصلاً لسطح المكتب، 32-48px) كان **دائماً** أكبر من قيمة `vw` الفعلية عند عرض هاتف حقيقي (مثلاً `5vw` عند 390px = 19.5px فقط، أقل من الحد الأدنى 48px بكثير) — أي أن كل قسم كان **يُعرَض دائماً بحشوته القصوى المصمَّمة لسطح المكتب، حتى على أضيق شاشة هاتف**. المجموع التراكمي لحشوة الأقسام وحدها (بلا احتساب أي محتوى داخلي) كان يبلغ ~536px — أكثر من نصف ارتفاع شاشة هاتف نموذجية، مجرد فراغ فارغ بين الأقسام. هذا يفسّر تماماً شكوى المستخدم: كل تحسينات هذه الجلسة (تقليص عدد البطاقات/الرقاقات، إلخ) لم تكن لتُشعِر بفرق حقيقي على الموبايل ما دامت المسافات الفارغة بين الأقسام ضخمة كما كانت دوماً.

### موقعها
`app/page.tsx` — الحشوة العمودية (`padding`) لكل قسم رئيسي في الصفحة (7 مواضع) + الفوتر.

### أثرها على المستخدم
هذا على الأرجح **السبب الأهم** وراء شعور "لا تغيير" على الموبايل — أكبر من أي تغيير آخر تم هذه الجلسة، لأنه يؤثر على كل تمريرة (scroll) في الصفحة الرئيسية بالكامل.

### مستوى الأولوية
عالٍ جداً — هذا بالضبط نوع المشكلة التي طلب المستخدم معالجتها.

### الحل المنفَّذ
خفضت الحد الأدنى (floor) فقط في كل قيمة `clamp()` لسبعة أقسام، مع إبقاء الحد الأقصى (قيمة سطح المكتب) كما هو تماماً:
- الهيرو: `clamp(48px→28px, 6vw, 88px)` أعلى، `clamp(56px→32px, 7vw, 96px)` أسفل
- المعاملات: `clamp(48px→28px, 5vw, 80px)`
- الفئات: `clamp(32px→20px, 4vw, 56px)`
- رحلات الحياة: `clamp(48px→28px, 5vw, 80px)`
- كيف يعمل: `clamp(48px→28px, 5vw, 80px)`
- الثقة: `clamp(40px→24px, 5vw, 72px)`
- الفوتر: `clamp(40px→28px, 5vw, 64px)` أعلى، `clamp(24px→20px, 3vw, 40px)` أسفل

النتيجة: إجمالي حشوة الأقسام على الموبايل ينخفض من ~536px إلى ~316px (تخفيض ~40%). **تحقّقت رياضياً أن سطح المكتب غير متأثر إطلاقاً**: عند عرض سطح مكتب نموذجي (1280-1440px)، قيمة `Nvw` تتجاوز الحد الأدنى الجديد (والقديم) بكثير، فتبقى `clamp()` تُرجِع نفس قيمة `Nvw` كما كانت قبل التعديل — التعديل عملياً موبايل-فقط.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. **قيد التنفيذ**: لم أتمكّن من التقاط لقطة شاشة فعلية لعرض هاتف ضيّق في هذه الجلسة بسبب عطل في أداة تغيير حجم نافذة المتصفح المتاحة — طلبت من المستخدم مراجعة الموقع الحي على هاتفه الفعلي بعد النشر للتأكيد.

---

## تحديث (batch #356) — تدقيق ارتفاع TopNav مقابل مواصفة 56-64px — ✅ تحقّق بلا حاجة لتعديل

فحصت `components/TopNav.tsx` سطراً بسطر: الارتفاع الفعلي ثابت `height: 64` (السطر 112)، أي عند الحد الأقصى المسموح تماماً في الموجز الأصلي (56-64px). لا تعديل مطلوب — موثَّق هنا حتى لا يُعاد فحصه لاحقاً.

---

## تحديث (batch #364) — إصلاح ChatLanguageToggleChip: شريحة بلا زر إغلاق — ✅ منفَّذ

### المشكلة
أثناء متابعة بند "تبسيط أعلى شاشة المحادثة" (المتبقي من batch #353)، تبيّن أن `ChatLanguageToggleChip` كانت تُعرَض من أول رسالة (`messageCount >= 1`) **بلا أي زر إغلاق** — الطريقة الوحيدة لإخفائها كانت الضغط عليها فعلياً وتنفيذ طلب تبديل لغة الرد قد لا يريده المستخدم إطلاقاً. أي مستخدم لا يريد هذه الميزة كان يراها معلّقة أعلى كل محادثة إلى الأبد.

### موقعها
`components/ChatLanguageToggleChip.tsx`.

### أثرها على المستخدم
عنصر دائم الظهور بلا وسيلة تجاهل حقيقية — يتراكم بصرياً فوق `ChatSummaryCard` (5+ رسائل) و`ChatSessionSummaryChip` (10+ رسائل، وهذه لديها زر إغلاق أصلاً) في نفس المنطقة أعلى المحادثة.

### الحل المنفَّذ
أضفت حالة `dismissed` وزر "✕" منفصل بجانب الشريحة (بنفس نمط `ChatSessionSummaryChip` الموجود مسبقاً). لا تغيير في السلوك عند الضغط على الشريحة نفسها — فقط أصبح بالإمكان الآن تجاهلها دون استخدامها.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف.

---

## سجل الدفعات المرتبط بهذا الملف — إضافة
- **batch #364**: إضافة زر إغلاق لـ `ChatLanguageToggleChip` (كانت بلا وسيلة تجاهل حقيقية).

---

## تحديث (batch #357) — Lazy-load لثلاث نوافذ منبثقة ثقيلة — ✅ منفَّذ

### المشكلة
`GuidedFlow` و`TransactionStarter` و`ServiceGroupSheet` كانت مستوردة بشكل eager في `app/page.tsx` رغم أنها نوافذ منبثقة (modals) لا تُعرض إلا بعد فعل مستخدم صريح (زر "ابدأ معاملة"، بطاقة فئة خدمة...) — أي أن معظم الزيارات لا تفتحها أبداً، لكن كودها كان يُحمَّل ضمن الحزمة الرئيسية لكل تحميل للصفحة الرئيسية.

### الحل
تحويل الثلاثة إلى `next/dynamic` مع `ssr: false` (نوافذ عميل بحتة، لا محتوى SEO/مؤشرات فيها). تحقّقت من أن `ServiceGroupSheet` و`TransactionStarter`/`GuidedFlow` كلها تُرجِع `null` داخلياً عند عدم الفتح (أو مُغلَّفة بشرط `{show... && (...)}` في `page.tsx`)، فلا خطر من تحميلها متأخرة. استيراد النوع `StarterResult` (type-only، يُحذَف عند البناء) أُبقي كاستيراد ساكن منفصل عن الاستيراد الديناميكي للمكوّن نفسه.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. أثر الأداء: تقليل حجم الحزمة الأولى للصفحة الرئيسية (bundle-size)، صفر تغيير وظيفي أو بصري.

---

## تحديث (batch #358) — تبسيط شريط أدوات صندوق إدخال المحادثة — ✅ منفَّذ جزئياً

### الفحص
دقّقت كل عناصر صف "أدوات الإدخال" فوق مربع الكتابة (`ModeSelector`, `ChatResponseLength`, `ChatContextBar`, `ChatKeyboardSendHint`, `ChatInputCharCounter`, `ChatDraftAutosave`, `ChatQuickReplies`). النتيجة: معظمها **مموَّه ذاتياً بالفعل** ولا يظهر إلا عند الحاجة — `ChatContextBar` يختفي عند الفراغ، `ChatInputCharCounter` لا يظهر تحت 200 حرف، `ChatKeyboardSendHint` مخفي على اللمس أصلاً (لا يظهر على الموبايل إطلاقاً)، `ChatDraftAutosave` لا يُرسم شيئاً مرئياً (مساعد خفي بالكامل)، و`ModeSelector` يتقلّص فعلاً إلى زر واحد مضغوط + bottom sheet على الموبايل (نمط جيد موجود مسبقاً).

العنصر الوحيد الذي **لم يكن مضغوطاً**: `ChatResponseLength` — شريط تبديل من 3 أزرار بنص كامل، ظاهر دائماً بجانب زر `ModeSelector` المضغوط، ما يخلق عدم تناسق ويستهلك عرضاً غير ضروري على الشاشات الضيّقة.

### الحل
أضفت breakpoint مطابق لنفس نقطة `ModeSelector` (640px) داخل `ChatResponseLength.tsx`: تحت 640px تُخفى النصوص وتبقى الأيقونات فقط + `aria-label`/`title` صريح على كل زر (تحسين وصولية إضافي، لأن الاسم الوصولي كان يعتمد سابقاً فقط على النص المرئي). لا حذف وظيفي — كل الأزرار الثلاثة (موجز/عادي/مفصّل) تعمل كما هي، فقط أضيق مساحةً على الهاتف.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. **متبقٍ للمستقبل**: توحيد `ChatResponseLength` مع `ModeSelector` في نمط "زر واحد + sheet" الكامل بدل أيقونات مضغوطة فقط — تحسين أعمق لكن يتطلب مكوّن sheet جديد، أجّلته لتفادي مخاطرة أكبر في دفعة واحدة.

---

## تحديث (batch #359) — تدقيق مناطق Landmark (WCAG 2.2 "Bypass Blocks") — ✅ منفَّذ

استخدمت Explore agent لتغطية كل صفحات `app/**/page.tsx` ومكوّنات `<nav>`/`<header>`/`<footer>`. النتائج والإصلاحات:

1. **رابط "تخطّي إلى المحتوى" كان معطَّلاً على صفحتين**: `app/professional/page.tsx` و`app/settings/page.tsx` — الغلاف الجذري فيهما كان `<div>` عادياً بلا `id="main-content"`، فرابط `#main-content` في `app/layout.tsx` لم يكن يوصل الفوكس إلى أي مكان فيهما. **الإصلاح**: حوّلت غلاف المحتوى في كلا الملفين إلى `<main id="main-content">` (في `settings/page.tsx` أُبقي الهيدر خارج `<main>` و`BottomNav` خارجه أيضاً، بنفس النمط المتّبع في `procedures/page.tsx`). كل الصفحات الـ18 الأخرى المفحوصة كانت صحيحة أصلاً (main واحد فقط، id واحد فقط).
2. **عناصر `<nav>` المتعددة**: كلها موسومة بـ`aria-label` مميّز (`TopNav`، `BottomNav`، `Breadcrumbs`، `DraftingStudio`) — لا تعارض.
3. **مَعلَمان `<footer>` يُعرَضان معاً على الصفحة الرئيسية في وضع الترحيب** (`app/page.tsx`): فوتر التسويق (داخل `<main>`) وشريط إدخال الدردشة الدائم (خارج `<main>`، يُستخدم عنصر `<footer>` عليه فعلياً رغم أنه شريط أدوات لا تذييل صفحة). كلاهما بلا `aria-label` يُميّزهما. **الإصلاح**: أضفت `aria-label` مميّزاً لكل منهما ("تذييل الصفحة" / "شريط إدخال الرسالة") دون تغيير نوع العنصر نفسه (تفادياً لأي مخاطرة أنماط CSS مرتبطة بوسم `footer`).

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف.

---

## تحديث (batch #360) — تدقيق ترتيب التنقّل بلوحة المفاتيح (Tab order) — ✅ منفَّذ جزئياً + بنية تحتية جاهزة

### المشكلة
تدقيق شامل (`grep` لكل استخدامات `tabIndex`) أكّد عدم وجود أي `tabIndex` موجب (خطأ شائع) — نقطة إيجابية. لكن الفحص كشف أن **كل مكوّنات `role="dialog"` الـ13 في التطبيق** (`GlobalSearch`, `MobileMenu`, `ServiceGroupSheet`, `MobileModeSheet`, `GuidedFlow`, `TransactionStarter`, `ProcedureSearchModal`, `ProcedureFilterDrawer`, `PrintProcedureModal`, `EscalationModal`, `DocumentIntelligenceView`, `AccessibilityBar`, `FloatingHelpButton`) تتعامل مع Escape للإغلاق وقفل تمرير الخلفية، لكن **لا تحبس تنقّل Tab داخلها** — أي أن مستخدم لوحة المفاتيح يمكنه الخروج بـTab من نافذة مفتوحة إلى عناصر الصفحة المخفية خلفها (مخالفة WCAG 2.4.3).

### الحل
بنيت خطّافاً قابلاً لإعادة الاستخدام `lib/useFocusTrap.ts`: يحبس Tab/Shift+Tab داخل حاوية النافذة (يلتف من آخر عنصر قابل للتركيز إلى أوّله والعكس)، وينقل الفوكس تلقائياً لأول عنصر عند الفتح إن لم يكن الفوكس بداخلها أصلاً، ويُعيد الفوكس لمكانه الأصلي عند الإغلاق. طبّقته هذه الدفعة على أكثر مكوّنين استخداماً من لوحة المفاتيح: `GlobalSearch` (Cmd+K) و`MobileMenu` (قائمة التنقّل الرئيسية على الموبايل).

### الحالة
✅ منفَّذ بالكامل (batch #360 كان جزئياً، batch #361 أكمله). `tsc --noEmit` نظيف.

---

## تحديث (batch #361) — إكمال Focus Trap على الـ11 مكوّناً المتبقّية — ✅ منفَّذ

طبّقت `useFocusTrap` على الـ11 مكوّناً المتبقّية من قائمة batch #360: `ServiceGroupSheet`، `MobileModeSheet`، `GuidedFlow`، `TransactionStarter`، `ProcedureSearchModal`، `ProcedureFilterDrawer`، `PrintProcedureModal`، `EscalationModal`، `DocumentIntelligenceView` (تحديداً `GenerateDraftModal` الداخلي فيه)، `AccessibilityBar`، `FloatingHelpButton`.

لكل مكوّن تحقّقت أولاً كيف يُغلَق/يُفتَح (بعضها يُركَّب دائماً أثناء وجوده لأن الأب يستخدم `{show && <X/>}`، وبعضها الآخر يملك حالة `open` داخلية) قبل تمرير القيمة الصحيحة كوسيط `active` — وليس افتراض `true` للجميع. في `FloatingHelpButton` استخدمت `panelRef` الموجود مسبقاً (كان يُستخدم فقط لكشف النقر خارج اللوحة) بدل إضافة ref جديد، لأنه يُغلّف الزر المُشغِّل واللوحة معاً — بالضبط الحاوية الصحيحة للفخ. في المكوّنات التي تُركِّز عنصراً محدداً عند الفتح (`closeRef.current?.focus()` في `GuidedFlow`/`TransactionStarter`/`EscalationModal`) تحقّقت أن ترتيب الـ`useEffect` يضمن أن التركيز الصريح للمكوّن نفسه يُطبَّق بعد التركيز التلقائي لأول عنصر من `useFocusTrap` — فلا يوجد أي تغيير في أي مكوّن أين يُركَّز الفوكس أولاً عند الفتح.

### الحالة
✅ منفَّذ بالكامل — كل الـ13 مكوّن `role="dialog"` في التطبيق الآن يحبس Tab بشكل صحيح. `tsc --noEmit` نظيف.

---

## تحديث (batch #362) — تحسين أداء مؤقّت جلسة المحادثة — ✅ منفَّذ

`ChatSessionTimer` كان يُحدَّث كل ثانية (setInterval 1000ms) بعرض HH:MM:SS، ما يعني إعادة رسم للمكوّن كل ثانية طوال مدة أي محادثة مفتوحة — لا أحد يحتاج دقة الثانية لمؤشر "مدة الجلسة". غيّرت العرض إلى دقة الدقيقة (`Xm` / `Xد`) والتحديث إلى كل 30 ثانية بدل كل ثانية — تخفيض 30× في وتيرة إعادة الرسم، نفس الميزة المرئية تماماً، صفر حذف وظيفي. باقي عناصر الصف العلوي (`ChatSummaryCard`, `HomepageChatSuggestionsBar`, `ChatSessionSummaryChip`, `ChatPinnedBanner`, `ChatLanguageToggleChip`) تحقّقتُ أنها جميعاً مموَّهة ذاتياً بالفعل (تختفي حين لا تنطبق شروطها) — لا حاجة لتعديلها.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف.

---

## تحديث (batch #363) — Lazy-load مكوّنَين ثقيلين إضافيَين — ✅ منفَّذ

- **`DocumentIntelligenceView.tsx`** (944 سطراً — أكبر مكوّن في التطبيق) كان مستورَداً بشكل eager داخل `ChatMessage.tsx` رغم أن `msg.documentAnalysis` لا يكون صحيحاً إلا لرسالة نتجت فعلياً عن تحليل مستند مرفوع — أقلية صغيرة من الرسائل. كل محادثة كانت تُحمِّل هذا المكوّن الضخم بلا داعٍ. حوّلته إلى `next/dynamic` مع `ssr:false`.
- **`UserOnboarding.tsx`** (433 سطراً) كان مستورَداً بشكل eager في `app/page.tsx` رغم أنه مموَّه داخلياً بالكامل (`if (!show) return null`، والـ`show` يُحدَّد من localStorage — لا يظهر إلا لزائر جديد فعلياً). حوّلته إلى `next/dynamic` مع `ssr:false`.

كلاهما مكوّنات عميل بحتة (بلا محتوى SEO)، فـ`ssr:false` آمن تماماً. **متبقٍ للمستقبل**: `KeyboardShortcutsHelp` (168 سطراً) و`AppointmentReminder` (188 سطراً) مؤهّلان أيضاً لكن حجمهما أصغر بكثير — أُجِّلا لتفادي دفعة كبيرة غير ضرورية.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف.

---

## سجل الدفعات المرتبط بهذا الملف
- **دفعة اليوم (2026-07-25)، إضافات لاحقة**: batch #354 (إزالة استيرادات ميتة) + batch #355 (كثافة حشوة الموبايل) + batch #356 (تحقّق ارتفاع TopNav) + batch #357 (lazy-load 3 نوافذ) + batch #358 (تبسيط شريط أدوات الإدخال) + batch #359 (تدقيق Landmark regions) + batch #360 (بنية Focus Trap + تطبيقها على مكوّنين) + batch #361 (إكمال Focus Trap على الـ11 مكوّناً المتبقّية — 13/13 مكوّن الآن) + batch #362 (تحسين أداء ChatSessionTimer) + batch #363 (lazy-load DocumentIntelligenceView + UserOnboarding).

---

## المرحلة 3 — "Calm Government Digital Service" — إعادة بناء الواجهة من الصفر (v4.0) — 🟡 قيد التنفيذ (batch #366)

### السياق
بعد كل التحسينات التدريجية أعلاه، أرسل المستخدم لقطة شاشة فعلية من الهاتف وقال بوضوح إن الشكل "لازال هكذا" وطلب تغيير الواجهة كلها دفعة واحدة، ثم أرسل موجزاً تصميمياً كاملاً من 29 بنداً يطلب فيه صراحة: **عدم** الاستمرار بالتعديل التدريجي، إعادة بناء فعلية للواجهة (وليس تقريراً فقط)، نفس الهوية (الاسم/الشعار/اللون العنّابي) مع تنفيذ بصري جديد كلياً باسم "Calm Government Digital Service" — بلا تدرّجات لونية (gradient)، بلا ظلال، بلا زخرفة غير وظيفية، مساحات بيضاء واسعة، لون أساسي واحد يُستخدم بحدّ أقصى 10-15% من الشاشة. حدّد المستخدم قيم الألوان/الخطوط/المسافات/الزوايا حرفياً، وترتيب أقسام الصفحة الرئيسية العشرة بالضبط، وأولوية القرار: **جودة الموبايل البصرية ← الوضوح ← البساطة ← الاتساق ← سهولة الاستخدام ← الوصول ← الأداء ← تكيّف سطح المكتب.**

### الرموز البصرية الجديدة (v4.0 tokens — `app/globals.css`)
اللون الأساسي `#9F1D2F` (hover `#861827`, soft `#F8ECEF`)، الخلفية `#F8F8F6`، السطح `#FFFFFF`، النص الأساسي `#181817`، النص الثانوي `#65635F`، الحدود `#E5E2DD`. زوايا: إدخال/بطاقة 14px، زر 12px، نافذة منبثقة 20px. كل الظلال أُلغيت تقريباً (`0 1-2px، شفافية منخفضة جداً`)، وكل متغيّرات "gradient" السابقة أصبحت أسماء مستعارة للّون الواحد المسطّح (لا يوجد تدرّج فعلي في أي مكان بعد الآن).

### ما نُفِّذ فعلياً هذه الدفعة
1. **الهيدر (`TopNav.tsx`)** — على الموبايل: أصبح الشعار + زر القائمة فقط (كان يحوي أيضاً جرس الإشعارات، البحث، زر "محادثة جديدة"، وتبديل اللغة متزاحمين في صف 56-64px). العناصر الأربعة المُزاحة **لم تُحذف** — انتقلت إلى داخل `MobileMenu`.
2. **`MobileMenu.tsx`** — استوعب البحث والإشعارات (كمكوّنين حقيقيين مُعاد استخدامهما، لا إعادة تنفيذ) + صفوف جديدة لإمكانية الوصول/المساعدة/حول التطبيق/سياسة الخصوصية.
3. **صفحة جديدة `app/privacy/page.tsx`** — محتوى تقني دقيق فقط (ما يُخزَّن محلياً، بيانات الحساب، المحادثة الذكية، التواصل) بلا أي ادعاء قانوني غير مؤكَّد، وموسوم صراحة في الكود كمسودة تحتاج مراجعة قانونية فعلية قبل اعتمادها نهائياً.
4. **الصفحة الرئيسية (`app/page.tsx`)** أُعيد بناؤها للترتيب الحرفي المطلوب (10 أقسام):
   - الهيدر الصغير ← عنوان "ما المعاملة التي تبحث عنها؟" ← نص مساعد ← شريط بحث واحد (بلا زر إرسال منفصل؛ المسح/التحسين بالذكاء الاصطناعي/الميكروفون كلها داخل الحقل) ← صف إجراءين سريعين فقط.
   - "الأكثر بحثاً": من شبكة بطاقات إلى قائمة صفوف موحّدة (أيقونة + اسم + سهم)، نفس الإجراءات الأربعة الحقيقية.
   - "تصفّح حسب الفئة": شبكة ثابتة عمودين × 6 فئات حقيقية، أيقونات خطية جديدة لكل فئة.
   - **أُزيلت من العرض** (البيانات والمنطق لم تُحذف، ما زالت قابلة للوصول عبر البحث/القائمة): شبكة "أحداث الحياة"، قسم "كيف يعمل دليلك" التوضيحي بثلاث خطوات، شبكة الثقة الرباعية — استُبدلت بسطر ثقة واحد كما طلب الموجز حرفياً.
   - **الفوتر**: من فوتر ثلاثي الأعمدة داكن (8 روابط + تبديل لغة مضمّن) إلى صف واحد فاتح مضغوط (الشعار، حقوق النشر، رابط "حول"، رابط "الخصوصية"، سطر إخلاء المسؤولية القانوني). كل الروابط المحذوفة من الفوتر (الخدمات/المعاملات/الجهات/النماذج/الأسئلة/ملفاتي/استوديو الصياغة) ما زالت متاحة بضغطة واحدة من قائمة الهامبرغر — لم تُحذف أي وظيفة، فقط أُعيد تنظيمها.
   - زر "الرئيسية" (في `BottomNav` و`MobileMenu`) يُنفّذ الآن نفس تسلسل الحفظ والتفريغ الذي كان زر "محادثة جديدة" في الهيدر القديم يقوم به (تعويض كامل عن إزالة ذلك الزر من الهيدر).
5. **`BottomNav.tsx`** — أُزيل الزر العائم الدائري المتدرّج اللون لِـ"محادثة" (كان يحمل ظلاً ملوّناً وارتفاعاً عن باقي الأزرار وحركة قفز) — أصبح تبويباً رابعاً مسطّحاً كالباقي، بلون `var(--brand)` عند التفعيل فقط، بلا شريط علوي طويل يشير للتفعيل (اللون والوزن وحدهما كافيان، كما في باقي التبويبات). أُعيدت تسمية التبويب "اسأل دليلك" حسب نص الموجز الحرفي. `min-height:64px` + `env(safe-area-inset-bottom)`.

### ما لم يُنفَّذ بعد من الموجز (متابَع صراحة، وليس منسياً)
- استخلاص المكوّنات الموحّدة كملفات مستقلة (`MobileHeader`, `SearchInput`, `ServiceCategoryCard`, إلخ) — كل الشيفرة الجديدة أعلاه ما زالت مضمّنة داخل `page.tsx`/`TopNav.tsx`/`MobileMenu.tsx`.
- إعادة بناء صفحة قائمة الإجراءات (`/procedures`) وصفحة تفاصيل الإجراء حسب المواصفات التفصيلية في الموجز.
- إعادة بناء واجهة المحادثة (الهيدر/صندوق الكتابة/الاقتراحات) حسب المواصفات.
- ملف `MOBILE_UI_AUDIT.md` المطلوب صراحة كمُخرَج مستقل.
- تدقيق WCAG 2.2 AA مخصّص للرموز والمكوّنات الجديدة (تباين الألوان الجديدة، حجم أهداف اللمس 44×44، الحركة).
- التحقق البصري الفعلي (لقطات شاشة متعددة القياسات، Lighthouse) — **قيد بيئة العمل نفسه المذكور أعلى الملف**: لا يوجد متصفح فعلي أو Lighthouse في بيئة التنفيذ؛ البديل هو المراجعة الحية على الرابط المنشور بعد كل `push.sh`.

### حالة التنفيذ
🟡 قيد التنفيذ — القسم الأعلى أولوية (الهيدر + الصفحة الرئيسية + شريط التنقّل السفلي، أي "جودة الموبايل البصرية" حسب أولوية المستخدم المعلنة) منفَّذ ومتحقَّق منه عبر `tsc --noEmit` (نظيف بعد كل ملف). الأقسام المتبقية (صفحات الإجراءات، المحادثة، استخلاص المكوّنات) تُكمَل في الدفعات التالية.

### تحديث (batch #367) — بدء `/procedures`: الهيدر + الإحصائيات + رقائق الوزارات + شريط البحث — ✅ جزء منفَّذ

طُبِّقت رموز v4.0 على الجزء الأعلى ظهوراً من صفحة قائمة الإجراءات (`app/procedures/page.tsx`):
- **الهيدر**: من بانر متدرّج عنّابي (خلفية gradient + عناصر شبه شفافة بيضاء + زر تبديل لغة) إلى هيدر مسطّح (سطح أبيض، حدّ سفلي رفيع، عنوان + زر رجوع فقط) — نفس نمط الصفحة الرئيسية الجديدة تماماً. زر تبديل اللغة أُزيل من هنا (متاح فعلياً عبر `MobileMenu`).
- **شريط الإحصائيات الثلاثي**: من بطاقة "مميّزة" بخلفية متدرّجة + ظل ملوّن إلى 3 بطاقات موحّدة الشكل بلا تمييز لوني.
- **رقائق فلترة الوزارات**: من رقائق بحدود سميكة/ظل عند التفعيل إلى رقائق مسطّحة (`var(--brand-soft)` عند التفعيل فقط، بلا ظل).
- **شريط البحث**: من حدّ 1.5px + ظل تركيز مزدوج إلى حدّ 1px بلا ظل، زوايا 14px حسب المقياس المعتمد.
- **صفوف الإجراءات (الحالة المطوية فقط)**: أُزيلت الظلال والتدرّجات من حدود البطاقة، الشارة الأيقونية، وشارات "مُرشدة/عدد الوثائق/عدد الخطوات" — كلها الآن تستخدم `var(--brand-soft)`/`var(--bg)` المسطّحة بدل الألوان شبه الشفافة العنّابية القديمة.

**ملاحظة صريحة عن نطاق لم يُنفَّذ بعد**: الصف الموسّع لكل إجراء "enriched" (عند الضغط على البطاقة) ما زال يعرض ~10 شارات/رقائق مكدّسة في الصف المطوي نفسه (الوزارة، عدد الوثائق، عدد الخطوات، شارة الصعوبة، شارة الأولوية، عدّاد المشاهدات، شارة "نموذج"، شارة الرسوم، وسم الإصدار، جرس التذكير، شارة التقدّم) — هذا يخالف صراحة مطلب الموجز "لا تصميم بطاقات ضخمة / لا رقائق فلترة متكدسة" لصفوف القائمة. إصلاح هذا يتطلّب طيّ أغلب هذه الشارات داخل الحالة الموسّعة (نمط "المزيد" المستخدم سابقاً في batch #353 لأزرار رسائل المحادثة) — قرار نطاق أكبر مؤجَّل لدفعة مخصّصة قادمة، موثَّق هنا بدل تجاهله.

**لم يُلمَس عمداً هذه الدفعة**: منطق الفلترة/البحث بالكامل، وكل المحتوى الداخلي للحالة الموسّعة لكل بطاقة (35+ مكوّناً فرعياً مجمّعة مسبقاً عبر `ProcedureSectionGroup` من دفعات سابقة) — هذا الجزء معقّد وعالي الخطورة لإعادة بنائه ضمن دفعة واحدة غير مُتحقَّق منها بصرياً.

`tsc --noEmit` نظيف بعد كل تعديل.

### تحديث (batch #368) — صفحة تفاصيل الإجراء (`ProcedureDetailClient.tsx`) — ✅ الجزء الأعلى ظهوراً منفَّذ

طُبِّقت رموز v4.0 على صفحة تفاصيل الإجراء الفردية:
- **الهيدر**: من بانر متدرّج + زر تبديل لغة إلى هيدر مسطّح (نفس نمط باقي الصفحات) يعرض عنوان الإجراء مباشرة (بدل عنوان "الدليل" العام) — العنوان لم يعد مكرَّراً كـ `<h1>` ضخم داخل البطاقة الرئيسية لتفادي عنوانين بنفس الوزن البصري في نفس الشاشة.
- **البطاقة الرئيسية**: من ظل + إطار سميك + شارة أيقونية متدرّجة إلى بطاقة مسطّحة بحدّ 1px.
- **الأزرار**: "اسأل دليلك" (أساسي) + "الرئيسية" (ثانوي) — تدرّج/ظل أُزيلا، الزر الثانوي أصبح حدّاً رفيعاً بدل حدّ عنّابي سميك. زر "دليل التنفيذ" أصبح بنفس نمط الزر الثانوي.
- **قسم المستندات المطلوبة وقسم الخطوات**: إطارات/خلفيات الأقسام (`Section`) أصبحت مسطّحة بالكامل؛ دوائر ترقيم الخطوات فقدت التدرّج والظل (لون واحد `var(--brand)`)؛ الشارات الملوّنة (وقت المعالجة بالأصفر، إلخ) وُحِّدت إلى رمادي محايد (`var(--bg)`/`var(--text-2)`) عدا حيث اللون له معنى فعلي (شارة الصعوبة/التعقيد في القائمة، محفوظة كما هي لأنها تحمل معلومة).
- **قسم "تفاصيل إضافية" المطوي** (الجهة/الرسوم): مسطّح بالكامل، وبطاقة الدعوة الختامية لسؤال دليلك فقدت التدرّج والظل.

هذا يستكمل تطبيق الرموز على المسار الأساسي الثلاثي (الرئيسية ← قائمة الإجراءات ← تفاصيل الإجراء) الذي يمثّل غالبية تنقّل المستخدم الفعلي في التطبيق. `tsc --noEmit` نظيف.

### تحديث (batch #369) — واجهة المحادثة (`app/page.tsx`) — ✅ الألوان + التدرّجات

**اكتشاف مهم أثناء التدقيق**: صندوق المحادثة (الهيدر، شريط الكتابة، مؤشر التسجيل، فقاعة "يكتب...") يستخدم متغيّرات CSS محلية معرَّفة داخل `<style>` خاص بهذا الملف: `--red`, `--red-dark`, `--red-light` — وكانت هذه لا تزال مثبَّتة يدوياً على اللون العنّابي **القديم** (`#8F1D2C`/`#741622`) من قبل v4.0، بينما كل صفحة أخرى في التطبيق انتقلت فعلياً إلى رمز v4.0 (`#9F1D2F`/`#861827`). هذا يعني أن زر الإرسال، حالة التسجيل الصوتي النشطة، مؤشر التسجيل، وحدود حقل الإدخال عند التركيز كانت **كلها** ترسم باللون القديم رغم أن باقي التطبيق تغيّر — على الأرجح أحد أسباب شعور "الشكل ما زال قديماً" الذي أبلغ عنه المستخدم. الإصلاح: تحديث الاسم المستعار المحلي ليشير إلى `var(--brand)`/`var(--brand-hover)`/`var(--brand-soft)` الحقيقية بدل قيم لونية ثابتة — يُصحّح هذا تلقائياً كل الاستخدامات في الملف دفعة واحدة.

**تسطيح إضافي**: خلفية شريط الإدخال السفلي (لون التمويه الأبيض مع الشفافية، حُدِّث للون الخلفية الجديد `#F8F8F6`)، خلفية مؤشر "جاري الاستماع" (تدرّج → `var(--brand-soft)` مسطّح)، خلفية رقاقتَي "اقتراح التحسين" (أزرق) و"تحسين الإدخال الصوتي" (كهرماني) (تدرّج → لون واحد مسطّح)، حدود وظل صندوق الكتابة (أُزيل الظل، الحدّ من 1.5px إلى 1px)، دوائر فقاعة "يكتب..." وحدودها (أُزيل الظل)، أزرار التسجيل الصوتي/الإرسال النشطة (أُزيل التدرّج والظل الملوّن)، وزر "المزيد" أسفل كل رسالة (رموز مسطّحة بدل قيم rgba ثابتة).

**لم يُلمَس عمداً**: التكديس الوظيفي أعلى قائمة الرسائل (ChatSummaryCard، مؤقّت الجلسة، رقائق الاقتراح، شريحة ملخّص الجلسة، شريط التثبيت، شريحة تبديل اللغة) — هذه كلها عناصر شرطية الظهور (تظهر فقط بعد عدد رسائل معيّن) وأُعيد تنظيمها بالفعل في دفعات سابقة (batch #362/#364)؛ إعادة هيكلتها بالكامل لتطابق حرفياً "3 اقتراحات بداية فقط، بلا عناصر زخرفية" يتطلّب قرار حذف/دمج وظائف فعلية أكبر من نطاق تسطيح الألوان الآمن لهذه الدفعة، ومؤجَّل لدفعة مخصّصة. أزرار الإرفاق/التحسين/الميكروفون كانت بالفعل بحجم معقول (38×38) وليست "كبيرة الحجم" كما يحذّر الموجز — لا تغيير مطلوب هناك. `tsc --noEmit` نظيف.

### تحديث (batch #370) — 🔴 إصلاح عطل حقيقي: أزرار شريط التنقّل السفلي لا تستجيب على الهاتف

**بلاغ المستخدم**: "شريط التنقّل السفلي" — عند الضغط على أي من أزراره الأربعة، "لا يحدث أي شيء إطلاقاً".

**السبب الجذري المؤكَّد**: كل زر في `BottomNav.tsx` يستخدم `className="bn-tab-btn tap-hit-8"`. صنف `.tap-hit-8` (المعرَّف في `globals.css` ضمن مجموعة `.tap-hit-N`) يضيف طبقة شفافة `::before` بـ `position: absolute; inset: -8px;` لتوسيع منطقة اللمس داخل `@media (pointer: coarse)` (أي أجهزة اللمس تحديداً — الهواتف). التعليق أعلى هذا الصنف في `globals.css` يوضّح صراحة: **"العنصر الذي يستخدم أحد هذه الأصناف يجب أن يملك `position: relative` (أو أي قيمة موضع صريحة أخرى) ضمن الـ style المباشر كي ترتكز ::before عليه بشكل صحيح"**.

زر `BottomNav` لم يكن يملك `position` في كائن الـ `style` الخاص به إطلاقاً. بما أن أقرب سلف له بموضع فعلي هو `<nav>` نفسه (`position: fixed`)، فإن كل طبقات `::before` الأربعة (لكل الأزرار الأربعة) كانت ترتكز جميعها على صندوق الـ `<nav>` الكامل (العرض الكامل للشاشة) بدل صندوق كل زر على حدة — أي أن الطبقات الشفافة الأربعة توسّعت لتغطي شريط التنقّل بأكمله متراكبة فوق بعضها، بدل أن تبقى محصورة بكل زر. هذا يخالف نمط اللمس المصمَّم فعلياً في أي مكان آخر بالتطبيق (`TopNav.tsx` و`MobileMenu.tsx` يطبّقان `position: relative` بشكل صحيح — تحقّقتُ بمقارنة مباشرة).

**نطاق الفحص**: بحثتُ عن كل استخدام لأصناف `tap-hit-N` المضافة هذه الجلسة تحديداً (وليس التي أُضيفت واختُبرت في دفعات سابقة) ووجدتُ **3 مواضع** تعاني من نفس العطل بالضبط:
1. `components/BottomNav.tsx` — أزرار التبويبات الأربعة (الأهم، وهو بلاغ المستخدم الفعلي).
2. `app/procedures/page.tsx` — رقائق فلترة الوزارات (`tap-hit-5`).
3. `app/procedures/page.tsx` — زر مسح البحث (`tap-hit-8`).

**الإصلاح**: أُضيف `position: 'relative'` إلى كائن الـ `style` المباشر لكل من الأزرار الثلاثة أعلاه. لا تغيير في المنطق أو المسارات أو أي شيء آخر — سطر واحد لكل زر.

**ملاحظة أمانة**: لا يمكن التحقق البصري من هذا الإصلاح فعلياً على جهاز هاتف حقيقي من بيئة التنفيذ هذه (قيد بيئة عمل معروف ومذكور أعلى الملف — لا يوجد محاكي حجم شاشة فعلي متاح). التحقق تم عبر: (أ) قراءة الكود المصدري لصنف `.tap-hit-N` وتأكيد مخالفة الشرط الموثَّق صراحة في تعليق الكود نفسه، (ب) مقارنة مباشرة مع مكوّنات أخرى (`TopNav.tsx`, `MobileMenu.tsx`) تستخدم نفس الصنف وتطبّق `position: relative` بشكل صحيح فتعمل بدون مشاكل، (ج) `tsc --noEmit` نظيف. **يُرجى من المستخدم تجربة الشريط السفلي فعلياً على الهاتف بعد تشغيل `push.sh` هذه المرة والتأكيد.**

### تحديث (batch #371) — v4.0: /faq + /authorities + /forms

استُكملت رموز v4.0 المسطّحة على ثلاث صفحات إضافية، بنفس نمط الهيدر المسطّح المطبَّق سابقاً على `/procedures`:
- **`/faq`**: هيدر مسطّح (حُذف زر تبديل اللغة منه)، بطاقات إحصائيات موحّدة، شريط بحث بلا ظل مزدوج، رقائق تصنيف بحدود 1px ونصف قطر 999px، شريط "الأكثر بحثاً" بلون علامة مسطّح، أكورديون الأسئلة بحدود/ظلال مسطّحة، دوائر ترقيم الخطوات مسطّحة (بدل التدرّج).
- **`/authorities`**: نفس نمط الهيدر، بطاقات إحصائيات موحّدة (أُزيلت ميزة "البطاقة المميّزة" ذات التدرّج)، بطاقات الجهات بحدود 1px بلا ظل وبلا حركة `translateY` عند التحويم (استبدلت بتغيير لون الحدّ فقط)، خريطة ألوان أنواع الجهات (`TYPE_COLORS`) حُدِّثت لتستخدم رموز v4.0 الحقيقية بدل الألوان العنّابية القديمة لنوع "council".
- **`/forms`**: نفس النمط، تبويبات العرض الثلاثة (نماذج PDF / الكل / منظّمة) وشريط فلترة الوزارات ورقائق الحالة كلها مسطّحة الآن، بطاقات المعاملات/النماذج بحدود 1px بلا ظل.

في الحالات الثلاث: زر تبديل اللغة أُزيل من الهيدر (بنفس القرار المتّخذ في batch #367 لصفحة `/procedures`) لأن الموجز يمنع وجود عنصر لغة داخل الهيدر على الجوّال. **ملاحظة أمانة**: هذه الصفحات (خلافاً للصفحة الرئيسية) لا تعرض `MobileMenu`، لذا زر تبديل اللغة غير متاح مباشرة من داخلها — يتطلّب العودة للرئيسية أولاً. هذا نفس القرار المتّخذ سابقاً وغير المصحَّح بعد؛ مُسجَّل هنا بدل تجاهله، ويُترك كنطاق متابعة مستقبلي إن رغب المستخدم بإصلاحه.

**لم يُنفَّذ بعد**: `/services` (909 سطر) — مؤجَّلة لحجمها، مسجَّلة كمهمة منفصلة.

`tsc --noEmit` نظيف بعد كل ملف.

### تحديث (batch #372) — v4.0: /services (آخر صفحة رئيسية متبقية)

أُكمل تطبيق رموز v4.0 على `/services` (909 سطر — أكبر صفحة متبقية):
- **الهيدر**: مسطّح بنفس نمط باقي الصفحات (زر رجوع 38×38 + عنوان)، بدون الـ badge الإحصائي القديم في الهيدر (بقي شارة عدد النتائج عند التصفية فقط).
- **شريط البحث**: حدّ 1px بلا ظل مزدوج.
- **بطاقات الإحصائيات** (داخل قسم "نظرة عامة" القابل للطي): وُحِّدت، أُزيلت ميزة "البطاقة المميّزة" ذات التدرّج.
- **رقائق الفئات**: حدود 1px، امتلاء `var(--brand-soft)` فقط عند التفعيل، بلا ظل ملوّن.
- **بطاقات الخدمات في الشبكة**: أُزيل تأثير `translateY` + الظل الملوّن عند التحويم (استبدل بتغيير لون الحدّ فقط)، شارة الأيقونة أصبحت مسطّحة بدل التدرّج.
- **ورقة تفاصيل الخدمة (`ServiceSheet`, bottom sheet)**: نفس المعاملة — شارة الأيقونة والأزرار المسطّحة، زر "اسأل دليلك" الأساسي أصبح `var(--brand)` مسطّح بدل التدرّج، ظل الورقة نفسه (الرمادي المحايد الذي يفصلها عن الخلفية) أُبقي عليه لأنه ظل حقيقي غير ملوّن ويخدم وظيفة رفع الورقة عن الشاشة (مسموح للنوافذ المنبثقة حسب الموجز).

هذا يُكمل تطبيق رموز v4.0 على كل الصفحات الرئيسية في التطبيق (الرئيسية، /procedures وتفاصيل الإجراء، المحادثة، /faq، /authorities، /forms، /services). `tsc --noEmit` نظيف.

هذا الملف سيُحدَّث مع كل دفعة تالية — لا يُعاد كتابته من الصفر، بل تُضاف/تُحدَّث بنوده.
