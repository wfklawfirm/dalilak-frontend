# تدقيق تجربة المستخدم — دليلك (UX_AUDIT.md)

## دفعة #535 — إصلاح فخّ لوحة مفاتيح في زر توسيع ملخص المحادثة (ChatSummaryCard)

### التشخيص
`components/ChatSummaryCard.tsx` (مستورد ومُستخدَم في `app/page.tsx` عند تجاوز 5 رسائل) كان زر التوسيع/الطي لعرض الإحصائيات (أسئلة/ردود/مواضيع) عبارة عن `<div onClick=...>` عادي بلا `role`/`tabIndex`/`onKeyDown` — أي يعمل بالماوس/اللمس فقط. مستخدم لوحة المفاتيح أو قارئ الشاشة لا يستطيع الوصول لهذا العنصر إطلاقاً، ما يجعل لوحة الإحصائيات الموسّعة غير قابلة للوصول كلياً لهم (مخالفة WCAG 2.1.1 Keyboard). المكوّن الشقيق `FeedbackWidget.tsx` يطبّق نفس نمط الواجهة (صف قابل للطي) بشكل صحيح كـ`<button>` حقيقي، ما يؤكد أن هذا تراجع محلي في هذا الملف فقط.

### الإصلاح
لا يمكن ببساطة تحويل الصف كاملاً إلى `<button>` لأنه يحتوي بالفعل زر إغلاق "×" متداخلاً (سيُعيد إنتاج خلل التداخل الذي أُصلح في دفعة #534). الحل: إبقاء الصف الخارجي `<div>` غير تفاعلي، وتحويل منطقة العنوان/الأيقونة/السهم (كل شيء عدا زر الإغلاق) إلى `<button>` حقيقي قابل للتشغيل بلوحة المفاتيح مع `aria-expanded`، كعنصر شقيق حقيقي لزر الإغلاق — نفس النمط المُثبت أصلاً في `FeedbackWidget.tsx` و`ChatSessionSummaryChip.tsx` (دفعة #534).

### التحقق
`npx tsc --noEmit` نظيف.

---

## دفعة #534 — إصلاح `<button>` متداخل داخل `<button>` في ChatSessionSummaryChip

### التشخيص
`components/ChatSessionSummaryChip.tsx` (مستورد ومُستخدَم فعلياً في `app/page.tsx`، يظهر تلقائياً عند تجاوز الجلسة 10 رسائل) كان يحتوي زر إغلاق "✕" متداخلاً داخل الزر الرئيسي "اضغط لتلخيص الجلسة" — أي `<button>` داخل `<button>`، وهذا غير صالح في HTML. المتصفح يُغلق الزر الخارجي تلقائياً فور وصوله للزر الداخلي أثناء التحليل، فتصبح شجرة الـDOM الفعلية مختلفة عمّا يصفه الكود: منطقة النقر الخارجية لا تحيط فعلياً بزر "✕" كما يُفترض، وقارئات الشاشة ترى زرّين متجاورين غير مرتبطين بدل "شريحة + زر إغلاق متداخل". وجود `e.stopPropagation()` على نقرة الزر الداخلي كان دليلاً على أن الكاتب توقّع تداخلاً حقيقياً في الـDOM (bubbling)، ما يؤكد أن هذا خطأ غير مقصود.

### الإصلاح
تحويل العنصر الخارجي من `<button>` إلى `<div role="group">` غير تفاعلي، مع إبقاء الزرّين (التلخيص + الإغلاق) كعنصرين شقيقين حقيقيين بدل متداخلين. نفس السلوك عند النقر، لكن شجرة DOM/accessibility صحيحة الآن.

### التحقق
`npx tsc --noEmit` نظيف.

---

## دفعة #533 — تصحيح رقم مؤسسة مياه بيروت وجبل لبنان في مكوّنين (بيانات اتصال حقيقية)

### التشخيص
نفس وكيل البحث الذي كشف تضارب رقم ISF (دفعة #532) وجد تضارباً مشابهاً لجهة أخرى: "مؤسسة مياه بيروت وجبل لبنان" كانت مدرَجة كـ`01-567000` في `MinistryQuickDial.tsx` وكـ`1650` في `FloatingHelpButton.tsx`.

بيانات اتصال حقيقية وحسّاسة، فلم يُعدَّل شيء قبل التحقق والتأكيد الصريح من المستخدم. تم التحقق عبر بحث ويب (كشف مرشحاً ثالثاً `1713` من مصدر ثانوي)، ثم التأكيد المباشر من **الموقع الرسمي لمؤسسة مياه بيروت وجبل لبنان** (ebml.gov.lb/contactus.php)، الذي ينص حرفياً على صفحة "اتصل بنا" الخاصة به: "الخط الساخن: 1713" — وهو رقم مختلف عن كلا الرقمين الموجودين مسبقاً في التطبيق.

طُرِح السؤال على المستخدم مباشرة مع الأدلة والمصدر، ووافق على التصحيح إلى `1713`.

### الإصلاح
تصحيح الحقل `phone`/`number` لِـ"مؤسسة مياه بيروت" إلى `1713` في الملفين، مع تعليق يوثّق المصدر (ebml.gov.lb) في كل موضع. تم أيضاً تحديث تعليق التوثيق العلوي في `FloatingHelpButton.tsx` الذي كان لا يزال يذكر `1650`. تحقّق (عبر grep على كامل المشروع) من عدم وجود أي موضع ثالث يحتوي هذا الرقم — `ProcedureRelatedMinistries.tsx` لا يحتوي جهة "مياه" أصلاً، فلم يُلمَس.

### التحقق
`npx tsc --noEmit` نظيف. تم التأكيد الصريح من المستخدم قبل التعديل.

---

## دفعة #532 — تصحيح رقم قوى الأمن الداخلي (ISF) في 3 مكوّنات (بيانات اتصال حقيقية)

### التشخيص
تدقيق أوسع (عبر وكيل بحث) كشف تضارباً في رقم طوارئ حقيقي: "قوى الأمن الداخلي (ISF)" كان مدرَجاً كـ`1717` في `MinistryQuickDial.tsx` و`ProcedureRelatedMinistries.tsx`، وكـ`1735` في `FloatingHelpButton.tsx` — ثلاثة مكوّنات، رقمان مختلفان لنفس الجهة المفترضة.

بما أن هذا بيانات اتصال حقيقية وحسّاسة (رقم طوارئ)، لم يُعدَّل شيء قبل التحقق والتأكيد الصريح من المستخدم (وفق القيد الثابت "لا تخمّن تعديل معلومات اتصال حقيقية"). التحقق تم من **الموقع الرسمي لقوى الأمن الداخلي نفسه** (isf.gov.lb/ar/contact-us)، الذي يعرض رابط اتصال مباشر `tel:112` كرقم قوى الأمن الداخلي الرسمي. تأكّد أيضاً (عبر مصدر إخباري مستقل) أن `1717` يخص جهة مختلفة تماماً (الأمن العام)، وهي مُدرَجة أصلاً بشكل صحيح ومنفصل في `MinistryQuickDial.tsx` برقم `01-425610` — أي أن `1717`/`1735` لم يكونا حتى تكراراً لرقم صحيح آخر بل خطأ صريح.

طُرِح السؤال على المستخدم مباشرة مع الأدلة، ووافق على التصحيح إلى `112`.

### الإصلاح
تصحيح الحقل `phone`/`number` لِـ"قوى الأمن الداخلي (ISF)" إلى `112` في الملفات الثلاثة، مع تعليق يوثّق المصدر (isf.gov.lb) في كل موضع. لم تُمَسّ أي جهة أخرى (بما فيها "الأمن العام" ذات الرقم الصحيح أصلاً `01-425610`).

### التحقق
`npx tsc --noEmit` نظيف. تم التأكيد الصريح من المستخدم قبل التعديل (سؤال مباشر عبر الأداة المخصّصة، وليس تخميناً).

---

## دفعة #531 — إصلاح "تحليل مستند" في لوحة تحكم /professional

### التشخيص
تدقيق أوسع (عبر وكيل بحث مخصّص) لكل أنماط "أزرار ميتة" المشابهة لدفعات #523-#530 عبر المشروع كاملاً. وجد أن `app/professional/page.tsx` — ضمن قائمة "إجراءات سريعة" — كان زر "🔍 تحليل مستند" ينفّذ `router.push('/')` فقط، تماماً كأي زر "عودة للرئيسية" آخر، بلا أي إشارة لتشغيل ميزة التحليل الفعلية (لا `sessionStorage`، لا معامل رابط). بقية عناصر نفس القائمة (إنشاء استمارة، إعداد مسودة، إنشاء ملف معاملة) توجّه بشكل صحيح كل واحدة لوجهتها.

### الإصلاح
أُضيف معامل `?openAnalyze=true` يُعالَج في نفس تأثير الرابط الموجود في `app/page.tsx` (بجانب `?reset=`/`?focusChat=`)، ويفتح نافذة `TransactionStarter` (المُعتمَدة أصلاً كمدخل "ابدأ معاملة" في الصفحة الرئيسية) — بحيث يختار المستخدم "تحليل مستند" داخلها فعلياً، وهذا يُشغّل منتقي الملفات عبر نقرة حقيقية داخل النافذة (لا عبر محاولة نقر آلي عن بعد، الذي قد يرفضه المتصفح لغياب "user gesture" حقيقي).

### التحقق
`npx tsc --noEmit` نظيف.

---

## دفعة #530 — السبب الحقيقي لعدم تركيز حقل الكتابة: يوجد حقلان مختلفان وليس واحداً

### التشخيص
بعد نشر دفعة #528 (حلقة إعادة المحاولة)، أُعيد التحقّق مباشرة على الموقع المنشور (بعد التأكد من اكتمال نشر Vercel عبر تغيّر معرّف الإصدار `dpl_...`) — المشكلة استمرت: `document.activeElement` بقي `<body>`. فحص مباشر لعنصر `<textarea>` عبر `document.querySelector('textarea')` أظهر أنه **غير موجود إطلاقاً** في الصفحة عند عدم وجود محادثة نشطة.

السبب الحقيقي: الصفحة الرئيسية تعرض **حقلين مختلفين تماماً** حسب الحالة:
- عندما لا توجد محادثة (`messages.length === 0`): تُعرض شاشة هبوط تحتوي حقل بحث علوي (مربوط بمتغيّر `heroInput`) بلا أي `ref` على الإطلاق، بالإضافة لزر "اسأل دليلك" الذي يرسل رسالة ترحيبية جاهزة عند الضغط عليه.
- عندما توجد محادثة (`messages.length > 0`): يظهر عندئذٍ حقل الكتابة السفلي (`textareaRef`) الذي كانت دفعتا #526/#528 تفترضان وجوده دائماً.

بما أن معظم المستخدمين (وأي اختبار على محادثة فارغة) يقعون في الحالة الأولى، كان `textareaRef.current` يبقى `null` طوال الوقت فعلياً — لا مجرد سباق توقيت كما افتُرض في دفعة #528 (والتي تبقى تحسيناً صحيحاً ومفيداً بحد ذاته لحالة المحادثة القائمة).

### الإصلاح
`app/page.tsx`: أُضيف `heroInputRef` وربط بحقل البحث العلوي، وعُدِّلت حلقة إعادة المحاولة في معالج `focusChat=true` لتجرّب `textareaRef.current || heroInputRef.current` — أي حقل موجود فعلياً في الحالة الراهنة للصفحة.

### التحقق
`npx tsc --noEmit` نظيف.

---

## دفعة #529 — إزالة تجاوزات `onHomeClick` الزائدة على صفحتي تفاصيل المعاملة والنموذج

### التشخيص
أثناء تدقيق كل استخدامات `BottomNav` في المشروع (14 ملفاً) للتأكد من عدم بقاء أي أثر لمشكلة دفعة #526/#528، وُجد أن `app/forms/[slug]/FormDetailClient.tsx` و`app/procedures/[slug]/ProcedureDetailClient.tsx` يمرران `onHomeClick={() => router.push('/')}` — وهذا يتجاوز سلوك `BottomNav` الافتراضي (`routes.home = '/?reset=true'`) الذي تعتمد عليه بقية كل الصفحات. النتيجة: من هاتين الصفحتين تحديداً، زر "الرئيسية" لا يقوم بحفظ-ثم-تفريغ المحادثة (لا "هبوط جديد نظيف")، بخلاف كل صفحة أخرى في التطبيق — تناقض صغير لكنه حقيقي في نفس عائلة المشكلة التي أبلغ عنها المستخدم.

### الإصلاح
أُزيل `onHomeClick` من كلا الملفين، تاركاً `BottomNav` يستخدم مساره الافتراضي `/?reset=true` مثل كل صفحة أخرى. تم التحقق من أن `router` لا يزال مستخدَماً في مكان آخر بكل ملف (لا متغيرات ميتة).

### التحقق
`npx tsc --noEmit` نظيف.

---

## دفعة #528 — إصلاح فعلي لعدم تركيز حقل الكتابة عند الضغط على "اسأل دليلك"

### البلاغ
بعد نشر دفعة #526، أبلغ المستخدم مجدداً: "زر اسال دليلك في اسفل التطبيق على الهاتف تاخذني لنفس المحل عالجه لا زال زر الرئيسية و[اسأل دليلك يتصرفان بنفس الشكل]".

### التشخيص (تحقّق مباشر عبر Chrome MCP على الموقع المنشور)
تأكّد أولاً أن إصلاح دفعة #526 منشور فعلياً (الحزمة المبنية تحتوي `focusChat`/`dalilak_prefill`)، ثم أُعيد إنتاج نقر "اسأل دليلك" من صفحة أخرى (`/procedures`) عدة مرات بطرق مختلفة (نقر مباشر، فتح `/?focusChat=true` مباشرة). في كل مرة: الرابط يعود إلى `/` بنجاح (أي أن الشرط في الكود يُفعَّل)، لكن `document.activeElement` يبقى `<body>` — حقل الكتابة **لا يتم تركيزه فعلياً أبداً**، رغم وجود العنصر في الـ DOM.

عبر حقن مراقب (`monkey-patch`) على `HTMLElement.prototype.focus` و`requestAnimationFrame`، تبيّن:
1. بعد النقر بحوالي 180ms، عنصر `<div>` غير متعلّق بكودنا يستقبل `focus()` — هذا على الأرجح آلية Next.js الداخلية لإدارة التركيز بعد كل تنقّل من نوع App Router (تُحرّك التركيز إلى عنصر مخفي "route announcer" لقارئات الشاشة).
2. استدعاء `requestAnimationFrame` الخاص بنا في الكود يُسجَّل، لكنه **لا يُنفَّذ فعلياً** ضمن نافذة الانتظار — ما يعني أن محاولة تركيز حقل الكتابة (المعتمدة على استدعاء واحد فقط لـ `requestAnimationFrame`) قد تخسر السباق أمام آلية Next.js نفسها، أو قد لا تُنفَّذ إطلاقاً إذا حصل أي تأخير/تقييد على الإطار المطلوب.

النتيجة العملية: لا تُركَّز حقل الكتابة أبداً بعد الضغط على "اسأل دليلك" من صفحة أخرى — لا لوحة مفاتيح تظهر، لا فرق مرئي عن الضغط على "الرئيسية" (خصوصاً حين لا توجد محادثة سابقة لتفريغها) — وهذا يطابق تماماً وصف المستخدم.

### الإصلاح
`app/page.tsx`: استُبدل الاستدعاء الواحد لـ `requestAnimationFrame(() => textareaRef.current?.focus())` بحلقة إعادة محاولة (`tryFocusChat`) تُستدعى أول مرة عبر `requestAnimationFrame` ثم تُعيد المحاولة كل 100ms حتى 10 مرات (حوالي ثانية واحدة كحد أقصى)، وتتوقف فور نجاح التركيز الفعلي (`document.activeElement === textarea`). هذا يضمن الفوز في أي سباق توقيت مع آلية Next.js الداخلية أو أي تأخير آخر، بدل الاعتماد على إطار واحد فقط قد لا يُنفَّذ.

### التحقق
`npx tsc --noEmit` نظيف. لم تُغيَّر منطق `?reset=true` (زر "الرئيسية") ولا أي مسار آخر — فقط آلية تركيز حقل الكتابة الخاصة بـ `?focusChat=true`.

---

## دفعة #527 — إصلاح `activeTab="services"` غير الموجود + `activeTab` مفقود في الإعدادات

### البلاغ
استمراراً لطلب المستخدم الأصلي "دقق في كل الخدمات الباقية و قم بالتدقيق فيها و اصلاحها"، اكتُشفت هذه المشكلة أثناء تدقيق كل استخدامات `BottomNav` في المشروع (فحص إضافي بعد إصلاح دفعة #526).

### التشخيص
`components/BottomNav.tsx` يحتوي فعلياً على 4 تبويبات فقط (`home`, `procedures`, `chat`, `account`) — تبويب `'services'` أُسقط سابقاً ضمن دفعة "تسطيح إلى 4 تبويبات" (v4.0)، والمكوّن يحسب التبويب المُفعَّل عبر `activeTab === tab.id` لكل تبويب. عبر `grep` على كل استخدامات `<BottomNav`، تبيّن أن **5 صفحات** كانت لا تزال تمرر `activeTab="services"` (قيمة لا تطابق أي تبويب حالي)، ما يعني عدم تفعيل أي تبويب بصرياً في شريط التنقل السفلي على هذه الصفحات — عطل بصري صامت بلا أي خطأ في الـ console:
- `app/services/page.tsx`
- `app/services/expat-property/page.tsx`
- `app/authorities/page.tsx`
- `app/drafting-studio/page.tsx`
- `app/faq/page.tsx`

كما تبيّن أن `app/settings/page.tsx` لا يمرر `activeTab` إطلاقاً، فيسقط على القيمة الافتراضية للمكوّن (`'home'`) — ما يُفعِّل تبويب "الرئيسية" خطأً بينما المستخدم فعلياً في صفحة الإعدادات.

### الإصلاح
في الصفحات الخمس أعلاه: تغيير `activeTab="services"` إلى `activeTab="procedures"` (أقرب تبويب حالي لعائلة صفحات "تصفح المعاملات/الخدمات"، مع تعليق توضيحي في كل ملف). في `app/settings/page.tsx`: إضافة `activeTab="account"` (التبويب المرتبط بالحساب/الإعدادات).

### التحقق
`npx tsc --noEmit` نظيف على الملفات الستة. لم تُغيَّر أي مسارات صفحات فعلية ولا منطق تنقّل — فقط قيمة `activeTab` المستخدَمة حصراً للتظليل البصري في شريط التنقل السفلي.

---

## دفعة #526 — إصلاح "الرئيسية و اسأل دليلك نفس الصفحة" في شريط التنقل السفلي

### البلاغ
المستخدم أبلغ: "اكد ان الرئيسية و اسأل دليلك نفس الصفحة عالج الامر باحترافية".

### التشخيص
تأكيد المشكلة في الكود: `components/BottomNav.tsx` يحتوي على 4 تبويبات، اثنان منها ("الرئيسية" و"اسأل دليلك") كانا يشيران لنفس المسار (`route: '/'`)، ودالة `handleTab` كانت تسقط كلاهما على نفس السطر البديل `router.push('/')` في **كل صفحة غير الصفحة الرئيسية نفسها**. الصفحة الرئيسية وحدها كانت تمرر `onHomeClick`/`onChatClick` مخصَّصين لـ`BottomNav` (`onHomeClick`: يحفظ المحادثة الحالية في "الجلسات الأخيرة" ثم يفرّغها لعرض شاشة هبوط جديدة؛ `onChatClick`: لا شيء، لأن المستخدم أصلاً في المحادثة) — لكن من أي صفحة أخرى (`/procedures`, `/services`, `/archive`, إلخ)، كان الزرّان يؤديان لنفس النتيجة الحرفية بلا أي تمييز، ما يجعلهما يبدوان (ويتصرفان) كأنهما نفس الصفحة تماماً.

### الإصلاح
`components/BottomNav.tsx`: بدل السقوط المشترك على `'/'`، أصبح "الرئيسية" يرسل إلى `/?reset=true` و"اسأل دليلك" يرسل إلى `/?focusChat=true` — معالَجان في نفس تأثير `?q=`/`?draft=` الموجود مسبقاً في `app/page.tsx`:
- **`reset=true`**: يُطبّق بالضبط سلوك `onHomeClick` الحالي على الصفحة الرئيسية نفسها (نفس نمط الحفظ-ثم-التفريغ المستخدَم أصلاً في `onNewChat`/`onHomeClick`/`onHome`) — عرض شاشة هبوط جديدة نظيفة.
- **`focusChat=true`**: لا يغيّر المحادثة الحالية إطلاقاً، فقط يُركّز حقل الكتابة ويُمرّر إليه (`scrollIntoView`)، جاهزاً للكتابة فوراً — تماماً كما لو أن المستخدم "موجود بالفعل" في المحادثة.

**نقطة تقنية مهمة تم تفاديها أثناء الإصلاح**: تأثير استرجاع سجل المحادثة وتأثير `?q=/?reset=` كلاهما يُفعَّلان معاً في نفس اللحظة (عند `authChecked`)، وقيمة `messages` داخل تأثير `?reset=` تكون لا تزال القيمة القديمة (قبل تطبيق الاسترجاع) بسبب تأخّر React في تطبيق التحديثات — فاستدعاء `saveChatSession(messages)` مباشرة كان سيحفظ مصفوفة فارغة خاطئة بدل المحادثة الفعلية المستعادة، ما يعني ضياعها بصمت دون أرشفتها في "الجلسات الأخيرة". الحل: قراءة المحادثة مباشرة من `localStorage` (نفس المصدر الذي يقرأ منه تأثير الاسترجاع نفسه) بدل الاعتماد على متغيّر React المتأخر، قبل إفراغ الحالة.

### التحقق
`npx tsc --noEmit` نظيف. لم تُغيَّر أي مسارات صفحات فعلية (لا تزال جميعها `/`, `/procedures`, `/services`, إلخ) — فقط أُضيف استعلام (`query param`) جديد يُقرأ ثم يُزال فوراً عبر `window.history.replaceState`، بنفس النمط المتّبع مسبقاً لـ`?q=` و`?draft=true`. لا تغيير في الـ Backend ولا في منطق المصادقة.

---

## دفعة #524 — توسيع حزمة الاختبارات الآلية: `savedItems.test.ts`
استكمالاً للمسار الكبير الرابع (Big Track 4)، أُضيف ملف اختبار سادس: `lib/__tests__/savedItems.test.ts`، يغطّي `loadSavedItems`/`saveItem`/`unsaveItem`/`isSaved` من `lib/savedItems.ts` (نظام "المفضّلة" المعتمد على `localStorage`) — بما يشمل الحد الأقصى 30 عنصراً (مع الاحتفاظ بالأحدث)، منع التكرار عند حفظ نفس المعرّف مرتين، والتعامل الآمن مع بيانات JSON تالفة (إرجاع `[]` بدل رمي استثناء). استُخدم بديل يدوي بسيط لـ`localStorage` (بدل إضافة `jsdom` كتبعية جديدة) عبر `globalThis.localStorage` في `beforeEach`. كل النتائج المتوقَّعة في الاختبارات تحقَّقت أولاً عبر إعادة تنفيذ مطابقة في Node عبر `mcp__workspace__bash` قبل كتابتها. `npx tsc --noEmit` نظيف (الملف مستثنى أصلاً من فحص البناء عبر `tsconfig.json`).

---

## دفعة #525 — إصلاح "المسودة المولّدة بالذكاء الاصطناعي لا تظهر" + تدقيق أنماط مشابهة

### البلاغ
المستخدم أبلغ: "عندما اطلب صياغة مسودة لا تظهر المسودة المولدة بالذكاء الاصطناعي عالجها و دقق في كل الخدمات الباقية و قم بالتدقيق فيها و اصلاحها".

### التشخيص (تحقّق مباشر على الموقع المنشور)
أُعيد إنتاج المشكلة فعلياً عبر متصفح Chrome حي: تعبئة نموذج "استوديو الصياغة" (`/drafting-studio`) كاملاً، والضغط على "أرسل للمساعد" — النتيجة: الصفحة الرئيسية أظهرت محادثة **قديمة** محفوظة من جلسة سابقة بدل طلب الصياغة الجديد، مع شارة "استعدت محادثة سابقة".

السبب الجذري في `app/page.tsx`: `useEffect`ان منفصلان يُفعَّلان معاً بمجرد أن يصبح `authChecked = true`:
1. تأثير استرجاع سجل المحادثة من `localStorage` — ينادي `setMessages(clean)` (استبدال مطلق كامل للقائمة).
2. تأثير معالجة `?q=`/`?draft=true`/الاستعلام المعلَّق — ينادي `sendMessage()` التي تضيف رسالة المستخدم الجديدة عبر `setMessages(prev => [...prev, ...])`.

كان تأثير `?draft=` (رقم 2) مُعرَّفاً في الكود **قبل** تأثير استرجاع السجل (رقم 1). وبما أن React ينفّذ تأثيرات نفس المكوّن بترتيب تعريفها ضمن نفس دورة الالتزام (commit)، كان الترتيب الفعلي: تُضاف رسالة الصياغة الجديدة أولاً، ثم يُستدعى مباشرة `setMessages(clean)` **المطلق** من تأثير الاسترجاع، فيمحو رسالة الصياغة الجديدة بالكامل ويستبدلها بالمحادثة القديمة المحفوظة — وهذا يحدث فقط لمستخدم عائد لديه سجل محادثة محفوظ فعلاً، ما يفسّر لماذا لم يظهر هذا في كل اختبار سابق.

### الإصلاح
نُقل تأثير استرجاع السجل ليُعرَّف (وبالتالي يُنفَّذ) **قبل** تأثير `?q=`/`?draft=`/الاستعلام المعلَّق، دون أي تغيير في منطق أيٍّ منهما. الآن: `setMessages(clean)` (المطلق) يُنفَّذ أولاً، ثم `setMessages(prev => [...prev, ...])` (التراكمي) من إرسال الصياغة يُنفَّذ فوقه بشكل صحيح — تماماً كإضافة سؤال جديد فوق محادثة موجودة، وهو السلوك الصحيح المتوقَّع أصلاً.

### تدقيق أنماط مشابهة في "الخدمات الباقية"
بحثاً عن نفس نمط "توليد عبر الذكاء الاصطناعي ثم التوجيه للصفحة الرئيسية" في بقية الصفحات، عُثر على علّة إضافية حقيقية وقائمة فعلاً (وليست افتراضية): زر "اسأل الذكاء الاصطناعي" في صفحة تفاصيل النموذج (`app/forms/[slug]/FormDetailClient.tsx` — `handleAskAI`) كان يخزّن الطلب تحت مفتاح `dalilak_prefill` في `sessionStorage` ثم يوجّه لـ`/` — لكن `app/page.tsx` لم يكن يقرأ هذا المفتاح إطلاقاً في أي مكان؛ أي أن الضغط على هذا الزر كان **لا يفعل شيئاً على الإطلاق** (لا رسالة تُرسَل، لا خطأ يظهر). أُضيفت قراءة هذا المفتاح إلى نفس التأثير المُصحَّح أعلاه (بعد فحص `dalilak_pending_query` مباشرة)، بنفس نمط الاستهلاك المستخدَم لبقية المفاتيح. تحقَّق شامل عبر البحث في الكود عن كل استخدامات `sessionStorage.setItem/getItem('dalilak_...')` أكّد أن هذه الثلاثة (`dalilak_draft_prompt`, `dalilak_pending_query`, `dalilak_prefill`) هي الوحيدة، وكلها الآن له مستهلك مطابق في `app/page.tsx` — لا مفاتيح أخرى معلَّقة بلا مستهلك.

### التحقق
إعادة الإنتاج الحي (خطوات فعلية عبر Chrome على الموقع المنشور) أكّدت العلّة قبل الإصلاح. `npx tsc --noEmit` نظيف بعد التعديل. المسارات المباشرة (GuidedFlow/TransactionStarter/ServiceGroupSheet داخل `app/page.tsx` نفسها) غير متأثرة بهذا السباق أصلاً، لأنها تُستدعى من نفس المكوّن المُحمَّل فعلاً (لا إعادة توجيه صفحة)، فتأثير استرجاع السجل يكون قد اكتمل قبل أي تفاعل مستخدم معها. التحقق النهائي من ظهور المسودة فعلياً بعد النشر (Vercel) متروك للمستخدم، لأن هذا يتطلب نشر التغيير أولاً.

---

## دفعة #523 — إصلاح "شريط التنقل السفلي يحتاج عدة ضغطات حتى يعمل"

### البلاغ
المستخدم أبلغ مباشرة: "يوجد صعوبة في الضغط على الأزرار في الأسفل عدة مرات حتى تعمل" — وحدَّد لاحقاً أن المقصود تحديداً هو **شريط التنقل السفلي** (BottomNav)، الظاهر في كل الصفحات.

### التشخيص
`components/BottomNav.tsx` نفسه سليم (أزرار `<button>` حقيقية، `touch-action:manipulation` مطبَّق عالمياً في `globals.css`، `z-index:100` صريح وعالٍ). التحقق الحي عبر `document.elementsFromPoint()` على الموقع المنشور فعلياً كشف تراكباً هندسياً حقيقياً: عنصر `<textarea>` (حقل إدخال المحادثة في `app/page.tsx`) يمتد إلى نفس نطاق الإحداثيات الذي يشغله شريط التنقل السفلي.

السبب الجذري في `app/page.tsx`: مستمع `visualViewport 'resize'/'scroll'` (المقصود منه أصلاً التكيّف مع ظهور لوحة المفاتيح على الموبايل) كان يتعامل مع **أي** تغيّر إيجابي في الارتفاع كـ"لوحة مفاتيح مفتوحة" — لكن هذين الحدثين يُطلَقان أيضاً أثناء التمرير العادي على الموبايل، كلما طوى/فتح شريط عنوان المتصفح نفسه (تغيّر ~40-60px، مقابل لوحة مفاتيح حقيقية ~250-350px+). كل مرة كان هذا يحدث (وقد يتكرر عشرات المرات أثناء تمرير واحد سلس):
1. كان يُزال صنف `.bottom-nav-padding` عن شريط إدخال المحادثة (الحقل المسؤول عن حجز مسافة فوق شريط التنقل السفلي)، فيتمدد حقل النص فعلياً إلى داخل نطاق شريط التنقل.
2. كان يُستدعى `setFooterBottom` بقيمة متغيّرة باستمرار أثناء التمرير، ما يسبب إعادة رسم (re-render) متكررة لصفحة كبيرة (~2600 سطر)، وقد يُحدِث بطئاً حقيقياً في استجابة اللمس لحظياً — وهو بالضبط عرض "أحتاج الضغط عدة مرات" الذي أبلغ عنه المستخدم.

### الإصلاح
تعديل واحد مستهدف في `app/page.tsx` (لا تغيير في `BottomNav.tsx` ولا في أي صفحة أخرى — البحث أكد أن هذا النمط غير مكرَّر في أي ملف آخر): إضافة حد أدنى `KEYBOARD_MIN_OFFSET_PX = 150` قبل اعتبار الحدث "فتح لوحة مفاتيح حقيقية" — أي تغيّر أصغر من ذلك (ضجيج شريط العنوان) يُعامَل الآن كـ`0` (مثل حالة لوحة المفاتيح المغلقة تماماً)، ما يُبقي صنف `.bottom-nav-padding` مطبَّقاً دائماً أثناء التمرير العادي، ويحوّل نداءات `setFooterBottom` المتكررة أثناء التمرير إلى استدعاءات لا-تغيير (React يتجاهل إعادة الرسم عندما تكون القيمة البدائية نفسها). سلوك التكيّف الفعلي مع لوحة المفاتيح الحقيقية بقي كما هو تماماً — لا كسر لأي وظيفة موجودة.

### التحقق
`npx tsc --noEmit` نظيف (صفر أخطاء). لم يُعثر على نمط `visualViewport` مكرَّر في أي ملف آخر بالمشروع (تحقَّق عبر بحث شامل)، فلا حاجة لتكرار الإصلاح. التحقق الحي (`elementsFromPoint`) أكَّد التراكب الهندسي الفعلي قبل الإصلاح؛ إعادة الفحص الكاملة على جهاز لمس حقيقي بعد النشر متروكة للمستخدم (بيئة التنفيذ هنا لا تحاكي شاشة لمس حقيقية أو نافذة عرض موبايل فعلية — تأكَّد ذلك بمحاولات متعددة لتغيير حجم النافذة لم تنجح).

---

## المسار الكبير الثاني (Big Track 2) — قياس Web Vitals حقيقي على الموقع الحي

### ما أمكن قياسه فعلياً (بيانات حقيقية عبر Navigation/Resource Timing API)
حُقن كود JS مباشرة في تبويبات متصفح حقيقية متصلة بالموقع المنشور فعلياً (وليس محاكاة أو تخمين)، واستُخدمت واجهات المتصفح القياسية (`performance.getEntriesByType('navigation'/'resource')`) لقياس: زمن الاستجابة الأولى من الخادم (TTFB)، زمن اكتمال DOMContentLoaded، زمن اكتمال حدث Load الكامل، وحجم/عدد كل الموارد المُحمَّلة (خصوصاً JS بعد فك الضغط، أي `decodedBodySize` لا `transferSize`).

| الصفحة | TTFB | DOMContentLoaded | Load الكامل | إجمالي JS (مفكوك الضغط) | عدد ملفات JS |
|---|---|---|---|---|---|
| `/` (الرئيسية) | 213ms | 2132ms | 4312ms | 1,105 KB | 20 |
| `/procedures` | 258ms | 1862ms | 3511ms | 2,885 KB | 19 |
| `/archive` | 151ms | 886ms | 6320ms | **4,752 KB** | 12 |

**الخلاصة الأهم:** TTFB ممتاز وثابت على كل الصفحات (151–258ms) — لا مشكلة في الخادم/الاستضافة (Vercel Edge) إطلاقاً. المشكلة الحقيقية الوحيدة هي **حجم JS المُرسَل للمتصفح**، وهو يتصاعد بوضوح مع تعقيد الصفحة: الرئيسية أخف (1.1MB)، `/procedures` أثقل بمقدار 2.6x (2.9MB)، و`/archive` هي الأثقل بفارق كبير (**4.75MB مفكوك الضغط**، ضِعف حجم `/procedures` تقريباً) — هذا يؤكد كمياً ما وُثِّق سابقاً نظرياً في دفعة #518 (786kB مضغوط لصفحة `/archive`)، ويرجّح بقوة أن **يبدأ المسار الكبير الثالث (إعادة هيكلة البحث) بصفحة `/archive` أولاً**، وليس `/procedures`، لأنها الأثقل فعلياً بالقياس الحي وليس بالتخمين.

### ما لم يُمكن قياسه، ولماذا (بصراحة كاملة، لا تخمين ولا أرقام ملفَّقة)
- **FCP وLCP وCLS الحقيقية (Core Web Vitals):** لم تُسجَّل أي قيمة رغم عدة محاولات على صفحات متعددة. السبب المؤكَّد تقنياً: كل تبويبات المتصفح الآلي في هذه البيئة تُفتح بحالة `document.visibilityState === 'hidden'` (تأكَّد بالفحص المباشر: `hasFocus()` صحيح أحياناً لكن `visibilityState` يبقى `hidden` دائماً) — أي أنها ليست التبويب الأمامي الفعلي في نافذة Chrome. ومواصفة Paint Timing/Web Vitals الرسمية **تمنع صراحة** تسجيل FCP/LCP لأي تحميل صفحة يبدأ وهو غير مرئي. هذا قيد تقني حقيقي في بيئة التنفيذ، وليس خللاً في كود دليلك نفسه — تأكَّد أن Navigation/Resource Timing (التي لا تعتمد على الرؤية) عملت بشكل طبيعي وأعطت أرقاماً منطقية، بينما Paint Timing تحديداً فشلت لهذا السبب المحدَّد فقط.
- **محاولة بديلة عبر PageSpeed Insights (تُشغِّل Lighthouse حقيقياً على خوادم Google، فلا تتأثر بقيد رؤية التبويب المحلي):**
  - عبر الـAPI المباشر (`pagespeedonline/v5/runPagespeed`): فشل برسالة `429 - Quota exceeded for quota metric 'Queries per day'` — الحصة اليومية المجانية لمشروع Google المستخدَم في هذه البيئة مستنفدة بالكامل (على الأرجح من استخدام آخر مشترَك في نفس المشروع)، وليس بسبب استخدامنا نحن تحديداً.
  - عبر واجهة الويب (`pagespeed.web.dev`): تم تشغيل تحليل فعلي (ظهر معرّف تقرير حقيقي `g2t57yh674` في الرابط، ما يعني أن Lighthouse اشتغل فعلياً على خوادم Google) لكن واجهة عرض النتائج بقيت عالقة على "loading" لأكثر من دقيقتين كاملتين دون عرض أي رقم — على الأرجح تعارض بين جافاسكربت الصفحة وبيئة الأتمتة (headless-like) هنا، وليس خللاً حقيقياً في PageSpeed Insights نفسها.
- **لم تُدَّعَ أي قيمة CWV تخمينية بديلاً عن الفشل** — هذا القسم موثَّق بصراحة بدل تلفيق أرقام Lighthouse وهمية، وفق مبدأ الصدق المتَّبع في هذا الملف منذ بدايته.

### التوصية العملية
لقياس LCP/CLS/INP الحقيقية بدقة كاملة، الطريق الموثوق الوحيد هو تشغيل Lighthouse مباشرة من DevTools على جهاز المستخدم نفسه (تبويب Chrome حقيقي مرئي للمستخدم)، أو زيارة `https://pagespeed.web.dev` يدوياً من متصفح المستخدم الفعلي. الإصلاح الأعمق والمؤكَّد الأثر فعلياً (أرقام JS الحقيقية أعلاه) هو تخفيف حجم `/archive` تحديداً — وهذا بالضبط عمل المسار الكبير الثالث القادم.

### التحقق (Big Track 2)
لا تعديل كود في هذه الدفعة (قياس وتوثيق فقط، بلا تغيير في الكود) — `npx tsc --noEmit` غير مطلوب هنا لعدم وجود أي تعديل ملفات.

---

## المسار الكبير الثالث (Big Track 3) — إعادة هيكلة تحميل بيانات الأرشيف

### التشخيص الدقيق (بعد فحص كود فعلي، لا تخمين)
`lib/archiveDocuments.ts` يخزّن كامل أرشيف الـ5,234 وثيقة كسلسلة JSON مضمَّنة (`ARCHIVE_DOCS_JSON`، سطر واحد ~7.9 ميجابايت خام في الكود المصدري، ~3.47MB نص JSON فعلي بعد فك تشفير السلسلة) تُفسَّر بـ`JSON.parse` عند تحميل الوحدة (module load). هذا التصميم نفسه ليس مشكلة — المشكلة كانت **كيف يُستورَد**:
- `app/page.tsx` و`app/procedures/page.tsx`: يستخدمان بالفعل `await import('@/lib/archiveDocuments')` (استيراد ديناميكي بعد التركيب، دفعة #497/#504) — هذا يفصل الوحدة الثقيلة في حزمة (chunk) منفصلة لا تُحمَّل إلا عند الحاجة الفعلية.
- `app/procedures/[slug]/page.tsx`: **Server Component** (بلا `'use client'`) يستورد `ARCHIVE_DOCS` استيراداً ثابتاً — لكن هذا يعمل فقط وقت البناء/الخادم، لا يضيف أي بايت لحزمة المتصفح (موثَّق ومقصود في دفعة #518، تُرِك بلا أي تغيير).
- **`app/archive/page.tsx` وحدها** كانت الاستثناء: `'use client'` مع استيراد ثابت (`import { ARCHIVE_DOCS, ... } from '@/lib/archiveDocuments'`) في أعلى الملف — هذا يُحمِّل كامل الوحدة الثقيلة (والسلسلة الخام معها) ضمن حزمة JS الأولى للصفحة، وهو السبب المباشر المؤكَّد رقمياً في المسار الكبير الثاني (4.75MB مفكوك الضغط، أثقل صفحة في التطبيق بفارق كبير).

### الإصلاح المُنفَّذ
`app/archive/page.tsx` فقط — لا تعديل على `lib/archiveDocuments.ts` نفسه (صفر مخاطرة على الاستهلاكين الآخَرين):
- استيراد النوع `ArchiveDoc` بقي ثابتاً (`import type { ... }`) — أنواع TypeScript تُحذَف بالكامل وقت الترجمة، صفر تكلفة تشغيلية أو حجمية.
- استيراد البيانات (`ARCHIVE_DOCS`, `ARCHIVE_INSTITUTIONS`, `ARCHIVE_CATEGORIES`, `ARCHIVE_CATEGORY_LABELS`) تحوَّل لاستيراد ديناميكي واحد داخل `useEffect` بعد التركيب (`await import('@/lib/archiveDocuments')`)، مخزَّناً في حالة محلية (`archive` state) — **نفس النمط الذي يستخدمه الملف نفسه بنجاح في app/page.tsx وapp/procedures/page.tsx**، وليس نمطاً جديداً.
- عند عدم اكتمال التحميل بعد: تُعرَض نفس واجهة الهيدر/شريط التنقّل السفلي مع `LoadingSpinner` بدل ظهور "0 وثيقة/لا نتائج" مُضلِّل مؤقتاً.
- كل منطق البحث/الفلترة/العرض (بما فيه `ArchiveAISearch`) بقي مطابقاً 100% للسابق — فقط مصدر البيانات تغيّر من متزامن (ثابت) إلى غير متزامن (بعد التركيب).

### التحقق
`npx tsc --noEmit` نظيف (صفر أخطاء) بعد التعديل. لم يتيسّر تشغيل `next build` كاملاً داخل بيئة التنفيذ الحالية (العمليات الخلفية لا تنجو بين استدعاءات الأداة، وحد الوقت لكل استدعاء 45 ثانية أقل من زمن البناء الفعلي) — التحقق النهائي عبر `npm run build` سيتم على جهاز المستخدم كالعادة (نفس أسلوب كل الدفعات السابقة في هذه الجلسة) قبل الدفع.

---

## المسار الكبير الرابع (Big Track 4) — إعداد حزمة اختبارات آلية (Vitest)

### الوضع السابق
لا يوجد أي test runner في المشروع إطلاقاً (لا Jest ولا Vitest ولا أي شيء) — التحقق كان يعتمد كلياً على `tsc --noEmit` (فحص أنواع فقط، لا يتحقق من صحة المنطق الفعلي) وعلى الفحص اليدوي/الحي عبر المتصفح.

### ما تم تنفيذه
- إضافة **Vitest** (`vitest.config.ts` بسيط، بيئة `node`، بلا أي حاجة لـ jsdom أو React Testing Library بعد — هذه أول دفعة تغطّي فقط منطق منطقي بحت (pure logic) في `lib/` بلا اعتماد على DOM/React).
- إضافة سكريبتَي `npm test` (تشغيل مرة واحدة) و`npm run test:watch` (وضع المراقبة) في `package.json`، وإضافة `vitest` كـ devDependency.
- استُبعدت ملفات الاختبار (`**/__tests__/**`, `**/*.test.ts`) و`vitest.config.ts` من `tsconfig.json` الرئيسي — هذا يمنع أي احتمال لكسر `next build`/`tsc --noEmit` الفعليَين بسبب موديول `vitest` غير المثبَّت بعد على جهاز المستخدم؛ Vitest نفسه لا يحتاج tsconfig هذا لتشغيل الاختبارات (يستخدم esbuild داخلياً).
- كُتِبت أول حزمة اختبارات حقيقية (لا بيانات وهمية) لخمس وحدات منطق بحت في `lib/`، كل حالة اختبار تم التحقق منها يدوياً بتشغيل نفس المنطق فعلياً عبر Node قبل كتابتها (وليس تخميناً):
  - `lib/arabicNormalize.ts` — بما فيها اختبار انحدار (regression) صريح للخطأ التاريخي الموثَّق في تعليقات الملف نفسه (نطاق regex كان يحذف أرقام Arabic-Indic ٠-٩ خطأً).
  - `lib/sanitize.ts` — كشف حقن الأوامر (prompt injection)، قص الطول الأقصى، إزالة أحرف التحكم، وتنظيف مخرجات الذكاء الاصطناعي.
  - `lib/breadcrumbJsonLd.ts` — بنية BreadcrumbList الصحيحة وترتيب العناصر.
  - `lib/plan.ts` — منطق بوابات الخطط (Free/Pro/Trial/Admin) والقيمة الافتراضية الآمنة عند خطة غير معروفة.
  - `lib/archiveDocuments.ts` (`findArchiveDocByTitle`) — على البيانات الحقيقية الكاملة (5,234 وثيقة)، تغطي التطابق التام، التطابق الجزئي بالاتجاهين، والحالات التي يجب أن ترجع بلا نتيجة.

### التحقق
`npx tsc --noEmit` نظيف (صفر أخطاء) على كود التطبيق الفعلي بعد الاستبعاد أعلاه. تشغيل `npm test` فعلياً يتطلب `npm install` أولاً على جهاز المستخدم (تعذّر تثبيت الحزمة داخل بيئة التنفيذ الحالية بسبب امتلاء القرص هناك بالكامل — ليس خللاً في الكود). كل توقّعات (assertions) الاختبارات جرى التحقق منها يدوياً بتشغيل نفس دوال المنطق حرفياً عبر Node قبل كتابة ملفات الاختبار، فليست تخميناً.

---

## دفعات #506–511 — إصلاح بوابة الدخول العامة + تدقيق نهائي + next/image + رؤوس أمان (هذه الجلسة)

### المشكلة الأساسية (#506)
المسار الجذري `/` كان يُحوّل أي زائر غير مسجّل دخول مباشرة إلى `/login` قبل عرض أي محتوى — الموقع العام كان عملياً "شاشة تسجيل دخول" لا منصة. أُصلح: الزائر المجهول يرى الصفحة الرئيسية فوراً بدون تحويل؛ تسجيل الدخول يُطلب فقط عند إرسال سؤال فعلي (`sendMessage`)، مع حفظ نص السؤال في `dalilak_pending_query` لاستكماله تلقائياً بعد الدخول. شاشة البداية (splash) محدودة بسقف 6 ثوانٍ بدل الانتظار غير المحدود لاستجابة الباكيند.

### #507 — قائمة الحساب في TopNav
كانت تعرض زر "تسجيل خروج" مكسوراً (لا يمسح التوكن فعلياً) للزوار المجهولين. استُبدل بروابط دخول/تسجيل حقيقية للمجهولين، وزر خروج فعلي (`clearToken()`) للمسجّلين.

### #508 — حقول تسجيل الدخول/التسجيل
أُضيفت خصائص `name`/`autoComplete`/`inputMode` الصحيحة (كانت ناقصة، ما يمنع مدراء كلمات المرور من التعرّف على الحقول بشكل موثوق)، تلميح شرط كلمة المرور (6 أحرف) أصبح ظاهراً دائماً بدل الظهور بعد الفشل فقط، وأُضيف سطر موافقة على سياسة الخصوصية الحقيقية (بدون رابط شروط وهمي).

### #509–510 — تحقق نهائي + إصلاحات حقيقية من اللينت
بعد تثبيت `eslint` محلياً (لم يكن مثبَّتاً في المشروع أصلاً)، ظهرت 3 أخطاء ESLint حقيقية أُصلحت جميعها: (1) `React.useMemo` في `ProcedureDetailClient.tsx` كان يُستدعى بعد `return` شرطي (مخالفة فعلية لقاعدة rules-of-hooks) — نُقل الفحص داخل الـ`useMemo` نفسه؛ (2) علامات اقتباس غير مُهرَّبة في `DocumentIntelligenceView.tsx`؛ (3) تعليقات `eslint-disable` في `ChatVoiceInputBtn.tsx` كانت تشير لقاعدة `@typescript-eslint/no-explicit-any` غير مُحمَّلة أصلاً في إعداد المشروع (`eslint-config-next@14.2.5` لا يُفعّل بلَغن `@typescript-eslint`) — أُزيلت التعليقات الميتة. كما حوّلت 9 استخدامات لشعار `/logo-icon.png` الثابت من `<img>` إلى `next/image` (تحسين LCP)، تاركةً معاينة الملف المرفوع من المستخدم (`data:` URI) كـ`<img>` عمداً لأن next/image لا يُحسّنها. أصلحت أيضاً 3 تحذيرات `react-hooks/exhaustive-deps` حقيقية في `app/page.tsx`، أهمها: `isAr` كان ناقصاً من اعتماديات معالج رفع الملفات، ما يعني احتمال ظهور رسالة تجاوز حجم 10MB بلغة قديمة مجمّدة بدل اللغة الحالية فعلياً.

### #511 — رؤوس أمان + SEO
أُضيف رأس `Strict-Transport-Security` (كان غائباً، إضافة بلا مخاطر لأن الموقع مخدوم عبر HTTPS فقط على Vercel أصلاً). اكتُشف أن صفحة `/archive` (أكثر من 5200 وثيقة رسمية حقيقية، أُضيفت في دفعات سابقة هذه الجلسة) **لم تكن مدرجة في `sitemap.xml`** رغم وجود `metadata`/`canonical` كاملين لها في `app/archive/layout.tsx` — أُضيفت لملف `sitemap.ts`، وهذه فجوة SEO حقيقية (محتوى عام كبير غير قابل للاكتشاف عبر محركات البحث).

### التحقق
`tsc --noEmit`: نظيف بعد كل تعديل. `npm run lint` و`npm run type-check` و`npm run build` نُفّذت فعلياً على جهاز المستخدم (وليس في بيئة العمل المعزولة) بعد تثبيت `eslint` محلياً، ونجحت (السلسلة `&&` وصلت لخطوات git بنجاح). فُحص الموقع المنشور فعلياً عبر متصفح حقيقي: الصفحة الرئيسية تفتح مباشرة بلا تحويل لتسجيل الدخول، التنقل بالكيبورد (Tab) يُظهر إطار تركيز مرئياً، واتجاه RTL صحيح.

### #512 — تسمية نقاط تنقّل بطاقات الواجهة (Hero Cards carousel) + فحص إمكانية وصول حي
فحص إمكانية الوصول الحي على الموقع المنشور (شجرة إتاحة حقيقية عبر متصفح فعلي، وليس قراءة كود فقط) كشف أن أزرار نقاط التنقّل الخمس أسفل معاينة البطاقات الدوّارة (`HERO_CARDS` في `app/page.tsx`) كانت تحمل `aria-label` ثابتاً بالإنكليزية فقط ("Procedure 1" حتى "Procedure 5") بصرف النظر عن لغة الواجهة الفعلية — أي أن مستخدم قارئ الشاشة في الوضع العربي كان يسمع إعلاناً إنكليزياً مفاجئاً وغير مفيد (رقم عام لا يخبره أي معاملة تنتظره). أُصلح باستخدام العنوان الحقيقي المتوفر أصلاً في نفس بنية البيانات (`titleAr`/`titleEn`) + `aria-current` على النقطة النشطة، وفق نمط تبويبات/carousel القياسي.

**فحص إضافي (بلا تغيير مطلوب):** أداة فحص شجرة الإتاحة أبلغت عن 5 أزرار "بلا اسم" في الصفحة، لكن فحصاً مباشراً على DOM الحي (JS داخل المتصفح) لم يجد سوى زر واحد فعلياً بلا نص وبلا `aria-label` — وهذا الزر تحديداً أصله `<body>` مباشرة (لا أب من مكوّنات دليلك)، بلا أي صنف CSS من نظام التصميم، بلا أي محتوى، وموقعه ملاصق لنص حمولة Next.js الداخلية (`__next_f`) وليس لأي عنصر واجهة حقيقي. هذا نمط نموذجي لعنصر تُدخِله إضافة متصفح (password manager أو أداة مشابهة) في `document.body`، وليس عنصراً من كود دليلك نفسه — لذا لم يُتَّخذ أي إجراء بشأنه، ولا يُعدّ خللاً حقيقياً في التطبيق.

### التحقق (#512)
`npx tsc --noEmit`: نظيف، بلا أخطاء.

### #513 — إصلاح 9 أزرار أيقونة بلا اسم إتاحي (accessible name)
تدقيق يدوي (grep + قراءة كل ملف فعلياً، وليس اعتماداً على نتيجة البحث النصي وحدها لتفادي إيجابيات كاذبة) على كل زر يعرض رمزاً مجرداً (✕ / × / ✓ / ▼) بلا أي `aria-label` أو `title` أو نص مرئي، عبر المكوّنات المستخدمة فعلياً في التطبيق. من أصل 16 ملفاً ظهرت في البحث الأولي، تبيّن أن معظمها كان يملك بالفعل `aria-label` (مثل `AppointmentReminder`، `ProcedureMinistryMap`، `ProcedureRemindMeLater`، `ProcedureOfTheWeek`، `ProcedureNotesPanel`، `ProcedureStepTimer`، `ProcedureCompletionBadge`، `PrintProcedureModal`، `GovHolidayAlert`) — إيجابيات كاذبة من قصر نافذة السياق في البحث الأولي، فلم تُلمَس. الثغرات الحقيقية المؤكَّدة والمُصلَحة:
- `ChatHistoryPanel.tsx`: زر حذف محادثة محفوظة فردية (✕) بلا تسمية + إضافة `aria-expanded` لزر طيّ/فتح اللوحة.
- `SmartReminder.tsx`: زرَّا "تعليم كمنجز" و"حذف" لكل تذكير مستحق (بلا تسمية، والتسمية تتضمن الآن عنوان التذكير الفعلي)، وزر طيّ/عرض "القادمة" (▲/▼) بلا `aria-expanded`/`aria-label`.
- `ProcedureSearchModal.tsx`: زر مسح نص البحث (×) بلا تسمية.
- `ProcedureStartButton.tsx`: زر "إلغاء تعيين كمبدوءة" كان يملك `title` فقط (غير موثوق دائماً لقارئات الشاشة) — أُضيف `aria-label` مطابق للنمط الآمن المُتَّبع في بقية الملف.
- `HomepageChatSuggestionsBar.tsx`: زر إغلاق شريط الاقتراحات السريعة بلا تسمية.
- `ChatSummaryCard.tsx`: زر إغلاق بطاقة ملخص المحادثة بلا تسمية.
- `MinistryQuickDial.tsx`: زر إغلاق قائمة أرقام الوزارات (bottom sheet) بلا تسمية.
- `NotificationBell.tsx`: زر إغلاق قائمة التنبيهات المنسدلة بلا تسمية (زر حذف تنبيه فردي كان يملك تسمية بالفعل من دفعة سابقة #501).

### التحقق (#513)
`npx tsc --noEmit`: نظيف، بلا أخطاء، بعد تعديل 8 ملفات مكوّنات.

### #514 — خط Cairo عبر next/font/google بدل رابط Google Fonts الخارجي
تحذير ESLint حقيقي ظهر في مخرجات `npm run lint` الفعلية على جهاز المستخدم لم يُعالَج بعد: `app/layout.tsx` كان يجلب خط Cairo عبر `<link rel="stylesheet" href="https://fonts.googleapis.com/...">` — طلب شبكة خارجي يُعطّل عرض الصفحة (render-blocking) وهو بالضبط ما تحذّر منه قاعدة `@next/next/no-page-custom-font`. أُصلح باستخدام `next/font/google` (نفس عائلة الخط Cairo، نفس الأوزان بالضبط 400–900، نفس `display:'swap'`) الذي يستضيف الخط ذاتياً وقت البناء بدل طلب شبكة خارجي — لا تغيير مرئي في الخط نفسه، فقط في آلية تحميله. الخط مُطبَّق عبر CSS variable ‎`--font-cairo`‎ على `<html>` ومُستخدَم بالاسم في `fontFamily` الخاص بـ`<body>`، فبقيت السلسلة الاحتياطية (`Inter`, `sans-serif`) كما كانت.
**لم يُلمَس** `app/globals.css` (يستورد أيضاً Cairo بأوزان مختلفة + IBM Plex Sans Arabic فعلياً من Google Fonts) لأن IBM Plex Sans Arabic هو الخط الفعلي المطبَّق صراحة (inline style) في صفحات الدخول/التسجيل ومكوّنات الطباعة — دمج نظام الخطوط بالكامل قرار أكبر يستحق تدقيقاً مستقلاً، وليس تعديلاً عرضياً ضمن دفعة صغيرة. لذا أُبقيت وسوم `preconnect` في `<head>` (ما زالت تُسرِّع طلب globals.css الفعلي لخط IBM Plex Sans Arabic).

### التحقق (#514)
`npx tsc --noEmit`: نظيف. **ملاحظة صادقة:** هذا التغيير يتطلّب اتصال إنترنت فعلياً وقت `npm run build` (next/font يجلب ملفات الخط من Google وقت البناء ليُضمّنها محلياً) — لم يُتحقَّق من نجاح هذه الخطوة فعلياً إلا عبر `tsc` (فحص أنواع فقط، لا يبني الخط). التحقق الحقيقي الوحيد الموثوق هو نتيجة `npm run build` على جهاز المستخدم.

**تحقُّق لاحق (بعد push فعلي):** بناء المستخدم الحقيقي نجح لاحقاً (71/71 صفحة، commit `8fb8f1d`)، مؤكِّداً أن next/font/google جلب الخط بنجاح وقت البناء — لا حاجة لمزيد من التحقق هنا.

### #515 — إغلاق قائمة التنبيهات (NotificationBell) بمفتاح Escape
دفعة #344 غطّت 5 مكوّنات modal/sheet بمفتاح Escape لكنها فاتت هذه القائمة المنسدلة تحديداً (كانت تُغلَق بالنقر خارجها فقط). أُضيف مستمع `keydown` حقيقي يُغلق القائمة عند Escape ويُعيد التركيز (focus) لزر الجرس نفسه — نمط قياسي لقارئي الشاشة ومستخدمي لوحة المفاتيح حتى لا يفقدوا مكانهم بعد الإغلاق. أُضيف أيضاً `aria-expanded` على زر الجرس (كان غائباً).

### التحقق (#515)
`npx tsc --noEmit`: نظيف، بلا أخطاء.

### #516 — MinistryQuickDial: role="dialog" + حصر التركيز (focus trap)
هذا الشيت (20 صفاً قابلاً للنقر + حقل بحث) كان غائباً بالكامل عن قائمة الـ13 مكوّن التي غطّتها دفعة #360/#361 (`lib/useFocusTrap.ts`) — على الأرجح لأنه أُعيد بناؤه/فتحه عبر حدث مخصّص بعد تلك الدفعة. لم يكن يحمل `role="dialog"`/`aria-modal` أصلاً، ولم يكن Tab محصوراً داخله — مستخدم لوحة مفاتيح كان يستطيع الخروج بـTab إلى محتوى الصفحة المخفي خلف الخلفية الداكنة. أُضيف نفس النمط المستخدم في `MobileModeSheet`/`ServiceGroupSheet` بالضبط: `useFocusTrap` من نفس hook المشترك + `role="dialog"` + `aria-modal="true"` + `aria-label` + `tabIndex={-1}`. Escape كان يعمل مسبقاً ولم يُمَس.

### التحقق (#516)
`npx tsc --noEmit`: نظيف، بلا أخطاء.

### #517 — KeyboardShortcutsHelp: نفس ثغرة role="dialog" + حصر التركيز
نفس فجوة #516 بالضبط: نافذة اختصارات لوحة المفاتيح (تُفتَح بمفتاح `?`، مُركَّبة فعلياً في `app/page.tsx`) لم تكن ضمن القائمة المدقَّقة في `lib/useFocusTrap.ts`. أُضيف نفس النمط: `useFocusTrap` + `role="dialog"` + `aria-modal="true"` + `aria-label` + `tabIndex={-1}`. **لم تُلمَس** `UserOnboarding.tsx` عمداً — قرار سابق موثَّق صراحة في `push.sh` (دفعة أقدم): كونها ويزارد إجباري لأول زيارة، فإن السماح بتخطّيها عبر Escape قرار منتج (product decision) وليس خللاً في إمكانية الوصول، فلا يجوز حسمه بصمت دون سؤال المستخدم.

### التحقق (#517)
`npx tsc --noEmit`: نظيف، بلا أخطاء.

### #518 — نقل مطابقة وثائق الأرشيف لصفحة المعاملة إلى الخادم (أداء حقيقي كبير)
فحص أحجام حزم `npm run build` الفعلية (بعد نجاح بناء المستخدم في دفعة #514) كشف أن `/procedures/[slug]` كان يحمّل **804kB** JS للتحميل الأول — أكبر مسار في التطبيق بفارق كبير عن بقية الصفحات (معظمها 90–300kB)، و`/archive` نفسه 786kB. السبب الجذري في حالة `/procedures/[slug]`: `ProcedureDetailClient.tsx` (مكوّن `'use client'`) كان يستورد **كامل** مصفوفة `ARCHIVE_DOCS` (ملف مصدر 7.9 ميجابايت، 5,234 وثيقة) فقط لتصفيتها إلى ≤5 نتائج ذات صلة بكل معاملة — أي أن كل صفحة معاملة (28+ صفحة مُولَّدة ثابتاً) كانت تشحن كامل أرشيف الوثائق إلى المتصفح لعرض 5 عناصر منه فقط.

**الإصلاح:** `app/procedures/[slug]/page.tsx` مكوّن خادم (Server Component) أصلاً (`generateStaticParams`)، فنُقلت دالة المطابقة (`findRelatedArchiveDocs`) وحساب النتيجة إليه بالكامل — تعمل وقت البناء/الخادم فقط، ولا يعبر حدود الخادم/المتصفح سوى المصفوفة الصغيرة المُصفّاة (≤5 كائنات) كـ`prop` عادي لـ`ProcedureDetailClient`. أُزيل استيراد `ARCHIVE_DOCS` وuseMemo بالكامل من المكوّن العميل (استُبدل باستيراد `type ArchiveDoc` فقط، بلا أي تكلفة وقت تشغيل). نفس منطق المطابقة والترتيب والقص بالضبط — تحسين أداء بحت، لا تغيير في السلوك أو النتائج المعروضة.

**لم تُلمَس** `/archive` (786kB) — هذه الصفحة تحتاج فعلياً بحثاً حياً عبر كل الوثائق داخل المتصفح (بحث فوري أثناء الكتابة عبر 5,234 عنصراً)، فحجم حزمتها الكبير جزء من وظيفتها الفعلية وليس تكراراً أو خللاً، وإصلاحه (مثلاً تحويل البيانات لملف JSON يُجلَب عبر fetch بدل import) قرار معماري أكبر يستحق دفعة مستقلة.

### التحقق (#518)
`npx tsc --noEmit`: نظيف، بلا أخطاء. **تحقّق حقيقي متبقٍّ:** فرق الحجم الفعلي في `npm run build` (804kB → المتوقَّع أن ينخفض بشكل كبير) لم يُقَس بعد إلا عبر جهاز المستخدم — سيظهر في مخرجات البناء القادمة.

### #519 — المسار الكبير الأول: تدقيق WCAG 2.2 AA عبر axe-core على الموقع الحي
تنفيذاً لتوجيه "ابدأ بالمسارات الكبيرة باحترافية"، حُقن axe-core (v4.9.1) مباشرة في تبويب متصفح حي متصل بالموقع المنشور (وليس فحصاً نظرياً على الكود فقط)، وشُغِّل على الصفحة الرئيسية و`/procedures` بمعايير wcag2a/aa + wcag21a/aa + wcag22aa.

**اكتشاف جانبي مهم قبل أي إصلاح:** تبويب المتصفح كان مفتوحاً منذ بداية هذه الجلسة الطويلة دون إعادة تحميل صلبة (hard reload) منذ عدة عمليات نشر لاحقة — ما جعل axe يُبلغ بثغرة قديمة (تسمية "Procedure 1" الإنكليزية من قبل دفعة #512) رغم أن الكود المنشور فعلياً يحوي الإصلاح الصحيح (تحقّقتُ بجلب محتوى ملفات JS المبنية مباشرة عبر `fetch` وليس عبر DOM المتصفح المهترئ). إعادة تحميل صلبة للتبويب حلّت المشكلة فوراً — **درس للمستقبل:** أي ملاحظة على الموقع الحي في جلسة طويلة يجب أن تُسبَق بإعادة تحميل صلبة، وإلا فالنتائج قد تعكس حالة قديمة من الصفحة لا الكود المنشور فعلياً.

**ثغرة مؤكَّدة وليست من كود دليلك:** `button-name` على عنصر `#open-side-panel` — تحقّقتُ عبر فحص DOM حي (لا سمات دليلك، لا صنف من نظام التصميم، طفل مباشر لـ`<body>`، ملاصق لعنصر `DIV#thunderbit-crx-side-bar`) أن هذا ناتج عن إضافة متصفح مثبَّتة في بيئة الفحص ("Thunderbit")، وليس خللاً في التطبيق — لم يُتَّخذ أي إجراء بشأنه (نفس نمط الاكتشاف في دفعة #512).

**3 ثغرات حقيقية أُصلحت:**

1. **تباين لوني (1.4.3)** — نص عنوان فرعي أبيض فوق خلفية العلامة الحمراء في بطاقات المعاينة الدوّارة (`HERO_CARDS`, `app/page.tsx`) كان بشفافية 0.65 (نسبة تباين 4.07:1، دون الحد الأدنى 4.5:1). حُسبت النسبة الدقيقة عبر صيغة W3C الرسمية للسطوع النسبي (luminance) داخل كود JS محقون في المتصفح فعلياً، واختيرت شفافية 0.8 (نسبة ≈5.48:1، هامش أمان مريح).
2. **حجم هدف اللمس (2.5.8 — WCAG 2.2 AA جديد)** — نقاط التنقّل الخمس أسفل نفس البطاقات كانت 6×6px بفجوة شبه معدومة بينها — أقل بكثير من الحد الأدنى 24×24px المطلوب لأي نوع مؤشر (فأرة أو لمس، ليس اللمس فقط كصنف `.tap-hit-N` الحالي المقتصر على `@media (pointer:coarse)`). أُضيف صنف CSS جديد غير مشروط `.wcag-target-8` (نفس نمط `::before` الموسِّع لمساحة النقر المستخدم أصلاً في `.tap-hit-N`)، مع رفع حجم النقطة إلى 8px والفجوة إلى 16px — حسابات دقيقة (`8 + 2×8 = 24px` بالضبط، والفجوة 16px تمنع تداخل منطقتَي نقر متجاورتين وهو خطر حقيقي لو كانت الفجوة أضيق من ضعف مقدار التوسيع).
3. **تعشيش عناصر تفاعلية (`nested-interactive`) — 71 عنصراً على `/procedures`، الأخطر بينها.** كل بطاقة معاملة كانت تضع الرأس بالكامل داخل `<button>` واحد (نقرة توسيع/طي)، وهذا الزر نفسه يحتوي زرَّين حقيقيَّين آخرين بداخله فعلياً: `ProcedurePriorityTag` و`ProcedureReminderBell` (تأكَّد ذلك عبر `grep -c "<button"` على كل مكوّنات الشارات السبعة المستخدَمة في الصف — فقط هذان يحتويان أزراراً حقيقية، الباقي شارات عرض بحتة بلا تفاعل). هذا مخالف صريح لمواصفة HTML5/ARIA (لا يجوز تعشيش عنصر تفاعلي داخل آخر)، وقد يسبب أيضاً سلوكاً غير موثوق فعلياً في بعض المتصفحات (قد يُصحِّح المحلِّل الفعلي للـHTML إغلاق الزر الخارجي تلقائياً عند بدء الزر الداخلي، فيصبح DOM الفعلي مختلفاً عمّا كُتِب في JSX).
   **الإصلاح المطبَّق (نمط "الهدف الممدود / stretched target" القياسي، يُستخدَم أيضاً في مكتبات مثل Bootstrap's `.stretched-link`):** حُوِّل الزر الخارجي إلى زر خفي واحد فقط (`position:absolute; inset:0`) يغطي كامل الرأس ويحمل `aria-expanded`/`aria-label` الوحيدين لفتح/طي البطاقة. محتوى الرأس المرئي أصبح `<div>` عادياً غير تفاعلي بـ`pointer-events:none` (فتنتقل أي نقرة فيه تلقائياً للزر الخفي خلفه) **باستثناء** الشارتين الحقيقيتين (`ProcedurePriorityTag`/`ProcedureReminderBell`) اللتين لُفَّتا بـ`<span>` صغير بـ`pointer-events:auto` و`z-index` أعلى — فتبقيان قابلتين للنقر بشكل مستقل تماماً كما كانتا، دون أي تغيير بصري أو في التخطيط. تحقّقتُ أيضاً أن كلا المكوّنين لا يعتمدان على `stopPropagation()` (لم يكونا يحتاجانه أصلاً)، فلا تأثير جانبي على سلوكهما.

**إصلاح تباين إضافي مُطبَّق ثم مُوسَّع لنطاق أكبر:** لوّن `#918B82` في `ChatHistoryPanel.tsx` (نسبة ≈2.9:1، فاشلة) كان أول مثال مُصلَح يدوياً باستخدام المتغيّر الموجود أصلاً والمُدقَّق سابقاً `--text-3` في `app/globals.css` (مُعلَّق عليه صراحة "small-caption/meta only — WCAG AA verified"، نسبة ≈4.9:1+). بعد ذلك اكتُشف أن نفس اللون الفاشل مكرَّر **178 مرة عبر 47 ملفاً** في كامل المشروع — فجوة نظامية حقيقية وليست حالة معزولة. نُفِّذ تدقيق ونقل شامل (عبر وكيل فرعي مخصَّص للتحقق من سياق الخلفية قبل كل استبدال، تفادياً لجعل التباين أسوأ فوق خلفيات داكنة لو وُجدت):
- **172 استبدالاً مباشراً وآمناً** إلى `var(--text-3)` عبر 45 ملفاً — جميعها تأكَّد وجودها فوق خلفيات فاتحة (أبيض/`--surface`/`--bg`/تدرّجات علامة باهتة)، فلا توجد أي حالة فوق خلفية داكنة في كامل المشروع لهذا اللون تحديداً.
- **4 حالات استثناء مقصودة أُبقيت باللون الخام** في `DocumentIntelligenceView.tsx` و`ProcedureFlowchart.tsx`: اللون يُستخدَم فيها فقط لبناء خلفية شفافة (`` `${lonColor}14` ``-style)، وليس كلون نص — لا يمكن استبداله بـ`var()` لأن الدمج النصي مع `var()` ينتج CSS غير صالح. لون النص الفعلي في هذين الملفين (متغيّر منفصل، `confColor`/`STATUS_TEXT_COLORS`) كان مصلَحاً فعلياً بـ`var(--text-3)` بشكل صحيح.
- حالة خاصة في `ProcedurePrintSummary.tsx`: نافذة طباعة منفصلة (`document.write`) بلا وصول لمتغيّرات `globals.css`، فاستُخدمت القيمة الصريحة `#76716A` (نفس قيمة `--text-3`) بدل المتغيّر.
- `Breadcrumbs.tsx` كان يستخدم أصلاً `var(--text-3, #918B82)` (قيمة احتياطية) — بُسِّطت إلى `var(--text-3)` فقط.

### التحقق (#519)
`npx tsc --noEmit`: نظيف، بلا أخطاء، بعد كل تعديل (تحقُّق مباشر بعد إصلاح `/procedures`، وبعد كل مرحلة من نقل ألوان `#918B82`، وتحقّق نهائي شامل). تأكَّد يدوياً عبر `grep` أن الحالات الأربع المتبقية لـ`#918B82` في الكود هي فعلاً استخدامات خلفية شفافة مقصودة لا أخطاء منسية.

### #519 (تتمة) — تحقّق حي بعد النشر الفعلي + إصلاح ذاتي لخلل أُدخِل عن طريق الخطأ + إكمال الفحص على بقية الصفحات
بعد أن أكّد المستخدم نجاح البناء والنشر (commit `c64dd37`)، أُعيد فحص `/procedures` على الموقع الحي فعلياً (تبويب جديد تماماً، بلا خطر التبويب القديم المتجمّد الذي وُثِّق أعلاه) للتأكد أن إصلاح `nested-interactive` فعلاً حيّ ولم يكسر شيئاً:

- **تأكيد النجاح:** ثغرة `nested-interactive` (71 عنصراً) **اختفت بالكامل** من نتائج axe-core بعد النشر — الإصلاح يعمل فعلياً على الموقع الحي، وليس فقط في التحليل النظري.
- **خلل ذاتي اكتُشف وأُصلح فوراً:** الفحص نفسه كشف ثغرة **جديدة** لم تكن موجودة قبل هذه الدفعة: `aria-hidden-focus` (38 عنصراً). السبب: عند إعادة هيكلة رأس البطاقة، وُضِع `aria-hidden="true"` على الـ`<div>` المرئي (بدافع "هذا محتوى بصري مكرَّر، أخفِه عن قارئ الشاشة لتفادي ازدواج الإعلان") — لكن هذا كان قراراً خاطئاً: ذلك الـ`div` يحمل معلومات حقيقية (اسم الوزارة، عدد الوثائق/الخطوات، شارة الصعوبة، العنوان، إلخ) **غير متوفرة في أي مكان آخر** لمستخدم قارئ الشاشة، وليست مجرّد تكرار بصري. الأخطر: عنصرا `ProcedurePriorityTag`/`ProcedureReminderBell` القابلان للتركيز (focusable) كانا لا يزالان داخل هذا الـ`div` — أي أن مستخدم لوحة المفاتيح كان سيصل إليهما بـTab بينما قارئ الشاشة "يتجاهلهما" تماماً بسبب `aria-hidden` على أب لهما، وهذا فعلياً **أسوأ من الثغرة الأصلية** (محتوى مخفي عن التقنية المساعدة رغم كونه قابلاً للوصول بلوحة المفاتيح — تناقض في السلوك). أُزيل `aria-hidden="true"` فوراً بعد اكتشافه بفحص axe مباشر على الموقع الحي، قبل أي إعلان بأن الدفعة منتهية.
- **درس للعمل المستقبلي:** `pointer-events: none` (المستخدَم في نفس هذا الـ`div` لتمرير النقرات للزر الخفي خلفه) **لا علاقة له بشجرة إمكانية الوصول إطلاقاً** — يؤثر فقط على اختبار النقر بالماوس/اللمس. الخلط بين الاثنين (وهو ما حدث هنا) خطأ سهل الوقوع ويستحق الانتباه في أي إصلاح مشابه لاحقاً.

**فحص إضافي لبقية صفحات الموقع (تدقيق axe-core حي، تبويب جديد لكل صفحة):**
- `/archive`، `/forms`، `/faq`: نظيفة تماماً — الثغرة الوحيدة في الثلاثة هي نفس عنصر إضافة Thunderbit المؤكَّد سابقاً (`#open-side-panel`)، وليس خللاً في كود دليلك.
- واجهة المحادثة (بعد إرسال سؤال فعلي وظهور رد حقيقي): ثغرة واحدة حقيقية جديدة — `target-size` على زر إغلاق شريحة "تحدث بالإنجليزية" (`ChatLanguageToggleChip.tsx`، بُنيت في الدفعة #364). القياس الفعلي: 12.4×15px فقط (الحد الأدنى المطلوب 24×24). أُصلح بإعطاء الزر حجماً صريحاً 24×24px (بدل الاعتماد على `padding:2px` + خط 11px)، مع رفع المسافة بين هذا الزر وشريحة "تحدث بـ..." المجاورة من 4px إلى 10px لتفادي تداخل منطقتَي النقر المتوسّعتين (نفس نوع الخطر المذكور سابقاً في إصلاح نقاط تنقّل البطاقات، ولكن هنا الحل الأنسب كان تكبير الصندوق الفعلي مباشرة بدل حيلة `::before` لأن العنصر مجاور مباشرة لعنصر آخر).
- **ثغرتان إضافيتان حقيقيتان اكتُشفتا على `/procedures` بعد التأكد من نجاح النشر** (لم تكونا مرئيتين في الفحص الأول لأن التبويب حينها كان لا يزال على النسخة القديمة قبل اكتمال النشر):
  1. فاصل قسم "إجراءات موثّقة" (بين مجموعتَي الإجراءات) كان يستخدم خلفية `#F5F0EB` (كريمي أغمق قليلاً من الخلفيات المُدقَّقة) مع `var(--text-3)` — نسبة تباين 4.27:1 فقط (فشل، رغم أن نفس المتغيّر ناجح على كل الخلفيات الأخرى في الموقع). أُصلح بتحويل الخلفية إلى `var(--bg)` (الخلفية الافتراضية شبه البيضاء المُدقَّقة أصلاً)، بدل تغيير لون النص نفسه — يحافظ على نفس التناسق البصري تقريباً مع إصلاح جذر المشكلة (اختيار خلفية غير مدقَّقة، لا اللون).
  2. شرائح تصفية الوزارات (`ProcedureTagSearch.tsx`): رقم العدّاد `(count)` بجانب كل اسم وزارة كان يُعرَض بـ`opacity: 0.6` (أو 0.7 لشريحة "الكل") — وهذا **مزج بصري بحت يُقلّل التباين الفعلي** حتى لو كان لون النص الأساسي (من مصفوفة `COLORS` الست) مصمَّماً بتباين كافٍ عند الشفافية الكاملة. أسوأ حالة قِيست: 2.93:1 (فشل واضح). أُزيلت خاصية `opacity` من كلا العدّادين بدل تغيير الألوان الستة نفسها — إصلاح أبسط وأعمّ يغطي كل ألوان `COLORS` دفعة واحدة، ولا خسارة وظيفية أو بصرية تُذكر (العدّاد يبقى مقروءاً بوضوح أكبر فقط).

**اكتمال Big Track 1:** فُحصت أيضاً `/procedures/passport` (صفحة تفاصيل معاملة فردية عبر `[slug]`، محتوى حقيقي "جواز السفر اللبناني" — تأكَّد أن هذا المسار الديناميكي منفصل عن أكواد `proc-imu11-01` الخاصة بقائمة الـ466 إجراء المُغني، وله بيانات (`PROCEDURES_DATA`) ومسارات (`sitemap.xml`) خاصة به) — نظيفة تماماً، الثغرة الوحيدة هي Thunderbit المعروفة. بهذا اكتملت جولة axe-core الحية على كل الصفحات العامة الرئيسية (الرئيسية، `/procedures`، `/procedures/[slug]`، `/archive`، `/forms`، `/faq`، والمحادثة بحالة رسائل حقيقية) بلا أي ثغرة حقيقية متبقية غير مُصلَحة. **Big Track 1 مكتمل** — الخطوة التالية هي Big Track 2 (قياس Web Vitals الحقيقي).

### التحقق (#519 تتمة)
`npx tsc --noEmit`: نظيف بعد كل تعديل من هذه الدفعة الإضافية (إزالة `aria-hidden`، إصلاح خلفية الفاصل، إصلاح شرائح التصفية، إصلاح حجم زر إغلاق شريحة اللغة).

هذا الملف تدقيق حي (living document) لتجربة الاستخدام في تطبيق دليلك، يُحدَّث مع كل دفعة تنفيذ فعلية في الكود — وليس تقريراً نظرياً منفصلاً عن التنفيذ. كل بند فيه يعكس تغييراً حقيقياً حدث أو قراراً واعياً بتأجيله، مع السبب.

**ترتيب الأولوية المعتمد في كل قرار (كما طُلب):**
الوضوح ← البساطة ← سهولة إنجاز المهمة ← الوصول (Accessibility) ← الأداء ← الجمال البصري.

**قيد بيئة العمل (يجب معرفته قبل قراءة أي نتيجة "تحقق"):** بيئة التنفيذ الحالية (sandbox) لا تملك خادم تطوير (`npm run dev`) ولا Playwright ولا Lighthouse ولا متصفحاً حقيقياً لالتقاط لقطات شاشة عند 375/430/768/1440px. لذلك كل "تحقق" في هذا الملف هو تحقق على مستوى الكود (قراءة الملفات فعلياً، تتبّع الحالة، `tsc --noEmit`) وليس تحقق مرئي أو قياس Lighthouse فعلي. أي رقم Lighthouse أو لقطة شاشة مطلوبة في الموجز الأصلي **لا يمكن إنتاجها هنا فعلياً** — البديل المتّبع هو المراجعة الحية على الرابط المنشور بعد كل `push.sh`.

---

## المرحلة 1 — الأزرار العائمة (Floating Buttons) — ✅ منفَّذ

### المشكلة
كان التطبيق يعرض حتى **7 عناصر عائمة دائمة الظهور** فوق بعضها في نفس الزاوية على كل صفحة: `GlobalLangSwitch` (تبديل اللغة)، `AccessibilityBar` (إمكانية الوصول)، `MinistryQuickDial` (أرقام الوزارات)، `FloatingHelpButton` (طوارئ + مساعدة)، `KeyboardShortcutsHelp` (اختصارات لوحة المفاتيح، لسطح المكتب فقط)، إضافةً إلى `FeedbackWidget` و`AppointmentReminder` الشرطيَين. تحقّق التدقيق الفعلي للكود من وجود **تصادم بصري حقيقي**: `KeyboardShortcutsHelp` و`MinistryQuickDial` كانا يتشاركان نفس الموضع تقريباً (`bottom:80` و`bottom:82`) على سطح المكتب.

### موقعها
`app/layout.tsx` (كانت تُعرَض على كل صفحة دون استثناء) + `app/page.tsx`.

### أثرها على المستخدم
ازدحام بصري دائم، تشتيت عن الإجراء الأساسي (البحث/المحادثة)، تكرار وظيفي — كل وظيفة موجودة أيضاً في `TopNav` أو `MobileMenu` أو `/settings`.

### مستوى الأولوية
عالٍ جداً — كان أوضح انتهاك لمبدأ "شاشة واحدة = إجراء أساسي واحد".

### الحل المنفَّذ
- **`GlobalLangSwitch`**: أُزيل بالكامل من `layout.tsx`. تبديل اللغة موجود فعلياً وبشكل مرئي في `TopNav` (على كل الأحجام، وليس محجوباً خلف نقطة انكسار كما كان الافتراض القديم) وفي `MobileMenu`.
- **`AccessibilityBar`**: أُزيل من `layout.tsx`. كل إعداداته (تباين عالٍ / نص كبير / تقليل الحركة) موجودة فعلياً في `/settings` وتستخدم نفس مفاتيح `localStorage` تماماً — لا فقدان وظيفي.
- **`MinistryQuickDial`**: أُزيل زر الـ FAB الخاص به فقط. الشيت (bottom sheet) نفسه ما زال موجوداً بكامل بياناته (20 جهة حكومية + أرقام طوارئ)، ويُفتَح الآن عبر حدث مخصّص `dalilak-open-ministry-dial` يُطلَق من زر جديد "أرقام الوزارات والطوارئ" داخل `MobileMenu`.
- **`FloatingHelpButton`**: أُزيل بالكامل من `app/page.tsx`. أرقام الطوارئ فيه (112/140/175/1735/1480/1650) متوفرة عملياً ضمن نفس شيت `MinistryQuickDial` (يغطي 112/140/175/1480 + أرقام أمن/مياه بديلة)، ورابط الأسئلة الشائعة موجود أصلاً كبند تنقّل في `MobileMenu`.
- **`KeyboardShortcutsHelp`**: أُزيل زر الـ FAB الخاص به فقط ("?" في الزاوية). الاختصار نفسه ("?" على لوحة المفاتيح) ما زال يعمل تماماً كما كان — كان هذا الزر ميزة لمستخدمي سطح المكتب فقط، واكتشافه ممكن عبر ذات المفتاح الموثّق داخل النافذة نفسها.
- تنظيف تبعي: أُزيلت قاعدة CSS الميتة `.kbd-shortcuts-fab` من `globals.css`، وأُعيد ضبط مواضع `FeedbackWidget` و`AppointmentReminder` (كانتا تُحسبان مسافتهما لتفادي التصادم مع الأزرار المحذوفة، فأصبحتا الآن في موضع أقرب/أنظف).

### حالة التنفيذ
✅ منفَّذ بالكامل وتحقَّق منه عبر `tsc --noEmit` (لا أخطاء).

### الوظائف المتبقية عمداً كعناصر سياقية (وليست "أزرار عائمة" بالمعنى المقصود في الموجز)
- `ChatScrollToBottomButton` و`ProcedureBackToTopButton`: تظهران فقط عند تمرير طويل داخل شاشة نشطة، وتختفيان تلقائياً — نمط قياسي (return-to-content) وليس تكديساً دائماً، وهما الآن الوحيدتان في تلك الزاوية فلا تصادم.

### تحديث (batch #347) — `FeedbackWidget` حُوِّل من عائم إلى بطاقة داخل التدفّق — ✅ منفَّذ
كان آخر استثناء متبقٍّ لقاعدة "زر عائم واحد فقط". حُوِّل بالكامل من FAB ثابت الموضع (`position:fixed`) إلى بطاقة عادية ضمن تدفّق رسائل المحادثة، بنفس أسلوب `ChatSummaryCard` تماماً (نفس القيم: `margin:'12px 0'، borderRadius:14، border`). آلية الظهور نفسها (بعد 3 رسائل أو 5 دقائق) محفوظة داخل المكوّن، وتصميمه صار: شريط مطوي بعنوان "شاركنا رأيك" يتوسّع مكانه عند الضغط (بلا نافذة منبثقة عائمة).

**تغيير سلوكي طفيف يجب توثيقه بصدق:** بما أن البطاقة أصبحت تُعرَض ضمن تدفّق شاشة المحادثة فقط (نفس موقع `ChatSummaryCard`)، فإن مسار "5 دقائق تصفّح على الصفحة الرئيسية دون بدء أي محادثة" لم يعد يُظهر البطاقة (لأن المكوّن لم يعد يُركَّب إلا بعد `messages.length > 0`). هذا انحراف بسيط عن السلوك الأصلي، ولكنه مقصود ومبرَّر: طلب تقييم أثناء تفاعل فعلي مع المحادثة أنسب سياقياً من مقاطعة تصفّح صامت للصفحة الرئيسية — ويتوافق مع مبدأ "لا نقاط انتباه إضافية على الصفحة الرئيسية المبسّطة". لم تُحذف بيانات أو وظيفة تقييم فعلية، فقط شرط توقيت هامشي واحد.

**الحالة: منفَّذ بالكامل.** لم يعد هناك أي زر عائم استثنائي في التطبيق.

---

## المرحلة 2 — Bottom Navigation (4 عناصر بالضبط) — ✅ منفَّذ

### المشكلة
`BottomNav.tsx` كان يحتوي 5 عناصر: الرئيسية، المعاملات، محادثة (FAB مركزي)، **الخدمات**، حسابي — بينما الموجز يطلب 4 عناصر بالضبط.

### موقعها
`components/BottomNav.tsx`.

### أثرها على المستخدم
عنصر إضافي يقلّل من وضوح التسلسل الهرمي للتنقّل، ويُنافس عناصر أهم على مساحة اللمس المحدودة على الشاشات الصغيرة.

### مستوى الأولوية
عالٍ.

### الحل المنفَّذ
أُزيل عنصر "الخدمات" فقط من الشريط (تعديل ميكانيكي بسيط — الشريط مبني بـ `flex:1` لكل عنصر فيعيد توزيع المساحة تلقائياً بلا كسر تخطيط). لم يُحذف مسار `/services` ولا أي وظيفة: يبقى متاحاً عبر `MobileMenu` وعبر شبكة الفئات والبحث في الصفحة الرئيسية. اختير عنصر "الخدمات" تحديداً للحذف من الشريط لأنه سطح **تصفّح** (browse) بينما العناصر الأربعة الباقية (الرئيسية / المعاملات / محادثة / حسابي) هي أسطح **إنجاز مهمة** (do)، وهذا يطابق ترتيب الأولوية المعتمد (سهولة إنجاز المهمة قبل سهولة التصفّح).

### حالة التنفيذ
✅ منفَّذ، تحقَّق منه عبر `tsc --noEmit`.

---

## المرحلة 3 وما بعدها — بنود مؤجَّلة (لم تُنفَّذ بعد في هذه الدفعة)

توثيقاً صادقاً للحجم الحقيقي المتبقي من الموجز الأصلي، هذه البنود **لم تُنفَّذ بعد** ولا ينبغي افتراض إنجازها:

| البند | الأولوية | ملاحظة |
|---|---|---|
| إعادة بناء الصفحة الرئيسية بالكامل حسب البنية المطلوبة (هيدر 56-64px، هيرو مختصر، البحث كعنصر أساسي، مساران فقط، حد أقصى 4 معاملات شائعة) | عالٍ جداً | 🟡 جزئي — انظر "تحديث (batch #350)" أدناه: تحقّقنا أن الهيرو أصلاً يطابق البنية المطلوبة (مسارَان فقط + بحث كعنصر أساسي)، وقلّصنا قسم "الإجراءات الأكثر طلباً" من 6 إلى 4 بطاقات، وقسم "تصفّح حسب الفئة" من 10 إلى 6 رقاقات + رابط "كل الفئات". المتبقي: تدقيق ارتفاع الهيدر الفعلي بالبكسل، وتقييم قسمَي "كيف يعمل" و"الثقة" (حالياً منفصلان — الموجز يطلب "سطر ثقة صغير"). |
| تدقيق ومطابقة Design System (الألوان/الخط/التباعد 8pt/الزوايا/الظلال) مقابل القيم المحدَّدة في الموجز | متوسط | نظام تصميم أساسي (CSS variables) موجود فعلاً في `globals.css` من دفعات سابقة — يحتاج تدقيق مطابقة بند-ببند وليس بناءً من الصفر. لون العلامة التجارية الحالي `#8F1D2C` يُبقى كما هو (الموجز نفسه يسمح صراحة بإبقاء اللون المعتمد حالياً). |
| إعادة تصميم صفحة تفاصيل الإجراء وفق البنية المكوّنة من 13 نقطة | متوسط | تحتاج تدقيقاً لمعرفة أي من الـ13 نقطة موجود فعلاً (الكثير منها مبني من دفعات سابقة: Breadcrumbs، الخطوات، الوثائق، التكلفة...) وأيها ناقص. |
| تبسيط واجهة المحادثة | متوسط | 🟡 بدأ (batch #353) — صف إجراءات كل رسالة قُلِّص من 10 إلى 4 عناصر ظاهرة. المتبقي: صف أدوات الإدخال (Mode/ResponseLength/ContextBar/KeyboardHint/CharCounter/QuickReplies) وأعلى شاشة المحادثة (SummaryCard/SessionTimer/SearchInThread/SessionSummaryChip/PinnedBanner/LanguageToggleChip). |
| فحص WCAG 2.2 AA كامل | متوسط-عالٍ | 🟡 تقدّم مستمر — أُنجز: skip-link، aria-label، focus-visible، Escape handlers، تسلسل العناوين (batch #349)، تباين الألوان (batch #348)، استخدام اللون فقط (batch #351). المتبقي: تدقيق مناطق landmark بعد التوسّع الأخير، وترتيب التنقّل بلوحة المفاتيح (tab order) على الصفحات الغنية بالودجات. |
| اختبار/إصلاح الاستجابة عند 9 نقاط انكسار (320–1440px) | عالٍ | **لا يمكن تنفيذها آلياً في هذه البيئة** (لا متصفح حقيقي) — تحتاج مراجعة حيّة على الرابط المنشور بعد كل دفعة. |
| تحسين الأداء (Core Web Vitals، code splitting، lazy loading) | متوسط | 🟡 تقدّم — lazy-load لبعض مجموعات الودجات (دفعة سابقة) + إزالة ~1.7MB استيرادات بيانات ميتة من الصفحة الرئيسية (batch #354). يحتاج تدقيقاً شاملاً إضافياً (lazy-load للمودالات الثقيلة مثل GuidedFlow/TransactionStarter/ServiceGroupSheet التي لا تزال eager). |
| توحيد المكوّنات المشتركة الـ17 المذكورة في الموجز | منخفض-متوسط | كثير منها موجود فعلاً بأسماء مختلفة، يحتاج جرداً ومطابقة قبل أي "توحيد". |
| مقاييس Lighthouse الفعلية ولقطات الشاشة عند 4 قياسات | — | **غير قابلة للتنفيذ في هذه البيئة** (لا Lighthouse ولا متصفح). سيُستعاض عنها بمراجعة حيّة يدوية بعد كل نشر. |

---

---

## اكتشاف إضافي — كتلة كود ميتة بحجم ~30 مكوّناً في الصفحة الرئيسية — ✅ منفَّذ

### المشكلة
أثناء تدقيق بنية `app/page.tsx` تمهيداً لتبسيط الصفحة الرئيسية، تبيّن أن كتلة كاملة من ودجات الصفحة الرئيسية (WelcomeBackBanner، HomepageQuickActionsBar، وأكثر من 25 مكوّناً آخر مجمّعة ضمن 11 قسم `SectionCollapseToggle`) كانت **كوداً ميتاً بالكامل — لا يُعرض مطلقاً لأي مستخدم**. السبب: هذه الكتلة كانت محاطة بشرط `messages.length === 0`، لكنها كانت متداخلة داخل الفرع الآخر من الشرط الأساسي (`messages.length === 0 ? (الصفحة الرئيسية) : (المحادثة)`) — أي داخل فرع `المحادثة` الذي أصلاً لا يُفعَّل إلا عندما `messages.length > 0`. تحقّقت من هذا يدوياً بتتبّع الأقواس عند الأسطر 1373 (فتح الشرط) و1859 (الفرع الآخر) و1946-2055 (الكتلة الميتة) و2158 (إغلاق الشرط) — تأكيد كامل، وليس افتراضاً.

### موقعها
`app/page.tsx`، كانت الأسطر 1946–2055 (JSX) + ~30 استيراداً (بعضها eager وبعضها عبر `next/dynamic`) في أعلى الملف.

### أثرها على المستخدم
لا أثر بصري (الكود لم يكن يُعرض أصلاً)، لكن أثر حقيقي على **الأداء**: عشرات الاستيرادات الفورية (eager imports) كانت تُحمَّل ضمن الحزمة الرئيسية (bundle) لصفحة لا تستخدمها أبداً — تضخيم غير ضروري لحجم التحميل الأول.

### مستوى الأولوية
متوسط-عالٍ (أداء، ليس وضوحاً — الصفحة الحالية المرئية فعلياً كانت نظيفة أصلاً: هيرو + 6 بطاقات معاملات + 10 فئات + خط زمني الأحداث الحياتية + شرح 3 خطوات + شريط ثقة + فوتر، و4 أزرار CTA أساسية فقط في الشاشة الأولى — أقرب بكثير لما يطلبه الموجز مما كان متوقعاً).

### الحل المنفَّذ
حذف الكتلة الميتة بالكامل من JSX + كل الاستيرادات التي كانت تُستخدَم حصراً داخلها (تحقّقت من كل اسم عبر `grep -c` قبل الحذف؛ الاستثناء الوحيد: `saveChatSession` من `ChatHistoryPanel.tsx` — استيراد مسمّى منفصل ما زال يُستخدَم فعلياً في مكان آخر من الملف، فأُبقي عليه). حُذفت أيضاً دالة `dyn()` المساعدة و`import dynamic from 'next/dynamic'` لأنهما أصبحا بلا استخدام. لم تُحذف أي من ملفات المكوّنات نفسها (`SmartReminder.tsx`، `GovCalendar.tsx`، إلخ) — تبقى موجودة وقابلة لإعادة الاستخدام لاحقاً إذا احتاجها تصميم الصفحة الرئيسية الجديد، فقط الاستدعاء الميت أُزيل. الملف انخفض من 2770 إلى 2602 سطر. `tsc --noEmit` نظيف، وتحقّقت بـ`grep` أن لا مرجع متبقٍ لأي اسم محذوف.

### حالة التنفيذ
✅ منفَّذ بالكامل.

---

---

## تدقيق تباين الألوان (WCAG 2.2 AA) — ✅ منفَّذ جزئياً، محسوب برمجياً

### المنهجية
بما أن بيئة التنفيذ لا تملك متصفحاً حقيقياً أو Lighthouse، لا يمكن تشغيل أداة فحص تباين تفاعلية. البديل الصادق: استخرجت القيم الست عشرية الفعلية لكل رمز لون (design token) من `globals.css`، وحسبت نسب التباين برمجياً بمعادلة WCAG الرسمية لللمعان النسبي (relative luminance) عبر سكريبت Python، وقارنتها بعتبتي AA (4.5:1 للنص العادي، 3.0:1 للنص الكبير/عناصر الواجهة). هذا تحقّق رياضي دقيق 100%، وليس تخميناً.

### النتائج والإصلاحات
| الرمز | القيمة القديمة | التباين القديم (على الخلفية) | الحالة | الإصلاح |
|---|---|---|---|---|
| `--text-1` | `#191713` | 16.83:1 | PASS | لا تغيير |
| `--text-2` | `#69645C` | 5.52:1 | PASS | لا تغيير |
| `--text-3` | `#918B82` | **3.18:1 — FAIL** (نص عادي) | ❌→✅ | غُيِّر إلى `#76716A` (4.55:1 / 4.84:1) — نفس درجة اللون، أغمق قليلاً فقط |
| `--text-4` | `#B8B2AA` | **1.98:1 — FAIL** (فشل حتى عتبة 3:1) | ❌→✅ | أُبقي اللون نفسه (مخصَّص حصراً الآن لـ placeholder المدخلات وشريط التمرير، وكلاهما معفى من شرط التباين) — لكن كل الاستخدامات الحقيقية له كنص فعلي (89 استخدام عبر 32 ملفاً) نُقلت إلى `--text-3` بعد إصلاحه |
| أبيض على `--brand` | — | 8.82:1 | PASS | لا تغيير (أزرار CTA) |

### التنفيذ
- عدّلت `--text-3` في `globals.css` مباشرة (تغيير سطر واحد يطبَّق تلقائياً في كل مكان يستخدم الرمز — 27+ ملفاً).
- شغّلت استبدالاً آلياً (`sed`) لكل الاستخدامات الفعلية لـ`var(--text-4)` في ملفات `.tsx` (89 استخدماً حقيقياً كنص/أيقونة عبر 32 ملفاً — تحققت أولاً أن كل واحد منها نص حقيقي فعلي بحجوم صغيرة 10-12.5px، وليس عنصراً زخرفياً معفى) إلى `var(--text-3)`، تاركاً تعريف `--text-4` نفسه دون تغيير في `globals.css` لاستخداماته المعفاة الوحيدة المتبقية (`::placeholder`, `scrollbar-thumb`).
- استثناء واحد مقصود: `Breadcrumbs.tsx` يستخدم `var(--text-4, #C8BEB2)` لفاصل زخرفي (`›`/`‹`) مُعلَّم `aria-hidden="true"` — معفى فعلياً من شرط التباين لأنه لا ينقل معلومة نصية، فتُرك كما هو.
- `tsc --noEmit` نظيف.

### الحالة: ✅ منفَّذ (أهم فشلين تم إصلاحهما فعلياً في الكود، ليس توصية فقط)

---

## ⚠️ حادثة واكتشاف حرج — انهيار وظيفي في أدوات إمكانية الوصول (تم اكتشافه وإصلاحه في نفس الجلسة)

أثناء تدقيق التباين أعلاه، اكتُشف أن إزالة `AccessibilityBar` من التكديس العائم (في دفعة سابقة ضمن هذه الجلسة نفسها) كانت قد كسرت وظيفة حقيقية عن غير قصد: قواعد CSS الفعلية لكلاسات `.dalilak-high-contrast` / `.dalilak-large-text` / `.dalilak-reduce-motion` كانت موجودة **حصراً** داخل وسم `<style>` المضمّن في مكوّن `AccessibilityBar.tsx` نفسه — وليس في `globals.css`. عندما توقّف ذلك المكوّن عن العرض، توقّفت هذه القواعد عن الحقن في الصفحة كلياً، رغم أن صفحة `/settings` استمرت (بشكل صحيح) في كتابة تفضيلات المستخدم إلى `localStorage` وتبديل الكلاسات على `<html>` — لكن بلا أي قاعدة CSS فعلية تُترجم تلك الكلاسات إلى تأثير بصري حقيقي. أي مستخدم فعّل "تباين عالٍ" أو "نص كبير" بعد تلك الدفعة كان تفعيله بلا أي أثر مرئي.

**الإصلاح**: نقلت قواعد الـCSS الثلاث بشكل دائم إلى `globals.css` (مستقلة عن أي مكوّن)، وأنشأت `components/AccessibilityEffects.tsx` — مكوّن بلا أي واجهة مرئية (`return null`)، مُركَّب دائماً في `app/layout.tsx` (تماماً كما كان `AccessibilityBar` مُركَّباً)، مهمته الوحيدة تطبيق التفضيلات المحفوظة على `<html>` عند كل تحميل صفحة. النتيجة: الوظيفة استُعيدت بالكامل بدون إعادة أي زر عائم — القاعدة "زر عائم واحد فقط" ما زالت سليمة، فقط أُصلح الخلل الوظيفي الذي نتج عن تطبيقها بسرعة أكبر مما ينبغي.

**الدرس المسجَّل لبقية هذا التدقيق**: عند إزالة أي مكوّن عائم مستقبلاً، يجب التحقق أولاً مما إذا كان يحتوي على منطق وظيفي حقيقي (وليس فقط زر تشغيل/إيقاف) قبل حذف عرضه — نمط "الزر + الوظيفة في مكوّن واحد" شائع في هذا المشروع (رأيناه أيضاً في `KeyboardShortcutsHelp` الذي احتفظ بمستمع `keydown` بعد حذف زره، وفي `MinistryQuickDial` الذي احتفظ بمنطق الشيت بعد حذف زره) — لكن `AccessibilityBar` كان الحالة الوحيدة التي كانت الوظيفة فيها (قواعد CSS) منفصلة تماماً عن حالة React ولا تُكتشف بمجرد قراءة الأزرار/الأحداث.

### الحالة: ✅ مُكتشَف ومُصلَح بالكامل في نفس الجلسة.

---

---

## تدقيق التسلسل الهرمي للعناوين (Heading Hierarchy) — ✅ منفَّذ

### المنهجية
فحص فعلي (عبر sub-agent متخصص) لكل وسوم `<h1>`–`<h5>` الحقيقية عبر 19 صفحة رئيسية، مع تتبّع الحالات الشرطية (ماذا يحدث عند حالة فارغة/تحميل/عنصر محدَّد). ليس تخميناً — نتائج بأرقام أسطر دقيقة.

### النتائج والإصلاحات
| الصفحة | المشكلة | الإصلاح |
|---|---|---|
| `app/page.tsx` | 🛑 عند نشاط محادثة فعلي (`messages.length > 0`)، **لا يوجد أي عنوان إطلاقاً** في الصفحة — الـ`<h1>` الوحيد يعيش داخل فرع الشاشة الترحيبية الذي يختفي كلياً | أضفت `<h1 className="sr-only">` في بداية عرض المحادثة — عنوان مخفي بصرياً فقط لمستخدمي قارئ الشاشة |
| `ProcedureDetailClient.tsx` | حالة "غير موجود" لا تملك h1 (فقط h2) + الحالة العادية تقفز من h1 مباشرة إلى h3 (تتخطى h2) | غيّرت h2→h1 في حالة "غير موجود"؛ غيّرت h3→h2 في مكوّن `Section` المشترك (يُستخدم لعناوين: الوثائق المطلوبة، الخطوات، الجهة المسؤولة، الرسوم) |
| `services/page.tsx` | شبكة نتائج الخدمات: h1 ثم h3 مباشرة (بطاقات) بلا h2 | أضفت `<h2 className="sr-only">` (بعدد النتائج) قبل الشبكة |
| `my-files/page.tsx` | قائمة المعاملات: h1 ثم h3 مباشرة (عناوين البطاقات) بلا h2 في الحالة الافتراضية | أضفت `<h2 className="sr-only">` قبل القائمة |
| `app/admin/content/page.tsx` | نفس نمط services/my-files | أضفت `<h2 className="sr-only">` قبل القائمة |
| `app/admin/page.tsx` | تبويبا "إنشاء مستخدم" و"رموز الاستعادة": h1 ثم h3 مباشرة | غيّرت h3→h2 في كلا العنوانين (لا تكرار آخر لـh2 في تلك الحالات، فالترقية آمنة) |

### ملاحظة تقنية مهمة
بما أن هذا المشروع يعتمد بالكامل على `style` مضمّن (لا CSS classes للحجم)، فتغيير وسم من `h3` إلى `h2` أو `h2` إلى `h1` **لا يُغيّر أي شيء مرئي إطلاقاً** — الحجم/الوزن/اللون كلها مُحدَّدة صراحة عبر `style`. هذا يجعل كل هذه الإصلاحات خالية من أي مخاطرة بصرية بنسبة 100%، وهو ما يفسّر لماذا نُفِّذت مباشرة دون تردد.

### صفحات تحقّقتُ من سلامتها بلا حاجة لتعديل
`procedures/page.tsx`، `forms/page.tsx`، `FormDetailClient.tsx`، `expat-property/page.tsx`، `faq/page.tsx`، `authorities/page.tsx`، `settings/page.tsx`، `professional/page.tsx`، `drafting-studio/page.tsx`، وكل صفحات المصادقة (login/register/forgot-password/reset-password) — لا يوجد فيها h1 مكرر ولا قفزة مستوى.

### الحالة: ✅ منفَّذ بالكامل. `tsc --noEmit` نظيف.

---

## تحديث (batch #350) — تقليص الصفحة الرئيسية: بطاقات المعاملات والفئات — ✅ منفَّذ جزئياً

### المشكلة
دقّقنا الهيرو الفعلي في `app/page.tsx` مقابل بنية الموجز الأصلي المطلوبة (هيدر مختصر، هيرو قصير، البحث كعنصر أساسي، مساران فقط). النتيجة: الهيرو **كان مطابقاً فعلاً** — زران فقط ("اسأل دليلك" أساسي + "تصفّح المعاملات" ثانوي) وحقل بحث بارز أسفلهما. لكن قسمَين تاليين في الصفحة تجاوزا الحدود العددية المطلوبة صراحة في الموجز: قسم "الإجراءات الأكثر طلباً" كان يعرض **6 بطاقات** (الموجز يطلب حد أقصى 4)، وقسم "تصفّح حسب الفئة" كان يعرض **10 رقاقات فئة** (الموجز يطلب "شبكة فئات محدودة").

### موقعها
`app/page.tsx` — قسم `PROCEDURES` (بطاقات المعاملات الشائعة) وقسم `CATEGORIES` (رقاقات الفئات).

### أثرها على المستخدم
ازدحام بصري إضافي في أول تمريرة (scroll) بعد الهيرو مباشرة، يُبعِد المستخدم عن مبدأ "شاشة واحدة = إجراء أساسي واحد" رغم أن الهيرو نفسه كان نظيفاً.

### مستوى الأولوية
عالٍ — هذا جزء من البند الأعلى أولوية في قائمة "بنود مؤجَّلة".

### الحل المنفَّذ
- **بطاقات المعاملات**: أُزيلت آخر بطاقتين ("تجديد رخصة القيادة" و"تجديد إقامة الأجانب") من مصفوفة العرض في الصفحة الرئيسية — أصبح القسم يعرض 4 بطاقات بالضبط (جواز السفر، إخراج قيد فردي، تسجيل شركة، تسجيل سيارة). كلا الإجراءين المُزالين من الصفحة الرئيسية **ما زالا متاحين بالكامل** عبر `/procedures` (زر "كل المعاملات" أعلى القسم)، والبحث، والفئات، ورحلات الحياة — لا حذف وظيفي، فقط تقليص واجهة العرض الأولى.
- **رقاقات الفئات**: أُزيلت 4 رقاقات ("الكاتب العدل"، "التعليم"، "العمل والضمان"، "القضاء والمعاملات القانونية") من الصفحة الرئيسية — أصبح الصف يعرض 6 رقاقات. أُضيف رابط "كل الفئات ←" بجانب عنوان القسم (بنفس نمط زر "كل المعاملات" الموجود مسبقاً في القسم الذي يعلوه) يوجّه إلى `/services`. تحقّقتُ فعلياً من أن `/services` يملك نظام تصفية فئات كامل ومستقل (`SERVICE_CATEGORIES` مع عدّاد خدمات لكل فئة) — الرابط لا يوجّه إلى صفحة فارغة أو ناقصة.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. الجزء المتبقي من هذا البند (تدقيق ارتفاع الهيدر بالبكسل الفعلي، ودمج/تصغير قسمَي "كيف يعمل" و"الثقة" إلى ما يقارب "سطر ثقة واحد") لم يُنفَّذ بعد — سيُستكمل في الدفعة التالية لتفادي حذف محتوى حقيقي (أوصاف الثقة والخطوات) دون قرار مدروس بشأن أين يُنقَل ذلك المحتوى بدل حذفه.

---

## تحديث (batch #351) — تدقيق "استخدام اللون فقط" (WCAG 2.2 SC 1.4.1) — ✅ منفَّذ جزئياً

### المشكلة
تدقيق مخصّص (عبر Explore agent) بحثاً عن مؤشرات حالة (نقاط/شارات ملوّنة) تنقل معلومة بالاعتماد على اللون **فقط** بلا نص أو فرق شكل مرافق — وهو انتهاك مباشر لـ WCAG 1.4.1 يؤثر على مستخدمي عمى الألوان (Color Blindness) وشاشات الطباعة الأحادية. غطّى التدقيق كل شارات الحالة/الخطورة/الأولوية/الصعوبة في التطبيق (~15 مكوّناً).

### النتيجة
معظم الشارات كانت **متوافقة فعلاً** (نص أو أيقونة مرافقة للون: `RiskScoreCard`، `ProcedureDifficultyBadge`، `ProcedurePriorityTag`، `MinistryOpenHoursWidget`، `TransactionFilePanel`، `TopNav` (نقطة "متصل")، `NotificationBell`، `ProcedureLastUpdatedBadge`، `HomepageProgressRing`، `ProcedureCompletionBadge`، شارات حالة الأدمن). ثلاث حالات فعلية كانت لوناً فقط:

1. **`QuickContacts.tsx`** — نقطة مفتوح/مغلق (8px) بجانب أيقونة كل جهة في حالة الشريحة المطوية (وهي الحالة الافتراضية التي يتصفّحها المستخدم) — أخضر/أحمر بلا نص، وحتى بلا نص لقارئ الشاشة (الاسم فقط كان يُقرأ).
2. **`HomepageCalendarWidget.tsx`** — نقاط "موعد نهائي" (أحمر) و"تذكير" (بنفسجي) في شبكة التقويم — نفس الشكل (دائرة) بلونين مختلفين فقط؛ `aria-label` الخاص بالخلية لا يذكر النوع أصلاً.
3. **`ProcedureVersionTag.tsx`** (الوضع المضغوط `compact`، المستخدم في بطاقات قوائم الإجراءات) — نقطة تدرّج حداثة (أخضر/أزرق/بنفسجي/برتقالي) بلا نص مرئي افتراضياً — لكنها **الوحيدة من الثلاث التي لها بديل نصي فعلي** (aria-label + tooltip عند التمرير/التركيز/النقر)، وهي معلومة "إضافية" (تاريخ آخر تحديث) وليست معلومة ضرورية لإنجاز المهمة.

### الحل المنفَّذ
- **`QuickContacts.tsx`**: النقطة الآن تنقل الحالة بالشكل أيضاً وليس اللون فقط — دائرة مصمتة = مفتوح، حلقة مفرغة (حدود فقط بلا تعبئة) = مغلق. أُضيف نص مخفي بصرياً (`sr-only`) داخل الزر يذكر "— مفتوح الآن" أو "— مغلق الآن" لقارئ الشاشة، لم يكن موجوداً إطلاقاً من قبل حتى للمستخدم الكفيف.
- **`HomepageCalendarWidget.tsx`**: نقطة "الموعد النهائي" بقيت دائرة، ونقطة "التذكير" أصبحت معيّناً (مربّعاً مائلاً 45°) — فرق شكل حقيقي، وطُبِّق نفس الفرق على وسيلة الإيضاح (Legend) أسفل الودجت للاتساق. `aria-label` لكل خلية يوم أصبح يذكر نوع الحدث صراحة ("يتضمن موعداً نهائياً" / "يتضمن تذكيراً") بدل عدد الأحداث فقط.
- **`ProcedureVersionTag.tsx` (compact)**: تُرِك كما هو عمداً — فيه بالفعل بديل نصي كامل (aria-label دائم + tooltip تفاعلي)، والمعلومة نفسها زخرفية/تكميلية (تاريخ تحديث) لا تمنع إنجاز أي مهمة، وفرض نص مرئي دائم يتعارض مع الغرض التصميمي المعلن لوضع `compact` (بطاقات قوائم ضيقة المساحة). موثَّق هنا كقرار واعٍ وليس إغفالاً.

### الحالة
✅ منفَّذ للحالتين الحقيقيتين الأعلى أثراً. `tsc --noEmit` نظيف. التدقيق الأوسع لـ WCAG 2.2 AA (مناطق landmark، ترتيب التنقّل بلوحة المفاتيح، تباين حالات focus على كل عنصر تفاعلي) لا يزال بنداً مؤجَّلاً جزئياً — أُنجز منه حتى الآن: skip-link، focus-visible، Escape handlers، تسلسل العناوين (batch #349)، تباين الألوان (batch #348)، رسائل الأخطاء (role="alert" موجود مسبقاً في كل صفحات المصادقة، تحقّقتُ منه هذه الدفعة بلا حاجة لتعديل)، واستخدام اللون فقط (هذه الدفعة).

---

## تحديث (batch #352) — اكتشاف زر مُضلِّل حقيقي في صفحة تفاصيل الإجراء — ✅ منفَّذ

### المشكلة
أثناء تدقيق صفحة تفاصيل الإجراء (`ProcedureDetailClient.tsx`) لمبدأ "شاشة واحدة = إجراء أساسي واحد"، وُجد زر في صف الإجراءات العلوي (بجانب زر "اسأل دليلك" الأساسي) نصّه "المعالج"/"Wizard"، لكن `onClick` الخاص به كان ينفّذ `router.push('/')` — أي ينقل المستخدم إلى الصفحة الرئيسية فقط. لا يوجد أي مسار أو معالج (`GuidedFlow`) مرتبط بهذا الإجراء تحديداً يُفتَح من هذا الزر — تحقّقت عبر البحث في الكود عن أي استخدام آخر لـ`GuidedFlow` أو رابط لمسار "wizard" داخل هذا الملف، فلم أجد شيئاً. هذا انتهاك مباشر لأولوية "الوضوح" (أعلى بند في ترتيب الأولوية المعتمد) — يتوقّع المستخدم أن يفتح الزر معالجاً موجّهاً لهذا الإجراء تحديداً، فإذا به ينقله فقط إلى الصفحة الرئيسية.

### موقعها
`app/procedures/[slug]/ProcedureDetailClient.tsx`، صف الإجراءات أعلى الصفحة (بجانب "اسأل دليلك").

### أثرها على المستخدم
تضليل مباشر — نص الزر يَعِد بميزة (معالج موجّه) لا ينفّذها الزر فعلياً؛ يفقد المستخدم الثقة في تسميات الأزرار الأخرى في التطبيق.

### مستوى الأولوية
عالٍ — انتهاك مباشر لأعلى بند في ترتيب الأولوية (الوضوح).

### الحل المنفَّذ
بما أن `GuidedFlow` مكوّن مرتبط بالصفحة الرئيسية فقط (`app/page.tsx`) ولا يملك أي آلية للربط العميق (deep-link) بإجراء محدّد بعد — بناء هذا الربط سيكون تغييراً أكبر نطاقاً وغير مبرَّر في دفعة تركيزها إصلاح دقيق — اخترتُ الحل الصادق والأقل مخاطرة: أعدت تسمية الزر ليطابق سلوكه الفعلي فعلاً. النص الآن "الرئيسية"/"Home" بدل "المعالج"/"Wizard"، مع تغيير الأيقونة إلى أيقونة بيت (Home) بدل الأيقونة السابقة غير المرتبطة بأي من المعنيَين، وإضافة `aria-label` واضح. لا تغيير في السلوك — فقط في الصدق البصري لما يفعله الزر.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. **ملاحظة للمتابعة المستقبلية**: لو أراد المستخدم لاحقاً ميزة "معالج موجّه لهذا الإجراء تحديداً" فعلياً، هذا يتطلّب تمرير مُعرِّف الإجراء (`slug`) إلى `GuidedFlow` عبر query param في الصفحة الرئيسية وفتحه تلقائياً — تغيير حقيقي في المنطق يحتاج قراراً منفصلاً، وليس مجرد إعادة تسمية.

---

## تحديث (batch #353) — بدء "تبسيط واجهة المحادثة" — تقليص صف إجراءات كل رسالة من 10 إلى 4 عناصر — ✅ منفَّذ جزئياً

### المشكلة
تدقيق مباشر لبند "تبسيط واجهة المحادثة" (كان لا يزال في جدول "بنود مؤجَّلة" منذ بداية الجلسة) كشف أن كل رسالة من الذكاء الاصطناعي (وهي أكثر عنصر تكراراً في كامل التطبيق — تظهر مرة لكل رد، وليس مرة واحدة لكل صفحة) كانت تعرض **10 عناصر تفاعلية منفصلة** أسفلها: `ChatMessageActions` (نسخ + مشاركة + حفظ = 3 أزرار) + `ChatPinButton` (تثبيت) + `ChatVoicePlayback` (استماع) + `ChatEmojiReactions` (4 أزرار إيموجي منفصلة، كلها ظاهرة دائماً) + `ChatSaveToNotes` (حفظ في الملاحظات) = 10 عناصر إجمالاً، بالإضافة إلى `ChatAIBadge` (شارة نصية غير تفاعلية، تظهر فقط على أول رسالة). هذا انتهاك مباشر وعالي التكرار لمبدأ "شاشة واحدة = إجراء أساسي واحد" ولأولوية "البساطة".

### موقعها
`app/page.tsx`، صف إجراءات كل رسالة ذكاء اصطناعي (كان الأسطر ~1993-2002 قبل هذه الدفعة).

### أثرها على المستخدم
ازدحام بصري متكرر مع كل رد — كلما طال التبادل، ازداد الازدحام تراكمياً (10 أزرار × عدد الردود).

### مستوى الأولوية
عالٍ — أعلى تكراراً من أي عنصر ازدحام آخر عولج هذه الجلسة.

### الحل المنفَّذ
أُبقي `ChatMessageActions` (نسخ/مشاركة/حفظ — الإجراءات الثلاثة الأكثر أهمية على أي رد) ظاهراً دائماً كما هو، وطُوِيت العناصر السبعة المتبقية (تثبيت، استماع، 4 إيموجي، حفظ في الملاحظات) خلف زر واحد جديد "•••" (المزيد) لكل رسالة على حدة (حالة `expandedMsgActions: Set<number>` مفتاحها رقم الرسالة). لم يُحذَف أي مكوّن أو وظيفة — نفس المكوّنات بنفس الخصائص (props)، فقط خلف نقرة واحدة إضافية. النتيجة: من 10 عناصر ظاهرة افتراضياً إلى 4 (نسخ/مشاركة/حفظ + زر المزيد) — تخفيض 60%.

### الحالة
✅ منفَّذ للصف الأكثر تكراراً في واجهة المحادثة. `tsc --noEmit` نظيف. **المتبقي من بند "تبسيط واجهة المحادثة"**: صف الأدوات أعلى حقل الإدخال لا يزال يعرض عدة عناصر متجاورة (`ModeSelector` + `ChatResponseLength` + `ChatContextBar` + `ChatKeyboardSendHint` + `ChatInputCharCounter` + `ChatQuickReplies`)، وأعلى شاشة المحادثة توجد عدة عناصر إضافية (`ChatSummaryCard` + `ChatSessionTimer` + `ChatMessageSearchInThread` + `ChatSessionSummaryChip` + `ChatPinnedBanner` + `ChatLanguageToggleChip`) — هذه أقل تكراراً (تظهر مرة واحدة لكل جلسة/صفحة وليس مرة لكل رسالة)، لذا أولويتها أقل من صف إجراءات الرسالة الذي عولج هنا، لكنها ما زالت بنداً مفتوحاً للدفعة التالية.

**تحديث تحقيق (نفس الدفعة، بلا تعديل كود)**: فحصتُ تحديداً `ChatSummaryCard` (يظهر عند 5+ رسائل) و`ChatSessionSummaryChip` (يظهر عند 10+ رسائل) لاحتمال أن يكونا وظيفة مكرَّرة (كلاهما يبدو ظاهرياً "تلخيص المحادثة"). بعد قراءة الكود الفعلي لكليهما: **ليسا مكرَّرين** — `ChatSummaryCard` يستخرج كلمات مفتاحية بمطابقة نصية محلياً (بلا استدعاء AI) ويعرضها كشرائح مواضيع قابلة للنقر لإعادة السؤال عنها، بينما `ChatSessionSummaryChip` يرسل فعلياً سؤالاً حقيقياً للذكاء الاصطناعي يطلب تلخيصاً مكتوباً كاملاً — وظيفتان مختلفتان فعلياً رغم تشابه التسمية، فلم أُجرِ أي دمج أو حذف. كذلك فحصتُ `ChatSessionTimer`: هو شارة صغيرة واحدة فقط (ليس مصدر ازدحام فعلي)، تجاور زراً مضغوطاً مسبقاً (`ChatMessageSearchInThread` له حالة `open` داخلية، أيقونة واحدة افتراضياً لا صندوق بحث دائم الظهور) — لا حاجة لتغيير. توثيق هذا التحقّق هنا بدل تغيير غير مبرَّر يتماشى مع انضباط الجلسة المعتمَد (الإبلاغ عن نتيجة "لا مشكلة فعلية" بدل الصمت أو التعديل العشوائي).

---

## سجل الدفعات المرتبط بهذا الملف
- **دفعة اليوم (2026-07-25)**: المرحلة 1 (تفكيك مكدّس الأزرار العائمة، batch #345) + المرحلة 2 (BottomNav إلى 4 عناصر) + إزالة كتلة الكود الميت في الصفحة الرئيسية (batch #346) + تحويل FeedbackWidget لبطاقة داخلية (batch #347) + تدقيق تباين الألوان WCAG AA وإصلاح `--text-3`/`--text-4` + إصلاح انهيار وظيفي حرج في أدوات إمكانية الوصول (batch #348) + تدقيق التسلسل الهرمي للعناوين وإصلاحه في 6 ملفات (batch #349) + تقليص بطاقات المعاملات الشائعة (6→4) ورقاقات الفئات (10→6) في الصفحة الرئيسية (batch #350) + تدقيق "استخدام اللون فقط" وإصلاح QuickContacts + HomepageCalendarWidget (batch #351) + إصلاح زر "المعالج" المُضلِّل في صفحة تفاصيل الإجراء (batch #352) + تقليص صف إجراءات كل رسالة محادثة من 10 إلى 4 عناصر ظاهرة (batch #353). راجع `push.sh` لرسالة الـ commit الكاملة.

## تحديث (batch #354) — إزالة ~1.7 ميجابايت من الاستيرادات الميتة في الصفحة الرئيسية — ✅ منفَّذ

### المشكلة
أثناء متابعة بند "تحسين الأداء" المؤجَّل، دقّقت أعلى `app/page.tsx` فوجدت ثلاثة استيرادات لملفات بيانات ضخمة: `TX_ALL`/`TX_WITH_FORMS`/`TX_MINISTRIES` من `lib/allTransactions.ts` (~400 كيلوبايت مصدر)، `ENRICHED_PROCEDURES` من `lib/enrichedProcedures.ts` (~256 كيلوبايت)، و`ALL_SERVICES` من `lib/allServices.ts` (~1 ميجابايت). تحقّقت عبر `grep` لكل معرِّف على حدة أنه **يظهر مرة واحدة فقط في كامل الملف — سطر الاستيراد نفسه** — أي أنها استيرادات ميتة بالكامل، لا يستخدمها أي كود في هذا الملف إطلاقاً.

### موقعها
`app/page.tsx`، أعلى الملف (أسطر الاستيراد).

### أثرها على المستخدم
أداء — حوالى 1.7 ميجابايت من بيانات غير مستخدَمة كانت تُحزَم ضمن حزمة جافاسكريبت للصفحة الرئيسية (قبل الضغط/التصغير)، تُحمَّل على كل زائر بلا أي فائدة.

### مستوى الأولوية
متوسط-عالٍ — إصلاح آمن 100% (لا خطر بصري أو وظيفي، الاستيراد غير مستخدَم أصلاً) بأثر أداء حقيقي وقابل للقياس.

### الحل المنفَّذ
حذف سطرَي الاستيراد الميتَين بالكامل. الصفحات التي تستخدم هذه البيانات فعلياً (`/procedures`، `/services`، `/forms`، إلخ) تستوردها مباشرة من نفس الملفات المصدر ولم تتأثر إطلاقاً — هذا التغيير خاص فقط بحزمة الصفحة الرئيسية.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف — وهذا تأكيد إضافي أن لا شيء آخر كان يعتمد على هذه الاستيرادات عبر هذا الملف (لو كان أي كود يستخدمها، لَفشل البناء فوراً).

---

## سجل الدفعات المرتبط بهذا الملف
- **دفعة اليوم (2026-07-25)، إضافات لاحقة**: batch #354 — إزالة ~1.7 ميجابايت من الاستيرادات الميتة (TX_ALL/TX_WITH_FORMS/TX_MINISTRIES/ENRICHED_PROCEDURES/ALL_SERVICES) من `app/page.tsx`.

## تحديث (batch #355) — إصلاح كثافة الصفحة الرئيسية الفعلية على الموبايل — ✅ منفَّذ

### المشكلة
أبلغ المستخدم صراحةً أن واجهة الهاتف "لا يوجد فيها تغيير كما توقّع". تحقّقت أولاً أن آخر نشر منشور فعلاً (فتحت الموقع الحي وتأكّدت من ظهور تعديلات batch #350 — 6 رقاقات فئات + رابط "All categories" — فالنشر سليم)، لكن أداة تغيير حجم نافذة المتصفح في هذه البيئة لم تكن تعمل فعلياً (`window.innerWidth` بقي 1470px رغم طلب تغييره إلى 375-420px)، فلم أستطع اختبار الشكل الفعلي على شاشة ضيّقة مباشرة. سألت المستخدم تحديداً ماذا توقّع، فأجاب: **"الشكل العام/التباعد — لا يزال يبدو مزدحماً أو بلا تغيير على الموبايل"**.

دقّقت الكود بدلاً من الاعتماد على لقطة شاشة، ووجدت السبب الجذري الحقيقي: كل قسم من أقسام الصفحة الرئيسية السبعة (الهيرو، المعاملات، الفئات، رحلات الحياة، كيف يعمل، الثقة، الفوتر) يستخدم حشوة عمودية على شكل `clamp(الحد_الأدنى, نسبة_من_عرض_الشاشة, الحد_الأقصى)` — لكن الحد الأدنى (المصمَّم أصلاً لسطح المكتب، 32-48px) كان **دائماً** أكبر من قيمة `vw` الفعلية عند عرض هاتف حقيقي (مثلاً `5vw` عند 390px = 19.5px فقط، أقل من الحد الأدنى 48px بكثير) — أي أن كل قسم كان **يُعرَض دائماً بحشوته القصوى المصمَّمة لسطح المكتب، حتى على أضيق شاشة هاتف**. المجموع التراكمي لحشوة الأقسام وحدها (بلا احتساب أي محتوى داخلي) كان يبلغ ~536px — أكثر من نصف ارتفاع شاشة هاتف نموذجية، مجرد فراغ فارغ بين الأقسام. هذا يفسّر تماماً شكوى المستخدم: كل تحسينات هذه الجلسة (تقليص عدد البطاقات/الرقاقات، إلخ) لم تكن لتُشعِر بفرق حقيقي على الموبايل ما دامت المسافات الفارغة بين الأقسام ضخمة كما كانت دوماً.

### موقعها
`app/page.tsx` — الحشوة العمودية (`padding`) لكل قسم رئيسي في الصفحة (7 مواضع) + الفوتر.

### أثرها على المستخدم
هذا على الأرجح **السبب الأهم** وراء شعور "لا تغيير" على الموبايل — أكبر من أي تغيير آخر تم هذه الجلسة، لأنه يؤثر على كل تمريرة (scroll) في الصفحة الرئيسية بالكامل.

### مستوى الأولوية
عالٍ جداً — هذا بالضبط نوع المشكلة التي طلب المستخدم معالجتها.

### الحل المنفَّذ
خفضت الحد الأدنى (floor) فقط في كل قيمة `clamp()` لسبعة أقسام، مع إبقاء الحد الأقصى (قيمة سطح المكتب) كما هو تماماً:
- الهيرو: `clamp(48px→28px, 6vw, 88px)` أعلى، `clamp(56px→32px, 7vw, 96px)` أسفل
- المعاملات: `clamp(48px→28px, 5vw, 80px)`
- الفئات: `clamp(32px→20px, 4vw, 56px)`
- رحلات الحياة: `clamp(48px→28px, 5vw, 80px)`
- كيف يعمل: `clamp(48px→28px, 5vw, 80px)`
- الثقة: `clamp(40px→24px, 5vw, 72px)`
- الفوتر: `clamp(40px→28px, 5vw, 64px)` أعلى، `clamp(24px→20px, 3vw, 40px)` أسفل

النتيجة: إجمالي حشوة الأقسام على الموبايل ينخفض من ~536px إلى ~316px (تخفيض ~40%). **تحقّقت رياضياً أن سطح المكتب غير متأثر إطلاقاً**: عند عرض سطح مكتب نموذجي (1280-1440px)، قيمة `Nvw` تتجاوز الحد الأدنى الجديد (والقديم) بكثير، فتبقى `clamp()` تُرجِع نفس قيمة `Nvw` كما كانت قبل التعديل — التعديل عملياً موبايل-فقط.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. **قيد التنفيذ**: لم أتمكّن من التقاط لقطة شاشة فعلية لعرض هاتف ضيّق في هذه الجلسة بسبب عطل في أداة تغيير حجم نافذة المتصفح المتاحة — طلبت من المستخدم مراجعة الموقع الحي على هاتفه الفعلي بعد النشر للتأكيد.

---

## تحديث (batch #356) — تدقيق ارتفاع TopNav مقابل مواصفة 56-64px — ✅ تحقّق بلا حاجة لتعديل

فحصت `components/TopNav.tsx` سطراً بسطر: الارتفاع الفعلي ثابت `height: 64` (السطر 112)، أي عند الحد الأقصى المسموح تماماً في الموجز الأصلي (56-64px). لا تعديل مطلوب — موثَّق هنا حتى لا يُعاد فحصه لاحقاً.

---

## تحديث (batch #364) — إصلاح ChatLanguageToggleChip: شريحة بلا زر إغلاق — ✅ منفَّذ

### المشكلة
أثناء متابعة بند "تبسيط أعلى شاشة المحادثة" (المتبقي من batch #353)، تبيّن أن `ChatLanguageToggleChip` كانت تُعرَض من أول رسالة (`messageCount >= 1`) **بلا أي زر إغلاق** — الطريقة الوحيدة لإخفائها كانت الضغط عليها فعلياً وتنفيذ طلب تبديل لغة الرد قد لا يريده المستخدم إطلاقاً. أي مستخدم لا يريد هذه الميزة كان يراها معلّقة أعلى كل محادثة إلى الأبد.

### موقعها
`components/ChatLanguageToggleChip.tsx`.

### أثرها على المستخدم
عنصر دائم الظهور بلا وسيلة تجاهل حقيقية — يتراكم بصرياً فوق `ChatSummaryCard` (5+ رسائل) و`ChatSessionSummaryChip` (10+ رسائل، وهذه لديها زر إغلاق أصلاً) في نفس المنطقة أعلى المحادثة.

### الحل المنفَّذ
أضفت حالة `dismissed` وزر "✕" منفصل بجانب الشريحة (بنفس نمط `ChatSessionSummaryChip` الموجود مسبقاً). لا تغيير في السلوك عند الضغط على الشريحة نفسها — فقط أصبح بالإمكان الآن تجاهلها دون استخدامها.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف.

---

## سجل الدفعات المرتبط بهذا الملف — إضافة
- **batch #364**: إضافة زر إغلاق لـ `ChatLanguageToggleChip` (كانت بلا وسيلة تجاهل حقيقية).

---

## تحديث (batch #357) — Lazy-load لثلاث نوافذ منبثقة ثقيلة — ✅ منفَّذ

### المشكلة
`GuidedFlow` و`TransactionStarter` و`ServiceGroupSheet` كانت مستوردة بشكل eager في `app/page.tsx` رغم أنها نوافذ منبثقة (modals) لا تُعرض إلا بعد فعل مستخدم صريح (زر "ابدأ معاملة"، بطاقة فئة خدمة...) — أي أن معظم الزيارات لا تفتحها أبداً، لكن كودها كان يُحمَّل ضمن الحزمة الرئيسية لكل تحميل للصفحة الرئيسية.

### الحل
تحويل الثلاثة إلى `next/dynamic` مع `ssr: false` (نوافذ عميل بحتة، لا محتوى SEO/مؤشرات فيها). تحقّقت من أن `ServiceGroupSheet` و`TransactionStarter`/`GuidedFlow` كلها تُرجِع `null` داخلياً عند عدم الفتح (أو مُغلَّفة بشرط `{show... && (...)}` في `page.tsx`)، فلا خطر من تحميلها متأخرة. استيراد النوع `StarterResult` (type-only، يُحذَف عند البناء) أُبقي كاستيراد ساكن منفصل عن الاستيراد الديناميكي للمكوّن نفسه.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. أثر الأداء: تقليل حجم الحزمة الأولى للصفحة الرئيسية (bundle-size)، صفر تغيير وظيفي أو بصري.

---

## تحديث (batch #358) — تبسيط شريط أدوات صندوق إدخال المحادثة — ✅ منفَّذ جزئياً

### الفحص
دقّقت كل عناصر صف "أدوات الإدخال" فوق مربع الكتابة (`ModeSelector`, `ChatResponseLength`, `ChatContextBar`, `ChatKeyboardSendHint`, `ChatInputCharCounter`, `ChatDraftAutosave`, `ChatQuickReplies`). النتيجة: معظمها **مموَّه ذاتياً بالفعل** ولا يظهر إلا عند الحاجة — `ChatContextBar` يختفي عند الفراغ، `ChatInputCharCounter` لا يظهر تحت 200 حرف، `ChatKeyboardSendHint` مخفي على اللمس أصلاً (لا يظهر على الموبايل إطلاقاً)، `ChatDraftAutosave` لا يُرسم شيئاً مرئياً (مساعد خفي بالكامل)، و`ModeSelector` يتقلّص فعلاً إلى زر واحد مضغوط + bottom sheet على الموبايل (نمط جيد موجود مسبقاً).

العنصر الوحيد الذي **لم يكن مضغوطاً**: `ChatResponseLength` — شريط تبديل من 3 أزرار بنص كامل، ظاهر دائماً بجانب زر `ModeSelector` المضغوط، ما يخلق عدم تناسق ويستهلك عرضاً غير ضروري على الشاشات الضيّقة.

### الحل
أضفت breakpoint مطابق لنفس نقطة `ModeSelector` (640px) داخل `ChatResponseLength.tsx`: تحت 640px تُخفى النصوص وتبقى الأيقونات فقط + `aria-label`/`title` صريح على كل زر (تحسين وصولية إضافي، لأن الاسم الوصولي كان يعتمد سابقاً فقط على النص المرئي). لا حذف وظيفي — كل الأزرار الثلاثة (موجز/عادي/مفصّل) تعمل كما هي، فقط أضيق مساحةً على الهاتف.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف. **متبقٍ للمستقبل**: توحيد `ChatResponseLength` مع `ModeSelector` في نمط "زر واحد + sheet" الكامل بدل أيقونات مضغوطة فقط — تحسين أعمق لكن يتطلب مكوّن sheet جديد، أجّلته لتفادي مخاطرة أكبر في دفعة واحدة.

---

## تحديث (batch #359) — تدقيق مناطق Landmark (WCAG 2.2 "Bypass Blocks") — ✅ منفَّذ

استخدمت Explore agent لتغطية كل صفحات `app/**/page.tsx` ومكوّنات `<nav>`/`<header>`/`<footer>`. النتائج والإصلاحات:

1. **رابط "تخطّي إلى المحتوى" كان معطَّلاً على صفحتين**: `app/professional/page.tsx` و`app/settings/page.tsx` — الغلاف الجذري فيهما كان `<div>` عادياً بلا `id="main-content"`، فرابط `#main-content` في `app/layout.tsx` لم يكن يوصل الفوكس إلى أي مكان فيهما. **الإصلاح**: حوّلت غلاف المحتوى في كلا الملفين إلى `<main id="main-content">` (في `settings/page.tsx` أُبقي الهيدر خارج `<main>` و`BottomNav` خارجه أيضاً، بنفس النمط المتّبع في `procedures/page.tsx`). كل الصفحات الـ18 الأخرى المفحوصة كانت صحيحة أصلاً (main واحد فقط، id واحد فقط).
2. **عناصر `<nav>` المتعددة**: كلها موسومة بـ`aria-label` مميّز (`TopNav`، `BottomNav`، `Breadcrumbs`، `DraftingStudio`) — لا تعارض.
3. **مَعلَمان `<footer>` يُعرَضان معاً على الصفحة الرئيسية في وضع الترحيب** (`app/page.tsx`): فوتر التسويق (داخل `<main>`) وشريط إدخال الدردشة الدائم (خارج `<main>`، يُستخدم عنصر `<footer>` عليه فعلياً رغم أنه شريط أدوات لا تذييل صفحة). كلاهما بلا `aria-label` يُميّزهما. **الإصلاح**: أضفت `aria-label` مميّزاً لكل منهما ("تذييل الصفحة" / "شريط إدخال الرسالة") دون تغيير نوع العنصر نفسه (تفادياً لأي مخاطرة أنماط CSS مرتبطة بوسم `footer`).

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف.

---

## تحديث (batch #360) — تدقيق ترتيب التنقّل بلوحة المفاتيح (Tab order) — ✅ منفَّذ جزئياً + بنية تحتية جاهزة

### المشكلة
تدقيق شامل (`grep` لكل استخدامات `tabIndex`) أكّد عدم وجود أي `tabIndex` موجب (خطأ شائع) — نقطة إيجابية. لكن الفحص كشف أن **كل مكوّنات `role="dialog"` الـ13 في التطبيق** (`GlobalSearch`, `MobileMenu`, `ServiceGroupSheet`, `MobileModeSheet`, `GuidedFlow`, `TransactionStarter`, `ProcedureSearchModal`, `ProcedureFilterDrawer`, `PrintProcedureModal`, `EscalationModal`, `DocumentIntelligenceView`, `AccessibilityBar`, `FloatingHelpButton`) تتعامل مع Escape للإغلاق وقفل تمرير الخلفية، لكن **لا تحبس تنقّل Tab داخلها** — أي أن مستخدم لوحة المفاتيح يمكنه الخروج بـTab من نافذة مفتوحة إلى عناصر الصفحة المخفية خلفها (مخالفة WCAG 2.4.3).

### الحل
بنيت خطّافاً قابلاً لإعادة الاستخدام `lib/useFocusTrap.ts`: يحبس Tab/Shift+Tab داخل حاوية النافذة (يلتف من آخر عنصر قابل للتركيز إلى أوّله والعكس)، وينقل الفوكس تلقائياً لأول عنصر عند الفتح إن لم يكن الفوكس بداخلها أصلاً، ويُعيد الفوكس لمكانه الأصلي عند الإغلاق. طبّقته هذه الدفعة على أكثر مكوّنين استخداماً من لوحة المفاتيح: `GlobalSearch` (Cmd+K) و`MobileMenu` (قائمة التنقّل الرئيسية على الموبايل).

### الحالة
✅ منفَّذ بالكامل (batch #360 كان جزئياً، batch #361 أكمله). `tsc --noEmit` نظيف.

---

## تحديث (batch #361) — إكمال Focus Trap على الـ11 مكوّناً المتبقّية — ✅ منفَّذ

طبّقت `useFocusTrap` على الـ11 مكوّناً المتبقّية من قائمة batch #360: `ServiceGroupSheet`، `MobileModeSheet`، `GuidedFlow`، `TransactionStarter`، `ProcedureSearchModal`، `ProcedureFilterDrawer`، `PrintProcedureModal`، `EscalationModal`، `DocumentIntelligenceView` (تحديداً `GenerateDraftModal` الداخلي فيه)، `AccessibilityBar`، `FloatingHelpButton`.

لكل مكوّن تحقّقت أولاً كيف يُغلَق/يُفتَح (بعضها يُركَّب دائماً أثناء وجوده لأن الأب يستخدم `{show && <X/>}`، وبعضها الآخر يملك حالة `open` داخلية) قبل تمرير القيمة الصحيحة كوسيط `active` — وليس افتراض `true` للجميع. في `FloatingHelpButton` استخدمت `panelRef` الموجود مسبقاً (كان يُستخدم فقط لكشف النقر خارج اللوحة) بدل إضافة ref جديد، لأنه يُغلّف الزر المُشغِّل واللوحة معاً — بالضبط الحاوية الصحيحة للفخ. في المكوّنات التي تُركِّز عنصراً محدداً عند الفتح (`closeRef.current?.focus()` في `GuidedFlow`/`TransactionStarter`/`EscalationModal`) تحقّقت أن ترتيب الـ`useEffect` يضمن أن التركيز الصريح للمكوّن نفسه يُطبَّق بعد التركيز التلقائي لأول عنصر من `useFocusTrap` — فلا يوجد أي تغيير في أي مكوّن أين يُركَّز الفوكس أولاً عند الفتح.

### الحالة
✅ منفَّذ بالكامل — كل الـ13 مكوّن `role="dialog"` في التطبيق الآن يحبس Tab بشكل صحيح. `tsc --noEmit` نظيف.

---

## تحديث (batch #362) — تحسين أداء مؤقّت جلسة المحادثة — ✅ منفَّذ

`ChatSessionTimer` كان يُحدَّث كل ثانية (setInterval 1000ms) بعرض HH:MM:SS، ما يعني إعادة رسم للمكوّن كل ثانية طوال مدة أي محادثة مفتوحة — لا أحد يحتاج دقة الثانية لمؤشر "مدة الجلسة". غيّرت العرض إلى دقة الدقيقة (`Xm` / `Xد`) والتحديث إلى كل 30 ثانية بدل كل ثانية — تخفيض 30× في وتيرة إعادة الرسم، نفس الميزة المرئية تماماً، صفر حذف وظيفي. باقي عناصر الصف العلوي (`ChatSummaryCard`, `HomepageChatSuggestionsBar`, `ChatSessionSummaryChip`, `ChatPinnedBanner`, `ChatLanguageToggleChip`) تحقّقتُ أنها جميعاً مموَّهة ذاتياً بالفعل (تختفي حين لا تنطبق شروطها) — لا حاجة لتعديلها.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف.

---

## تحديث (batch #363) — Lazy-load مكوّنَين ثقيلين إضافيَين — ✅ منفَّذ

- **`DocumentIntelligenceView.tsx`** (944 سطراً — أكبر مكوّن في التطبيق) كان مستورَداً بشكل eager داخل `ChatMessage.tsx` رغم أن `msg.documentAnalysis` لا يكون صحيحاً إلا لرسالة نتجت فعلياً عن تحليل مستند مرفوع — أقلية صغيرة من الرسائل. كل محادثة كانت تُحمِّل هذا المكوّن الضخم بلا داعٍ. حوّلته إلى `next/dynamic` مع `ssr:false`.
- **`UserOnboarding.tsx`** (433 سطراً) كان مستورَداً بشكل eager في `app/page.tsx` رغم أنه مموَّه داخلياً بالكامل (`if (!show) return null`، والـ`show` يُحدَّد من localStorage — لا يظهر إلا لزائر جديد فعلياً). حوّلته إلى `next/dynamic` مع `ssr:false`.

كلاهما مكوّنات عميل بحتة (بلا محتوى SEO)، فـ`ssr:false` آمن تماماً. **متبقٍ للمستقبل**: `KeyboardShortcutsHelp` (168 سطراً) و`AppointmentReminder` (188 سطراً) مؤهّلان أيضاً لكن حجمهما أصغر بكثير — أُجِّلا لتفادي دفعة كبيرة غير ضرورية.

### الحالة
✅ منفَّذ. `tsc --noEmit` نظيف.

---

## سجل الدفعات المرتبط بهذا الملف
- **دفعة اليوم (2026-07-25)، إضافات لاحقة**: batch #354 (إزالة استيرادات ميتة) + batch #355 (كثافة حشوة الموبايل) + batch #356 (تحقّق ارتفاع TopNav) + batch #357 (lazy-load 3 نوافذ) + batch #358 (تبسيط شريط أدوات الإدخال) + batch #359 (تدقيق Landmark regions) + batch #360 (بنية Focus Trap + تطبيقها على مكوّنين) + batch #361 (إكمال Focus Trap على الـ11 مكوّناً المتبقّية — 13/13 مكوّن الآن) + batch #362 (تحسين أداء ChatSessionTimer) + batch #363 (lazy-load DocumentIntelligenceView + UserOnboarding).

---

## المرحلة 3 — "Calm Government Digital Service" — إعادة بناء الواجهة من الصفر (v4.0) — 🟡 قيد التنفيذ (batch #366)

### السياق
بعد كل التحسينات التدريجية أعلاه، أرسل المستخدم لقطة شاشة فعلية من الهاتف وقال بوضوح إن الشكل "لازال هكذا" وطلب تغيير الواجهة كلها دفعة واحدة، ثم أرسل موجزاً تصميمياً كاملاً من 29 بنداً يطلب فيه صراحة: **عدم** الاستمرار بالتعديل التدريجي، إعادة بناء فعلية للواجهة (وليس تقريراً فقط)، نفس الهوية (الاسم/الشعار/اللون العنّابي) مع تنفيذ بصري جديد كلياً باسم "Calm Government Digital Service" — بلا تدرّجات لونية (gradient)، بلا ظلال، بلا زخرفة غير وظيفية، مساحات بيضاء واسعة، لون أساسي واحد يُستخدم بحدّ أقصى 10-15% من الشاشة. حدّد المستخدم قيم الألوان/الخطوط/المسافات/الزوايا حرفياً، وترتيب أقسام الصفحة الرئيسية العشرة بالضبط، وأولوية القرار: **جودة الموبايل البصرية ← الوضوح ← البساطة ← الاتساق ← سهولة الاستخدام ← الوصول ← الأداء ← تكيّف سطح المكتب.**

### الرموز البصرية الجديدة (v4.0 tokens — `app/globals.css`)
اللون الأساسي `#9F1D2F` (hover `#861827`, soft `#F8ECEF`)، الخلفية `#F8F8F6`، السطح `#FFFFFF`، النص الأساسي `#181817`، النص الثانوي `#65635F`، الحدود `#E5E2DD`. زوايا: إدخال/بطاقة 14px، زر 12px، نافذة منبثقة 20px. كل الظلال أُلغيت تقريباً (`0 1-2px، شفافية منخفضة جداً`)، وكل متغيّرات "gradient" السابقة أصبحت أسماء مستعارة للّون الواحد المسطّح (لا يوجد تدرّج فعلي في أي مكان بعد الآن).

### ما نُفِّذ فعلياً هذه الدفعة
1. **الهيدر (`TopNav.tsx`)** — على الموبايل: أصبح الشعار + زر القائمة فقط (كان يحوي أيضاً جرس الإشعارات، البحث، زر "محادثة جديدة"، وتبديل اللغة متزاحمين في صف 56-64px). العناصر الأربعة المُزاحة **لم تُحذف** — انتقلت إلى داخل `MobileMenu`.
2. **`MobileMenu.tsx`** — استوعب البحث والإشعارات (كمكوّنين حقيقيين مُعاد استخدامهما، لا إعادة تنفيذ) + صفوف جديدة لإمكانية الوصول/المساعدة/حول التطبيق/سياسة الخصوصية.
3. **صفحة جديدة `app/privacy/page.tsx`** — محتوى تقني دقيق فقط (ما يُخزَّن محلياً، بيانات الحساب، المحادثة الذكية، التواصل) بلا أي ادعاء قانوني غير مؤكَّد، وموسوم صراحة في الكود كمسودة تحتاج مراجعة قانونية فعلية قبل اعتمادها نهائياً.
4. **الصفحة الرئيسية (`app/page.tsx`)** أُعيد بناؤها للترتيب الحرفي المطلوب (10 أقسام):
   - الهيدر الصغير ← عنوان "ما المعاملة التي تبحث عنها؟" ← نص مساعد ← شريط بحث واحد (بلا زر إرسال منفصل؛ المسح/التحسين بالذكاء الاصطناعي/الميكروفون كلها داخل الحقل) ← صف إجراءين سريعين فقط.
   - "الأكثر بحثاً": من شبكة بطاقات إلى قائمة صفوف موحّدة (أيقونة + اسم + سهم)، نفس الإجراءات الأربعة الحقيقية.
   - "تصفّح حسب الفئة": شبكة ثابتة عمودين × 6 فئات حقيقية، أيقونات خطية جديدة لكل فئة.
   - **أُزيلت من العرض** (البيانات والمنطق لم تُحذف، ما زالت قابلة للوصول عبر البحث/القائمة): شبكة "أحداث الحياة"، قسم "كيف يعمل دليلك" التوضيحي بثلاث خطوات، شبكة الثقة الرباعية — استُبدلت بسطر ثقة واحد كما طلب الموجز حرفياً.
   - **الفوتر**: من فوتر ثلاثي الأعمدة داكن (8 روابط + تبديل لغة مضمّن) إلى صف واحد فاتح مضغوط (الشعار، حقوق النشر، رابط "حول"، رابط "الخصوصية"، سطر إخلاء المسؤولية القانوني). كل الروابط المحذوفة من الفوتر (الخدمات/المعاملات/الجهات/النماذج/الأسئلة/ملفاتي/استوديو الصياغة) ما زالت متاحة بضغطة واحدة من قائمة الهامبرغر — لم تُحذف أي وظيفة، فقط أُعيد تنظيمها.
   - زر "الرئيسية" (في `BottomNav` و`MobileMenu`) يُنفّذ الآن نفس تسلسل الحفظ والتفريغ الذي كان زر "محادثة جديدة" في الهيدر القديم يقوم به (تعويض كامل عن إزالة ذلك الزر من الهيدر).
5. **`BottomNav.tsx`** — أُزيل الزر العائم الدائري المتدرّج اللون لِـ"محادثة" (كان يحمل ظلاً ملوّناً وارتفاعاً عن باقي الأزرار وحركة قفز) — أصبح تبويباً رابعاً مسطّحاً كالباقي، بلون `var(--brand)` عند التفعيل فقط، بلا شريط علوي طويل يشير للتفعيل (اللون والوزن وحدهما كافيان، كما في باقي التبويبات). أُعيدت تسمية التبويب "اسأل دليلك" حسب نص الموجز الحرفي. `min-height:64px` + `env(safe-area-inset-bottom)`.

### ما لم يُنفَّذ بعد من الموجز (متابَع صراحة، وليس منسياً)
- استخلاص المكوّنات الموحّدة كملفات مستقلة (`MobileHeader`, `SearchInput`, `ServiceCategoryCard`, إلخ) — كل الشيفرة الجديدة أعلاه ما زالت مضمّنة داخل `page.tsx`/`TopNav.tsx`/`MobileMenu.tsx`.
- إعادة بناء صفحة قائمة الإجراءات (`/procedures`) وصفحة تفاصيل الإجراء حسب المواصفات التفصيلية في الموجز.
- إعادة بناء واجهة المحادثة (الهيدر/صندوق الكتابة/الاقتراحات) حسب المواصفات.
- ملف `MOBILE_UI_AUDIT.md` المطلوب صراحة كمُخرَج مستقل.
- تدقيق WCAG 2.2 AA مخصّص للرموز والمكوّنات الجديدة (تباين الألوان الجديدة، حجم أهداف اللمس 44×44، الحركة).
- التحقق البصري الفعلي (لقطات شاشة متعددة القياسات، Lighthouse) — **قيد بيئة العمل نفسه المذكور أعلى الملف**: لا يوجد متصفح فعلي أو Lighthouse في بيئة التنفيذ؛ البديل هو المراجعة الحية على الرابط المنشور بعد كل `push.sh`.

### حالة التنفيذ
🟡 قيد التنفيذ — القسم الأعلى أولوية (الهيدر + الصفحة الرئيسية + شريط التنقّل السفلي، أي "جودة الموبايل البصرية" حسب أولوية المستخدم المعلنة) منفَّذ ومتحقَّق منه عبر `tsc --noEmit` (نظيف بعد كل ملف). الأقسام المتبقية (صفحات الإجراءات، المحادثة، استخلاص المكوّنات) تُكمَل في الدفعات التالية.

### تحديث (batch #367) — بدء `/procedures`: الهيدر + الإحصائيات + رقائق الوزارات + شريط البحث — ✅ جزء منفَّذ

طُبِّقت رموز v4.0 على الجزء الأعلى ظهوراً من صفحة قائمة الإجراءات (`app/procedures/page.tsx`):
- **الهيدر**: من بانر متدرّج عنّابي (خلفية gradient + عناصر شبه شفافة بيضاء + زر تبديل لغة) إلى هيدر مسطّح (سطح أبيض، حدّ سفلي رفيع، عنوان + زر رجوع فقط) — نفس نمط الصفحة الرئيسية الجديدة تماماً. زر تبديل اللغة أُزيل من هنا (متاح فعلياً عبر `MobileMenu`).
- **شريط الإحصائيات الثلاثي**: من بطاقة "مميّزة" بخلفية متدرّجة + ظل ملوّن إلى 3 بطاقات موحّدة الشكل بلا تمييز لوني.
- **رقائق فلترة الوزارات**: من رقائق بحدود سميكة/ظل عند التفعيل إلى رقائق مسطّحة (`var(--brand-soft)` عند التفعيل فقط، بلا ظل).
- **شريط البحث**: من حدّ 1.5px + ظل تركيز مزدوج إلى حدّ 1px بلا ظل، زوايا 14px حسب المقياس المعتمد.
- **صفوف الإجراءات (الحالة المطوية فقط)**: أُزيلت الظلال والتدرّجات من حدود البطاقة، الشارة الأيقونية، وشارات "مُرشدة/عدد الوثائق/عدد الخطوات" — كلها الآن تستخدم `var(--brand-soft)`/`var(--bg)` المسطّحة بدل الألوان شبه الشفافة العنّابية القديمة.

**ملاحظة صريحة عن نطاق لم يُنفَّذ بعد**: الصف الموسّع لكل إجراء "enriched" (عند الضغط على البطاقة) ما زال يعرض ~10 شارات/رقائق مكدّسة في الصف المطوي نفسه (الوزارة، عدد الوثائق، عدد الخطوات، شارة الصعوبة، شارة الأولوية، عدّاد المشاهدات، شارة "نموذج"، شارة الرسوم، وسم الإصدار، جرس التذكير، شارة التقدّم) — هذا يخالف صراحة مطلب الموجز "لا تصميم بطاقات ضخمة / لا رقائق فلترة متكدسة" لصفوف القائمة. إصلاح هذا يتطلّب طيّ أغلب هذه الشارات داخل الحالة الموسّعة (نمط "المزيد" المستخدم سابقاً في batch #353 لأزرار رسائل المحادثة) — قرار نطاق أكبر مؤجَّل لدفعة مخصّصة قادمة، موثَّق هنا بدل تجاهله.

**لم يُلمَس عمداً هذه الدفعة**: منطق الفلترة/البحث بالكامل، وكل المحتوى الداخلي للحالة الموسّعة لكل بطاقة (35+ مكوّناً فرعياً مجمّعة مسبقاً عبر `ProcedureSectionGroup` من دفعات سابقة) — هذا الجزء معقّد وعالي الخطورة لإعادة بنائه ضمن دفعة واحدة غير مُتحقَّق منها بصرياً.

`tsc --noEmit` نظيف بعد كل تعديل.

### تحديث (batch #368) — صفحة تفاصيل الإجراء (`ProcedureDetailClient.tsx`) — ✅ الجزء الأعلى ظهوراً منفَّذ

طُبِّقت رموز v4.0 على صفحة تفاصيل الإجراء الفردية:
- **الهيدر**: من بانر متدرّج + زر تبديل لغة إلى هيدر مسطّح (نفس نمط باقي الصفحات) يعرض عنوان الإجراء مباشرة (بدل عنوان "الدليل" العام) — العنوان لم يعد مكرَّراً كـ `<h1>` ضخم داخل البطاقة الرئيسية لتفادي عنوانين بنفس الوزن البصري في نفس الشاشة.
- **البطاقة الرئيسية**: من ظل + إطار سميك + شارة أيقونية متدرّجة إلى بطاقة مسطّحة بحدّ 1px.
- **الأزرار**: "اسأل دليلك" (أساسي) + "الرئيسية" (ثانوي) — تدرّج/ظل أُزيلا، الزر الثانوي أصبح حدّاً رفيعاً بدل حدّ عنّابي سميك. زر "دليل التنفيذ" أصبح بنفس نمط الزر الثانوي.
- **قسم المستندات المطلوبة وقسم الخطوات**: إطارات/خلفيات الأقسام (`Section`) أصبحت مسطّحة بالكامل؛ دوائر ترقيم الخطوات فقدت التدرّج والظل (لون واحد `var(--brand)`)؛ الشارات الملوّنة (وقت المعالجة بالأصفر، إلخ) وُحِّدت إلى رمادي محايد (`var(--bg)`/`var(--text-2)`) عدا حيث اللون له معنى فعلي (شارة الصعوبة/التعقيد في القائمة، محفوظة كما هي لأنها تحمل معلومة).
- **قسم "تفاصيل إضافية" المطوي** (الجهة/الرسوم): مسطّح بالكامل، وبطاقة الدعوة الختامية لسؤال دليلك فقدت التدرّج والظل.

هذا يستكمل تطبيق الرموز على المسار الأساسي الثلاثي (الرئيسية ← قائمة الإجراءات ← تفاصيل الإجراء) الذي يمثّل غالبية تنقّل المستخدم الفعلي في التطبيق. `tsc --noEmit` نظيف.

### تحديث (batch #369) — واجهة المحادثة (`app/page.tsx`) — ✅ الألوان + التدرّجات

**اكتشاف مهم أثناء التدقيق**: صندوق المحادثة (الهيدر، شريط الكتابة، مؤشر التسجيل، فقاعة "يكتب...") يستخدم متغيّرات CSS محلية معرَّفة داخل `<style>` خاص بهذا الملف: `--red`, `--red-dark`, `--red-light` — وكانت هذه لا تزال مثبَّتة يدوياً على اللون العنّابي **القديم** (`#8F1D2C`/`#741622`) من قبل v4.0، بينما كل صفحة أخرى في التطبيق انتقلت فعلياً إلى رمز v4.0 (`#9F1D2F`/`#861827`). هذا يعني أن زر الإرسال، حالة التسجيل الصوتي النشطة، مؤشر التسجيل، وحدود حقل الإدخال عند التركيز كانت **كلها** ترسم باللون القديم رغم أن باقي التطبيق تغيّر — على الأرجح أحد أسباب شعور "الشكل ما زال قديماً" الذي أبلغ عنه المستخدم. الإصلاح: تحديث الاسم المستعار المحلي ليشير إلى `var(--brand)`/`var(--brand-hover)`/`var(--brand-soft)` الحقيقية بدل قيم لونية ثابتة — يُصحّح هذا تلقائياً كل الاستخدامات في الملف دفعة واحدة.

**تسطيح إضافي**: خلفية شريط الإدخال السفلي (لون التمويه الأبيض مع الشفافية، حُدِّث للون الخلفية الجديد `#F8F8F6`)، خلفية مؤشر "جاري الاستماع" (تدرّج → `var(--brand-soft)` مسطّح)، خلفية رقاقتَي "اقتراح التحسين" (أزرق) و"تحسين الإدخال الصوتي" (كهرماني) (تدرّج → لون واحد مسطّح)، حدود وظل صندوق الكتابة (أُزيل الظل، الحدّ من 1.5px إلى 1px)، دوائر فقاعة "يكتب..." وحدودها (أُزيل الظل)، أزرار التسجيل الصوتي/الإرسال النشطة (أُزيل التدرّج والظل الملوّن)، وزر "المزيد" أسفل كل رسالة (رموز مسطّحة بدل قيم rgba ثابتة).

**لم يُلمَس عمداً**: التكديس الوظيفي أعلى قائمة الرسائل (ChatSummaryCard، مؤقّت الجلسة، رقائق الاقتراح، شريحة ملخّص الجلسة، شريط التثبيت، شريحة تبديل اللغة) — هذه كلها عناصر شرطية الظهور (تظهر فقط بعد عدد رسائل معيّن) وأُعيد تنظيمها بالفعل في دفعات سابقة (batch #362/#364)؛ إعادة هيكلتها بالكامل لتطابق حرفياً "3 اقتراحات بداية فقط، بلا عناصر زخرفية" يتطلّب قرار حذف/دمج وظائف فعلية أكبر من نطاق تسطيح الألوان الآمن لهذه الدفعة، ومؤجَّل لدفعة مخصّصة. أزرار الإرفاق/التحسين/الميكروفون كانت بالفعل بحجم معقول (38×38) وليست "كبيرة الحجم" كما يحذّر الموجز — لا تغيير مطلوب هناك. `tsc --noEmit` نظيف.

### تحديث (batch #370) — 🔴 إصلاح عطل حقيقي: أزرار شريط التنقّل السفلي لا تستجيب على الهاتف

**بلاغ المستخدم**: "شريط التنقّل السفلي" — عند الضغط على أي من أزراره الأربعة، "لا يحدث أي شيء إطلاقاً".

**السبب الجذري المؤكَّد**: كل زر في `BottomNav.tsx` يستخدم `className="bn-tab-btn tap-hit-8"`. صنف `.tap-hit-8` (المعرَّف في `globals.css` ضمن مجموعة `.tap-hit-N`) يضيف طبقة شفافة `::before` بـ `position: absolute; inset: -8px;` لتوسيع منطقة اللمس داخل `@media (pointer: coarse)` (أي أجهزة اللمس تحديداً — الهواتف). التعليق أعلى هذا الصنف في `globals.css` يوضّح صراحة: **"العنصر الذي يستخدم أحد هذه الأصناف يجب أن يملك `position: relative` (أو أي قيمة موضع صريحة أخرى) ضمن الـ style المباشر كي ترتكز ::before عليه بشكل صحيح"**.

زر `BottomNav` لم يكن يملك `position` في كائن الـ `style` الخاص به إطلاقاً. بما أن أقرب سلف له بموضع فعلي هو `<nav>` نفسه (`position: fixed`)، فإن كل طبقات `::before` الأربعة (لكل الأزرار الأربعة) كانت ترتكز جميعها على صندوق الـ `<nav>` الكامل (العرض الكامل للشاشة) بدل صندوق كل زر على حدة — أي أن الطبقات الشفافة الأربعة توسّعت لتغطي شريط التنقّل بأكمله متراكبة فوق بعضها، بدل أن تبقى محصورة بكل زر. هذا يخالف نمط اللمس المصمَّم فعلياً في أي مكان آخر بالتطبيق (`TopNav.tsx` و`MobileMenu.tsx` يطبّقان `position: relative` بشكل صحيح — تحقّقتُ بمقارنة مباشرة).

**نطاق الفحص**: بحثتُ عن كل استخدام لأصناف `tap-hit-N` المضافة هذه الجلسة تحديداً (وليس التي أُضيفت واختُبرت في دفعات سابقة) ووجدتُ **3 مواضع** تعاني من نفس العطل بالضبط:
1. `components/BottomNav.tsx` — أزرار التبويبات الأربعة (الأهم، وهو بلاغ المستخدم الفعلي).
2. `app/procedures/page.tsx` — رقائق فلترة الوزارات (`tap-hit-5`).
3. `app/procedures/page.tsx` — زر مسح البحث (`tap-hit-8`).

**الإصلاح**: أُضيف `position: 'relative'` إلى كائن الـ `style` المباشر لكل من الأزرار الثلاثة أعلاه. لا تغيير في المنطق أو المسارات أو أي شيء آخر — سطر واحد لكل زر.

**ملاحظة أمانة**: لا يمكن التحقق البصري من هذا الإصلاح فعلياً على جهاز هاتف حقيقي من بيئة التنفيذ هذه (قيد بيئة عمل معروف ومذكور أعلى الملف — لا يوجد محاكي حجم شاشة فعلي متاح). التحقق تم عبر: (أ) قراءة الكود المصدري لصنف `.tap-hit-N` وتأكيد مخالفة الشرط الموثَّق صراحة في تعليق الكود نفسه، (ب) مقارنة مباشرة مع مكوّنات أخرى (`TopNav.tsx`, `MobileMenu.tsx`) تستخدم نفس الصنف وتطبّق `position: relative` بشكل صحيح فتعمل بدون مشاكل، (ج) `tsc --noEmit` نظيف. **يُرجى من المستخدم تجربة الشريط السفلي فعلياً على الهاتف بعد تشغيل `push.sh` هذه المرة والتأكيد.**

### تحديث (batch #371) — v4.0: /faq + /authorities + /forms

استُكملت رموز v4.0 المسطّحة على ثلاث صفحات إضافية، بنفس نمط الهيدر المسطّح المطبَّق سابقاً على `/procedures`:
- **`/faq`**: هيدر مسطّح (حُذف زر تبديل اللغة منه)، بطاقات إحصائيات موحّدة، شريط بحث بلا ظل مزدوج، رقائق تصنيف بحدود 1px ونصف قطر 999px، شريط "الأكثر بحثاً" بلون علامة مسطّح، أكورديون الأسئلة بحدود/ظلال مسطّحة، دوائر ترقيم الخطوات مسطّحة (بدل التدرّج).
- **`/authorities`**: نفس نمط الهيدر، بطاقات إحصائيات موحّدة (أُزيلت ميزة "البطاقة المميّزة" ذات التدرّج)، بطاقات الجهات بحدود 1px بلا ظل وبلا حركة `translateY` عند التحويم (استبدلت بتغيير لون الحدّ فقط)، خريطة ألوان أنواع الجهات (`TYPE_COLORS`) حُدِّثت لتستخدم رموز v4.0 الحقيقية بدل الألوان العنّابية القديمة لنوع "council".
- **`/forms`**: نفس النمط، تبويبات العرض الثلاثة (نماذج PDF / الكل / منظّمة) وشريط فلترة الوزارات ورقائق الحالة كلها مسطّحة الآن، بطاقات المعاملات/النماذج بحدود 1px بلا ظل.

في الحالات الثلاث: زر تبديل اللغة أُزيل من الهيدر (بنفس القرار المتّخذ في batch #367 لصفحة `/procedures`) لأن الموجز يمنع وجود عنصر لغة داخل الهيدر على الجوّال. **ملاحظة أمانة**: هذه الصفحات (خلافاً للصفحة الرئيسية) لا تعرض `MobileMenu`، لذا زر تبديل اللغة غير متاح مباشرة من داخلها — يتطلّب العودة للرئيسية أولاً. هذا نفس القرار المتّخذ سابقاً وغير المصحَّح بعد؛ مُسجَّل هنا بدل تجاهله، ويُترك كنطاق متابعة مستقبلي إن رغب المستخدم بإصلاحه.

**لم يُنفَّذ بعد**: `/services` (909 سطر) — مؤجَّلة لحجمها، مسجَّلة كمهمة منفصلة.

`tsc --noEmit` نظيف بعد كل ملف.

### تحديث (batch #372) — v4.0: /services (آخر صفحة رئيسية متبقية)

أُكمل تطبيق رموز v4.0 على `/services` (909 سطر — أكبر صفحة متبقية):
- **الهيدر**: مسطّح بنفس نمط باقي الصفحات (زر رجوع 38×38 + عنوان)، بدون الـ badge الإحصائي القديم في الهيدر (بقي شارة عدد النتائج عند التصفية فقط).
- **شريط البحث**: حدّ 1px بلا ظل مزدوج.
- **بطاقات الإحصائيات** (داخل قسم "نظرة عامة" القابل للطي): وُحِّدت، أُزيلت ميزة "البطاقة المميّزة" ذات التدرّج.
- **رقائق الفئات**: حدود 1px، امتلاء `var(--brand-soft)` فقط عند التفعيل، بلا ظل ملوّن.
- **بطاقات الخدمات في الشبكة**: أُزيل تأثير `translateY` + الظل الملوّن عند التحويم (استبدل بتغيير لون الحدّ فقط)، شارة الأيقونة أصبحت مسطّحة بدل التدرّج.
- **ورقة تفاصيل الخدمة (`ServiceSheet`, bottom sheet)**: نفس المعاملة — شارة الأيقونة والأزرار المسطّحة، زر "اسأل دليلك" الأساسي أصبح `var(--brand)` مسطّح بدل التدرّج، ظل الورقة نفسه (الرمادي المحايد الذي يفصلها عن الخلفية) أُبقي عليه لأنه ظل حقيقي غير ملوّن ويخدم وظيفة رفع الورقة عن الشاشة (مسموح للنوافذ المنبثقة حسب الموجز).

هذا يُكمل تطبيق رموز v4.0 على كل الصفحات الرئيسية في التطبيق (الرئيسية، /procedures وتفاصيل الإجراء، المحادثة، /faq، /authorities، /forms، /services). `tsc --noEmit` نظيف.

### تحديث (batch #373) — إصلاح ثغرة الوصول إلى زر تبديل اللغة

عند تسطيح هيدرات /procedures و/faq و/authorities و/forms و/services (batches #367/#371/#372) أُزيل زر تبديل اللغة من كل هيدر بافتراض أنه "متاح عبر MobileMenu" — لكن التحقق الفعلي (قراءة الكود مباشرة، وليس افتراضاً) أثبت أن هذا غير صحيح: لا هذه الصفحات الخمس ولا `TopNav` ولا `MobileMenu` يُعرَضان فيها إطلاقاً (كل صفحة تبني هيدرها الخاص بشكل مستقل). النتيجة: لم يكن هناك أي طريقة لتبديل اللغة من هذه الصفحات الخمس — لا على الموبايل ولا على الديسكتوب. تم إصلاحها بإضافة زر تبديل لغة مضغوط (EN/AR) داخل هيدر كل صفحة من الخمس، بنفس نمط زر الرجوع (حدود 1.5px، ارتفاع 38px، `tap-hit-2` لتوسعة منطقة اللمس على الشاشات اللمسية). لا تغيير في منطق `useLanguage()`/`toggleLang` نفسه — فقط إعادة توصيله في الهيدر. `tsc --noEmit` نظيف على الملفات الخمسة.

### تحديث (batch #374) — تقليل ازدحام الشارات (badges) في بطاقة الإجراء المُثراة (/procedures)

كانت البطاقة المُثراة (enriched procedure) في حالتها المطوية (collapsed) تعرض نحو 13 عنصر شارة/رقاقة موزعة على 4 صفوف فرعية — نقطة كانت موثّقة كملاحظة متابعة معلّقة منذ batch #367. عند الفحص المباشر للكود تبيّن أن الازدحام أسوأ مما وُصف سابقاً: اسم الوزارة كان يظهر 3 مرات (شارة علوية + سطر نص عادي + رقاقة hashtag)، والرسوم كانت تظهر مرتين (شارة داخل صف الشارات الأولى + `ProcedureEstimatedFeeChip` منفصلة)، وعدد المستندات/الخطوات كانا رقاقتين منفصلتين. تم:
- دمج رقاقتَي "عدد المستندات" و"عدد الخطوات" في رقاقة واحدة.
- حذف شارة الرسوم المضمّنة (كانت مكررة مع `ProcedureEstimatedFeeChip` الأدق والأكثر تفصيلاً — أُبقي عليها وحدها).
- حذف سطر اسم الوزارة النصي المكرر ورقاقة الـ hashtag المكررة لنفس اسم الوزارة (كلاهما كان يكرر ما تعرضه الشارة العلوية للوزارة دون معلومة إضافية).
- أُزيل استيراد `ProcedureHashtagChips` بعد أن أصبح غير مستخدم في الملف.
- لم يُحذف أي عنصر تفاعلي أو فريد: شارة الصعوبة، شارة الأولوية القابلة للتعديل، عداد المشاهدات، علامة "نموذج"، وسم الإصدار، جرس التذكير، وشارة التقدّم — كلها بقيت كما هي لأنها إما تفاعلية (جرس التذكير/الأولوية) أو معلومة لا تظهر في أي مكان آخر من البطاقة.
النتيجة: من 4 صفوف فرعية مزدحمة إلى صفّين أوضح. `tsc --noEmit` نظيف.

### تحديث (batch #375) — تدقيق تباين الألوان (WCAG AA) لرموز v4.0 — تدقيق فقط، بلا تغييرات كود

نفّذت تدقيقاً رقمياً كاملاً (بمعادلة الإضاءة النسبية relative luminance، وليس تقديراً بصرياً) لكل أزواج رموز الألوان الفعلية المستخدمة في تصميم v4.0 مقابل معايير WCAG AA (4.5:1 للنص العادي، 3:1 للنص الكبير وحدود عناصر الواجهة):

**نتائج النص — كلها ناجحة:**
- `text-1` (#181817) على `surface`/`bg`/`brand-soft`: 17.8:1 / 16.7:1 / 15.4:1 ✅
- `text-2` (#65635F) على `surface`/`bg`: 6.0:1 / 5.6:1 ✅
- `text-3` (#76716A) على `surface`/`bg`: 4.84:1 / 4.55:1 ✅ (هذا هو نفس الإصلاح الذي تم في batch سابق، تأكّد رقمياً الآن أنه لا يزال صحيحاً)
- `brand` (#9F1D2F) على `surface`/`bg`/`brand-soft`: 7.8:1 / 7.34:1 / 6.78:1 ✅
- نص أبيض على أزرار `brand`/`brand-hover`: 7.8:1 / 9.66:1 ✅

**ثغرة حقيقية موجودة، غير مُصلَحة عمداً — تحتاج قراراً تصميمياً وليس إصلاحاً آلياً:**
رمز `--border` (#E5E2DD) على `--surface` (أبيض) = **1.29:1 فقط** — أقل بكثير من الحد الأدنى 3:1 المطلوب في معيار WCAG 1.4.11 (Non-text Contrast) لحدود عناصر الواجهة التفاعلية (حقول الإدخال، البطاقات). تحقّق إضافي (عبر Explore agent) أكّد أن هذا يؤثر عملياً على 6 أشرطة بحث رئيسية (procedures/faq/authorities/forms/services + هيدر الرئيسية) و~52 حقل إدخال عبر 32 ملفاً، وأن حدود البطاقات (`--bg` مقابل `--surface`) لا تُخفّف المشكلة (تباين ~1.04:1 بينهما، غير مُدرَك بصرياً). **الجانب الإيجابي**: كل الحقول التفاعلية تكتسب حدوداً بلون `var(--brand)` + halo عند التركيز (focus)، فالمشكلة تخص فقط "مسح الصفحة قبل التفاعل" لمستخدمي ضعف البصر، وليس إمكانية الاستخدام الفعلي بالكيبورد/الفأرة.

**لماذا لم يُصلَح الآن**: تغيير `--border` رمز تصميم مشترك يُستخدم في كل بطاقة وحقل وفاصل في التطبيق بالكامل — تغييره يتطلب تحققاً بصرياً حياً (screenshots/Lighthouse) غير متاح في هذه البيئة، وقد يكسر الهوية البصرية "الهادئة" التي يطلبها الموجز صراحة (حدود خفيفة جداً). هذا قرار يحتاج موافقة المستخدم على القيمة الجديدة، وليس تخميناً تلقائياً. مُوثّق هنا كنقطة متابعة صريحة بدل تجاهله.

### تحديث (batch #376) — تدقيق الحركة (motion audit): إزالة easing نابض + نبض زخرفي مستمر

الموجز يطلب انتقالات هادئة (150-200ms تقريباً)، بلا "bounce/spring" وبلا تأثيرات عائمة (floating) زخرفية. نفّذت تدقيقاً شاملاً (عبر Explore agent) لكل `transition`/`@keyframes`/`animation: infinite` في التطبيق. الغالبية العظمى (~50+ من keyframes) هي حركات دخول لمرة واحدة (fade/slide/scale) بمنحنى `cubic-bezier(0.22,1,0.36,1)` الهادئ المُعتمد كرمز تصميم قياسي — هذه سليمة ولم تُمس. وجدت وأصلحت فقط الحالات المخالفة الحقيقية:
- 3 مواضع تستخدم منحنى نابض `cubic-bezier(0.34,1.56,0.64,1)` (overshoot واضح): شريط تقدّم `ReadinessChecker.tsx`، شريط تقدّم `ProcedureTimeline.tsx`، ونقاط مؤشر الكاروسيل في هيدر الرئيسية `app/page.tsx`. الثلاثة استُبدلت بمنحنى النظام القياسي الهادئ.
- نقطة "متصل" (Online) في `TopNav.tsx` (تظهر للديسكتوب فقط) كانت تنبض باستمرار (`animation: tn-pulse 2.5s infinite`) طوال بقاء المستخدم على الصفحة — حركة زخرفية بحتة وليست إشارة وظيفية متغيرة. أُزيلت الحركة، وبقيت النقطة الخضراء الثابتة + توهج خفيف (glow) كافية لتوصيل المعنى.

**تُرك عمداً بلا تغيير** (موثّق وليس متجاهلاً): كل حركات spinner/shimmer/typing-dots (وظيفية، تشير لحالة تحميل)، نبض "urgent" في `NotificationBell`، حلقة "جديد" (ping) في `ProcedureVersionTag`، اهتزاز جرس التذكير النشط في `ProcedureReminderBell`، وتوهج الخطوة الحالية في `ProcedureFlowchart` — كلها حركات مستمرة لكنها **وظيفية** (تنقل حالة فعلية للمستخدم)، وليست مجرد زخرفة. كذلك حركة الاحتفال لمرة واحدة عند إنجاز كل المعاملات (`ProcedureCompletionCelebration`) أُبقيت لأنها مكافأة لحظية نادرة الحدوث، لا حركة مستمرة. لم يُعثر على أي تأثير parallax مرتبط بالتمرير (scroll-linked). `tsc --noEmit` نظيف.

### تحديث (batch #377) — تطبيع آخر رموز الألوان الحرفية المتبقية في /services

نقطة متابعة صغيرة كانت موثّقة منذ batch #372: بقيت في `/services` 4 مواضع بحدود `1.5px` بينما كل صفحة أخرى أُعيد تصميمها هذا الموسم انتقلت إلى `1px` (بطاقة "نظرة عامة"، زر "إغلاق" في `ServiceSheet`، زر "عرض كل الخدمات"، وبطاقة الخدمة نفسها في الشبكة) — تم توحيدها. كذلك استبدلت كل قيم `#fff` الحرفية المتبقية (خلفية `ServiceSheet`، بطاقة "نظرة عامة"، `ServiceCardSkeleton`، وحالة onTouchEnd لبطاقة الخدمة) برمز `var(--surface)`. **لم تُمس** حدود زر الرجوع وزر تبديل اللغة في الهيدر (تبقى `1.5px` عمداً — هذا هو النمط القياسي المتّبع في كل هيدرات الموقع منذ batch #367)، ولا نصوص الأزرار البيضاء (`color: '#fff'` على أزرار بلون العلامة التجارية — ليست جزءاً من نظام حدود/خلفيات البطاقات). `tsc --noEmit` نظيف.

### تحديث (batch #378) — استخراج مكوّن `MobileHeader` مشترك من 5 هيدرات مكررة

بعد سلسلة الدفعات الأخيرة (v4.0 flat headers + إصلاح زر تبديل اللغة في batch #373)، أصبحت صفحات `/procedures` و`/faq` و`/authorities` و`/forms` و`/services` تحمل نفس ترميز الهيدر (زر رجوع + عنوان + زر تبديل لغة) منسوخاً حرفياً في كل ملف — وهذا بالضبط نوع التكرار الذي طلبه الموجز الأصلي إزالته عبر مكوّنات مشتركة مُسمّاة (البند 22: `MobileHeader`, `AppLogo`, `SearchInput`, ...). أُنشئ `components/MobileHeader.tsx` (props: العنوان AR/EN، `isAr`، `onBack`، `toggleLang`، ونص aria-label لزر الرجوع القابل للتخصيص، وslot اختياري لمحتوى إضافي، وmaxWidth قابل للتخصيص) وحُوّلت الصفحات الخمس لاستخدامه بدل الترميز اليدوي المكرر.

**حرصت على عدم إحداث أي فرق بصري أثناء الاستخراج**: صفحة `/services` كانت تستخدم عرضاً أقصى حرفياً 1024px (بينما البقية تستخدم `var(--container-md)`=720px) ونص aria-label مختلف لزر الرجوع ("الرجوع"/"Go back" بدل "الرئيسية"/"Home") — لم أُوحّد هذه الفروقات ضمن هذه الدفعة (ذلك تحسين مختلف يحتاج تحققاً بصرياً منفصلاً)، بل مررتها كـ props صريحة (`maxWidth={1024}`, `backLabelAr`, `backLabelEn`) للحفاظ على نفس السلوك الحالي بالضبط. `tsc --noEmit` نظيف بعد التحويل. لم يوجد مكوّن `PageHeader` قديم في `components/ui/index.tsx` (لا يزال مستخدماً في صفحة playbook فقط) بالخلط — تم اختيار اسم مختلف (`MobileHeader`) عمداً لتجنّب أي التباس.

### تحديث (batch #379) — استخراج مكوّن `SearchInput` مشترك من 4 أشرطة بحث مكررة

استُخرج `components/SearchInput.tsx` من ترميز شريط البحث شبه المتطابق في `/faq` و`/authorities` و`/forms` و`/services` (نمط "search-wrap": أيقونة مطلقة الموضع + إدخال + زر مسح دائري 36px). المكوّن يدير حالة التركيز (focus) داخلياً بنفسه، ما ألغى الحاجة لكل صفحة لتتبّع `searchFocused`/`setSearchFocused` بشكل منفصل — أُزيل هذا الـ state غير المستخدم من الملفات الأربعة بعد النقل.

**استثناء متعمّد**: شريط بحث `/procedures` لم يُنقل — بنيته مختلفة جوهرياً (تخطيط صف مرن بدل التموضع المطلق، ويحتوي زرّين إضافيين — بحث متقدم وفلتر — داخل نفس الشريط المرئي). إجباره على المكوّن الجديد كان سيغيّر تخطيطه فعلياً، فتُرك كما هو، موثّق هنا صراحة وليس متجاهلاً.

**تطبيع طفيف كنتيجة جانبية موثّقة (وليس صدفة)**: أثناء الاستخراج وُحّدت بعض الفروقات الصغيرة القائمة أصلاً بين الصفحات الأربع: حجم الأيقونة (كان 15px في بعضها، 16px في `/services`)، لون الأيقونة غير المركّزة (بعضها استخدم `#B0A090` حرفياً بدل `var(--text-3)`)، لون خلفية زر المسح (`var(--border)` في `/forms` مقابل `var(--bg)` في البقية)، ومسافة padding اليسرى الشاذة في `/forms` (36px بلا سبب - كانت من مخلّفات نسخ/لصق). **الأثر الأوضح**: صفحة `/services` لم تكن تستخدم صنف `search-wrap` فعُقّم منها توهّج التركيز (focus ring) الموجود في كل صفحة أخرى — الآن تحصل عليه أيضاً عبر المكوّن المشترك، بدل أن تبقى الاستثناء الوحيد. `tsc --noEmit` نظيف على الملفات الأربعة.

### تحديث (batch #380) — استخراج مكوّن `StatsRow` مشترك من 4 أشرطة إحصائيات مكررة

استُخرج `components/StatsRow.tsx` من شريط "بطاقات الإحصائيات" (رقم كبير + تسمية صغيرة) المكرر في `/procedures` و`/faq` و`/authorities` و`/services`. المكوّن يقبل مصفوفة `{value, label}` وعدد أعمدة اختياري (`/authorities` تمرر `columns={2}` لعرض 4 بطاقات بتخطيط 2×2، بقية الصفحات تستخدم الافتراضي = عدد العناصر).

**تطبيع طفيف موثّق** (الفروقات الأربع كانت متباعدة قليلاً قبل الاستخراج): نصف قطر الحدود (`/procedures` كان 14 وباقي الصفحات 12 — وُحّد للجميع على 12)، الحركة عند الظهور (كانت غائبة تماماً في `/procedures` بينما البقية تتحرك بنفس التوقيت والمنحنى — الآن `/procedures` تتحرك أيضاً)، حجم/لون تسمية البطاقة (`/procedures` كان 11px بدل 9.5px، و`/services` كان يستخدم `var(--text-3)` بدل `var(--text-2)` — وُحّدا). حُذفت أيضاً حركة `svcStatsIn` غير المستخدمة بعد النقل من `/services` (كانت الحركة الوحيدة المتبقية بلا استخدام؛ حركات `faqEnter`/`authEnter` بقيت لأنها ما زالت تُستخدم في عناصر أخرى بنفس الصفحتين). `tsc --noEmit` نظيف.

### تحديث (batch #381) — استخراج مكوّن `AppLogo` مشترك من 6 نسخ مكررة من شعار "دليلك"

استُخرج `components/AppLogo.tsx` من كل موضع يعرض شعار التطبيق (أيقونة + كلمة "دليلك" ± شعار فرعي): زر العلامة التجارية في `TopNav.tsx` (نسخة سطح المكتب ونسخة الموبايل)، رأس درج `MobileMenu.tsx`، صفحات الدخول الأربع (`/login`, `/register`, `/forgot-password`, `/reset-password`)، شاشة تحميل المصادقة على الصفحة الرئيسية، وشعار التذييل المصغّر. المكوّن يدعم عبر خصائص صريحة كل الفروقات الحقيقية بين المواضع الستة: حجم الشارة ونصف قطرها، تخطيط أفقي (nav/menu/footer) أو عمودي متمركز (auth/splash)، نص شعار فرعي (بلا/قصير/طويل/نص مخصّص لشاشة التحميل التي لها صياغة مختلفة تماماً)، عنصر العنوان (`h1` لصفحات الدخول لأسباب SEO/a11y، `div`/`span` في البقية)، وجود الشارة الدائرية من عدمه (لا شارة إطلاقاً في التذييل).

**إصلاح وظيفي حقيقي (وليس تجميلياً فقط)**: عنوان الشعار في صفحات الدخول الأربع كان نصاً عربياً ثابتاً "دليلك" بلا أي شرط على اللغة، بعكس كل موضع آخر في التطبيق الذي يبدّل بين "دليلك"/"Dalilak" حسب اللغة — أي أن مستخدمي الواجهة الإنجليزية كانوا يرون الشعار بالعربي دائماً في هذه الصفحات الأربع. المكوّن الجديد يطبّق شرط اللغة تلقائياً فحُلّت هذه المشكلة كأثر مباشر للاستخراج، لا كتغيير منفصل.

**تطبيع طفيف موثّق**: الأيقونة الزخرفية داخل الشارة كانت تحمل أحياناً `alt="دليلك"` رغم وجود نص "دليلك" المرئي بجانبها مباشرة (تكرار غير ضروري لقارئ الشاشة)، وأحياناً أخرى `alt="" aria-hidden`. وُحّدت جميعها على `alt="" aria-hidden="true"` — الاسم المُعلن يأتي من العنوان النصي المرئي أو من `aria-label` الزر، وهذا يطابق أفضل ممارسات WCAG لتفادي الإعلان المكرر. كما وُحّدت ألوان `MobileMenu.tsx` من قيم hex ثابتة قديمة (`#F8EDEF`, `rgba(143,29,44,0.15)`, `#191713`, `#918B82`) إلى متغيرات التصميم (`var(--brand-soft)`, `var(--brand-ring)`, `var(--text-1)`, `var(--text-3)`) — نفس نمط تطبيع batch #377. لا تغيير في الأحجام أو التخطيط الفعلي لأي موضع؛ كل صفحة تحتفظ بأبعادها الأصلية عبر الخصائص. `tsc --noEmit` نظيف على كل الملفات الثمانية المعدَّلة.

### تحديث (batch #382) — استخراج مكوّن `SectionHeader`، وقرار بعدم استخراج `QuickAction`، وحذف كود ميت

قبل الاستخراج، جرى بحث عن مكوّن `QuickAction` (مطلوب في القسم 22 من مواصفات التصميم) لكن تبيّن وجود استخدام حي واحد فقط له (صف الزرّين في الصفحة الرئيسية "تصفّح المعاملات"/"اسأل دليلك")، فلا يوجد تكرار حقيقي يبرّر الاستخراج — تم تخطّيه بدل فرض مكوّن بخاصية واحدة فقط بلا فائدة DRY فعلية.

بدلاً من ذلك استُخرج `components/SectionHeader.tsx` من 4 صفوف "عنوان + إجراء ثانوي، بمحاذاة space-between" مكررة فعلياً: عنوان "تصفّح حسب الفئة" في الصفحة الرئيسية (مع رابط "كل الفئات")، وثلاثة عناوين تبويبات في لوحة التحكم `/admin` (رموز استعادة كلمة المرور، تقييمات المستخدمين، طلبات التصعيد — كل منها بزر "تحديث").

**استثناء متعمّد**: ألوان `/admin` السداسية القديمة (`#191713`, `#741622`) لم تُطبَّع إلى متغيرات التصميم رغم أن ذلك النمط طُبِّق في دفعات سابقة (مثل batch #381) — لأن بقية صفحة `/admin` (البطاقات، الشارات، الحدود) ما زالت بالكامل على نفس اللوحة السداسية القديمة ولم تخضع بعد لأي تحديث v4.0. تطبيع لون العنوان وحده كان سيجعله غير متّسق بصرياً مع محيطه المباشر في نفس الصفحة، فحُفظت الألوان كما هي حرفياً عبر خصائص صريحة (`titleColor`, `trailingColor`)، موثّق هنا كاستثناء وليس إغفالاً.

**حذف كود ميت**: حُذف `components/HomepageQuickActionsBar.tsx` بعد تأكيد عبر بحث شامل في المستودع أنه غير مستورد ولا مُستخدَم في أي مكان — بقايا تصميم سابق لدفعة #366 التي أعادت بناء الصفحة الرئيسية. الحذف الفعلي للملف يتم عبر `push.sh` (`rm -f` قبل `git add`) لأن أدوات الملفات هنا لا تملك صلاحية حذف مباشرة على المجلد المُثبَّت. `tsc --noEmit` نظيف.

### تحديث (batch #383) — بحث عن `PopularTransactionRow`/`ServiceCategoryCard` (لا استخراج)، واستخراج `AskDalilakButton` من تكرار حقيقي عبر 4 ملفات

بحثاً عن استكمال مكوّنات القسم 22 من مواصفات التصميم، جرى فحص صفّي "الأكثر بحثاً" وبطاقات "تصفّح حسب الفئة" في الصفحة الرئيسية. تبيّن أن كلا النمطين مكتوب مرة واحدة فقط في `app/page.tsx` (عبر `.map()`) ولا يتكرر يدوياً في أي ملف آخر بنفس القيم الحرفية — الأنماط المشابهة في `/services` و`/authorities` و`/faq` مختلفة فعلياً (شبكة متغيرة الأعمدة، حركات دخول خاصة، شارات إضافية)، وليست نسخاً متطابقة. تماشياً مع قرار batch #382 بشأن `QuickAction`، لم يُستخرج أي من المكوّنين لعدم وجود تكرار حقيقي يبرر ذلك.

بدلاً من ذلك استُخرج `components/AskDalilakButton.tsx` من زر "اسأل دليلك" (أيقونة فقاعة حوار + نص، خلفية `var(--brand)`) المكرر حرفياً (نفس مسار SVG بايت-لبايت، ونفس قيم `padding/borderRadius/fontSize/fontWeight` تقريباً) في 4 حالات "لا نتائج" منفصلة: `/services`، `/authorities`، `/procedures`، `/faq`. المكوّن يقبل `onClick` مخصصاً لكل صفحة (بعضها ينتقل لصفحة المحادثة عبر `router.push`، وبعضها يستدعي دالة `askAI`/`handleAsk` محلية)، وخاصية `searchTerm` اختيارية تُغيّر النص إلى "اسأل دليلك عن: {الكلمة}" في `/procedures` و`/faq` فقط (كما كان الأصل، بينما `/services` و`/authorities` يستخدمان النص الثابت دوماً).

**تطبيع طفيف موثّق**: فروقات الحشو/الحجم الصغيرة بين النسخ الأربع (`padding` 9-10px/20-22px، `fontSize` 12.5-13، `gap` 6-7) وُحّدت على القيم الأكثر شيوعاً. أُزيلت أيضاً معالجات اللمس اليدوية (`onTouchStart`/`onTouchEnd`) الخاصة بـ`/services` التي كانت تكرر تأثير الضغط بديلاً عن صنف `btn-primary` المستخدم في الصفحات الثلاث الأخرى — المكوّن الجديد يستخدم `btn-primary` للجميع الآن (يوفّر نفس تأثير اللمس عبر `:active` في globals.css)، توحيداً للسلوك بدل بقاء `/services` استثناءً غير موثّق سابقاً. `tsc --noEmit` نظيف على الملفات الأربعة.

### تحديث (batch #384) — استخراج مكوّن `LoadingSpinner` من ملفَي `loading.tsx` المتطابقين تقريباً + 3 نسخ إضافية

استُخرج `components/LoadingSpinner.tsx` من نمط دوّارة التحميل (دائرة حدودها رمادية مع قوس علوي بلون العلامة التجارية + نص "جارٍ التحميل...") الذي كان مكرراً حرفياً تقريباً في `app/procedures/[slug]/loading.tsx` و`app/forms/[slug]/loading.tsx` (نفس القيم العددية/اللونية بالكامل؛ الفارق الوحيد بين الملفّين كان اسم الـ`@keyframes` المحلي فقط). المكوّن يولّد اسم `@keyframes` فريداً تلقائياً لكل نسخة عبر `useId()` لتفادي أي تعارض عند وجود أكثر من دوّارة في نفس الصفحة، ويقبل `fullPage` لعرض غلاف الصفحة الكاملة (100vh، توسيط، `dir`) أو بدونه للتضمين داخل تخطيط صفحة قائم.

نُقل أيضاً استخدام مطابق تماماً في `app/my-files/page.tsx` (كان يستخدم نفس القيم الحرفية بالضبط)، وحُذفت حركة `mf-spin` غير المستخدمة بعد النقل من `<style>` تلك الصفحة. كما نُقلت نسخة مقاربة في `app/procedures/[slug]/playbook/page.tsx` (حجم 26 بدل 36، سماكة حدود 2.5 بدل 3، ونص مخصّص "جارٍ توليد خارطة الإجراء بالذكاء الاصطناعي...") عبر خصائص `size`/`borderWidth`/`label` الصريحة، لأنها تطابق نفس التخطيط العمودي (دائرة ثم نص أسفلها) وإن اختلفت القيم.

**استثناء متعمّد**: دوّارة صغيرة أخرى في `app/forms/[slug]/FormDetailClient.tsx` (14px، تصميم مضمّن أفقياً داخل زر مع نص مجاور، بلون شريط شفاف `rgba(143,29,44,0.25)`) لم تُنقل — بنيتها مختلفة جوهرياً (صف أفقي داخل زر، لا عمود مركزي مستقل)، فإجبارها على `LoadingSpinner` كان سيتطلب متغيّر تخطيط جديد كلياً لفائدة محدودة. تُركت كما هي، موثّق هنا صراحة.

القيم اللونية (`#E6E2DC`, `#8F1D2C`, `#918B82`, `#F8F8F6`) أُبقيت حرفية كقيم افتراضية للمكوّن (لا تحويل إلى متغيرات التصميم) تفادياً لأي تغيير بصري غير مقصود ضمن هذه الدفعة تحديداً؛ فرصة تحويلها لاحقاً لبقية الصفحات المتبقية خارج نطاق v4.0 تبقى بنداً مفتوحاً. `tsc --noEmit` نظيف على كل الملفات الأربعة.

### تحديث (batch #385) — استخراج `ModalCloseButton` و`PageBackHeader` من تكرار حرفي عبر ملفات لوحة التحكم/الصفحات الفرعية

استُخرج `components/ModalCloseButton.tsx` (زر إغلاق دائري صغير بأيقونة X) من ثلاث نسخ حرفية تقريباً: نافذة "تعديل مستخدم" في `/admin` (28px)، لوحة تفاصيل المحتوى في `/admin/content` (26px، بلا `transition`)، ونافذة "إنشاء محتوى جديد" في نفس الصفحة (28px). المكوّن يقبل `size`/`iconSize` لضبط الفروقات الصغيرة الموجودة أصلاً.

استُخرج أيضاً `components/PageBackHeader.tsx` (رأس صفحة بسيط: زر رجوع دائري الحواف + عنوان ± شعار فرعي) من `/settings` و`/privacy` — القيم العددية متطابقة تماماً (الأبعاد، الحشو، حجم الأيقونة، مسار السهم المتجه حسب اللغة)، والفارق الوحيد الحقيقي هو مصدر الألوان: `/settings` (صفحة أقدم لم تخضع لتحديث v4.0) تستخدم قيم hex ثابتة، بينما `/privacy` (صفحة أُنشئت ضمن batch #366) تستخدم متغيرات التصميم. أُضيفت خاصية `variant: 'legacy' | 'tokens'` للحفاظ على مظهر كل صفحة تماماً كما كان بدل تطبيع الألوان قسراً — نفس منطق الاستثناء المتعمّد في batch #382 لصفحة `/admin`. `/settings` تحتفظ أيضاً بترويستها الفرعية ("تفضيلاتك في مكان واحد") والشعار التعبيري "⚙️" ووزن خط 800 عبر خصائص `subtitle`/`icon`/`titleWeight` الاختيارية.

`tsc --noEmit` نظيف على الملفات الخمسة المعدَّلة (`admin/page.tsx`, `admin/content/page.tsx`, `settings/page.tsx`, `privacy/page.tsx`, والمكوّنين الجديدين).

### تحديث (batch #386) — حذف 49 ملف مكوّن ميت (~8700 سطر) غير مستخدَم في أي مكان

فحص شامل (grep عبر app/ وcomponents/ وlib/، بالإضافة إلى كل استدعاءات next/dynamic()) لكل ملف مكوّن على حدة أظهر أن 49 من أصل 183 ملفاً في `components/` غير مستوردة أو معروضة في أي مكان بالتطبيق — بقايا تكرارات تصميم قديمة (ودجات الصفحة الرئيسية مثل `HomepageStreakCounter`، `HomepageLiveStats`، `HomepageWeatherBanner`، وودجات تفاصيل الإجراء مثل `ProcedureComparator`، `ProcedureBookmarks`، `ProcedureHashtagChips`) تم تجاوزها في دفعات لاحقة دون تنظيف. تم التحقق يدوياً من ~15 ملفاً إضافياً للتأكد من دقة الفحص الآلي.

من ضمن الـ49: `EscalationModal.tsx` كان مذكوراً خطأً في تعليق توثيقي داخل `lib/useFocusTrap.ts` كأحد الـ13 حواراً التي تملك focus trap، رغم أنه لم يُعرَض فعلياً في أي مكان — تم تصحيح التعليق ليعكس 12 حواراً حقيقياً بدل 13، مع ملاحظة سبب الحذف.

الحذف الفعلي يتم عبر أسطر `rm -f` داخل `push.sh` (لا يمكن حذف الملفات مباشرة من أدوات الجلسة على المجلد المُتصل). `tsc --noEmit` نظيف — لا شيء في التطبيق يشير لأي من الـ49 ملفاً، فالحذف لا يغيّر أي سلوك ملحوظ، فقط يقلّص رسم الحزمة (bundle graph) وسطح فحص TypeScript ويزيل مصدر ارتباك محتمل بين أسماء متشابهة (مثال: `HomepageTodayTasks` الحي مقابل `HomepageTodaysTasks` الميت).

### تحديث (batch #387) — حذف كتلة CSS ميتة كاملة (~65 صنف، ~430 سطر) من `app/globals.css`

فحص شامل لكل صنف CSS معرّف في `globals.css` (استخراج جميع الـselectors + grep مطابق دقيق `className="..."` عبر التطبيق بأكمله، بما في ذلك التحقق من عدم وجود أي بناء ديناميكي للأصناف عبر template literals أو `clsx`/`classnames` — لم يُعثر على أي منها في المشروع) كشف أن الملف يحتوي "نظام تصميم" كامل (مقاييس طباعة، أزرار، شارات، تنبيهات، بطاقات، حاويات، شبكات) تم بناؤه في مرحلة مبكرة (batch #116) لكنه لم يُستخدم فعلياً أبداً — كل الصفحات تستخدم `style={{}}` مباشرة بدل هذه الأصناف.

الأقسام المحذوفة بالكامل (صفر استخدام حقيقي مؤكَّد): مقياس الطباعة الكامل (`.text-display/.text-h1/.text-h2/.text-h3/.text-title/.text-body*/.text-caption/.text-label`)، `.container-sm/.container-lg/.container-xl` (أُبقي على `.container-md` فقط، المستخدم فعلياً)، حركات الدخول الميتة (`.page-enter/.scale-in/.slide-up/.fade-in` + الـ`@keyframes` المرتبطة بها `scaleIn/slideUp/fadeIn`، مع الإبقاء على `.msg-in` الحي)، `.btn-secondary/.btn-ghost/.btn-destructive/.btn-icon` (أُبقي على `.btn-primary` والأصناف القديمة المنفصلة `.icon-btn/.mode-btn/.send-btn`)، عناصر النماذج (`.ds-input/.ds-textarea/.ds-label`)، البطاقات (`.ds-card/.ds-card-hover`)، `.card-hover` القديم، الشارات كاملة (`.badge` + 8 متغيرات)، التنبيهات كاملة (`.alert` + 4 متغيرات)، `.section-header/.section-accent`، `.skeleton` (+ `@keyframes shimmer`)، `.empty-state`، `.divider/.divider-v`، `.checklist-item`، `.source-card`، `.step-card/.step-number`، `.trust-bar`، مكوّنات "شاشة الترحيب" القديمة (`.wlc-svc-grid/.wlc-svc-btn/.wlc-hero-band`)، `.hero-enhance-label/.hero-enhance-btn`، وشبكات الصفحة الرئيسية القديمة (`.pgrid/.pcard/.hwgrid/.tgrid/.fgrid` بنسختيه) و`.cat-grid-mobile`/`.proc-page-content` (كانتا مُعرَّفتين فقط داخل `@media` بلا صنف أساسي يستخدمهما أي مكان).

جميع هذه الأصناف تنتمي لتكرارات تصميم قديمة (homepage v3، welcome screen) تم تجاوزها بالكامل في إعادة بناء v4.0 (batch #366+) دون تنظيف الأصناف القديمة. الحذف آمن بالتعريف: صنف CSS بلا أي `className` يستخدمه لا يمكن أن يغيّر أي شيء مرئي عند حذفه. تم التحقق من توازن الأقواس `{}` بعد كل تعديل (125 فتح = 125 إغلاق) و`tsc --noEmit` نظيف. الأصناف المتبقية (23 صنفاً أساسياً) جميعها مؤكَّدة الاستخدام الفعلي.

### تحديث (batch #388) — إضافة metadata مفقودة لصفحة `/privacy`

مقارنة كل مسارات `page.tsx` (client component) مقابل ملفات `layout.tsx` المرافقة لها كشفت أن `/privacy` هي الصفحة العامة الوحيدة في التطبيق بلا `layout.tsx` خاص بها — كل صفحة أخرى (`/faq`, `/procedures`, `/forms`, `/authorities`, `/services`, `/login`, ... إلخ) تتبع نمطاً ثابتاً: بما أن `page.tsx` هو `'use client'` (لا يمكنه تصدير `Metadata` مباشرة)، تُضاف `layout.tsx` شقيقة تُصدّر `metadata` الخاصة بالمسار (title/description/openGraph/twitter/canonical). بما أن `/privacy` تفتقد هذا الملف، كانت تسقط تلقائياً على الـmetadata العامة للصفحة الرئيسية المعرَّفة في `app/layout.tsx` (عنوان ووصف عن الجوازات/السجل المدني/تأسيس الشركات) — وهذا مضلِّل لصفحة سياسة خصوصية ويضرّ بمحركات البحث (تظهر نتيجة البحث بعنوان الصفحة الرئيسية بدل عنوان الخصوصية).

أُنشئ `app/privacy/layout.tsx` مطابقاً تماماً لنمط `app/faq/layout.tsx` الموجود: عنوان "سياسة الخصوصية | دليلك"، وصف يشرح محتوى الصفحة الفعلي، Open Graph + Twitter card، و`alternates.canonical: '/privacy'` (يُحل بشكل صحيح عبر `metadataBase` المُعرَّف مسبقاً في `app/layout.tsx` منذ batch #345). `tsc --noEmit` نظيف تماماً.

### تحديث (batch #389) — إصلاح رابط Backend خاطئ في `/professional`

`app/professional/page.tsx:8` كان يحتوي `const API_URL = process.env.NEXT_PUBLIC_API_URL || 'https://dalilak-backend.onrender.com'` — عنوان مختلف عن كل مكان آخر في المشروع. فحص بـ`grep` عبر التطبيق بأكمله أظهر أن 7 ملفات أخرى (`app/page.tsx`, `app/admin/page.tsx`, `app/admin/content/page.tsx`, `app/my-files/page.tsx`, `components/EscalationModal.tsx`, `components/AgentResponseRenderer.tsx`, `lib/auth.ts`) تتفق جميعها على نفس القيمة الاحتياطية الصحيحة: `https://dalilak-backend-bvb9.onrender.com` — و`/professional` هي الاستثناء الوحيد (خطأ نسخ ولصق واضح، ناقص لاحقة `-bvb9`).

الأثر الحالي محدود لأن `API_URL` مُعرَّف في هذا الملف لكنه غير مُستخدَم فعلياً في أي `fetch` بعد (الصفحة لا تزال "قريباً" — واجهة إنشاء روابط استقبال الموكّلين وبطاقات إحصائية فقط)، لكنه فخ كامن: بمجرد ربط أي نداء API بهذا الثابت مستقبلاً، سيتوجه بصمت لعنوان خاطئ/غير موجود في أي بيئة لا يوجد فيها `NEXT_PUBLIC_API_URL` (كالتطوير المحلي أو معاينات Vercel)، بينما كل صفحة أخرى تصل للـbackend الصحيح — خطأ صعب التشخيص لاحقاً. تم توحيد القيمة مع بقية المشروع. لا تغيير في منطق أو مسارات Backend نفسها — تصحيح لعنوان احتياطي في الواجهة فقط. `tsc --noEmit` نظيف.

### تحديث (batch #390) — تدقيق فقط، بلا تعديل كود: بحث موسّع عن ثغرات إضافية لم يُعثر على مرشّح قوي جديد

بعد نجاح batch #389 في كشف انحراف `API_URL` عبر مقارنة ثوابت مكررة، امتد نفس المنطق لفحص أرقام الهاتف المكررة: `components/ProcedureHelpRequest.tsx` و`app/layout.tsx` (JSON-LD) يتفقان على رقم واتساب الدعم `9616694794` (من batch #327)، بينما `components/MobileMenu.tsx:426-428` يعرض رقماً مختلفاً `+9613460608` في بطاقة "تواصل معنا". فحص أعمق كشف أن هذه البطاقة مقترنة بـ`mailto:wissam@aijur.ai` — بريد المستخدم الحقيقي نفسه — مما يعني أن هذا على الأرجح رقم تواصل شخصي/مكتبي حقيقي منفصل عن خط واتساب الدعم الخاص بالإجراءات، وليس نفس النمط من الانحراف الذي كان في `API_URL`. لا يوجد أساس للجزم بأن أحدهما خطأ، وتعديل بيانات تواصل حقيقية بلا يقين يخالف قاعدة "لا تستبدل المحتوى الحقيقي" — فتُرك بلا تعديل، وتُرصد هنا للمراجعة اليدوية من المستخدم إن أراد.

فحوصات إضافية لم تُسفر عن مرشّحين صالحين (كلها موجودة ومطبَّقة بالفعل، لا حاجة لتعديل):
- كل استدعاءات `JSON.parse(localStorage...)` العشرة في المشروع مُغلَّفة بالفعل بـ`try/catch`.
- لا توجد روابط `href="#"` بديلة (placeholder) في أي صفحة.
- كل صورة `<img>` تملك `alt` مناسباً (بما فيها الأيقونات الزخرفية `alt=""` مع `aria-hidden`).
- كل رابط `target="_blank"` (13 موقعاً) يملك `rel="noopener noreferrer"` مرافقاً.
- تعليقات `TODO` الموجودة (في `lib/procedures.ts`, `lib/analytics.ts`, `lib/plan.ts`) كلها ملاحظات معمارية متعمّدة بخصوص ربط Backend مستقبلي — خارج نطاق العمل المسموح به (لا تغيير على Backend APIs).

### تحديث (batch #391) — إصلاح خطأ "stale closure" حقيقي: سؤال البدء السريع من onboarding قد يُرسَل بلغة خاطئة

`app/page.tsx` عند السطر ~766 يسجّل مستمع حدث `dalilak_onboarding_question` داخل `useEffect(() => {...}, [])` (مصفوفة اعتماديات فارغة، يعمل مرة واحدة فقط عند التركيب). كان الـhandler يستدعي `sendMessage(q)` مباشرة — لكن `sendMessage` (المعرَّفة أدناه في نفس المكوّن) تُعاد بناؤها في كل render وتقرأ `lang`/`isAr`/`messages` من الـclosure الخاص بها، بما في ذلك التوجيه الصريح للغة الرد الذي أُضيف في batch #334 (`langDirective` المبني على `lang`). بما أن الـeffect لا يُعاد تسجيله أبداً بعد التركيب الأول، كان الـhandler يستدعي دائماً نسخة `sendMessage` من لحظة أول render فقط — أي بقيم `lang`/`isAr` الأصلية، بلا وعي بأي تغيير لاحق.

هذا قابل للحدوث فعلياً وليس نظرياً: `components/UserOnboarding.tsx` (معالج ترحيب من 4 خطوات لأول مستخدم) يستدعي `toggleLang()` في الخطوة الأولى (`handleLangSelect`) عند اختيار المستخدم للغته، ثم يُطلق نفس الحدث `dalilak_onboarding_question` في الخطوة الثالثة عند الضغط على سؤال مقترح. أي مستخدم جديد يبدّل اللغة أثناء onboarding ثم يضغط سؤالاً مقترحاً كان سيحصل على رد الذكاء الاصطناعي باللغة القديمة (قبل التبديل) — إعادة إنتاج صامتة لنفس الخلل الذي أُصلح في batch #334 عبر مسار مختلف تماماً.

الإصلاح: أُضيف `sendMessageRef` (عبر `useRef`) بجانب مجموعة الـrefs الموجودة أعلى المكوّن، يُحدَّث بقيمة `sendMessage` الحالية في كل render مباشرة بعد تعريفها (`sendMessageRef.current = sendMessage`)، ويستدعي الـhandler الآن `sendMessageRef.current(q)` بدل `sendMessage(q)` مباشرة — فيصل دائماً لآخر نسخة محدَّثة من الدالة رغم أن الـeffect نفسه لا يزال يُسجَّل مرة واحدة فقط. لا تغيير في سلوك بقية الدالة أو في أي مسار آخر يستدعي `sendMessage` مباشرة (كـ`handleSubmit`). `tsc --noEmit` نظيف.

### تحديث (batch #392) — نفس فئة خطأ "stale closure" من batch #391، وُجدت في `/admin`

بعد إصلاح batch #391، امتد البحث عن نفس النمط (`useEffect` بمصفوفة اعتماديات فارغة يستدعي دالة تقرأ حالة متغيّرة) عبر بقية المكوّنات المعقّدة (DraftingStudio, GlobalSearch, ChatMessage, TopNav, MobileMenu, NotificationBell, MinistryQuickDial, KeyboardShortcutsHelp وغيرها) — لم يُعثر على حالة أخرى ذات أثر حقيقي سوى واحدة في `app/admin/page.tsx`.

الـeffect عند السطر ~87 (`if (!isAdmin()) {...}; loadStats(); loadUsers()`, `[]`) يستدعي `loadStats`/`loadUsers` عند تركيب صفحة الإدارة فقط. كلتا الدالتين تقرآن `isAr` (من `useLanguage()`) لبناء نص رسالة الخطأ الاحتياطية عند فشل الطلب. بما أن الـeffect لا يُعاد تشغيله، فإن أي مدير يبدّل اللغة أثناء انتظار الطلب الأول (قد يستغرق حتى 30 ثانية بسبب "cold start" الخادم المجاني، موثّق في batches أخرى) ثم يفشل الطلب — سيرى رسالة الخطأ بلغة ما قبل التبديل. الأثر محدود (رسالة خطأ احتياطية فقط، تظهر فقط عند فشل فعلي بلا `.message` من الخادم)، لكنه نفس فئة الخلل المؤكَّدة الحدوث في batch #391، وتصحيحه بسيط ومعزول.

الإصلاح: أُضيف `isArRef` (عبر `useRef` مع مزامنة في `useEffect([isAr])`)، واستُبدل `isAr` بـ`isArRef.current` داخل رسالتَي الخطأ الاحتياطيتين في `loadStats`/`loadUsers` فقط (بقية استدعاءات هاتين الدالتين من معالجات أحداث حيّة — مثل `refreshAll`/`handleCreate` — لم تكن بحاجة لهذا التغيير أصلاً، لكن استخدام المرجع فيهما لا يغيّر شيئاً هناك لأن القيمة نفسها). `tsc --noEmit` نظيف.

### تحديث (batch #393) — إصلاح رقم إحصائي غير دقيق: "52+ جهة مختصة" بدل الرقم الحقيقي

`lib/allTransactions.ts`'s `TX_MINISTRIES` عبارة عن مصفوفة من 52 عنصراً، لكن 24 فقط منها تملك قيمة `slug` فريدة — 10 وزارات مكرّرة (`labor` و`interior` وَ`finance` وَ`agriculture` وَ`health` وَ`digital` وَ`culture` وَ`telecom` وَ`libanpost` كل واحدة مرتين)، وقيمة `slug: 'other'` وحدها تتكرر ~20 مرة — نتيجة تقسيم كل وزارة حسب القسم الفرعي/مصدر البوابة الإلكترونية أثناء توليد البيانات الأصلي دون إزالة التكرار لاحقاً.

`app/procedures/page.tsx:276` كان يستخدم طول المصفوفة الخام مباشرة كإحصائية معروضة للمستخدم: `{ value: String(TX_MINISTRIES.length) + '+', label: 'جهة مختصة' }` — يعرض "52+ جهة مختصة" رغم أن العدد الحقيقي للجهات الحكومية المتمايزة هو 24. تأكّد أن هذا سهو حقيقي وليس رقماً مقصوداً للتسويق: `app/forms/page.tsx` يستخدم نفس المصفوفة `TX_MINISTRIES` لكنه يزيل التكرار صراحة عبر `Set` قبل الاستخدام (نمط موجود مسبقاً في المشروع، لم يُطبَّق هنا فقط).

الإصلاح: أُضيف ثابت `UNIQUE_AUTHORITY_COUNT = new Set(TX_MINISTRIES.map(m => m.slug)).size` بجانب `PROCEDURES_TOTAL` الموجود مسبقاً (نفس نمط الحساب المُسبَق على مستوى الملف)، واستُبدل استخدامه في شريط الإحصائيات. `tsc --noEmit` نظيف. كما فُحصت (بلا حاجة لتعديل) أربع صفحات كبيرة البيانات (procedures/forms/authorities/services) للتأكد من أن كل منطق البحث/الفلترة فيها مُغلَّف بـ`useMemo` بشكل صحيح — لا يوجد فلترة inline على كل ضغطة مفتاح.

### تحديث (batch #394) — خطأ نحوي عربي حقيقي: عدم مطابقة العدد مع "يوم" في NotificationBell

`components/NotificationBell.tsx` (مكوّن حيّ، مُركَّب بلا شرط في `TopNav.tsx:274` وَ`MobileMenu.tsx` — يظهر عملياً في كل صفحة لأي مستخدم لديه وثيقة تنتهي صلاحيتها قريباً أو موعد قادم محفوظ في localStorage) كان يبني نص العدّ التنازلي بالعربية بصيغة مفرد ثابتة بلا مراعاة لقواعد التمييز العددي العربي: `` `خلال ${item.days} يوم` `` (السطر 216 قبل الإصلاح)، وبنفس النمط في نصَّي `promptAr` المُرسلَين للذكاء الاصطناعي عند الضغط على تنبيه (السطر 70 لتنبيهات الوثائق، والسطر 88 لتنبيهات المواعيد). أي عدد أيام غير 1 كان يُعرض بصياغة خاطئة نحوياً — مثلاً "خلال 5 يوم" بدل "خلال 5 أيام"، و"خلال 11 يوم" بدل "خلال 11 يوماً" — بينما النسخة الإنجليزية المرافقة في نفس الملف كانت تجمع الكلمة بشكل صحيح (`day${item.days > 1 ? 's' : ''}`)، ما يؤكد أن هذا سهو حقيقي غير مقصود وليس اختياراً أسلوبياً.

تم التحقق من أن المكوّن حيّ وليس من الملفات الـ49 المحذوفة في batch #386 (بحث `grep` شامل أكّد أنه لا يزال مستورَداً فعلياً في `TopNav.tsx` وَ`MobileMenu.tsx`).

الإصلاح: أُضيفت دالة مساعدة `arDays(n)` تطبّق قواعد التمييز العددي العربي القياسية (1 ← "يوم واحد"، 2 ← "يومين"، 3-10 ← "N أيام" جمع، 11 فأكثر ← "N يوماً" تمييز مفرد منصوب)، واستُبدلت الصياغة الثابتة بها في المواضع الثلاثة (`promptAr` للوثائق، `promptAr` للمواعيد، ونص `dayLabel` المعروض في قائمة التنبيهات). لا تغيير على النسخة الإنجليزية (كانت صحيحة أصلاً). `tsc --noEmit` نظيف.

### تحديث (batch #395) — نفس فئة خطأ التمييز العددي العربي من batch #394، وُجدت في ChatSessionSummaryChip

بعد إصلاح batch #394، امتد البحث عن نفس النمط (اسم عربي مفرد ثابت مع عدد متغيّر) عبر بقية المكوّنات — وُجدت حالة أخرى حقيقية في `components/ChatSessionSummaryChip.tsx` (مكوّن حيّ، مُستورَد ومُركَّب في `app/page.tsx` السطر 1834 — واجهة المحادثة الرئيسية، ويظهر تلقائياً لأي مستخدم تتجاوز جلسته عتبة 10 رسائل).

السطر 74 (قبل الإصلاح) كان: `` `${messageCount} رسالة — اضغط لتلخيص الجلسة` `` — يستخدم صيغة المفرد "رسالة" بلا مراعاة للعدد. بما أن الشرط `messageCount < threshold` (افتراضياً 10) يمنع ظهور المكوّن، فإن أول وأكثر قيمة شيوعاً لظهوره فعلياً هي 10 بالضبط — أي "10 رسالة" بدل الصيغة الصحيحة "10 رسائل" (جمع، مطلوب للأعداد 3-10 بالعربية). النسخة الإنجليزية المرافقة كانت صحيحة بالفعل (`messages` بصيغة الجمع الثابتة، مناسبة إنجليزياً لأي عدد ≥ 2).

الإصلاح: أُضيفت دالة مساعدة `arMessages(n)` بنفس نمط `arDays(n)` من batch #394 (1 ← "رسالة واحدة"، 2 ← "رسالتين"، 3-10 ← "N رسائل"، 11 فأكثر ← "N رسالة")، واستُبدلت بها الصياغة الثابتة في السطر الوحيد المتأثر. لا تغيير على النسخة الإنجليزية. `tsc --noEmit` نظيف.

### تحديث (batch #396) — حالة ثالثة من نفس فئة خطأ التمييز العددي العربي، في ProcedureCountdownTimer

استمرّ البحث عن نفس نمط batches #394/#395 (اسم عربي مفرد ثابت مع عدد متغيّر) عبر بقية المكوّنات — وُجدت حالة ثالثة حقيقية في `components/ProcedureCountdownTimer.tsx` (مكوّن حيّ، مُستورَد ومُركَّب لكل بطاقة إجراء موسَّعة في `app/procedures/page.tsx` السطر 862؛ يسمح المستخدم بضبط أي مهلة بين 1 و365 يوماً وتتحدّث القيمة المعروضة كل دقيقة، فالأعداد الصغيرة 1-10 — وهي بالضبط النطاق الذي يحتاج تمييزاً عددياً خاصاً بالعربية — هي الحالة الأكثر شيوعاً عملياً).

الدالة `formatCountdown` (الأسطر 27-29 قبل الإصلاح) كانت تبني النص العربي بصيغة مفرد ثابتة للوحدات الثلاث: `` `${days} يوم و ${hours} ساعة` ``, `` `${hours} ساعة و ${minutes} دقيقة` ``, `` `${minutes} دقيقة` `` — بلا أي مراعاة للعدد. فمثلاً مهلة 3 أيام كانت تُعرض "3 يوم و 5 ساعة" بدل "3 أيام و 5 ساعات"، ومهلة يوم واحد "1 يوم" بدل "يوم واحد"، وساعتان "2 ساعة" بدل "ساعتين". النسخة الإنجليزية المرافقة (`Nd Nh` / `Nh Nm` / `Nm`) لا تتأثر بهذه المشكلة أصلاً لأنها لا تستخدم كلمات كاملة تحتاج جمعاً.

الإصلاح: أُضيفت دالة مساعدة عامة `arUnit(n, sing, dual, plural, many)` تطبّق قواعد التمييز العددي العربي الكاملة الأربع (1 مفرد، 2 مثنى، 3-10 جمع، 11+ تمييز مفرد منصوب) — أعمّ قليلاً من `arDays`/`arMessages` في الدفعتين السابقتين لأنها تغطي أيضاً حالتَي المثنى الصريح ("يومين"، "ساعتين") بدل الانتقال المباشر من مفرد إلى جمع — مع ثلاث دوال مختصرة `arDaysUnit`/`arHoursUnit`/`arMinutesUnit` تستخدمها بالكلمات الصحيحة لكل وحدة، واستُبدلت بها الصياغة الثابتة في الأسطر الثلاثة المتأثرة. لا تغيير على النسخة الإنجليزية. `tsc --noEmit` نظيف.

### تحديث (batch #397) — عدم اتساق في نظام الأرقام: ProcedureEstimatedFeeChip يعرض أرقاماً هندية شرقية بخلاف بقية التطبيق

`components/ProcedureEstimatedFeeChip.tsx` (مكوّن حيّ، يُعرض على كل بطاقة إجراء موسَّعة ذات رسوم `low`/`mid`/`high` في `app/procedures/page.tsx:645`) كانت دالة `formatAmount` (الأسطر 69-74 قبل الإصلاح) تستخدم `n.toLocaleString('ar-EG')` عند `isAr === true`. هذا الاستدعاء لا يضيف فاصلاً عربياً للآلاف فحسب — بل يحوّل الأرقام فعلياً إلى الأرقام الهندية الشرقية (١٢٣٤٥٦٧٨٩٠) بدل الأرقام الغربية المعتادة (0-9). تم التحقق فعلياً: `(150000).toLocaleString('ar-EG')` يُنتج `"١٥٠٬٠٠٠"`.

هذا يخالف الاصطلاح المستخدم في بقية التطبيق بالكامل: `CostEstimator.tsx` يعرض `$${total}` بأرقام غربية، وكذلك `ProcedureFeeHistory.tsx` وشرائح الإحصائيات المختلفة (`HomepageMiniStats` وغيرها) — كلها أرقام غربية حتى في الوضع العربي. النتيجة: شريحة الرسوم وحدها كانت تعرض أرقاماً هندية شرقية بجانب شرائح أخرى (عدد المستندات/الخطوات) بأرقام غربية على نفس البطاقة، ما يبدو كخطأ تنسيق وليس اختياراً متعمَّداً.

الإصلاح: حُذف التحويل الشرطي حسب اللغة، وأصبحت الدالة تستخدم `en-US` دائماً لتنسيق الأرقام (فاصل الآلاف فقط، أرقام غربية) بغض النظر عن `isAr` — يتغيّر فقط النص المرافق للوحدة (`ل.ل.` أو `LBP`)، مطابقةً للاصطلاح المستخدم في كل مكان آخر بالتطبيق. `tsc --noEmit` نظيف.

### تحديث (batch #398) — خطأ حقيقي: فشل تسجيل الدخول قد يُعرَض زوراً رغم نجاحه فعلياً على الخادم

`lib/auth.ts` — دالتا `setToken` (الأسطر 22-24) وَ`setUser` (الأسطر 37-39) كانتا تستدعيان `localStorage.setItem` مباشرة بلا `try/catch`، خلافاً لكل استدعاءات `localStorage.setItem` الأخرى في المشروع (كلها محاطة بـ`try/catch`، تحقّقنا عبر `grep`).

المسار الحقيقي المتأثر: `app/login/page.tsx` (الأسطر 22-26) وَ`app/register/page.tsx` يستدعيان `apiLogin`/`apiRegister` ثم مباشرة `setToken(data.token)` وَ`setUser(data.user)` ثم `router.push('/')`، وكل ذلك داخل بلوك `try` واحد تُمسك استثناءاته بـ`catch (err) { setError(...) }`. إن رمت `setToken` استثناءً (مثال حقيقي وليس نظرياً: وضع التصفح الخاص في Safari يجعل حصة `localStorage` صفراً ويرمي `QuotaExceededError` عند أي `setItem`؛ يحدث أيضاً عند تعطيل التخزين المحلي من إعدادات المتصفح أو امتلاء المساحة فعلياً) — فإن الاستثناء يُمسَك بواسطة الـ`catch` الخارجي ويُعرَض للمستخدم كخطأ عام في تسجيل الدخول، رغم أن الخادم (`apiLogin`) كان قد قَبِل بيانات الدخول فعلاً بنجاح. والأسوأ: بما أن `setToken` ترمي قبل تنفيذ `setUser`/`router.push('/')`، لا يُحفَظ الرمز المميّز إطلاقاً ولا يُنقَل المستخدم فعلياً للصفحة الرئيسية رغم صحة بياناته — يبقى عالقاً يعيد إدخال بيانات صحيحة يستمر الخادم بقبولها بينما تستمر الواجهة بإخفاقها.

الإصلاح: أُحيطت `setToken`/`setUser` بـ`try/catch` (يُتجاهَل الاستثناء بصمت، بنفس نمط الحماية المستخدم في بقية استدعاءات `localStorage.setItem` بالمشروع) — بذلك تكتمل عملية الدخول فعلياً (`router.push('/')`) حتى لو تعذّر حفظ الحالة محلياً، بدل الإبلاغ الزائف عن فشل الدخول. كما أُضيفت حماية مماثلة لـ`clearToken` (كانت تستخدم `removeItem` بلا حماية أيضاً) للاتساق. `tsc --noEmit` نظيف.

### تحديث (batch #399) — استدعاء localStorage غير محمي آخر، فاتته دفعة #398، في ProcedureHistoryLog

بعد إصلاح batch #398 لـ`lib/auth.ts`، امتد البحث عن استدعاءات `localStorage.setItem` الأخرى غير المحمية بـ`try/catch` عبر المشروع بالكامل — وُجدت حالة واحدة حقيقية في `components/ProcedureHistoryLog.tsx` (مكوّن حيّ، يُعرض داخل كل بطاقة إجراء موسَّعة على `/procedures`، السطر 873 في `app/procedures/page.tsx`).

الأسطر 87-93 (قبل الإصلاح) داخل `useEffect` الذي يعمل عند كل تركيب للمكوّن (أي كل مرة يوسّع فيها المستخدم بطاقة إجراء) كانت تستدعي `localStorage.getItem`/`localStorage.setItem` مباشرة بلا حماية — بينما بقية استدعاءات `localStorage` في نفس الملف بالضبط (`addHistoryEvent` السطر 45-53، وَ`loadHistory` السطر 56-59) محميتان بـ`try/catch` بشكل صحيح، وكذلك كل استدعاء آخر من هذا النوع تم فحصه عبر المشروع (أكثر من 30 موضعاً). لو رمى `localStorage` استثناءً (نفس السيناريو الحقيقي المستخدَم لتبرير batch #398 — وضع التصفح الخاص في Safari، أو تعطيل التخزين، أو امتلاء المساحة)، فإن الاستثناء غير الملتقَط داخل `useEffect` كان سيتسرّب ويكسر صفحة `/procedures` بالكامل لذلك المستخدم، بخلاف بقية استدعاءات المشروع المطابقة التي تتدهور بهدوء بلا كسر.

الإصلاح: أُحيط الكود بـ`try/catch` مطابقاً للنمط المستخدم في بقية الملف نفسه بالضبط. `tsc --noEmit` نظيف.

### تحديث (batch #400) — اكتشاف كود ميت آخر لم يكن مدرَجاً في قائمة الحذف: AppointmentTracker.tsx

اقترح أحد وكلاء البحث إصلاح خطأ حدّ (off-by-one) حقيقي في `components/AppointmentTracker.tsx` (السطر 97: `appts.filter(a => daysUntil(a.date) >= -1)` بينما التعليق أعلاه يقول صراحة "Upcoming = not more than 30 days past" — أي أن العتبة الصحيحة يجب أن تكون `-30` لا `-1`). قبل التنفيذ، وطبقاً للقاعدة المتّبعة طوال الجلسة («لا تُصلح كوداً ميتاً»، تأكّدت في batch #394)، تحقّقتُ مما إذا كان هذا المكوّن حيّاً فعلاً — فتبيّن أنه **غير مُستورَد أو مُركَّب في أي مكان في التطبيق حالياً** (`grep` شامل عبر `app/` وَ`components/` وَ`lib/`، بما في ذلك فحص `next/dynamic`، لم يجد أي `import AppointmentTracker` أو `<AppointmentTracker`). المكوّن الوحيد الذي كان يستهلك نفس مفتاح `localStorage` الخاص به (`HomepageTodayTasks.tsx`) هو أيضاً من ضمن الملفات الـ49 المحذوفة سابقاً في batch #386.

الملاحظة المهمة: هذا الملف لم يكن مدرَجاً أصلاً ضمن قائمة الحذف في batch #386 — بل ظل معتمَداً كمكوّن حيّ حتى batch #344 (حيث أُضيفت له تسميات aria-label)، ثم أصبح غير مُستخدَم في وقت لاحق (على الأرجح أثناء إحدى دفعات إعادة تصميم v4.0 التي أزالت استدعاءه من الصفحة الرئيسية بلا حذف الملف نفسه) دون أن يُكتشف ذلك حتى الآن.

بما أن إصلاح خطأ داخل كود ميت غير مُتاح للمستخدم يُعَد جهداً ضائعاً، فالإصلاح الصحيح هنا هو حذف الملف نفسه، تماشياً مع نمط batch #386. أُضيف `components/AppointmentTracker.tsx` (502 سطر) إلى قائمة `rm -f` في `push.sh`. `tsc --noEmit` نظيف (الملف لا يزال موجوداً فعلياً في الـsandbox حتى يُشغِّل المستخدم `push.sh` بنفسه؛ الحذف مُجدوَل فقط).

### تحديث (batch #401) — خطأ حقيقي في CostEstimator: رسوم بالليرة اللبنانية بفواصل نقطية تُقرأ كصفر دولار

`components/CostEstimator.tsx` (مكوّن حيّ، مُستورَد ومُركَّب في `app/procedures/page.tsx` السطر 712، يُغذَّى بقيم `fees` الحقيقية من `lib/enrichedProcedures.ts`) — دالة `parseFeesUSD` (السطر 48 قبل الإصلاح) كانت تستخدم `raw.match(/([\d,]+)\s*(?:ل\.ل|L\.L|LBP|ليرة)/i)` لاستخراج المبلغ بالليرة. فئة الأحرف `[\d,]+` تسمح بالفواصل الإنجليزية فقط، بينما بيانات الرسوم الحقيقية في `lib/enrichedProcedures.ts` تستخدم **نقاطاً** كفواصل آلاف (مثال حقيقي من السطر 346: `'1.500.000 ل.ل.'`). بما أن التعبير النمطي لا يستطيع تجاوز النقاط، فإنه كان يلتقط فقط الجزء الأخير قبل "ل.ل" — أي `"000"` بدل `"1.500.000"` — فيُحسَب `Math.round(0 / 89500) = 0`. وبما أن الدالة تُعيد `0` (وليس `null`)، فإن fallback القيمة الافتراضية `?? 20` لا يُفعَّل أبداً، فيُعرَض للمستخدم "الرسوم الرسمية: $0" بدل القيمة الصحيحة (~$17 لهذا المثال).

تم التحقق فعلياً عبر Node: النمط القديم يلتقط `"000"` فقط من `"1.500.000 ل.ل."`، بينما النمط الجديد يلتقط `"1.500.000"` كاملاً ويُحسَب $17 بشكل صحيح. تُصادف هذه الصيغة (فواصل نقطية) في معظم قيم `fees` الحقيقية بالليرة اللبنانية بالمصدر (رسوم إجازات العمل، الفئات المختلفة، إلخ) — وهي بيانات حقيقية يعرضها التطبيق للمستخدمين النهائيين حول رسوم حكومية فعلية، وهي بالضبط نوع المحتوى الذي يُفترض بالتطبيق أن يعرضه بدقة.

الإصلاح: وُسِّعت فئة الأحرف لتشمل النقاط `([\d.,]+)`، وأصبحت إزالة الفواصل قبل `parseFloat` تحذف كلا الرمزين (`.` وَ`,`) بدل الفاصلة الإنجليزية فقط. `tsc --noEmit` نظيف.

### تحديث (batch #402) — خطأ حقيقي في ProcedureEstimatedCompletion: نصوص "مدة المعالجة" المتدرّجة تُقرأ خطأً كيوم واحد فقط

`components/ProcedureEstimatedCompletion.tsx` (مكوّن حيّ، مُستورَد ومُركَّب في `app/procedures/page.tsx` السطر 867) — دالة `parseProcessingDays` كانت تتحقق من `lower.includes('يوم واحد')` **قبل** تشغيل أي من تعبيرات الأرقام النمطية (regex)، وتُعيد `1` فوراً عند وجود هذه العبارة في أي مكان بالنص — حتى لو احتوى نفس النص على رقم أكبر وأكثر صلة لاحقاً يمثّل فئة معالجة أطول.

بيانات حقيقية من `lib/enrichedProcedures.ts` تُصادف هذا الخلل فعلياً:
- السطر 807 (إجراء طلب شتول حرجية وزينة): `processingTime:'فوراً | فوراً | يوم واحد | 3 – 4 أيام'` — قيمة متدرّجة حسب حجم الطلب، آخر فئة (تتطلب موافقة الوزير) تستغرق 3-4 أيام. كانت الدالة تُعيد `1` فوراً بدل `4`.
- السطر 1474: `processingTime:'يوم واحد — 3 أيام عمل'` (نطاق) — كانت تُعيد `1` بدل `3`.

تم التحقق فعلياً عبر Node: بالترتيب القديم، كلا المثالين يُعيدان `1`. بالترتيب الجديد يُعيدان `4` وَ`3` على التوالي (المطابقة الصحيحة)، بينما الحالات النقية بلا أرقام (مثل السطر 784 `'يوم واحد'` وحدها، والسطر 1497 `'يوم واحد — فوري في كثير من الأحيان'`) لا تزال تُعيد `1` بشكل صحيح كما كانت.

الأثر: أي مستخدم يبدأ إجراءً من الفئة الأعلى (تستغرق فعلياً 3-4 أيام) كان يرى "تاريخ الإنجاز المتوقّع" بعد يوم واحد فقط من البدء، وقد يُصنَّف الإجراء خطأً كـ"متأخر" (شارة حمراء) قبل يومين إلى ثلاثة من موعده الحقيقي.

الإصلاح: أُعيد ترتيب الدالة بحيث تُفحَص تعبيرات الأرقام النمطية (أيام/أسابيع/أشهر، عربي وإنجليزي) **أولاً**، وتُفحَص العبارات الاسمية الثابتة («يوم واحد»، «أسبوعين»، «أسبوع»، «شهر») فقط إذا لم يُعثَر على رقم صريح — بما أن `.match()` بلا علم `/g/` يُعيد أول تطابق رقم+وحدة في النص، وعبارة «يوم واحد» نفسها لا تحتوي رقماً، فإن هذا الترتيب يجد الرقم الأكبر ذا الصلة تلقائياً عند وجوده، مع الحفاظ على السلوك الصحيح للنصوص الخالية من الأرقام. `tsc --noEmit` نظيف.

### تحديث (batch #403) — نفس فئة خطأ الفواصل النقطية من batch #401، وُجدت في ProcedureEstimatedFeeChip

`components/ProcedureEstimatedFeeChip.tsx` (مكوّن حيّ، السطر 645 في `app/procedures/page.tsx`) — دالة `parseFeesAr` كانت تستخدم حارساً `!f.match(/\d{5,}/)` لتحديد ما إذا كان النص "طابع فقط" (لا رسوم حقيقية إضافية) قبل تصنيفه كذلك. لكن بيانات الرسوم الحقيقية في `lib/enrichedProcedures.ts` تستخدم فواصل نقطية لتقسيم الآلاف (نفس السبب الجذري لـ batch #401 لكن في نقطة فشل مختلفة هنا) — فمبلغ مثل `1.800.000` يتقسّم بالنقاط إلى مجموعات من 3 أرقام، فلا يظهر أبداً تسلسل من 5 أرقام متتالية حتى مع وجود رسم حقيقي كبير.

مثال حقيقي من السطر 369 (تجديد إجازة عمل): `'رسم 480.000 ل.ل. فئة ثالثة | 960.000 ل.ل. فئة ثانية | 1.800.000 ل.ل. فئة أولى. طابع أميري 1000 ل.ل. لكل مستند.'`. تحقّقتُ عبر Node: الحارس `\d{5,}` لا يجد تطابقاً، فيُصنَّف هذا الإجراء خطأً كـ"طابع فقط" (شارة بنفسجية، توحي برسم ~1000 ل.ل.) رغم أن تكلفته الفعلية تصل إلى 1,800,000 ل.ل. لنفس السبب، دالة استخراج الأرقام `f.match(/[\d,]+/g)` كانت تفوّت أيضاً الأرقام النقطية عند وصولها لتصنيف الفئة (منخفضة/متوسطة/مرتفعة).

الإصلاح: وُسِّع الحارس ليقبل أيضاً تسلسلات من 4+ أرقام مقسَّمة بفاصلة أو نقطة (`\d{1,3}(?:[.,]\d{3})+`)، ووُسِّعت فئة استخراج الأرقام لتشمل النقطة `[\d.,]+` مع حذف كلا الرمزين قبل `parseInt`. تم التحقق أن الحالات الحقيقية "طابع فقط بلا رسوم إضافية" (مثال: `'طابع مالي بقيمة 1000 ل.ل. على كل مستند.'` — رقم واحد بلا فواصل تجميع) لا تزال تُصنَّف بشكل صحيح بعد الإصلاح (`1000` بدون فواصل لا يطابق النمط الجديد). `tsc --noEmit` نظيف.

### تحديث (batch #404) — خطأ حقيقي في ProcedureCostBreakdown: رسوم متدرّجة كبيرة تُصنَّف كطابع فقط

`components/ProcedureCostBreakdown.tsx` (مكوّن حيّ، مُستورَد ومُركَّب في `app/procedures/page.tsx` السطر 828) — دالة `parseLines` كانت تقسّم نص الرسوم الخام إلى بنود منفصلة عبر السطر الجديد، أو `|`، أو الشرطة الطويلة `—`، لكنها **لا تقسّم على الشرطة العادية `-` المستخدمة كنقطة رصدية داخل السطر** (فقط تُزيل شرطة بادئة من بداية كل جزء بعد التقسيم، لا تقسّم عليها بحد ذاتها).

بيانات حقيقية من `lib/enrichedProcedures.ts` (السطر 346) تستخدم هذا النمط تحديداً بلا أسطر جديدة أو `|` أو شرطات طويلة: `'- رسم بـ 1.500.000 ل.ل. للعامل من الفئة الأولى.- رسم بـ 800.000 ل.ل. للعامل من الفئة الثانية.- رسم بـ 400.000 ل.ل. من الفئة الثالثة.حسب قانون موازنة 2002 تاريخ 8/2/2002.- طابع أميري بقيمة 1000 ل.ل. لكل مستند.'` — تحقّقتُ عبر Node أن هذا النص بأكمله (بنوده الأربعة: 1.5 مليون، 800 ألف، 400 ألف، وطابع الـ1000 ل.ل.) كان يبقى **بندأ واحداً غير مقسَّم**، ودالة `classify` تفحص `طابع`/`stamp` قبل `fee`، فبما أن الجملة الأخيرة تذكر "طابع أميري"، يُصنَّف البند كاملاً خطأً كـ`stamp` (شارة 🏷️ توحي برسم رمزي)، فيختفي من واجهة المستخدم أن هذا الإجراء يكلّف فعلياً 400,000-1,500,000 ل.ل. حسب فئة العامل.

الإصلاح: أُضيف تقسيم إضافي على شرطة عادية تسبقها نقطة أو رقم وتليها كلمة عربية/إنجليزية جديدة (بنفس منطق قاعدة الشرطة الطويلة الموجودة مسبقاً). تم التحقق: النص أعلاه يُقسَّم الآن بشكل صحيح إلى 4 بنود منفصلة (رسم 1.5 مليون=fee، رسم 800 ألف=fee، رسم 400 ألف=variable بسبب ذكر «حسب» القانون، طابع=stamp). كما تم فحص جميع الـ60 نص رسوم في `lib/enrichedProcedures.ts` عبر سكريبت Node للتأكد من عدم وجود تقسيمات خاطئة أو أجزاء قصيرة غير منطقية بعد التعديل — صفر مشاكل. `tsc --noEmit` نظيف.

### تحديث (batch #405) — حقل "مكان التقديم" متعدد القيم يُعرض بخط أنبوب `|` خام بدل فصل واضح

نفس اصطلاح "قيم متعددة مفصولة بـ`|`" المستخدم في حقل `fees` (والذي عولجت مشاكله في batches #401-#404) موجود أيضاً في حقل `whereToApply` — لكن لا أحد من مستهلكيه الثلاثة الحيّة كان يقسّم النص، فيُعرض الخط الأنبوبي الخام للمستخدم النهائي:

- `app/procedures/page.tsx` السطر 578/689 (شريحة "📍 مكان التقديم" على كل بطاقة إجراء موسَّعة، مع `CopyBtn` لنسخ نفس النص الخام)
- `components/PrintProcedureModal.tsx` السطر 44/189 (`MetaCell` في نسخة الطباعة)
- `components/ProcedureComparator.tsx` السطر 140 (صف "مكان التقديم" في جدول المقارنة)

مثال حقيقي من `lib/enrichedProcedures.ts` (السطر 188، وما يقابله بالإنجليزية السطر 178): `whereToApply:'أقرب مخفر درك أو شرطة | الشعبة القانونية العسكرية'` — موقعان متمايزان لتقديم الطلب، لكن الواجهة كانت تعرض النص الخام بالكامل بما فيه رمز `|` داخل شريحة مُسمّاة "مكان التقديم" (مفرد)، بلا فصل واضح. وُجدت حالة ثانية مماثلة في السطر 464. تم التحقق أن هذا الاصطلاح (`|` كفاصل قيم متعددة) هو نفسه المستخدَم فعلاً ومُعالَج مسبقاً في `ProcedureCostBreakdown.tsx` لحقل `fees`، فتأكّد أنه سهو في التطبيق لا اختلافاً في تصميم البيانات.

الإصلاح: أُضيف تقسيم `split(/\s*\|\s*/).filter(Boolean).join(isAr ? '، ' : ', ')` في المواضع الثلاثة — يستبدل رمز `|` الخام بفاصلة عربية (،) أو إنجليزية (,) حسب اللغة، فيُعرض النص كقائمة مقروءة بدل نص أنبوبي خام. تم التحقق عبر Node أن المثال الحقيقي أعلاه يُصبح "أقرب مخفر درك أو شرطة، الشعبة القانونية العسكرية". `tsc --noEmit` نظيف على الملفات الثلاثة.

### تحديث (batch #406) — تطبيق ناقص للإصلاح: fees وَprocessingTime في PrintProcedureModal بقيا بلا فصل رغم إصلاح whereToApply في نفس الملف

بعد إصلاح batch #405 لحقل `whereToApply` في ثلاثة ملفات — من بينها `components/PrintProcedureModal.tsx` (مكوّن حيّ، مُستورَد ومُركَّب في `app/procedures/page.tsx` السطر 24/1143) — تبيّن أن نفس الملف كان يحتوي على نفس المشكلة بالضبط في حقلين مجاورين لم يُلمَسا: `fees` (السطر 42) وَ`processingTime` (السطر 43)، كلاهما يستخدم نفس اصطلاح الفصل بـ`|` في البيانات الحقيقية (وهو الاصطلاح الذي عُولِج بالفعل في مكوّنات أخرى تستهلك نفس الحقلين — `CostEstimator`/`ProcedureEstimatedFeeChip`/`ProcedureCostBreakdown` لـ`fees`، وَ`ProcedureEstimatedCompletion` لـ`processingTime` — لكن ليس هنا في نسخة الطباعة).

مثال حقيقي من `lib/enrichedProcedures.ts` السطر 369: `fees:'رسم 480.000 ل.ل. فئة ثالثة | 960.000 ل.ل. فئة ثانية | 1.800.000 ل.ل. فئة أولى. طابع أميري 1000 ل.ل. لكل مستند.'` — عند فتح نافذة الطباعة لهذا الإجراء، كان النص الخام بأكمله (بما فيه رموز `|`) يُطبَع كما هو. نفس الأمر لحقل `processingTime` (مثال السطر 807).

الإصلاح: طُبِّق نفس نمط `split(/\s*\|\s*/).filter(Boolean).join(isAr ? '، ' : ', ')` المستخدَم بالفعل لـ`where` في نفس الملف (منذ batch #405) على `fees` وَ`processingTime` أيضاً، ليتّسق سلوك الملف بالكامل. `tsc --noEmit` نظيف.

### تحديث (batch #407) — شارة الرسوم في نتائج البحث تقصّ الرقم قبل وحدته

`components/ProcedureSearchModal.tsx` (مكوّن حيّ، مُستورَد ومُركَّب في `app/procedures/page.tsx` السطر 25/1151 — يُفتح بزر البحث في صفحة المعاملات) — شارة "💰 الرسوم" في كل نتيجة بحث كانت تُقصّ (`fees.slice(0, 18)`) النص الخام مباشرة، دون العلم بأن حقل `fees` يستخدم نفس اصطلاح الفصل بـ`|` متعدد القيم المُعالَج في batches #401/#403/#404/#405/#406 لمكوّنات أخرى — فكان القصّ عند 18 حرفاً يحدث أحياناً **قبل** الوصول إلى وحدة العملة نفسها، وليس فقط قبل الوصول إلى القيمة الثانية بعد الفاصل.

مثال حقيقي من `lib/enrichedProcedures.ts` (السطر 277، إجراء الترخيص بإقامة الحفلات والمهرجانات): `fees:'طابع أميري بـ 1000 ل.ل. على الطلب | رسم تحقيق حسب حجم الفعالية.'`. تحقّقتُ عبر Node: `.slice(0, 18)` القديم يُنتج `"طابع أميري بـ 1000"` — يقطع الرقم `1000` عن وحدته `ل.ل.` مباشرة بعده، فيظهر في نتيجة البحث رقم بلا وحدة عملة واضحة، ويختفي تماماً ذكر القيمة الثانية (رسم التحقيق) دون أي إشارة لوجودها.

الإصلاح: أصبح القصّ يعمل على أول قيمة بعد الفصل بـ`|` فقط (`fees.split(/\s*\|\s*/)[0].trim()`) بدل النص الخام الكامل، مع رفع حد الطول إلى 22 حرفاً (يكفي لتضمين الوحدة `ل.ل.` في معظم الحالات الحقيقية) وإضافة `…` عند التقصير الفعلي. تم التحقق عبر Node أن المثال أعلاه يُصبح `"طابع أميري بـ 1000 ل.ل…"` — يحافظ على الرقم ووحدته معاً. `tsc --noEmit` نظيف.

### تحديث (batch #408) — خطأ حقيقي في بيانات: شارة "نموذج" تظهر لـ5 إجراءات بلا نموذج PDF فعلي

`app/procedures/page.tsx` — شريحة "نموذج/Form" (السطر 642) تظهر على البطاقة المطوية اعتماداً على `proc.hasForm` فقط، بينما رابط تنزيل PDF الفعلي (السطر 843) لا يُعرَض إلا عند `proc.pdfUrls.length > 0`. تحقّقتُ عبر Node بفحص كل الإجراءات الـ62 في `lib/enrichedProcedures.ts`: العلاقة بين الحقلين متّسقة تماماً في 57 حالة (`hasForm:false` ⇔ `pdfUrls:[]` دائماً بلا استثناء واحد بين 9 حالات)، لكن **5 إجراءات فقط** كانت `hasForm:true` مع `pdfUrls:[]` فارغة — تناقض بيانات واضح وليس تصميماً مقصوداً: بطاقة الإعاقة والدعم الاجتماعي (السطر 1178)، دعم الأسر المحتاجة (السطر 1201)، معادلة الشهادات الجامعية (السطر 1247)، رخصة البناء (السطر 1385)، وتصريح حفر الطريق العام (السطر 1615).

الأثر: مستخدم يفتح مثلاً "استخراج بطاقة الإعاقة والحصول على الدعم الاجتماعي" يرى شارة "نموذج" الصفراء التي تَعِد بنموذج قابل للتنزيل، فيوسّع البطاقة بحثاً عنه، ولا يجد قسم التنزيل مطلقاً — طريق مسدود يتناقض مع إشارة التطبيق نفسه. نفس الحقل `hasForm` يُستخدَم أيضاً في فلتر البحث المتقدّم (`advFilters.hasForm`، السطر 208-209)، فتصفية "لديه نموذج" كانت تُرجع هذه الخمسة أيضاً رغم عدم توفّر أي نموذج فعلي لها.

الإصلاح: بما أن الاتساق التام بين الحقلين في الـ57 حالة الأخرى يؤكد أن `hasForm` يعني فعلياً "نملك ملف النموذج" وليس "هذا الإجراء يتطلّب نموذجاً مفاهيمياً"، صُحِّح مصدر البيانات مباشرة: غُيِّر `hasForm` إلى `false` في الحالات الخمس بدل تعديل شرط العرض فقط — هذا يُصلح الشارة والفلتر معاً بتغيير واحد متّسق مع بقية الملف. تحقّقتُ عبر Node أن العدد المتبقي من الحالات الشاذة أصبح صفراً. `tsc --noEmit` نظيف.

### تحديث (batch #409) — عدم تطابق ministrySlug مع مفاتيح البحث في مكوّنين حيّين — بطاقة الوزارة والروابط الرسمية تختفيان لـ6 إجراءات

بعد نجاح batch #408 في اكتشاف خطأ بتقاطع حقلين مرتبطين، طبّقت نفس الأسلوب على حقل `ministrySlug` مقابل جداول البحث المستخدمة به في مكوّنين حيّين: `ProcedureRelatedMinistries.tsx` (بطاقة "الجهة المختصة" بالهاتف وساعات العمل، مُستورَدة ومُركَّبة في `app/procedures/page.tsx` السطر 41/706) و`ProcedureExternalLinks.tsx` (روابط البوابات الرسمية، السطر 40/712).

تحقّقتُ عبر Node من كل قيم `ministrySlug` الفعلية عبر الـ71 إجراءً في `lib/enrichedProcedures.ts`: 6 إجراءات تستخدم `'general-security'` (3 إجراءات) أو `'public-works'` (2 إجراءان) أو `'customs'` (إجراء واحد) — وهي نفس الصيغة المُستخدَمة فعلياً في رقاقات فلتر الوزارة الخاصة بـ`app/procedures/page.tsx` نفسه (`{ slug: 'general-security', ... }` و`{ slug: 'public-works', ... }`، السطر 91/101) — بينما جدولا البحث في المكوّنين المذكورين كانا يستخدمان المفاتيح بلا واصلة (`security`، `publicworks`) فقط. النتيجة: `SLUG_MAP[ministrySlug]` و`LINKS_BY_SLUG[ministrySlug]` لا يجدان تطابقاً لهذه الـ6 إجراءات، فتختفي بطاقة "الجهة المختصة" بالكامل (المكوّن يُعيد `null` عند عدم وجود تطابق)، وتظهر روابط "بوابة دولتي" العامة بدل الروابط الحكومية المخصّصة فعلياً.

الإصلاح: أُضيفت مفاتيح مستعارة بصيغة الواصلة (`'general-security'`، `'public-works'`) في كلا المكوّنين، تشير إلى **نفس** البيانات الموثّقة الموجودة مسبقاً (رقم الهاتف/ساعات العمل/روابط البوابات) — لا بيانات جديدة أو مُخمَّنة. أُصلح 5 من 6 إجراءات بهذا التغيير (3 general-security + 2 public-works). الإجراء السادس (`customs`، استيراد سيارة عبر الجمارك) **تُرِك دون إصلاح عمداً**: لا يوجد في أي ملف بالمشروع رقم هاتف أو رابط رسمي موثّق للجمارك اللبنانية، وتخمين معلومة اتصال حقيقية غير مسموح به — يبقى المكوّنان يتصرّفان بأمان (بطاقة الوزارة تختفي، روابط البوابات تعرض البديل العام) بدل عرض معلومة قد تكون خاطئة. `tsc --noEmit` نظيف.

### تحديث (batch #410) — 6 إجراءات لمجلس الجنوب/مصلحة ري الجنوب مصنَّفة خطأً كوزارة التربية أو الشؤون الاجتماعية أو الأشغال العامة

استمراراً لنجاح batch #408 وbatch #409 في اكتشاف تناقضات بتقاطع حقول مرتبطة، دقّقتُ حقل `ministry`/`ministry_en` (الاسم الفعلي المعروض) مقابل `ministrySlug` (المفتاح المستخدَم في الفلترة والشارة والأيقونة في `app/procedures/page.tsx`، مكوّن حيّ). تحقّقتُ عبر Node من الـ71 إجراءً: **6 إجراءات** قيمة `ministry` فيها فعلياً "مجلس الجنوب" (5 إجراءات) أو "مصلحة ري الجنوب" (إجراء واحد) — جهتان حكوميتان مختلفتان تماماً عن وزارة التربية والشؤون الاجتماعية والأشغال العامة — لكن `ministrySlug` كانت مضبوطة خطأً على `education`/`social`/`public-works` (رموز فلاتر فعلية موجودة في `MINISTRY_CHIPS`، أسطر 883-1009).

الأثر مزدوج وحقيقي: (1) فلتر "التربية" في `/procedures` كان يُرجع 3 إجراءات لمجلس الجنوب (`cots1-07`، `cots1-16`، `cots1-17`) رغم أنها لا علاقة لها بوزارة التربية، وفلتر "الشؤون الاجتماعية" يُرجع إجراءين آخرين (`cots1-19`، `lita11-01`)، وفلتر "الأشغال العامة" يُرجع إجراء مصلحة الري (`lita11-03`) — تلوّث فعلي لنتائج الفلترة. (2) شارة الوزارة المعروضة على البطاقة (`MINISTRY_CHIPS.find(c => c.slug === proc.ministrySlug)?.ar`، السطر 631) كانت تعرض "التربية"/"الشؤون الاجتماعية"/"الأشغال العامة" بدل "مجلس الجنوب"/"مصلحة ري الجنوب" الصحيحين — معلومة مضلِّلة فعلياً معروضة للمستخدم. اكتشفت أيضاً خطأ إضافي مستقل في `cots1-07`: كانت قيمة `ministry_en` هي `'Ministry of Tourism'` (وزارة السياحة!) بينما الـ5 إجراءات الشقيقة الأخرى تتفق جميعاً على `'Council of the South'` — تناقض ثلاثي واضح (عربي يقول مجلس الجنوب، إنجليزي يقول وزارة السياحة، والفلتر يصنّفها كتربية).

الإصلاح: صُحِّح `ministry_en` لـ`cots1-07` إلى `'Council of the South'` (مطابقة للنمط المؤكَّد في الإجراءات الخمسة الشقيقة، وليس تخميناً). أُعيد ضبط `ministrySlug` للإجراءات الـ5 الخاصة بمجلس الجنوب إلى قيمة مستقلة `council-south` (لا تتقاطع مع أي رقاقة فلتر موجودة)، وللإجراء السادس (مصلحة ري الجنوب) إلى `south-irrigation`. لم تُضَف رقاقات فلتر جديدة لهاتين الجهتين عمداً — بما أن كلا المكوّنين (الشارة والأيقونة) لديهما بالفعل نمط رجوع آمن (`|| proc.ministry`) يعرض الاسم الصحيح تلقائياً عند عدم وجود رقاقة مطابقة، فهذا يوقف التصنيف الخاطئ فوراً دون الحاجة لتصميم UI إضافي؛ يبقى بإمكان دفعة مستقبلية إضافة رقاقتين مخصّصتين إذا أراد المستخدم إمكانية الفلترة الصريحة لهاتين الجهتين تحديداً. تحقّقتُ عبر Node أن عدد التناقضات المتبقية أصبح صفراً. `tsc --noEmit` نظيف.

### تحديث (batch #411) — تدقيق فقط، بلا تعديل كود: لم يُعثر على مرشّح قوي جديد بعد بحث موسّع

بعد 3 دفعات متتالية ناجحة (#408-#410) اعتمدت على تقاطع الحقول داخل `lib/enrichedProcedures.ts`، امتد الفحص هذه الدفعة إلى نطاقات جديدة كلياً عبر جولتين من الفحص المستقل: (1) تناسق حقول `lib/allTransactions.ts` (2484 معاملة) مقابل مستهلكه الحيّ `app/forms/page.tsx` — صفر تناقضات في `hasForm`/`pdfUrl`، وكل شرائح `ministrySlug` الـ24 موجودة فعلياً في `TX_MINISTRIES`؛ (2) تناسق `lib/serviceFAQ.ts` مقابل `app/faq/page.tsx` — الحارس الموجود (`!item.fees.startsWith('{')`) يعمل بشكل صحيح على كل الحالات التسعة الفاسدة؛ (3) فحص شامل لنقاط تعطّل محتملة (`.reduce`/`.find()`/`[0]`/`.split()` بلا حراسة) عبر كل المكوّنات الحيّة — كلها محروسة فعلياً؛ (4) تحقّقت بنفسي عبر Node من تطابق طول `requiredDocuments`/`requiredDocuments_en` وَ`steps`/`steps_en` عبر الـ71 إجراءً (المرشّحان المقترَحان في نهاية batch #409 ولم يُفحَصا بعد) — صفر تناقضات في كليهما؛ (5) جولة ثانية بحثت في نماذج التحقق، الإعدادات، وتواريخ الحافة، ورقم هاتف مكرر بقيمة مختلفة — لكن الحالة الوحيدة المكتشفة (رقم `MobileMenu.tsx` مقابل رقم `ProcedureHelpRequest.tsx`/`layout.tsx`) هي **نفس الحالة المفحوصة والمرصودة مسبقاً في batch #390**: الرقم المختلف مقترن ببريد المستخدم الحقيقي `wissam@aijur.ai`، ما يرجّح أنه رقم تواصل شخصي/مكتبي منفصل حقيقي وليس خطأً — القرار السابق بعدم التعديل («لا تستبدل المحتوى الحقيقي») لا يزال سليماً ولم يتغيّر.

هذه نتيجة صادقة ومتوقَّعة في هذه المرحلة المتقدّمة من سلسلة التحسين (411 دفعة) — لا كود جُدِّل هذه الدفعة، ولا داعٍ لفرض إصلاح ضعيف أو تجميلي فقط لتسجيل «إنجاز».

### تحديث (batch #412) — بحث صفحة /authorities لا يعمل إطلاقاً في الوضع الإنجليزي

`app/authorities/page.tsx` (مكوّن حيّ، مسار كامل) — دالة الفلترة `filtered` (الأسطر 145-156) كانت تقارن نص البحث فقط مع `a.name_ar` و`a.categories` (نسخ عربية)، بصرف النظر عن اللغة الحالية — رغم أن بطاقة الجهة نفسها تعرض `auth.name_en` في الوضع الإنجليزي (السطر 285: `isAr ? auth.name_ar : (auth.name_en || auth.name_ar)`) وحقل `SearchInput` يعرض placeholder إنجليزياً أيضاً في ذلك الوضع.

الأثر: أي مستخدم يبدّل التطبيق إلى الإنجليزية ويكتب اسم جهة بالإنجليزية في مربّع البحث (مثال حقيقي محقَّق: "General Directorate" — القيمة الفعلية لحقل `authority_en` على عشرات الخدمات في `lib/allServices.ts`) لن يحصل على أي نتيجة أبداً — `a.name_ar.includes(q)` مستحيل التطابق لأن `name_ar` نص عربي، ونفس الأمر لـ`categories`. تظهر حالة "لا توجد جهات مطابقة" رغم وجود جهات مطابقة فعلياً — البحث معطَّل عملياً بالكامل في الوضع الإنجليزي، وليس مجرد نتائج ناقصة.

الإصلاح: أصبحت المقارنة تعتمد على اللغة الحالية (`isAr`)، بنفس منطق العرض المستخدَم أصلاً في بطاقة الجهة:
```ts
list = list.filter(a =>
  (isAr ? a.name_ar : (a.name_en || a.name_ar)).includes(q) ||
  (isAr ? a.categories : (a.categories_en.length ? a.categories_en : a.categories)).some(c => c.includes(q))
)
```
وأُضيف `isAr` إلى مصفوفة اعتماديات `useMemo` (كانت مفقودة، ما كان سيُبقي نتائج الفلترة قديمة عند تبديل اللغة دون تغيير نص البحث). تحقّقتُ أن `authority_en` مُعبَّأ فعلياً على نطاق واسع في `lib/allServices.ts` (وليس فارغاً) فيُفعِّل الإصلاح استخداماً فعلياً وليس نظرياً. `tsc --noEmit` نظيف.

### تحديث (batch #413) — نفس خطأ batch #412 موجود أيضاً في بحث صفحة /services

بعد نجاح batch #412 في اكتشاف أن بحث `/authorities` لا يعمل بالإنجليزية، فحصتُ بقية صفحات البحث/الفلترة الحيّة بحثاً عن نفس فئة الخطأ (مطابقة نص البحث فقط بالعربية بصرف النظر عن اللغة الفعلية). وُجد نفس الخطأ بالضبط في `app/services/page.tsx` (مكوّن حيّ، مسار كامل): دالة `filtered` (الأسطر 435-446) كانت تقارن نص البحث فقط مع `s.name_ar`/`s.authority_ar`/`s.description`/`s.category` — رغم أن بطاقة الخدمة نفسها (السطر 734/742) وكذلك `ServiceSheet` الداخلي (`displayName`/`displayAuthority`/`displayCategory`/`displayDescription`، الأسطر 32-37) يبدّلان إلى `name_en`/`authority_en`/`category_en`/`description_en` في الوضع الإنجليزي.

مثال حقيقي: بطاقة تعرض "Registration of Marriage Contract" (`name_en`) في الوضع الإنجليزي، لكن كتابة "Marriage" أو "Registration" في مربّع البحث لا تُرجع أي نتيجة — `s.name_ar` ("تسجيل عقد زواج") لا يحتوي أبداً على هذه الكلمات الإنجليزية، ولم يكن `name_en` يُفحَص إطلاقاً. البحث الإنجليزي في هذه الصفحة كان معطَّلاً عملياً بنفس درجة `/authorities` قبل الإصلاح.

تحقّقتُ أيضاً من صفحتين إضافيتين لنفس فئة الخطأ ولم أجد فيهما مشكلة: `GlobalSearch.tsx` (بحث Cmd+K) يفحص العنوان والعنوان الفرعي بكلتا اللغتين بلا شرط `isAr`، و`app/forms/page.tsx` يعرض المحتوى بشكل واعٍ للغة بالفعل عبر دوال الفلترة الخاصة به.

الإصلاح: نفس النمط المستخدَم في batch #412 — أصبحت المقارنة تعتمد على `isAr` لاختيار الحقل الصحيح، مطابقةً لمنطق العرض الموجود أصلاً في الملف، وأُضيف `isAr` إلى مصفوفة اعتماديات `useMemo` (كانت مفقودة). `tsc --noEmit` نظيف.

### تحديث (batch #414) — استوديو الصياغة يفرض العربية دائماً على الطلب المُرسَل للذكاء الاصطناعي حتى في الوضع الإنجليزي

`components/DraftingStudio.tsx` (مكوّن حيّ، مُستورَد ومُركَّب في `app/drafting-studio/page.tsx` السطر 5/93) — دالة `buildPrompt` (الأسطر 167-179 سابقاً) كانت الوحيدة في هذا الملف التي لا تتفرّع حسب `isAr`، رغم أن كل عنصر واجهة آخر فيه (عناوين القوالب، تسميات الحقول، الأزرار) يتفرّع بشكل صحيح. كانت الدالة تبني دائماً نصاً عربياً بالكامل — عنوان الطلب، تسميات الحقول (`field.labelAr` فقط)، وتنتهي بجملة صريحة «اكتب بالعربية فقط» — بصرف النظر عن لغة الواجهة الفعلية.

الأثر: مستخدم في الوضع الإنجليزي يفتح `/drafting-studio`، يختار قالباً معروضاً بالإنجليزية (مثال: "Contract Extension")، يملأ حقولاً بتسميات إنجليزية ("Party One"، "Original Contract Date")، ثم يضغط معاينة — معاينة المرحلة الثالثة (`previewPrompt`) تعرض نصاً عربياً بالكامل بتسميات حقول عربية وتعليمة صريحة تجبر الذكاء الاصطناعي على الرد بالعربية فقط. هذا النص يُرسَل فعلياً عبر `onSend`/`sessionStorage` إلى المحادثة — أي أن اختيار المستخدم الواعي للغة الإنجليزية كان يُلغى بصمت عند إرسال طلب صياغة وثيقة.

الإصلاح: أصبحت `buildPrompt` تتفرّع حسب `isAr` بنفس نمط بقية الملف — عنوان الطلب، عنوان قسم «المعلومات المدخلة»، تسمية كل حقل (`field.labelAr`/`field.labelEn`)، وجملة الطلب الختامية، جميعها الآن باللغة الصحيحة. تحقّقتُ أن `titleEn` و`labelEn` موجودان فعلياً على كل القوالب وتعريفات الحقول (`TEMPLATES`/`FIELD_DEFS`/`GENERIC_FIELDS`) قبل استخدامها. `tsc --noEmit` نظيف.

### تحديث (batch #415) — تاريخ إغلاق الدوائر الحكومية يظهر بالإنجليزية داخل جملة عربية

`components/GovHolidayAlert.tsx` (مكوّن حيّ، مُستورَد ومُركَّب في `app/procedures/page.tsx` السطر 31/268) — نفس فئة الخطأ المكتشفة في batches #412-#414 (جزء واحد فقط من مكوّن ثنائي اللغة لا يتفرّع حسب `isAr` رغم أن بقيته تتفرّع بشكل صحيح). كانت دالة `checkAlert()` تحسب `dateLabel` مرة واحدة عبر `tomorrow.toLocaleDateString('en-GB', {...})` — لغة إنجليزية ثابتة — قبل معرفة لغة الواجهة أصلاً (الدالة تُستدعى من `useEffect` بلا معرفة بـ`isAr`)، ثم يُدرَج هذا النص الإنجليزي مباشرة داخل الجملة العربية: `` `تأكد من إنهاء معاملاتك قبل الغد (${alert.dateLabel})` ``.

مثال حقيقي: في وضع اللغة العربية، عند إغلاق وشيك (عطلة أو نهاية أسبوع)، تظهر الجملة: "تأكد من إنهاء معاملاتك قبل الغد (Friday, 24 Jul)" — اسم اليوم والشهر بالإنجليزية مُقحَمان في نص عربي RTL بدل "(الجمعة، ٢٤ يوليو)".

الإصلاح: أُزيل حساب `dateLabel` المبكر من `checkAlert()`، وأصبحت `AlertInfo` تخزّن كائن `Date` الخام (`date`) بدل نص جاهز. يُحسَب النص المعروض الآن وقت العرض فعلياً باستخدام `isAr` الحقيقي: `alert.date.toLocaleDateString(isAr ? 'ar-LB' : 'en-GB', {...})` — نفس النمط المستخدَم بالفعل في مكوّنات أخرى من المشروع (`ArabicDateDisplay`). `tsc --noEmit` نظيف.

### تحديث (batch #416) — نفس فئة الخطأ في تاريخ "آخر تسجيل دخول" داخل لوحة التحكم

بعد batch #415، بحثتُ بشكل موسّع عن كل استدعاءات `toLocaleDateString`/`toLocaleTimeString`/`toLocaleString` في المشروع (٦٠+ استدعاءً) بحثاً عن نفس النمط: مكوّن ثنائي اللغة يُثبّت لغة واحدة بدل التفرّع حسب `isAr`. `app/admin/page.tsx` (صفحة مسار كاملة، وليست مكوّناً مستورَداً) — دالة `fmtDate` (السطر ٢٠٦-٢٠٩) كانت الوحيدة في هذا الملف التي لا تتفرّع، رغم أن دالة شقيقة مجاورة تماماً `fmtTs` (السطر ١٥٠-١٥٣، تُنسّق طابع زمني مختلف) تتفرّع بشكل صحيح: `toLocaleString(isAr ? 'ar-LB' : 'en-US')`. كانت `fmtDate` تستخدم `'ar-LB'` ثابتة: `new Date(s).toLocaleDateString('ar-LB')`.

الأثر: `fmtDate` تُستخدَم لعرض عمود "آخر تسجيل دخول" لكل مستخدم في جدول المستخدمين بلوحة تحكم الأدمن (السطر ٣٧٦). عند تبديل اللغة إلى الإنجليزية، يتحوّل كل شيء آخر في الصفحة (التبويبات، الأزرار، طابع `fmtTs` المجاور) إلى الإنجليزية، بينما يبقى عمود "آخر تسجيل دخول" بصيغة التاريخ العربية — صف جدول غير متّسق لغوياً جزئياً.

الإصلاح: `try { return new Date(s).toLocaleDateString(isAr ? 'ar-LB' : 'en-US') } catch { return s }` — نفس النمط المستخدَم في `fmtTs` المجاورة. `tsc --noEmit` نظيف.

**ملاحظة للمتابعة المستقبلية**: البحث الموسّع كشف حالات إضافية مماثلة محتملة يجب فحصها في دفعة لاحقة منفصلة (لم تُفحَص أو تُصلَح بعد في هذه الدفعة، تفادياً لتغيير أكثر من ملف واحد دون مراجعة دقيقة لكل حالة): `app/admin/content/page.tsx` (4 استخدامات لـ`toLocaleDateString('ar-LB')`/`toLocaleString('ar-LB')` ثابتة، الأسطر 243/277/281/286) وَ`app/admin/page.tsx` السطر 483 (`toLocaleTimeString('ar-LB')` لعرض وقت انتهاء رمز الاستعادة). كلتا الصفحتين إداريتان (admin-only)، وأثرهما على المستخدم النهائي محدود مقارنة بالحالات المُصلَحة سابقاً — لذا أُجِّلتا عمداً بدل تجميعهما في هذه الدفعة.

### تحديث (batch #417) — استكمال فئة "فجوات التفرّع اللغوي" في صفحتَي الأدمن المؤجَّلتين من batch #416

**`app/admin/content/page.tsx`** — تحقّقتُ أولاً أن الصفحة ثنائية اللغة فعلياً (تستورد `useLanguage`، وتستخدم `isAr` بكثافة للاتجاه والعناوين والأزرار وaria-label — نموذج البيانات نفسه `title_ar`/`body_ar` عربي فقط بالتصميم، وهذا ليس خللاً بل خاصية محتوى حقيقي غير مُترجَم). أصلحتُ 5 استخدامات لتاريخ/وقت ثابتة اللغة رغم أن الصفحة تتفرّع في كل مكان آخر حولها:
- السطر ٢٤٣ (تاريخ بطاقة العنصر في القائمة): `toLocaleDateString('ar-LB')` → `toLocaleDateString(isAr ? 'ar-LB' : 'en-US')`.
- الأسطر ٢٧٧/٢٨١/٢٨٦ (لوحة التفاصيل: "أُنشئ"/"آخر تعديل"/"نُشر"): نفس الإصلاح + تسميات الحقول نفسها أصبحت تتفرّع (`{isAr ? 'أُنشئ' : 'Created'}` وهكذا) بدل نص عربي ثابت ملاصق لتاريخ كان أحياناً إنجليزياً.
- السطر ٣٧٠ (لوحة سجل التدقيق — Audit Log): اكتُشفت أثناء نفس القراءة (لم تكن مذكورة صراحة في قائمة batch #416)، نفس الإصلاح، بالإضافة لتسميتين مجاورتين "بواسطة:"/"من:" أصبحتا `{isAr ? 'بواسطة' : 'By'}:` و`{isAr ? 'من' : 'From'}:`.

**`app/admin/page.tsx`** — قسم تبويب "رموز استعادة كلمة المرور" (الأسطر ٤٥٥-٤٩٤) تبيّن أنه أوسع نطاقاً مما أشارت إليه batch #416: لم يكن السطر الوحيد المُعلَّم (`toLocaleTimeString('ar-LB')`) هو المشكلة الوحيدة، بل القسم بأكمله كان عربياً ثابتاً بلا أي تفرّع — عنوان القسم وزر "تحديث" (خصائص `SectionHeader`)، الفقرة التوضيحية، رسالة "لا توجد طلبات نشطة"، تسمية "ينتهي:"، وزر "نسخ" — رغم أن باقي هذا الملف نفسه (`fmtTs`، `TABS`، رسائل `flash`) يتفرّع حسب `isAr` بشكل متسق تماماً. بدل إصلاح سطر التاريخ فقط وترك بقية القسم عربياً ثابتاً (نمط "إصلاح جزئي داخل ملف" الذي حُدِّد سابقاً كخطأ منهجي يجب تفاديه)، تُرجم القسم بأكمله بنفس نمط `isAr ?` المستخدَم في بقية الملف.

الإصلاح مُوثَّق أيضاً بتعليق داخل الكود يشرح لماذا وُسِّع النطاق ليشمل القسم كاملاً. `tsc --noEmit` نظيف على كلا الملفين.

### تحديث (batch #418) — اكتشاف كود ميت: مكوّن مكرَّر تماماً استُبدِل بمكوّن آخر دون حذفه

`components/ProcedureRequiredDocsCounter.tsx` (63 سطراً) — مكوّن مكتمل التنفيذ (يقرأ نفس مفاتيح `localStorage` بصيغة `dalilak_doc_ready_{code}_{index}` ويعرض شارة "X من Y وثيقة جاهزة")، لكنه **غير مستورَد وغير مُركَّب في أي مكان بالتطبيق إطلاقاً**. تحقّق فعلي عبر `grep -rn "ProcedureRequiredDocsCounter" app components --include="*.tsx"`: النتيجتان الوحيدتان هما تعريف المكوّن نفسه وتصديره (السطران 4 و29) — لا سطر `import` ولا استخدام JSX خارج ملفه، أي أنه يفشل تحديداً في اختبار الحيوية المعتمد منذ batch #400 (استيراد + استخدام JSX خارج الملف نفسه، كلاهما).

اكتُشف أيضاً أن تعليق التوثيق داخل الملف (السطر 6) خاطئ: يزعم أن المفاتيح "مكتوبة بواسطة `ProcedureDocumentStatus`"، لكن `ProcedureDocumentStatus.tsx` الفعلي يكتب إلى نطاق مختلف تماماً (`dalilak_doc_expiry_{code}_{i}`). المكوّن الذي يكتب فعلياً المفاتيح التي يقرأها `ProcedureRequiredDocsCounter` هو `components/ProcedureDocReadinessBar.tsx` — وهو المكوّن **الحي فعلياً**، المُستورَد والمُركَّب في `app/procedures/page.tsx` (السطر 65 للاستيراد، 740 للعرض)، ويؤدي نفس وظيفة "X من Y جاهزة" بصرياً. يبدو أن `ProcedureDocReadinessBar` حلّ محل `ProcedureRequiredDocsCounter` أثناء إعادة تصميم سابقة، دون حذف الملف القديم أو تصحيح تعليقه.

**الإصلاح**: بما أن ملفات مجلد العمل المتصل لا يمكن حذفها مباشرة من بيئة التنفيذ (قيد بيئة معروف)، اتُّبع نفس النمط المعتمد في batch #400 (`AppointmentTracker.tsx`): أُضيف `rm -f components/ProcedureRequiredDocsCounter.tsx` إلى قسم الحذف في `push.sh` لتنفيذه عند تشغيل السكربت من جهاز المستخدم. لا حذف فعلي في هذه الجلسة، فقط طابور حذف مؤكّد الصحة. `tsc --noEmit` نظيف (الملف ما زال موجوداً فعلياً في بيئة التنفيذ حتى الآن، وهذا متوقع وغير خطأ).

### تحديث (batch #419) — سلسلة كاملة من الكود الميت: 5 مكوّنات "مراجعة المخاطر" بلا أي نقطة دخول حيّة

بعد نجاح batch #418 في اكتشاف مكوّن ميت واحد بنفس تقنية `grep`، طُبِّقت التقنية على بقية مجلد `components/` فوُجدت سلسلة كاملة من 5 ملفات مترابطة، بمجموع ~1,300 سطر، لا يستدعيها أي مسار حيّ في التطبيق:

- `TransactionFilePanel.tsx` (297 سطراً) — لا استيراد له إطلاقاً في `app/`/`components/`/`lib/` (تحقّق عبر `grep -rn`).
- `DocumentAnalysisPanel.tsx` (404 سطراً) — نفس الحال، لا استيراد إطلاقاً.
- `HumanReviewCTA.tsx` (212 سطراً) — نفس الحال، لا استيراد إطلاقاً.
- `RiskScoreCard.tsx` (196 سطراً) — يُستورَد فقط من داخل `TransactionFilePanel.tsx` و`DocumentAnalysisPanel.tsx` (الملفَّين الميتَين أعلاه)، فهو ميت بالتبعية (unreachable transitively) رغم وجود استيراد ظاهري.
- `MissingDocumentsChecklist.tsx` — نفس حالة `RiskScoreCard.tsx`، يُستورَد فقط من داخل الملفَّين الميتَين.

تحقّقتُ أيضاً أن `app/my-files/page.tsx` — المكان المنطقي الوحيد الذي كانت هذه اللوحات مصمَّمة له (عرض معاملة/تحليل وثيقة) — يستورد فقط `BottomNav`، `LoadingSpinner`، `getToken`، `useLanguage`؛ لا وجود لأي منها هناك ولا عبر `next/dynamic`. يبدو أن هذه الميزة (لوحة تفاصيل المعاملة مع تقييم المخاطر) أُزيلت أو لم تُوصَّل قط بعد إضافتها في دفعة مبكرة من السلسلة (commit مبكر `f96b51f`، الدفعات #284-332 المجمَّعة تاريخياً).

ملاحظة جانبية غير مُصلَحة (لأن الملفات ميتة أصلاً فلا أثر عملي): كل الأربعة الأولى تتفرّع حسب `isAr` في كل نص لكنها تُثبِّت `dir="rtl"` على العنصر الجذر بلا تفرّع — نفس فئة "فجوة التفرّع اللغوي" المكتشفة سابقاً، مذكورة هنا فقط للمرجعية إذا استُعيد أي من هذه الملفات لاحقاً.

**الإصلاح**: أُضيف `rm -f` للملفات الخمسة إلى `push.sh` (نفس نمط batch #400/#418) — لا حذف فعلي في بيئة التنفيذ (قيد معروف: لا يمكن حذف ملفات مجلد العمل المتصل مباشرة). `tsc --noEmit` نظيف.

### تحديث (batch #420) — ⚠️ اكتشاف حرج: ميزة "ملفاتي" بأكملها تستدعي مساراً غير موجود في الـ Backend — تدقيق فقط، لا تعديل كود (يتطلب قراراً خارج نطاق الفرونتند)

**المشكلة**: صفحة `/my-files` وزر "حفظ في ملفاتي" في التطبيق يستدعيان مساراً `POST/GET/PUT/DELETE /my-procedures` على الـ Backend، لكن هذا المسار **غير موجود إطلاقاً** فيه.

**التحقّق**: بحث شامل عبر `grep -n "@app\.\(get\|post\|put\|delete\|patch\)" backend/main.py` (السجل الكامل لكل الـ ~70 مساراً المسجَّلة فعلياً في التطبيق الوحيد لـ FastAPI) لا يحتوي أي مسار باسم `my-procedures` أو `my_procedures`. تأكيد إضافي عبر `grep -rn "my-procedures\|my_procedures" --include="*.py"` على كل مجلد الـ backend (بما فيه النسخة الاحتياطية `dalilak_AI_transfer/backend`) — صفر نتائج. بالمقابل، مسار `/transactions` موجود فعلياً بكل عمليات CRUD (`POST`/`GET`/`PATCH`/`DELETE` على الأسطر 1809/1830/1850/1864).

**نقاط الاستدعاء الحيّة الفعلية في الفرونتند** (تحقّقت أنها مستوردة ومُستخدَمة فعلياً خارج ملفاتها، وليست كوداً ميتاً):
- `lib/auth.ts:405` — `startTrackingProcedure()`.
- `app/my-files/page.tsx:66/94/141` — تحميل/تحديث/حذف المعاملات المتتبَّعة.
- `components/SaveToMyFilesButton.tsx` — مُستورَد ومُركَّب فعلياً في `app/procedures/[slug]/playbook/page.tsx`، `app/forms/[slug]/FormDetailClient.tsx`، `app/services/page.tsx`.
- الوصول لصفحة `/my-files` نفسها متاح من `BottomNav`، `MobileMenu`، `TopNav`، `app/professional/page.tsx` — أي أنها في مسار تنقّل أساسي يراه كل مستخدم.

**الأثر الفعلي على المستخدم**: كل زيارة لـ `/my-files` تفشل بصمت وتظهر حالة خطأ التحميل (`loadError`)، وكل ضغطة على زر "حفظ في ملفاتي" في أي صفحة إجراء/نموذج/خدمة تنتهي بحالة `error` — الميزة معطَّلة بالكامل من طرف إلى طرف في الإنتاج على الأرجح، وليس مجرد خلل بصري.

**لماذا لم يُصلَح كود في هذه الدفعة**: القيد الثابت المعتمد لهذه السلسلة يمنع صراحة تغيير Backend APIs («لا تغيّر Backend APIs»)، فإضافة المسار الناقص هناك خارج النطاق المسموح دون إذن المستخدم. البديل الوحيد داخل الفرونتند وحده هو إعادة توجيه نقاط الاستدعاء الثلاث إلى `/transactions` الموجود فعلياً — لكن هذا يتطلب تحقّقاً دقيقاً من تطابق شكل البيانات (procedure tracking له حقول مختلفة عن transaction الكاملة) قبل أي تنفيذ، وهو تحقيق أعمق من نطاق دفعة واحدة وقد يُدخل خللاً وظيفياً حقيقياً إذا نُفِّذ باستعجال. لذلك تُوثَّق هذه الدفعة كتدقيق فقط، بانتظار قرار المستخدم: إمّا إضافة المسار الناقص في الـ Backend (يتطلب إذناً صريحاً)، أو تفويض دفعة مستقبلية مخصّصة لدراسة إعادة التوجيه إلى `/transactions` بعناية.

### تحديث (batch #421) — نفس فئة الخطأ من batch #420 في صفحة إدارة المحتوى: مسارات Backend غير موجودة + فشل صامت مُصلَح على مستوى الفرونتند

بعد اكتشاف batch #420 لميزة "ملفاتي" بأكملها، وسّعت التدقيق ليشمل بقية نقاط استدعاء الـ API عبر مطابقة كل مسارات `fetch` في الفرونتند بقائمة مسارات `backend/main.py` الفعلية (`grep -n "@app\.\(get\|post\|put\|delete\|patch\)"`).

**اكتشاف جديد**: صفحة `app/admin/content/page.tsx` ("حوكمة المحتوى"، مرتبطة فعلياً من لوحة `/admin` الرئيسية) تستدعي 4 مسارات غير موجودة إطلاقاً في الـ backend: `GET /admin/content`، `POST /admin/content`، `POST /admin/content/{id}/transition`، `GET /admin/audit-log`. تحقّقتُ أن كل مسارات `/admin/*` الفعلية الموجودة هي: `users`، `stats`، `resets`، `feedback`، `content-gaps*` (ميزة مختلفة تماماً — تسجيل فجوات المحتوى، وليست إدارة عناصر محتوى)، `procedures`، `sources`، `failed-questions`، `escalations` — لا شيء باسم `content` المستقل أو `audit-log`.

**العلّة المُركَّبة (وهي الجزء الذي أُصلِح فعلاً في الفرونتند)**: دالة `loadItems()` (السطر 76-88) كانت تستدعي `res.json()` مباشرة دون التحقّق من `res.ok` أولاً. بما أن FastAPI يُعيد 404 بجسم JSON صالح (`{"detail":"Not Found"}`) للمسارات غير المسجَّلة، كان الطلب "ينجح" ظاهرياً، فيصبح `data.items` غير معرَّف، فتُضبَط `items` على مصفوفة فارغة `[]` بصمت تام — يرى المسؤول (admin) صفحة فارغة تبدو طبيعية دون أي مؤشر خطأ، وهذا أسوأ من فشل ظاهر لأنه يُخفي أن الميزة كلها معطَّلة.

**الإصلاح المُنفَّذ (فرونتند فقط)**: أُضيف تحقّق `!res.ok` في `loadItems()` يرمي خطأً برسالة `detail` القادمة من الـ backend (أو نص عربي/إنجليزي احتياطي)، فيدخل في كتلة `catch` الموجودة أصلاً التي تضبط `loadError` — وهذه الحالة معروضة فعلياً في الواجهة (السطر ~202-205). النتيجة: الفشل يصبح ظاهراً بدل مخفي، بغضّ النظر عن متى (أو إن) أُضيفت المسارات الناقصة في الـ backend مستقبلاً.

**متبقٍّ خارج نطاق الفرونتند**: إضافة المسارات الثلاثة الناقصة فعلياً في `backend/main.py` — خارج الصلاحية المسموحة دون إذن صريح («لا تغيّر Backend APIs»)، تماماً كما في batch #420. كذلك، `createItem`/`transition` (السطر ~98-121) ما زالتا تفشلان بصمت دون رسالة خطأ للمستخدم — إصلاح فرونتند أصغر مؤجَّل لدفعة لاحقة (نفس نمط `res.ok` يمكن تطبيقه هناك أيضاً).

`tsc --noEmit` نظيف.

### تحديث (batch #422) — إصلاح حالة تسابق (race condition) حقيقية: أسئلة متابعة (follow-up) من سؤال سابق تحلّ محل أسئلة السؤال الحالي

**المشكلة**: في `app/page.tsx`، ضمن `sendMessage()`، يُطلَق طلب `POST /suggest_followup` بلا انتظار (fire-and-forget) في نهاية كل دورة محادثة لاقتراح أسئلة متابعة، وعند اكتماله يُستدعى `setFollowupQuestions(...)` على حالة (state) واحدة مشتركة بين كل الأسئلة في الجلسة. لم يكن هناك `AbortController` ولا معرِّف طلب ولا أي تحقّق من أن الاستجابة الواردة ما زالت تخص آخر سؤال طُرح. `followupQuestions` تُصفَّر فقط بشكل متزامن في بداية `sendMessage()` التالية، وليس أثناء انتظار طلب سابق ما زال معلَّقاً.

**سيناريو واقعي محقَّق**: يسأل المستخدم سؤالاً 1 → تنتهي الإجابة وتُطلَق `suggest_followup` الخاصة به → `loading` يعود `false` قبل اكتمال هذا الطلب (يحدث في `finally` قبل انتظار fetch غير المنتظَر) → يسأل المستخدم فوراً سؤالاً 2 → يُصفَّر `followupQuestions`ويحصل السؤال 2 على استجابته الأسرع → ثم تصل استجابة السؤال 1 الأبطأ متأخرةً فتستبدل شرائح (chips) السؤال 2 بأسئلة متابعة لموضوع سابق غير ذي صلة بالمحادثة المعروضة حالياً — عيب مرئي بالكامل للمستخدم وقد يقوده لسؤال متابعة عن موضوع خاطئ.

**التحقّق**: تتبّع كامل لدورة الحياة (`loading` guard، `finally`، توقيت `setFollowupQuestions`) أكّد إمكانية الحدوث الفعلية. تحقّقتُ أيضاً من وجود نقطتَي استدعاء أخريين لنفس `/suggest_followup` في الملف (للأسطر ~661 و~2185) لكنهما تستخدمان حالة مختلفة تماماً (`setVisibleQ`، اقتراحات الإدخال الفورية) وتُطلَقان بضغطة مستخدم صريحة واحدة لا تلقائياً بعد كل رسالة — نطاق مخاطرة أقل بكثير، تُركتا بلا تعديل عمداً.

**الإصلاح المُنفَّذ (فرونتند فقط)**: أُضيف `sendSeqRef = useRef(0)`، يُزاد بمعرِّف تسلسلي (`mySeq`) عند بداية كل `sendMessage()`، ويُتحقَّق منه في معالج `.then()` الخاص بـ`suggest_followup` (`if (sendSeqRef.current === mySeq)`) قبل تطبيق النتيجة — أي طلب متأخر من سؤال سابق يُتجاهَل بصمت إذا كان هناك سؤال أحدث. `tsc --noEmit` نظيف.

### تحديث (batch #423) — نفس فئة حالة التسابق من batch #422 وُجدت في نقطة ثانية داخل نفس الدالة: تحليل مستند متأخر يُدرَج في مكان المحادثة الخطأ

**المشكلة**: في `app/page.tsx`، ضمن `sendMessage()` نفسها، هناك كتلة IIFE منفصلة تماماً عن `suggest_followup` (المُصلَحة في batch #422) تستدعي `fetch(API_URL + '/documents/universal-analysis', ...)` بلا انتظار متزامن عند إرفاق ملف، ثم تُدرِج بطاقة نتيجة التحليل في `messages` عبر البحث عن رسالة `assistant` بحالة `streaming` (`findIndex`). لم يكن لهذا الاستدعاء أي تحقّق من أن الإرسال الذي أطلقه ما زال هو الأحدث — نفس العلّة المُصلَحة في `suggest_followup` لكن في موقع مختلف من نفس الدالة، لم يُغطِّها إصلاح batch #422 لأنه طبَّق الحارس فقط على استدعاء `suggest_followup`.

**سيناريو الفشل المُتحقَّق منه**: يرفق المستخدم ملفاً ويرسله → تبدأ كتلة استخراج التحليل بالتوازي مع بث الإجابة الرئيسي → يكتمل البث الرئيسي بسرعة و`loading` يعود `false` بينما طلب الاستخراج ما زال معلَّقاً (سلسلة async منفصلة فعلياً) → يرسل المستخدم رسالة ثانية فوراً (بلا ملف) دون أي مانع (`loading` أصبح `false`) → تُضاف رسالة مستخدم جديدة ورسالة مساعد جديدة قيد البث → يكتمل طلب الاستخراج الأول متأخراً، فيبحث عن رسالة `streaming` ليُدرِج البطاقة بجانبها — فيضعها بجانب التبادل الثاني غير ذي الصلة، أو يُلحِقها بنهاية المحادثة بمعزل تام عن الملف الذي تخصّه إذا كان `streamIdx` قد أصبح `-1`.

**الإصلاح المُنفَّذ (فرونتند فقط)**: طُبِّق نفس حارس `sendSeqRef.current === mySeq` المستخدَم أصلاً في نقطة `suggest_followup` (batch #422) حول إدراج `setMessages` في معالج تحليل المستند — استجابة متأخرة من إرسال سبق تجاوزه إرسال أحدث تُهمَل بصمت بدل أن تُدرَج في المكان الخطأ.

`tsc --noEmit` نظيف.

### تحديث (batch #424) — إصلاح شريحة "إعادة المحاولة" (retry) في `app/page.tsx`: كانت عربية ثابتة بلا أي تفرّع على isAr

**ملاحظة منهجية مهمة أولاً**: أثناء البحث عن هذه الدفعة، اقترح Explore agent أول إصلاحاً على `components/RiskScoreCard.tsx` (تغيير `dir="rtl"` الثابت إلى `isAr ?`)، مستنداً إلى grep وجد فيه إشارات لـ`RiskScoreCard` داخل `TransactionFilePanel.tsx`/`DocumentAnalysisPanel.tsx` وفسّرها خطأً كدليل على أن المكوّن حيّ. لكن كلا الملفين ضمن سلسلة الكود الميت الخمسة المكتشفة والمؤكَّدة في batch #419 (مُدرَجة فعلاً في طابور الحذف `rm -f` داخل `push.sh`) — لا استيراد ولا استخدام JSX لأيٍّ منها خارج هذه السلسلة الميتة نفسها. طُبِّق التعديل ثم اكتُشف الخطأ عبر تحقّق مستقل (`grep -rn "TransactionFilePanel" app components lib`) فأُعيد التعديل إلى أصله فوراً قبل أي تنفيذ لـ`tsc`. **الدرس**: وجود اسم مكوّن داخل استيراد ملف آخر لا يعني أن ذلك الملف الآخر نفسه حيّ — يجب تتبّع السلسلة كاملة حتى نقطة دخول فعلية ضمن `app/`.

**المشكلة الفعلية المُصلَحة**: شريحة "إعادة المحاولة" في `app/page.tsx` (تظهر عند `retryMsg && !loading` بعد فشل اتصال، تستدعي `sendMessage(retryMsg!)`) كانت تحتوي `direction: 'rtl'` ثابتة على الحاوية ونص الزر `إعادة المحاولة` عربياً ثابتاً بلا أي تفرّع على `isAr` — بعكس عناصر مجاورة أخرى في نفس الصفحة (مثل زر إغلاق `voiceError` القريب الذي يتفرّع بشكل صحيح فعلاً).

**التحقّق**: `app/page.tsx` هو مكوّن المسار الجذري (`/`) نفسه، لا حاجة لتتبّع سلسلة استيراد — يُعرَض دائماً عند زيارة الصفحة الرئيسية. أي مستخدم بدّل اللغة إلى الإنجليزية (ميزة حقيقية عاملة عبر `LanguageContext`) ثم واجه فشل اتصال يرى زر "إعادة المحاولة" بالعربية فقط وسط صفحة إنجليزية بالكامل، مع اتجاه الحاوية مفروضاً RTL بمعزل عن بقية الصفحة.

**الإصلاح**: `direction: 'rtl'` → `direction: isAr ? 'rtl' : 'ltr'`، والنص الثابت → `{isAr ? 'إعادة المحاولة' : 'Retry'}`.

`tsc --noEmit` نظيف. لم تُلمَس أي من ملفات سلسلة الكود الميت الخمسة (`RiskScoreCard`/`TransactionFilePanel`/`DocumentAnalysisPanel`/`HumanReviewCTA`/`MissingDocumentsChecklist`).

### تحديث (batch #425) — إصلاح تسرّب مؤقّت (debounce) بلا تنظيف عند إلغاء تركيب `ProcedureNotesPanel`: كتابة متأخرة قد تُدمِّر ملاحظة أحدث

**المشكلة**: `components/ProcedureNotesPanel.tsx` يستخدم `debounceRef.current = setTimeout(() => persist(val), 900)` عند كل ضغطة مفتاح في حقل الملاحظات، ويُلغى المؤقّت فقط عند `handleChange` التالي أو `handleBlur` — لم يكن هناك أي `useEffect` تنظيف (`cleanup`) يُلغي المؤقّت المعلَّق عند إلغاء تركيب المكوّن (unmount).

**التحقّق من الحيوية**: `ProcedureNotesPanel` مُستورَد ومُركَّب فعلياً في `app/procedures/page.tsx:30/880` (مسار `/procedures` نفسه) — ضمن `ProcedureSectionGroup` ("تتبّع التقدّم والتذكيرات")، الذي يُلغي تركيب أبنائه فعلياً (وليس إخفاءً بصرياً فقط) عند طيّه: `{open && (<div>{children}</div>)}` في `components/ProcedureSectionGroup.tsx:54`. لا علاقة لهذا الملف بسلسلة الكود الميت الخمسة من batch #419 (`TransactionFilePanel`/`DocumentAnalysisPanel`/`HumanReviewCTA`/`RiskScoreCard`/`MissingDocumentsChecklist`) — تحقّق مستقل مؤكَّد.

**سيناريو الفشل**: يوسّع المستخدم بطاقة إجراء ويفتح قسم "تتبّع التقدّم" ويكتب في حقل الملاحظات، ثم يطوي القسم (أو يُغلق البطاقة) قبل مرور 900ms — فيُلغى تركيب `ProcedureNotesPanel` فعلياً بينما المؤقّت المعلَّق ما زال حياً (لم يُلغَ عند unmount). بعد ~900ms يُطلَق المؤقّت المتيتّم فيستدعي `persist(val)` → `saveNote(code, val)` ويكتب إلى `localStorage` بلا قيد — إذا أعاد المستخدم فتح القسم بسرعة وحفظت نسخة جديدة نصاً مختلفاً بالفعل، فإن الكتابة المتأخرة اليتيمة قد تُدمِّر النص الأحدث المحفوظ.

**الإصلاح**: أُضيف `useEffect` تنظيف مخصَّص (مرتبط بـ`code`) يُلغي `debounceRef.current` عند إلغاء التركيب أو قبل إعادة التشغيل لكود إجراء جديد، بعد `useEffect` تحميل البيانات الموجود أصلاً.

`tsc --noEmit` نظيف.

### تحديث (batch #426) — نفس فئة خطأ batch #425 (تسرّب مؤقّت بلا تنظيف) وُجدت في مكوّن ثانٍ مستقل: زر الإدخال الصوتي

**المشكلة**: `components/ChatVoiceInputBtn.tsx` — دالة `showToast()` كانت تُنشئ `setTimeout(() => setToast(''), 3000)` بلا أي مرجع (ref) محفوظ وبلا تنظيف مسجَّل. تأثير التنظيف الوحيد الموجود في المكوّن (`useEffect(() => () => stopListening(), [])`) كان يُلغي فقط `pulseTimer` الخاص بالنبض البصري، وليس مؤقّت التوست هذا.

**التحقّق من الحيوية**: `ChatVoiceInputBtn` مُستورَد ومُركَّب فعلياً في `app/page.tsx:38/1453`، ضمن نموذج البحث في الشاشة الترحيبية التي تُعرَض فقط عند `messages.length === 0` (السطر 1363) — وتُلغى بالكامل (unmount حقيقي) بمجرد أن يرسل المستخدم أول رسالة. لا علاقة لهذا الملف بسلسلة الكود الميت الخمسة من batch #419 — تحقّقتُ عبر grep أن لا إشارة متبادلة بينهما إطلاقاً.

**سيناريو الفشل الحقيقي**: يضغط المستخدم على زر الميكروفون على متصفح لا يدعم `SpeechRecognition`/`webkitSpeechRecognition` (شائع خارج متصفحات Chromium) → تُستدعى `showToast(...)` وتُجدوَل مؤقّت 3 ثوانٍ لإخفاء الرسالة → يرسل المستخدم استعلامه فوراً (خلال أقل من 3 ثوانٍ) → ينتقل `messages.length` من صفر إلى واحد فيُلغى تركيب نموذج البحث بأكمله بما فيه `ChatVoiceInputBtn` → يُطلَق المؤقّت اليتيم لاحقاً ويستدعي `setToast('')` على نسخة مكوّن لم تعد موجودة — تسرّب حقيقي وقابل للتكرار، وليس افتراضياً.

**الإصلاح**: أُضيف `toastTimer` (مرجع `useRef`)، وأصبحت `showToast` تُلغي أي مؤقّت سابق معلَّق قبل جدولة مؤقّت جديد وتُصفِّر مرجعها الخاص عند الإطلاق الطبيعي، وأُضيف `clearTimeout(toastTimer.current)` إلى تأثير التنظيف عند إلغاء التركيب بجانب `stopListening()` الموجودة أصلاً.

`tsc --noEmit` نظيف.

### تحديث (batch #427) — تدقيق فقط، لا تعديل كود: فئة "تسرّب المؤقّتات" أصبحت مستنفَدة، وثلاث فئات إضافية فُحصت ولم تُثمر

**الخطوة 1 — إتمام مسح `setTimeout`/`setInterval` (مستنفَد)**: بعد إيجاد حالتين حقيقيتين في batch #425 و#426، فُحص بقية ~70 استدعاءً عبر ~40 ملفاً (`components/*.tsx` و`app/**/*.tsx`، باستثناء سلسلة الكود الميت). كل استدعاءات `setInterval` (13 موقعاً، من `MinistryOpenHoursWidget.tsx` إلى `app/page.tsx`) لها `clearInterval` مطابقة ضمن تنظيف `useEffect`. بقية استدعاءات `setTimeout` هي إخفاءات توست/تأكيد لمرّة واحدة (`setCopied(false)`، `setShared(false)`) تُطلَق من معالجات نقر مباشرة، لا تُعيد الجدولة بلا إلغاء سابق، ولا تكتب بيانات قديمة فوق أحدث — أسوأ سيناريو عند إلغاء التركيب أثناء الانتظار هو `setState` عديم الأثر، وليس عطلاً قابلاً للتكرار كما كان في batch #425.

**الخطوة 2 — ثلاث فئات إضافية فُحصت، كلها آمنة فعلياً**:
- التوكيدات غير الفارغة (`!`): فُحصت جميع الحالات غير الواضحة (`DocExpiryBanner.tsx`، `GuidedFlow.tsx`، `AgentResponseRenderer.tsx`، `app/page.tsx` عدة مواضع، `RecentActivityFeed.tsx`، `app/forms/[slug]/page.tsx`) — كل واحدة محمية فعلياً بشرط سابق مطابق منطقياً (نفس الشرط، أو machine حالة يضمن التزامن، أو `notFound()` الذي يرمي استثناءً، أو نوع union ثابت وقت الترجمة يضمن وجود العنصر).
- تحويلات `as any`: كلها على كشف ميزات `window.*`، أو أسماء وسوم JSX مقيَّدة بأنواع الخصائص، أو تحليل استجابة خطأ احتياطي برسالة افتراضية — لا شيء منها يُخفي عدم تطابق نوع حقيقي.
- `dir="rtl"` الثابت المتبقي: نسخة واحدة فقط في `app/layout.tsx:128` على `<html>` الجذر — وهذا الافتراض المتعمَّد للتطبيق (عربي أولاً)، وليس خطأ تفرّع لغوي داخل مكوّن.

**الحالة**: لم يُعدَّل أي كود في هذه الدفعة — تدقيق صادق فقط، يتسق مع سوابق batches #390 و#411.

### تحديث (batch #428) — جرس تذكير الإجراء يفشل في مطابقة التذكيرات المحفوظة بالإنجليزية

**المشكلة**: `components/ProcedureReminderBell.tsx` — دالة `refresh()` (السطر 63-66) كانت تُصفّي التذكيرات المحفوظة عبر `r.title.includes(titleAr)` (البحث عن العنوان العربي فقط)، بينما منطق إنشاء التذكير في نفس الملف (`quickAdd`، الأسطر 88-92) وفي المكوّن الشقيق `ProcedureRemindMeLater.tsx` يبنيان النص المحفوظ فعلياً حسب اللغة الحالية: `${isAr ? titleAr : (titleEn || titleAr)}`.

**التحقّق من الحيوية**: `ProcedureReminderBell` مُستورَد ومُركَّب فعلياً في `app/procedures/page.tsx:71/647` — `<ProcedureReminderBell code={proc.code} titleAr={proc.title} titleEn={proc.title_en} isAr={isAr} />`. لا علاقة له بسلسلة الكود الميت الخمسة من batch #419. `isAr` قادم من `LanguageContext` القابل للتبديل فعلياً من المستخدم. المكوّن الشقيق `ProcedureRemindMeLater.tsx` (حيّ أيضاً، `app/procedures/page.tsx:881`) يكتب إلى نفس مفتاح `localStorage` (`dalilak_reminders`) بنفس منطق العنوان حسب اللغة — مؤكِّداً أن التذكيرات تُحفَظ فعلاً بالإنجليزية عند تفعيل الوضع الإنجليزي.

**سيناريو الفشل**: يبدّل المستخدم إلى الإنجليزية → يفتح إجراءً ويضغط جرس التذكير ويختار "غداً" → `quickAdd()` يبني `label = "Reminder: <English title> — <date>"` ويحفظه → يستدعي `refresh()` فوراً الذي يُصفّي حسب `titleAr` فقط — لكن النص المحفوظ للتوّ يحتوي فقط النسخة الإنجليزية، فلا يطابق أبداً. النتيجة: `remCount` يبقى صفراً، ولا يُفعَّل الجرس النابض/شارة العدد رغم نجاح الحفظ فعلياً — ونفس الأثر يتكرّر في أي تحميل لاحق للصفحة لأي تذكير أُنشئ أثناء الوضع الإنجليزي، فيفقد المستخدم أي تأكيد بصري لوجود تذكير فعلي (يُبطِل الغرض الكامل من الشارة، ويزيد احتمال إنشاء تذكيرات مكرَّرة).

**الإصلاح**: `refresh()` أصبحت تُطابِق النص العربي أو الإنجليزي معاً: `r.title.includes(titleAr) || (titleEn && r.title.includes(titleEn))` — بغضّ النظر عن اللغة التي كانت مفعَّلة وقت إنشاء التذكير.

`tsc --noEmit` نظيف.

### تحديث (batch #429) — نفس فئة خطأ batch #428 (عدم تطابق الكتابة/القراءة اللغوي) في شرائح تصفية الوزارة بصفحة `/procedures`

**المشكلة**: `components/ProcedureTagSearch.tsx` (المُستخدَم في `app/procedures/page.tsx:561-565`) كان يُخزِّن اختيار الوزارة كنص العرض المُترجَم نفسه (`isAr ? proc.ministry : proc.ministry_en`) بدل مُعرِّف مستقر (`ministrySlug`). تبديل اللغة في هذا التطبيق لا يُعيد تحميل الصفحة (`LanguageContext` يستخدم `setState` صرفاً)، فيُعاد حساب تسميات الشرائح باللغة الجديدة فوراً بينما النص المحفوظ للاختيار يبقى باللغة القديمة.

**التحقّق من الحيوية**: `ProcedureTagSearch` مُستورَد في `app/procedures/page.tsx:45` ومُركَّب في السطر 561، يقود حالة `enrichedMinistrySlug` (كانت `enrichedMinistryFilter` سابقاً) التي تُصفّي فعلياً `filteredEnriched` (الأسطر 204-206). `/procedures` مسار حقيقي — لا علاقة له بسلسلة الكود الميت الخمسة من batch #419.

**سيناريو الفشل قبل الإصلاح**: يضغط المستخدم شريحة وزارة بالعربية → يُخزَّن النص العربي كاملاً كاختيار → يبدّل اللغة إلى الإنجليزية بلا إعادة تحميل → تُعاد حساب تسميات الشرائح بالإنجليزية، لكن الاختيار المحفوظ ما زال عربياً → `selectedMinistry === label` تقارن نصاً عربياً بتسميات إنجليزية فتُخفق دائماً، ولا شريحة "الكل" تُضاء لأن النص المحفوظ غير فارغ أصلاً → لا شريحة تظهر مُفعَّلة بصرياً، بينما التصفية الفعلية للنتائج (التي كانت تقارن أيضاً بالنص المترجَم) تبقى مُطبَّقة بصمت على القائمة دون أي مؤشر واجهة يُبيّن وجود مرشِّح فعّال.

**الإصلاح**: أعاد المكوّن ربط الاختيار بحقل `ministrySlug` الثابت الموجود أصلاً في كل `EnrichedProcedure` بدل نص العرض المترجَم — تجميع/إزالة التكرار حسب `slug`، مقارنة `selectedMinistry === slug`، واستدعاء `onSelect(slug)`. أُعيدت تسمية حالة الصفحة إلى `enrichedMinistrySlug` وبُسِّطت التصفية إلى `p.ministrySlug === enrichedMinistrySlug`. الاختيار الآن يبقى صحيحاً عبر تبديل اللغة بحكم البنية، وليس بإصلاح ظرفي.

`tsc --noEmit` نظيف.

### تحديث (batch #430) — شرائح تصفية الوزارة في `/forms` كانت عربية ثابتة بلا أي تفرّع على isAr (رغم أن حالة التصفية نفسها كانت صحيحة)

**المشكلة**: `app/forms/page.tsx` (الأسطر 156-186)، صف شرائح تصفية الوزارة — كل شريحة كانت تعرض `m.ar` (الاسم العربي) دون أي شرط `isAr`، بعكس شريحة "الكل" المجاورة مباشرة (`{isAr ? 'الكل' : 'All'}`) وبقية عناصر الصفحة التي تتفرّع بشكل صحيح. **ملاحظة مهمة**: خلافاً لِـ batch #428/#429، حالة التصفية الفعلية (`ministryFilter`) كانت سليمة أصلاً — تُقارَن بـ`m.slug` المستقر لا بالنص المترجَم، فلم تكن هذه المرة مشكلة "كتابة/مطابقة" بل مجرّد نص عرض ثابت لم يُفعَّل تفرّعه قط.

**التحقّق من الحيوية**: `app/forms/page.tsx` مسار حقيقي (`/forms`)، ومرتبط أيضاً من `components/TopNav.tsx:31` و`components/MobileMenu.tsx:78` — أي متاح فعلياً عبر التنقّل العادي بكلتا اللغتين. لا علاقة له بسلسلة الكود الميت الخمسة.

**الأثر**: عند تفعيل الوضع الإنجليزي، يُعرَض الهيدر والتبويبات وشريحة "الكل" وعدّاد النتائج ومحتوى البطاقات كلها بالإنجليزية بشكل صحيح — لكن كل شريحة وزارة (الاقتصاد، العمل، الشؤون الاجتماعية، الجمارك، الداخلية، المالية...) تبقى بالعربية داخل واجهة إنجليزية بالكامل (`dir="ltr"`) — قابل للتكرار في كل تحميل صفحة بالوضع الإنجليزي، وليس فقط بعد تبديل لغة أثناء الجلسة.

**الإصلاح**: استُبدِل `{m.ar.length > 22 ? m.ar.slice(0, 22) + '...' : m.ar}` الثابت بمنطق يختار `isAr ? m.ar : m.en` قبل التقصير — نفس النمط المستخدَم في بقية الصفحة. الحقل `MinistryItem.en` (معرَّف أصلاً في `lib/allTransactions.ts:10`) كان متوفراً ومملوءاً لكل الوزارات لكنه لم يُقرَأ قط في نقطة العرض هذه.

`tsc --noEmit` نظيف.

### تحديث (batch #431) — زر "اسأل دليلك" في `/authorities` كان يمرِّر اسم الجهة بالعربية دائماً حتى في الوضع الإنجليزي

**المشكلة**: `app/authorities/page.tsx`، زر "اسأل دليلك عن..." أسفل كل بطاقة جهة (السطر ~380) كان يستدعي `askAI(auth.name_ar)` بشكل ثابت — رغم أن `askAI()` نفسها (السطر 169-170) تبني نص الاستعلام بالكامل حسب `isAr` (سؤال عربي أو إنجليزي)، وأن الزر وبطاقته يعرضان فعلياً `isAr ? auth.name_ar : (auth.name_en || auth.name_ar)`. النتيجة في الوضع الإنجليزي: يُبنى سؤال بقالب إنجليزي كامل لكن مع اسم الجهة داخله بالعربية دائماً — مثلاً `"What services does وزارة الداخلية والبلديات provide..."`، بينما البطاقة نفسها والزر يعرضان الاسم بالإنجليزية.

**التحقّق**: `app/authorities/page.tsx` مسار حقيقي (`/authorities`)، لا حاجة لتتبّع سلسلة استيراد. اكتُشفت هذه الدفعة أثناء تدقيق موسَّع لصفحتَي `/authorities` و`/services` بحثاً عن نفس فئة "فجوة التفرّع اللغوي" المكتشفة في batches #428-430 — وهي المرة الرابعة التي تُثمر فيها هذه الفئة.

**الإصلاح**: `askAI(auth.name_ar)` → `askAI(isAr ? auth.name_ar : (auth.name_en || auth.name_ar))` — الآن الاسم الممرَّر للاستعلام يطابق دائماً الاسم المعروض فعلياً على البطاقة والزر (`aria-label` كان يستخدم هذا المنطق الصحيح مسبقاً؛ فقط `onClick` كان الاستثناء).

`tsc --noEmit` نظيف.

### تحديث (batch #432) — نفس فئة batch #431 في `GlobalSearch.tsx`: نص "اسألني" المُرسَل للمحادثة كان عربياً ثابتاً بلا أي تفرّع

**المشكلة**: `components/GlobalSearch.tsx` — `buildIndex()` كانت تبني حقلاً واحداً `aiPrompt` بالعربية فقط لكل نتيجة بحث (إجراءات مُثرَاة: `` `أخبرني بكل التفاصيل عن: ${p.title}...` ``؛ عناصر المعالج: `` `أرشدني خطوة بخطوة في معاملة: ${p.title_ar}` ``؛ رحلات الحياة: `` `أرشدني في رحلة: ${j.titleAr}` ``) — رغم أن العنوان المعروض فعلياً لكل نتيجة يتفرّع بشكل صحيح حسب `isAr`. `selectResult()` كانت تستدعي `onAsk(r.aiPrompt)` بلا شرط.

**التحقّق من الحيوية**: `GlobalSearch` مُستورَد ومُركَّب في `components/TopNav.tsx` و`components/MobileMenu.tsx`، و`TopNav` نفسه مُركَّب في `app/page.tsx:1337` مع `onAsk={q => sendMessage(q)}` — أي أن النص يُرسَل حرفياً كرسالة مستخدم ظاهرة في المحادثة. لا علاقة لهذا بسلسلة الكود الميت الخمسة من batch #419.

**سيناريو الفشل**: يبدّل المستخدم إلى الإنجليزية → يفتح البحث (Cmd+K) → يكتب "passport" → يرى عنوان النتيجة صحيحاً بالإنجليزية ("Passport Renewal") → يضغطها → `onAsk(r.aiPrompt)` يُرسِل نصاً عربياً بالكامل كفقاعة رسالة مستخدم ظاهرة داخل جلسة محادثة إنجليزية بالكامل — عطل واضح لاتساق اللغة، ونفس نمط batch #431 تحديداً (عرض صحيح ثنائي اللغة، لكن الوسيط الممرَّر للأمام كان لغة واحدة ثابتة).

**الإصلاح**: استُبدِل الحقل الواحد `aiPrompt` بحقلَين `aiPromptAr`/`aiPromptEn`، مملوءَين كلاهما وقت بناء الفهرس (باستخدام حقول `title_en`/`titleEn` الموجودة أصلاً في مصادر البيانات — بلا حاجة لبيانات جديدة)، وأصبحت `selectResult` تختار `isAr ? r.aiPromptAr : (r.aiPromptEn || r.aiPromptAr)` مع إضافة `isAr` إلى مصفوفة اعتماديات `useCallback`. عناصر الأسئلة الشائعة (نوع `'faq'`) ليس لها حقل عنوان/نص إنجليزي إطلاقاً في `lib/serviceFAQ.ts`، فكلا المتغيّرين يعودان لنفس `chatPrompt` العربي هناك — قيد بيانات موثَّق داخل الكود، خارج نطاق هذه الدفعة (ملف بيانات، لم يُلمَس).

`tsc --noEmit` نظيف.

### تحديث (batch #433) — فئة جديدة: `ProcedureCompletionBadge.tsx` كانت تتجاهل شرطها الوثائقي الخاص بها بالكامل — زر "أنهيت هذه المعاملة" يظهر بلا حاجة لبدء المعاملة أصلاً

**المشكلة**: توثيق المكوّن نفسه (السطور 8-9) ينصّ صراحة: "Not started → shows nothing (hides if no `dalilak_started_{code}` key either)" — أي يجب أن يختفي الزر تماماً ما لم تكن المعاملة قد بدأت فعلياً (`dalilak_started_{code}` موجود في localStorage). لكن الكود الفعلي كان يعرّف الثابت `LS_STARTED = 'dalilak_started_'` ثم لا يستخدمه إطلاقاً في أي مكان آخر بالملف — الشرط `if (!mounted) return null` كان الشرط الوحيد، فالزر يظهر فوراً عند التركيب بلا أي تحقّق من حالة "البدء".

**التحقّق من الحيوية**: `grep -rn "ProcedureCompletionBadge" app components --include="*.tsx"` يُظهر استيراداً واستخداماً JSX في `app/procedures/page.tsx` (سطر 33 و871) — وهو مسار حقيقي تحت `app/`، بلا أي علاقة بسلسلة الكود الميت من batch #419.

**سيناريو الفشل**: مستخدم يفتح `/procedures` لأول مرة، لم يضغط "بدأت هذه المعاملة" إطلاقاً، ويرى مع ذلك زر "أنهيت هذه المعاملة 🎉" يعمل بشكل كامل بجانب زر "بدأت هذه المعاملة" (`app/procedures/page.tsx:869-872` يعرض الاثنين معاً بلا شرط). يمكنه تعليم معاملة "منتهية" دون أن تكون "مبدوءة" أصلاً — تناقض دلالي مع بقية أدوات تتبّع التقدّم في التطبيق (مثلاً `ProcedureCompletionCelebration.tsx` يتحقّق من `started > 0 && started === completed`، فهذه المعاملات "اليتيمة" لا يمكن أن تُحتسَب ضمن أي احتفال إنجاز لأن `started` يبقى صفراً).

**الإصلاح**: استيراد `getStartDate` من `ProcedureStartButton.tsx` (مُصدَّرة أصلاً هناك)، وإضافة حالة `hasStarted` تُحدَّث في نفس `useEffect` (مع الاستماع أيضاً لحدثي `dalilak_saved_change`/`storage` كي يتزامن فوراً مع ضغط زر "بدأت هذه المعاملة" المجاور)، وإضافة شرط `if (!hasStarted && !completedDate) return null` بعد شرط `mounted` — يبقى العرض ظاهراً إذا كانت المعاملة مُنجَزة فعلاً (تفادياً لإخفاء بيانات إنجاز حقيقية موجودة مسبقاً حتى لو لم يُسجَّل تاريخ بدء)، ويُخفى فقط زر الدعوة للإجراء "أنهيت" حين لا توجد معاملة بدأت أصلاً.

`tsc --noEmit` نظيف.

### تحديث (batch #434) — نفس فئة batch #433 لكن على مستوى hook مشترك بين أكثر من مكوّن: `useResponseLength()` كان يتوقف عن العمل بعد أول ضغطة داخل الجلسة نفسها

**المشكلة**: `components/ChatResponseLength.tsx` — `useResponseLength()` (السطور 25-48) يُستدعى بشكل **مستقل** في ثلاثة أماكن: داخل مكوّن `ChatResponseLength` نفسه (شريحة "موجز/عادي/مفصّل" الظاهرة للمستخدم)، مباشرةً في `app/page.tsx:592` (النسخة التي تبني فعلياً بادئة `[Detailed]`/`[Brief]` قبل إرسال الرسالة في `sendMessage`، سطر 993)، وفي `app/settings/page.tsx:87`. كل استدعاء ينشئ حالة `useState` مستقلة خاصة به، و`useEffect` كان يقرأ `localStorage` مرة واحدة فقط عند التركيب (`[]` dependency) بلا أي مستمع لاحق — رغم أن `setMode()` نفسها تُطلِق حدثاً مخصّصاً `dalilak_response_length_change` **لا يستمع إليه أحد إطلاقاً**، حتى الـ hook نفسه.

**التحقّق من الحيوية**: `grep -rn "ChatResponseLength\|useResponseLength" app components --include="*.tsx"` يُظهر استيراداً واستخداماً فعلياً في `app/page.tsx` (استيراد سطر 49، استدعاء الـhook سطر 592، استخدام قيمته في `sendMessage` سطر 993، وعرض المكوّن نفسه سطر 2256) وفي `app/settings/page.tsx` (سطر 26/87) — مساران حقيقيان تحت `app/`، بلا أي علاقة بسلسلة الكود الميت من batch #419.

**سيناريو الفشل**: يفتح المستخدم المحادثة، يضغط "مفصّل"/"Detailed" في الشريحة، يراها تصبح مُفعَّلة بصرياً (لأن نسخة الـhook داخل `ChatResponseLength` نفسها تحدَّثت)، ثم يرسل رسالة — لكن `sendMessage` تقرأ من نسخة الـhook **المنفصلة** المُستدعاة مباشرة في `app/page.tsx:592`، والتي بقيت على قيمتها الأصلية عند تحميل الصفحة (غالباً فارغة) لأنها لم تستمع لحدث التغيير قط. النتيجة: التفضيل يبدو أنه نجح بصرياً، لكن بادئة الطول لا تُضاف لأي رسالة طوال الجلسة — يعمل فقط بعد إعادة تحميل الصفحة بالكامل (تركيب جديد يقرأ القيمة المحفوظة من localStorage)، ثم يتعطّل مجدداً بمجرد أي ضغطة تبديل تالية في نفس الجلسة.

**الإصلاح**: أضفتُ الاستماع لحدثي `dalilak_response_length_change` (المُطلَق أصلاً من `setMode` لكنه كان بلا مستمع) و`storage` داخل `useEffect` نفسه، بحيث تتزامن كل نسخ الـhook المُركَّبة في الصفحة نفسها فوراً مع أي تغيير — نفس نمط `getReminders`/`refresh` المستخدم بنجاح في مكوّنات أخرى مثل `ProcedureReminderBell.tsx`.

`tsc --noEmit` نظيف.

### تحديث (batch #435) — اكتشاف موثَّق فقط (بلا تعديل كود): ميزة كاملة في `ChatContextBar.tsx` مبنية لكن غير موصولة إطلاقاً بأي مصدر بيانات حقيقي

**المشكلة**: يوثّق `components/ChatContextBar.tsx` (السطور 6-14) ثلاثة أنواع من "شرائح السياق النشط" يُفترض أن تظهر فوق مربّع إدخال المحادثة: المعاملة قيد النقاش (`dalilak_active_proc`)، فلتر الوزارة النشط (`dalilak_active_ministry`)، ورحلة الحياة النشطة (`dalilak_active_journey`) — بالإضافة لشريحة "الوضع" (مفصّل/عادي) وهي الوحيدة الموصولة فعلياً. الملف نفسه يُصدِّر ثلاث دوال مساعدة مخصَّصة تحديداً لتفعيل هذه الشرائح: `setActiveProcedureContext()`، `setActiveMinistryContext()`، `setActiveJourneyContext()` — كل واحدة تكتب إلى `sessionStorage` وتُطلِق `dalilak_context_change`.

**التحقّق**: `grep -rn "ChatContextBar" app components --include="*.tsx"` يؤكد أن المكوّن حيّ فعلاً (`app/page.tsx:48` استيراد، `app/page.tsx:2260` استخدام JSX) — وليس جزءاً من الكتلة الميتة. لكن `grep -rn "setActiveProcedureContext\|setActiveMinistryContext\|setActiveJourneyContext\|dalilak_active_proc\|dalilak_active_ministry\|dalilak_active_journey" app components lib --include="*.tsx" --include="*.ts"` يُظهر **صفر** استدعاءات لهذه الدوال الثلاث، وصفر كتابة لمفاتيح `sessionStorage` الثلاثة، في أي مكان آخر بالمشروع بأكمله. النتيجة: `readChips()` لا يمكنها إطلاقاً إرجاع أي شريحة من هذه الأنواع الثلاثة — `chips.length` يبقى صفراً دائماً لكل مستخدم حقيقي، وتظهر فقط شريحة "الوضع".

**أثرها على المستخدم**: نصف الميزة المُصمَّمة (مؤشر بصري لما "يتذكره" دليلك حالياً من سياق: معاملة/وزارة/رحلة محدَّدة) غير موجود عملياً من منظور المستخدم رغم وجود كل بنية الكود له (القراءة، العرض، الحذف، الحدث المخصَّص) — كود حيّ (قابل للتنفيذ ومُركَّب في صفحة حقيقية) لكن غير قابل للوصول عملياً (unreachable data path)، فئة مختلفة قليلاً عن "الكود الميت" المعتاد.

**لماذا لم يُصلَح مباشرة في هذه الدفعة**: الإصلاح الحقيقي يتطلّب توصيل ثلاث دوال بثلاث نقاط تكامل مختلفة تماماً في أجزاء أخرى من التطبيق (مثلاً: عند فتح تفاصيل معاملة من المحادثة أو من `TransactionStarter`، عند تطبيق فلتر وزارة في `GlobalSearch`، عند فتح `JourneySheet`) — وكل نقطة تحتاج قراراً تصميمياً حقيقياً (متى بالضبط تُفعَّل الشريحة؟ متى تُمسَح تلقائياً — عند بدء محادثة جديدة؟ عند تغيير الموضوع؟). هذا أقرب لتنفيذ ميزة جديدة موزَّعة على عدة ملفات منها لإصلاح خطأ دقيق محدود النطاق، وخطأ التخمين هنا (توصيل خاطئ) أسوأ من ترك الميزة معطَّلة كما هي — يتّبع نفس منطق batch #420 (توثيق فقط عند تجاوز النطاق الآمن لدفعة واحدة).

**التوصية للمستخدم**: إما (أ) تخصيص دفعة/جلسة مستقبلية لتوصيل نقاط التكامل الثلاث فعلياً بعد قرار واضح بشأن سلوك المسح التلقائي، أو (ب) إذا لم تعد هذه الميزة مرغوبة، حذف الفروع الثلاثة غير المستخدمة من `readChips()` + الدوال المُصدَّرة الثلاث + تحديث التوثيق ليعكس أن `ChatContextBar` يعرض حالياً شريحة الوضع فقط.

`tsc --noEmit` نظيف (لم يتغيّر أي كود هذه الدفعة).

### تحديث (batch #436) — `ProcedureRelatedSuggestions.tsx`: خوارزمية "قد تحتاج أيضاً" كانت تخالف توثيقها الخاص بها في نقطتين، فتقترح معاملات غير ذات صلة من وزارات مختلفة

**المشكلة**: توثيق المكوّن (السطور 6-9) ينصّ على خوارزمية واضحة: (1) معاملات من **نفس الوزارة** أولاً، (2) إذا كانت أقل من 2 من نفس الوزارة، **تُستكمَل** بمطابقة الكلمات المشتركة في العنوان (بشرط تطابق كلمتين أو أكثر — التعليق المجاور صراحة "check 2+ word overlap")، (3) حد أقصى 3 اقتراحات. الكود الفعلي كان يخالف نقطتين من الثلاث: (أ) `overlap.length >= 1` بدل `>= 2` — كلمة مشتركة واحدة كافية للمطابقة رغم أن التعليق المجاور يذكر صراحة "2+"، (ب) `keywordMatch` كانت تُحسَب وتُدمَج دائماً بلا أي شرط يتحقق من عدد نتائج نفس الوزارة أولاً — أي لم تكن "استكمالاً عند الحاجة" كما هو موثَّق، بل مصدراً دائم الحضور حتى عندما توجد بالفعل نتائج كافية من نفس الوزارة.

**التحقّق من الحيوية**: `grep -rn "ProcedureRelatedSuggestions" app components --include="*.tsx"` يُظهر استيراداً واستخداماً فعلياً (بخصائص `proc`/`onSelect`/`onAsk` حقيقية) في `app/procedures/page.tsx:26,1092` — يظهر عملياً تحت أي معاملة موسَّعة في صفحة `/procedures` الحقيقية.

**أثرها على المستخدم**: نظراً لأن تصفية الكلمات تعتمد فقط على طول الكلمة (>3 أحرف) بلا أي قائمة كلمات وظيفية مستبعدة (stopwords)، كلمة عامة شائعة مثل "الحصول" (تتكرر في عناوين معاملات متعددة عبر وزارات مختلفة تماماً في `lib/enrichedProcedures.ts`) كانت كافية وحدها لتفعيل اقتراح "قد تحتاج أيضاً" بين معاملتين لا علاقة حقيقية بينهما — فيرى المستخدم اقتراحاً لمعاملة من وزارة العمل تحت معاملة من وزارة الاقتصاد لمجرد تشابه لفظي سطحي، حتى لو كانت هناك بالفعل معاملات كافية وذات صلة فعلية من نفس الوزارة.

**الإصلاح**: في `getSuggestions()`: (1) رفع الشرط إلى `overlap.length >= 2` ليطابق التعليق الموثَّق فعلياً، (2) تفعيل `keywordMatch` فقط عندما `sameMinistry.length < 2` (تعبير `sameMinistry.length >= 2 ? [] : others.filter(...)`)، بحيث تصبح المطابقة بالكلمات المفتاحية استكمالاً حقيقياً وليس مصدراً دائماً كما كانت.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل ملف واحد، بلا أي حاجة لتوصيل ملفات أخرى.

### تحديث (batch #437) — خطأ حقيقي في مولّد QR اليدوي: بيانات "معلومات التنسيق" كانت تُصفَّر بذاتها عبر XOR مع نسخة مطابقة لنفسها، ما يُفسِد كل رمز QR يُنشِئه التطبيق

**المشكلة**: `components/ProcedureQRShare.tsx` يحتوي مولّد QR مكتوباً يدوياً بالكامل (بلا مكتبة خارجية). عند بناء "معلومات التنسيق" (format info — الحقل الذي يخبر أي قارئ QR بمستوى تصحيح الخطأ ونمط القناع المستخدم)، كان الكود يُعرِّف `fmtBits` كسلسلة 15-بت **صحيحة ونهائية بالفعل** (كما يوضّح التعليق المجاور)، ثم يُعرِّف `fmtXor` بنفس القيمة **حرفياً** (نفس المصفوفة)، ثم يُطبِّق `fmtBits.map((b, i) => b ^ fmtXor[i])` — وبما أن أي بت مُطابَق بعملية XOR مع نفسه يساوي صفراً دائماً (`b ^ b === 0`)، فإن الناتج `fmt` كان دائماً 15 صفراً بلا استثناء، بغض النظر عن القيمة الصحيحة المحسوبة أصلاً في السطر السابق مباشرة.

**التحقّق من الحيوية**: `grep -rn "ProcedureQRShare" app components --include="*.tsx"` يُظهر استيراداً واستخداماً فعلياً بخصائص حقيقية (`code`, `titleAr`, `titleEn`) في `app/procedures/page.tsx:38,1043` — مساراً حقيقياً يستخدمه أي مستخدم يفتح خيار "مشاركة عبر QR" لأي معاملة.

**أثرها على المستخدم**: حقلا معلومات التنسيق (يظهران في موضعين ثابتين حول نمط الزاوية العلوية اليسرى) هما ما يمكّن أي قارئ QR حقيقي (كاميرا الهاتف) من فهم كيفية فك تشفير باقي الرمز. بتصفيرهما بالكامل، يُنتِج المكوّن مصفوفة "تبدو" كرمز QR بصرياً (أنماط الزوايا صحيحة) لكنها **غير قابلة للمسح فعلياً على الإطلاق أو تُفكّ بشكل خاطئ تماماً** — أي مستخدم يفتح مشاركة QR لأي معاملة، يمسحها بكاميرا هاتفه متوقعاً الانتقال لرابط المعاملة، ويحصل بدلاً من ذلك على فشل في القراءة أو بيانات عشوائية. هذا عطل كامل وصامت (100% من الحالات، وليس حافة نادرة) في ميزة كاملة ظاهرة للمستخدم.

**الإصلاح**: حُذف السطر الثاني (`fmtXor`) والعملية الذاتية `.map((b,i) => b ^ fmtXor[i])` بالكامل — `fmt` أصبحت تساوي `fmtBits` مباشرة (القيمة الصحيحة أصلاً بحسب التعليق التوضيحي المجاور نفسه، بلا حاجة لأي XOR إضافي).

**ملاحظة أمانة**: هذا إصلاح لخطأ واحد واضح وقابل للتحقّق (تصفير-ذاتي عبر XOR) داخل مولّد QR يدوي أكبر بكثير (يتضمن أيضاً ترميز Reed-Solomon وتوزيع بيانات مكتوبين يدوياً) — لم يُجرَ تدقيق شامل لبقية منطق هذا الملف في هذه الدفعة، فقط هذا العيب المحدَّد المؤكَّد.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل ملف واحد.

### تحديث (batch #438) — متابعة `ProcedureQRShare.tsx`: خطأ ثانٍ حقيقي في مولّد QR اليدوي — نمط "الباحث" (finder pattern) كان مقلوباً بالكامل

**السياق**: بعد إصلاح batch #437 (تصفير معلومات التنسيق عبر XOR ذاتي)، طُلِب فحص إضافي لبقية منطق `components/ProcedureQRShare.tsx` اليدوي (Reed-Solomon، وضع البتات، أنماط الزوايا). وُجد خطأ حقيقي ثانٍ منفصل تماماً في `placeFinderPattern()` (السطور 126-136 قبل الإصلاح).

**المشكلة**: نمط "الباحث" (finder pattern) في أي رمز QR هو مربّع 7×7 محاط بحلقة فاصلة بيضاء (separator) بعرض خلية واحدة. الدالة تكرّر إحداثيات نسبية من `-1` إلى `7` (كتلة 9×9 لتغطية المربّع + الفاصل). المتغيّر `inBorder` كان يُعرَّف كـ `r === -1 || r === 7 || c === -1 || c === 7` — وهذه بالضبط إحداثيات **الحلقة الفاصلة البيضاء الخارجية**، وليست حدود المربّع الأسود نفسه (والتي تقع فعلياً عند `r/c === 0` و`r/c === 6`). النتيجة: الحلقة السوداء الخارجية للمربّع 7×7 لم تُرسَم إطلاقاً (تصبح بيضاء خطأً)، بينما الحلقة الفاصلة التي يجب أن تبقى بيضاء تُرسَم سوداء خطأً — أي أن الشكل بأكمله مقلوب موضعياً بخلية واحدة.

**التحقّق اليدوي**: تتبّعت القيم يدوياً لكل فئة خلية بعد الإصلاح: `r=0` (حد المربّع) → أسود ✓، `r=1,c=1` (الحلقة البيضاء الداخلية) → أبيض ✓، `r=2,c=2` (النواة السوداء) → أسود ✓، `r=-1` أو `r=7` (الفاصل الخارجي) → أبيض ✓ — يطابق الآن الشكل القياسي 1:1:3:1:1 المطلوب لأي نمط باحث QR صحيح.

**التحقّق من الحيوية**: نفس المكوّن المؤكَّد حيّاً في batch #437 — `app/procedures/page.tsx:38,1043`.

**أثرها على المستخدم**: عطل إضافي، منفصل عن عطل batch #437، يزيد من استحالة مسح أي رمز QR يولّده هذا المكوّن — حتى لو أُصلِح عطل معلومات التنسيق فقط، فإن نمط الباحث المقلوب وحده كافٍ لمنع أي قارئ QR حقيقي من اكتشاف الرمز أصلاً (اكتشاف نمط الباحث هو أول خطوة يقوم بها أي ماسح).

**الإصلاح**: أُعيد تعريف `inBorder` لتقتصر فعلياً على حدود المربّع 7×7 (`within = r∈[0,6] && c∈[0,6]`، `inBorder = within && (r===0||r===6||c===0||c===6)`) بدل إحداثيات الفاصل الخارجي — بقية الشروط (`inInner`, `inCore`, سطر التعيين النهائي) بلا تغيير.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل ملف واحد (نفس الملف من batch #437).

### تحديث (batch #439) — خطأ حقيقي ثالث في `ProcedureQRShare.tsx`: حقل "عدد البايتات" في رأس الترميز لا يطابق البيانات المُقتَطَعة فعلياً

**السياق**: بعد إصلاح batch #437 (تصفير معلومات التنسيق) وbatch #438 (نمط الباحث المقلوب)، أُجري تدقيق ثالث لبقية منطق `components/ProcedureQRShare.tsx`. وُجد خطأ ثالث مستقل تماماً في `encodeBytes()` (السطور 100-124 قبل الإصلاح).

**المشكلة**: `V3_DATA_CW = 44` هو الحد الأقصى الصارم لعدد كلمات الترميز (codewords) التي يتّسع لها رمز QR إصدار 3. كانت الدالة تكتب طول النص الكامل غير المُقتَطَع (`bytes.length`) في حقل "عدد البايتات" (8 بتات) داخل تيار البتات — **قبل** أي اقتطاع. لكن السطر الأخير (`return cw.slice(0, V3_DATA_CW)`) كان يقتطع مصفوفة كلمات الترميز إلى الحد الأقصى **بعد** بنائها بالكامل، بلا أي تصحيح لاحق لحقل العدد المُعلَن أصلاً في بداية التيار — أي أن الرمز يُصرِّح بطول أكبر من البيانات الفعلية المتبقية بعد الاقتطاع.

**التتبّع الفعلي**: عنوان URL المُشفَّر دائماً بالصيغة `https://dalilak.app/procedures?q=${encodeURIComponent(العنوان)}` — والبادئة الثابتة وحدها 33 بايت. بالنسبة لعنوان معاملة عربي حقيقي من `Dalilak_RAG_Dataset.json` مثل "استخراج شهادة الولادة (تسجيل المولود)"، ينتج رابط طوله 233 بايت UTF-8 بعد الترميز (النص العربي يتمدّد نحو 6 أضعاف عبر `encodeURIComponent`) — بينما السعة الفعلية لا تتجاوز نحو 42 بايت بيانات فعلية. الرأس يُصرِّح بـ233 بايت، لكن لا يوجد فعلياً في الرمز سوى ~42 بايت حقيقياً قبل بايتات تصحيح الخطأ (Reed-Solomon) — أي قارئ QR حقيقي سيحاول قراءة 233 بايت نص من منطقة لا تحوي إلا جزءاً صغيراً منها، فيفشل أو يُنتِج نصاً تالفاً بالكامل. هذا يحدث لكل عنوان معاملة عربي تقريباً (اللغة الافتراضية الأساسية للتطبيق) — وحتى العناوين الإنجليزية الأطول من ~9 أحرف تتأثر بنفس المشكلة (33 بايت بادئة + مساحة متبقية ضئيلة جداً).

**التحقّق من الحيوية**: نفس المكوّن المؤكَّد حيّاً في batch #437/#438 — `app/procedures/page.tsx:38,1043`، مُستدعى فعلياً بقيم `proc.title`/`proc.title_en` الحقيقية.

**أثرها على المستخدم**: عطل ثالث صامت تماماً (لا استثناء يُرمى، `try/catch` في `buildQR()` لا يلتقط شيئاً) يضاف فوق عطلَي batch #437/#438 — حتى لو أُصلِح الاثنان الآخران، هذا العطل وحده كافٍ لإنتاج رمز QR غير قابل للقراءة أو يُنتِج بيانات تالفة لكل معاملة تقريباً.

**الإصلاح**: اقتطاع `bytes` إلى `maxPayloadBytes = V3_DATA_CW - 2` (حجز مساحة تقريبية لرأس النمط+العدد) **قبل** بناء `bitStream` وكتابة حقل العدد — بحيث يطابق العدد المُعلَن دائماً البيانات الفعلية المُرمَّزة، بدل الاقتطاع اللاحق بلا تصحيح.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل ملف واحد (نفس ملف batch #437/#438). **ملاحظة**: لم يُغيَّر بناء الرابط (`url`) نفسه أو مساره — فقط منطق الترميز الداخلي لرمز QR.

### تحديث (batch #440) — ملاحظة منهجية + إصلاح حقيقي: `parseProcessingDays()` كانت تُخطئ في قراءة صيغة المثنى العربي "شهرين/شهران" فتحسبها 30 يوماً بدل 60

**ملاحظة منهجية مهمة أولاً**: في هذه الدفعة، اقترح فحص أوّلي "إصلاح" `addWorkdays()` في نفس الملف (`components/ProcedureEstimatedCompletion.tsx`) لتغيير افتراض عطلة نهاية الأسبوع من "الجمعة والسبت" إلى "الأحد فقط"، استناداً لمقال إخباري خارجي عن القطاع العام اللبناني. عند التحقّق المتقاطع، تبيّن أن سلوك الكود الحالي (تخطّي الجمعة والسبت) **يتوافق فعلياً** مع تعليقه المجاور وأيضاً مع ملفين حيّين آخرين في المشروع نفسه (`GovHolidayAlert.tsx` و`MinistryOpenHoursWidget.tsx`) يعتمدان نفس الافتراض (جمعة+سبت = عطلة). فقط ملف واحد (`QuickContacts.tsx`) يخالف ذلك (يعتبر الأحد+السبت عطلة، مع تعليق داخلي "الإثنين-الجمعة" لا يطابق كوده هو نفسه). هذا تناقض حقيقي بين عدة ملفات حول قاعدة عمل واقعية غامضة، وليس خطأ نظيفاً قابلاً للإصلاح بتعديل ملف واحد — **رُفِض هذا الاقتراح ولم يُنفَّذ**؛ تعديل `ProcedureEstimatedCompletion.tsx` وحده كان سيجعله أقل اتساقاً مع الغالبية الداخلية، لا أكثر صحة. تم توثيق هذا كدرس منهجي: قبل الحكم على أي منطق يعتمد على يوم الأسبوع/التاريخ بأنه "خطأ"، يجب التحقّق من اتساقه مع الملفات الأخرى التي تتعامل مع نفس النطاق الواقعي، وليس فقط مقارنته بمصدر خارجي.

**الخطأ الفعلي المُصلَح**: في نفس الملف، دالة `parseProcessingDays()` (السطور 28-69) تحتوي حالة خاصة صحيحة لصيغة المثنى العربي في الأسابيع (`أسبوعين`/`اسبوعين` → 14 يوماً، تُفحَص قبل `أسبوع` العامة → 7 أيام) — لكن لم تُضَف حالة مماثلة للأشهر. كلمتا "شهرين"/"شهران" (صيغة مثنى تعني حرفياً "شهرين اثنين") تحتويان كلتاهما على السلسلة الفرعية "شهر"، فتقعان في الفحص العام `lower.includes('شهر')` الذي يُرجِع 30 يوماً فقط — أي **تنصيف صامت** لمدة معالجة حقيقية مدتها شهران إلى نصفها.

**بيانات حقيقية متأثرة فعلياً** (`lib/enrichedProcedures.ts`): `agr132-02` و`agr132-03` (`processingTime: 'شهران'`)، `agr12-03` (`'شهران (بعد اكتمال الملف وانعقاد اللجنة الفنية).'`)، `agr131-02` (`'شهر إلى شهرين بعد الكشف الميداني.'`) — تحقّقتُ من وجودها فعلياً في الملف عبر `grep`.

**التحقّق من الحيوية**: `app/procedures/page.tsx:34,873` يستورد المكوّن ويمرّر له `proc.processingTime` حيث `proc` مصدره `ENRICHED_PROCEDURES` من `lib/enrichedProcedures.ts` — سلسلة حيّة كاملة من الصفحة إلى البيانات الحقيقية.

**أثرها على المستخدم**: مستخدم يبدأ إجراء "ترخيص مزاولة نشاط تعبئة وتغليف المبيدات الزراعية" (agr132-02) يرى تاريخ إنجاز متوقَّع بعد نحو 30 يوماً فقط بدل الشهرين الموثَّقين رسمياً — توقّع خاطئ ومضلِّل بمقدار الضعف تقريباً.

**الإصلاح**: أُضيفت حالة `if (lower.includes('شهرين') || lower.includes('شهران')) return 60` قبل الفحص العام لـ"شهر"، بنفس نمط معالجة `أسبوعين` الموجود مسبقاً في السطر الذي يعلوه مباشرة.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل ملف واحد، ولم يُغيَّر أي منطق متعلّق بعطلة نهاية الأسبوع.

### تحديث (batch #441) — `ProcedureCostBreakdown.tsx`: فاصل "شرطة مسافة" (` - `) لم يكن يُقسِّم النص، فتُحجَب رسوم حقيقية (أحياناً أكبر بكثير) خلف تصنيف "طابع فقط" مُضلِّل

**المشكلة**: `parseLines()` في `components/ProcedureCostBreakdown.tsx` تحوي قاعدتين لتقسيم نص الرسوم إلى بنود منفصلة: الشرطة المائلة الطويلة (em-dash) والشرطة الملاصقة بلا مسافة (`.-كلمة`). لم تكن أي منهما تلتقط النمط الأكثر شيوعاً فعلياً في `fees_en` داخل `lib/enrichedProcedures.ts`: شرطة محاطة بمسافتين (` - `) — لأن قاعدة الشرطة الملاصقة تشترط عدم وجود مسافة قبل الشرطة، وقاعدة em-dash تبحث عن شرطة طويلة مختلفة تماماً. النتيجة: نصوص متعددة البنود بقيت مدمَجة في سطر واحد، وبما أنها غالباً تحتوي كلمة "طابع"/"stamp" في أحد بنودها، كانت `classify()` تُصنِّف السطر بأكمله كـ"طابع فقط" (`stamp`) — تحديداً لأن `classify()` تتحقق من `stamp` قبل `fee`/`variable` — فتُخفي البند الأكبر (رسم انتقال متغيّر، أو رسوم متدرّجة أكبر بكثير) خلف تصنيف مُضلِّل و**ملخّص مقتطَع لأول 22 حرفاً فقط**.

**أمثلة حقيقية متأثرة** (تحقّقتُ منها فعلياً بتشغيل منطق الدالة نفسه على بيانات `lib/enrichedProcedures.ts` الفعلية عبر Node):
- السطر 658/668 (تصريح إرث أساسي): `'Revenue stamp of 1,000 LBP - the transfer tax is calculated subsequently based on the value of the estate.'` وما يقابله بالعربية `'طابع مالي بقيمة 1000 ل.ل. — رسم الانتقال يُحتسب لاحقاً بحسب قيمة التركة.'` — كلاهما كان يبقى سطراً واحداً مُصنَّفاً "طابع" فقط، بينما رسم الانتقال المتغيّر (المحتمل أن يكون أكبر بكثير حسب قيمة التركة) يختفي تماماً من الملخّص.
- السطر 336 (تصريح عامل منزلي): سلسلة من 4 بنود بفواصل ` - ` (رسوم 1.5 مليون/800 ألف/400 ألف ل.ل. حسب الفئة + طابع) — كانت تندمج في سطر واحد ويُصنَّف "طابع" فقط، فيختفي الفارق الكبير بين فئات الرسوم (حتى 1.5 مليون ليرة) خلف ملخّص "طابع 1000 ل.ل." المقتطَع.

**اكتشاف إضافي أثناء التحقّق**: وُجد أيضاً أن القاعدة القديمة للشرطة الملاصقة كانت تُخطئ في الاتجاه المعاكس — تشترط فقط "رقم قبل الشرطة وحرف بعدها" بلا حاجة لنقطة، ما يجعلها تُقسِّم خطأً كلمات مركّبة حقيقية مثل `"24-hour"` (بيانات حقيقية، السطر 1095: `'... + stamps - 24-hour urgent service ...'`) إلى `"24"` و`"hour urgent service..."` منفصلين. أُصلِح هذا أيضاً بتضييق شرط ما قبل الشرطة إلى نقطة أو حرف عربي فقط (بدل أي رقم).

**التحقّق من الحيوية**: `app/procedures/page.tsx:47,834` يستورد المكوّن ويمرّر له `proc.fees`/`proc.fees_en` الحقيقيين مع `isAr` الفعلي — مسار حيّ كامل.

**التحقّق الكمّي الشامل**: شغّلتُ نسختي الدالة (القديمة والجديدة) على **كل الـ142 نص رسوم حقيقي** في `lib/enrichedProcedures.ts` عبر سكريبت Node: 27 نصاً تغيّر تقسيمها (تحسّناً)، منها 7 حالات كانت مُصنَّفة بالكامل "طابع فقط" خطأً وأصبحت تُقسَّم بشكل صحيح الآن، و**صفر تراجعات** (لا توجد حالة واحدة ينتج فيها التقسيم الجديد بنوداً أقل من القديم).

**الإصلاح**: أُضيفت قاعدة تقسيم ثالثة للشرطة المحاطة بمسافتين (` - `)، وُسِّع شرط em-dash ليشمل النقطة والحروف العربية (لا ASCII `\w` فقط)، وضُيِّق شرط الشرطة الملاصقة (حذف الرقم المجرَّد من الشرط، إبقاء النقطة/الحرف العربي فقط) لتفادي تقسيم كلمات مركّبة مثل "24-hour" خطأً.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل ملف واحد، تحقّق كمّي شامل ضد كل بيانات الرسوم الحقيقية في المشروع.

### تحديث (batch #442) — `lib/arabicNormalize.ts`: نطاق يونيكود خاطئ بحرف واحد كان يحذف الأرقام العربية-الهندية بالكامل من أي نص قبل المطابقة، فيُفسِد البحث كلياً لأي استعلام رقمي عربي

**المشكلة**: `removeTashkeel()` (المستخدَمة من `normalizeAr`/`normalizeForSearch`/`matchesAr`/`filterByQuery` — أساس كل بحث عربي في التطبيق) كانت تستخدم `text.replace(/[ً-ٰٟ]/g, '')`. فك تشفير النقاط الفعلية (Unicode code points) للحرفين في هذا النطاق يكشف أن الطرف الأخير `ٰ` هو **U+0670** (ألف خنجرية) وليس `ٟ` كما يوحي التعليق التوثيقي المجاور (U+064B–U+065F). النطاق الفعلي المطبَّق كان إذاً **U+064B–U+0670** بالكامل — أوسع من المقصود بمقدار كتلة كاملة، تشمل خطأً أرقام "٠١٢٣٤٥٦٧٨٩" العربية-الهندية (U+0660–U+0669).

**بيانات حقيقية متأثرة**: تحقّقتُ عبر `grep -rlP '[\x{0660}-\x{0669}]' lib/*.ts` أن `lib/lifeJourneys.ts` يحوي هذه الأرقام فعلياً في حقول مثل `subtitleAr: '٤ إجراءات — ٣٠ يوماً'`.

**أثرها على المستخدم**: أي مستخدم يكتب استعلام بحث بأرقام عربية-هندية بحتة (مثلاً سنة "٢٠٢٤" أو رقم "٣٠") في `ProcedureSearchModal.tsx` أو `GlobalSearch.tsx` — كلا المكوّنين حيّان ويستخدمان `normalizeForSearch` من نفس الملف — يواجه عطلاً: `removeTashkeel` تحذف كل حرف من النص المُدخَل (لأن كل الأرقام العربية-الهندية تقع ضمن النطاق الموسَّع خطأً)، فيصبح الاستعلام المُطبَّع سلسلة فارغة `''`. بما أن `String.prototype.includes('')` تُعيد `true` دائماً لأي نص، تُطابِق كل معاملة في القائمة استعلاماً فارغاً — أي أن `ProcedureSearchModal.tsx` يعرض 12 نتيجة عشوائية غير ذات صلة ويُسمّيها "12 نتيجة" بثقة كاملة (أسوأ من عدم إرجاع نتائج، لأن المستخدم يظن أنها تطابقت فعلاً)، بينما `GlobalSearch.tsx` (له حارس `if (!q) return []` بعد التطبيع) يُظهِر العكس: صفر نتائج رغم وجود محتوى حقيقي مطابق (كنص "٣٠ يوماً" في `lifeJourneys.ts`).

**التحقّق البرمجي**: شغّلت الدالة (القديمة والجديدة) فعلياً عبر Node على نص حقيقي `'٤ إجراءات — ٣٠ يوماً'` من `lifeJourneys.ts`: النسخة القديمة تحذف كل الأرقام (`'إجراءات — يوماً'` بعد حذف "٤" و"٣٠")، بينما نص فيه تشكيل حقيقي (`'مُحَمَّد'`) لا يزال يُعالَج بشكل صحيح (`'محمد'`) في كلتا النسختين — أي أن الإصلاح لا يكسر وظيفة إزالة التشكيل الأصلية، فقط يوقف حذف الأرقام.

**التحقّق من الحيوية**: `ProcedureSearchModal.tsx` مُستورَد ومُستخدَم في `app/procedures/page.tsx:25,1151`؛ `normalizeForSearch`/`matchesAr` من نفس الملف مُستخدَمة أيضاً في `GlobalSearch.tsx` الحيّ — مساران حقيقيان.

**الإصلاح**: تغيير النطاق إلى `/[ً-ٟ]/g` (استخدام escape صريح للنقطتين U+064B وU+065F بدل الحرفين الحرفيين، لتفادي تكرار نفس نوع الخطأ مستقبلاً عبر النسخ واللصق).

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل ملف واحد (`lib/arabicNormalize.ts`)، يؤثر إيجابياً على كل مكان يستخدم دوال هذا الملف (البحث الموحّد، بحث صفحة الإجراءات، أي فلترة عربية أخرى تعتمد عليه).

### تحديث (batch #443) — `app/procedures/page.tsx`: إصلاح `whereToApply` بفاصلة "|" لم يُطبَّق على `fees`/`processingTime` رغم أن التعليق المجاور يذكر صراحة أنهما يتبعان "نفس الاتفاقية"

**المشكلة**: عند عرض كل معاملة موسَّعة، يبني الكود متغيرات `displayFees` و`displayProcessingTime` و`displayWhereToApply` من الحقول الخام. `displayWhereToApply` وحدها كانت تمرّ عبر `.split(/\s*\|\s*/).filter(Boolean).join(isAr ? '، ' : ', ')` لتحويل الفاصل الخام "|" (المستخدَم في البيانات الحقيقية لعرض عدّة قيم في نفس الحقل) إلى فاصلة مقروءة — والتعليق المجاور يذكر صراحة: "whereToApply sometimes lists multiple distinct locations joined with ' | ' (**same multi-value convention as `fees`**)"، أي أن الكود نفسه يوثّق أن `fees` تتبع نفس الاتفاقية، لكن `displayFees` و`displayProcessingTime` بقيتا بلا أي معالجة — تُعرَضان بحرف "|" الخام مباشرة.

**بيانات حقيقية متأثرة** (تحقّقت عبر `grep` في `lib/enrichedProcedures.ts`): سجلّان لـ`processingTime` (مثلاً `'فوراً | فوراً | يوم واحد | 3 – 4 أيام'`) وأربعة سجلّات لـ`fees` (مثلاً `'رسم 480.000 ل.ل. فئة ثالثة | 960.000 ل.ل. فئة ثانية | 1.800.000 ل.ل. فئة أولى. طابع أميري 1000 ل.ل. لكل مستند.'`) تستخدم الفاصل الخام.

**أثرها على المستخدم**: يظهر حرف "|" الخام في عدّة أماكن حقيقية يعرضها المستخدم مباشرة: شريحة "مدة الإنجاز" في صفحة المعاملة الموسَّعة (بجانب شريحة "مكان التقديم" المُنسَّقة بشكل صحيح في نفس الصف — تناقض بصري مباشر)، `ProcedureFeeHistory` ("الرسوم: ...")، و`ProcedureCopySummaryLine` (النص المنسوخ للمشاركة عبر واتساب/الملاحظات).

**الإصلاح**: طُبِّق نفس نمط `.split(/\s*\|\s*/).filter(Boolean).join(isAr ? '، ' : ', ')` على `displayFees` و`displayProcessingTime`، وحُدِّث تمرير `ProcedureFeeHistory` (كان يمرّر `proc.fees`/`proc.fees_en` الخام مباشرة) ليستخدم `displayFees` المُنسَّقة بدلاً من ذلك، بحيث يستفيد كل مكان يستهلك هذه المتغيّرات تلقائياً.

**تحقّق إضافي**: راجعتُ `CostEstimator.tsx` (يستقبل `displayFees` عبر `feesRaw`) للتأكد أن تغيير الفاصل من "|" إلى "، "/", " لا يكسر استخراج المبلغ الرقمي — منطقه يعتمد على `.match()` للبحث عن أول نمط `$X`/`X ل.ل` في أي مكان بالنص، بلا اعتماد على حرف الفاصل نفسه، فلا تأثير سلبي.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `app/procedures/page.tsx`.

### تحديث (batch #444) — `app/services/page.tsx`: نفس ثغرة "|" الخام (batch #443) موجودة أيضاً في صفحة الخدمات، على قاعدة بيانات مختلفة (`lib/allServices.ts`)

**المشكلة**: نفس نمط الثغرة المكتشفة في batch #443 (`app/procedures/page.tsx`) وُجد في `app/services/page.tsx` بشكل مستقل، على حقلَي `fees`/`fees_en` و`processing_time`/`processing_time_en` من `lib/allServices.ts` (367 خدمة) بدلاً من `lib/enrichedProcedures.ts`. كانت أربعة مواضع تعرض هذه الحقول الخام بلا أي معالجة للفاصل "|": `displayFees`/`displayProcessingTime` (تُستخدَمان في `ServiceSheet` وتُغذّيان أيضاً `flowchartSource` — أي أن مولّد الفلوتشارت بالذكاء الاصطناعي كان يستقبل حرف "|" الخام كجزء من نص الوصف)، و`cardFees`/`cardProcessingTime` (شارة موجزة على بطاقة كل خدمة في الشبكة).

**بيانات حقيقية متأثرة** (تحقّقت بنفسي عبر `grep -c` على `lib/allServices.ts`): 15 سجلّاً في `fees` و14 في `fees_en` يحتويان الفاصل " | " (29 إجمالاً)، و7 في `processing_time` و7 في `processing_time_en` (14 إجمالاً). مثال حقيقي مؤكَّد — `passport_001` (إصدار جواز السفر اللبناني):
`fees_en: 'Regular passport fee (determined according to the current year's budget) | Express service at the Public Relations Department: 4,900,000 LBP in addition to the statutory fee'`
`processing_time_en: 'Regular: one to two weeks | Immediate at the Public Relations Department (Beirut – Adlieh)'`

**أثرها على المستخدم**: حرف "|" الخام يظهر مباشرة في تفاصيل الخدمة الموسَّعة وفي شارة الرسوم/مدة الإنجاز الموجزة على بطاقة كل خدمة بالشبكة (حيث تُقتطَع `cardFees` أصلاً إلى 18 حرفاً — الفاصل الخام كان يزيد من احتمال قطع النص في منتصف كلمة غير مفهومة)، إضافة إلى تلوّث نص `flowchartSource` المرسَل لتوليد الفلوتشارت بالذكاء الاصطناعي.

**الإصلاح**: طُبِّق نفس نمط batch #443 — `.split(/\s*\|\s*/).filter(Boolean).join(isAr ? '، ' : ', ')` — على المواضع الأربعة: `displayFees`، `displayProcessingTime` (السطر ~35-36، وهذا يُصلح `flowchartSource` تلقائياً لأنه يستهلك هاتين المتغيّرتين)، و`cardFees`، `cardProcessingTime` (داخل عرض شبكة بطاقات الخدمات).

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `app/services/page.tsx`.

### تحديث (batch #445) — `app/services/page.tsx`: زر مشاركة واتساب في `ServiceSheet` يعرض قائمة وثائق عربية دائماً تحت عنوان إنجليزي في وضع اللغة الإنجليزية

**المشكلة**: زر مشاركة واتساب داخل `ServiceSheet` يبني نص الرسالة بعنوان الخدمة والجهة مُفرَّعَين بشكل صحيح حسب `isAr`، وعنوان قسم "الوثائق المطلوبة" أيضاً ثنائي اللغة بشكل صحيح — لكن قائمة الوثائق نفسها كانت تُقرأ دائماً من الحقل العربي فقط بلا أي تفريع:
```js
const docs = service.required_documents?.slice(0, 4) || []
```
بينما نفس المكوّن يملك بالفعل المعالجة الصحيحة لنفس الحقل في مكانين آخرين (`displayRequiredDocuments` لعرض التفاصيل، و`cardDocs` لشارة عدد الوثائق على بطاقة الشبكة): `isAr ? service.required_documents : (service.required_documents_en?.length ? service.required_documents_en : service.required_documents)`.

**بيانات حقيقية متأثرة**: تحقّقت عبر `grep -c` في `lib/allServices.ts`: 368 سجلاً لحقل `required_documents_en`، منها 351 سجلاً غير فارغ (فقط 17 خدمة بلا ترجمة إنجليزية للوثائق).

**أثرها على المستخدم**: عند مشاركة أي خدمة عبر واتساب في وضع اللغة الإنجليزية، تظهر رسالة بعنوان "📁 Required documents:" إنجليزي تليها حتى 4 أسماء وثائق باللغة العربية — تناقض لغوي مباشر في رسالة تُشارَك مع طرف ثالث، لأغلب الخدمات الـ367.

**الإصلاح**: طُبِّق نفس نمط التفريع المستخدم بالفعل في نفس الملف (`displayRequiredDocuments`/`cardDocs`) على متغيّر `docs` داخل معالج زر واتساب.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `app/services/page.tsx`.

### تحديث (batch #446) — `lib/allTransactions.ts` + `app/forms/page.tsx`: حقلا `fee`/`duration` في `TxItem` كانا سلسلتين فارغتين ثابتتين لكل الـ2484 معاملة، ما يجعل شارتين في واجهة `/forms` ميتتَين تماماً

**المشكلة**: تُعرِّف `interface TxItem` حقلين `fee: string` و`duration: string` وكأنهما بيانات حقيقية. لكن المُنشئ الوحيد لمصفوفة `TX_ALL` (السطر 3408-3412) يضبطهما دائماً على قيمة نصية فارغة ثابتة: `fee:'',duration:''` — لا يُشتقّان من أي مصدر بيانات فعلي (مصفوفات `_R`/`_P`/`_M` لا تحمل أي معلومة رسوم أو مدة أصلاً). صفحة `/forms` (حيّة، مستوردة عبر `import { TX_ALL, TX_WITH_FORMS, ... } from '@/lib/allTransactions'` ومعروضة في تبويبَي "نماذج PDF"/"كل المعاملات") كانت تعرض شارتين مشروطتين: `{tx.fee && <span>...}` و`{tx.duration && <span>...}` — وبما أن كلا الحقلين فارغ دائماً، هاتان الشارتان لا يمكن أن تظهرا لأي معاملة من أصل 2484 معاملة، أبداً.

**التحقق**: تحقّقتُ مباشرة من الكود المصدر — سطر الإنشاء الفعلي في `lib/allTransactions.ts` يحتوي حرفياً `fee:'',duration:''` بلا أي شرط أو قيمة بديلة، ولا يوجد أي حقل رسوم/مدة في أي من مصفوفات المصدر الأخرى بالملف. فحصتُ أيضاً الحقلين المتجاورين `hasForm`/`pdfUrl` بنفس المُنشئ للتأكد من عدم وجود نمط تناقض مشابه لما اكتُشف سابقاً (batch #408) — تبيّن أنهما متّسقان تماماً (0 حالة تناقض).

**أثرها على المستخدم**: شارتا "الرسوم" و"المدة" على بطاقة كل معاملة في `/forms` (تبويبَي "نماذج PDF" و"كل المعاملات") كانتا كوداً ميتاً بصرياً — لا تُرسمان أبداً مهما كانت المعاملة، رغم وجود تصميم/تنسيق كامل لهما بانتظار بيانات لا وجود لها في مصدر البيانات الفعلي.

**الإصلاح**: بما أن لا وجود لبيانات رسوم/مدة حقيقية في أي مصدر توليد لهذا الملف، الإصلاح الأمين هو إزالة الشارتين الميتتين من `app/forms/page.tsx` وإزالة الحقلين غير المستخدَمين `fee`/`duration` من `TxItem` (التعريف + قيمتا الإنشاء الفارغتان) بدلاً من اختلاق بيانات غير موجودة. لا تغيير بصري فعلي لأن الشارتين لم تكونا تُعرَضان أصلاً.

`tsc --noEmit` نظيف. إصلاح محصور بملفين (`lib/allTransactions.ts`, `app/forms/page.tsx`).

### تحديث (batch #447) — `lib/serviceFAQ.ts`: 8 سجلات من أصل 60 في `SERVICE_FAQ` بلا عنوان/ملخص/نوع على الإطلاق — تظهر كبطاقات فارغة في `/faq` وترسل سؤالاً مشوَّهاً للذكاء الاصطناعي

**المشكلة**: 8 سجلات (`faq_001` إلى `faq_008`، كلها بفئة "أسئلة شائعة") فقدت حقول `title`/`summary`/`type` أثناء توليد البيانات — أصبحت الحقول الثلاثة سلاسل فارغة `''` رغم أن باقي حقول نفس السجل (`steps`, `requiredDocuments`, `authority`) تحمل بيانات حقيقية وكاملة. مثال (`faq_001`): `type:'', title:'', summary:''` بينما `steps` تحتوي 5 خطوات فعلية و`authority` تحتوي جهة حقيقية، و`chatPrompt` أصبح `'اعطني معلومات عن:  في لبنان'` (مسافة مزدوجة، بلا موضوع فعلي).

**التحقق**: تأكّدتُ عبر `grep` أن `SERVICE_FAQ` تحتوي 60 سجلاً بالضبط (`grep -c "^\s*id:'"`)، منها 8 سجلات بـ`title:'',` فارغ حرفياً (`grep -c "title:'',"`) — وهي `faq_001` إلى `faq_008` بالتتابع.

**بيانات حقيقية متأثرة/أثرها على المستخدم**: `SERVICE_FAQ` مستخدَمة في 4 استهلاكات حقيقية: `app/faq/page.tsx` (مستوردة مباشرة، والصفحة تصل إليها كل زائر عبر `TopNav`/`MobileMenu`)، `components/ProcedureFAQChips.tsx`، `components/GlobalSearch.tsx`، `components/HomepageFeaturedFAQ.tsx`. دالة `searchFAQ('')` (تُستدعى تلقائياً عند فتح `/faq` بلا بحث) تُعيد المصفوفة كاملة بلا فلترة — أي أن أي زائر لصفحة الأسئلة الشائعة يرى 8 بطاقات فارغة (أيقونة + عنوان فارغ) ضمن القائمة الافتراضية. عند الضغط على "اسأل دليلك" لأي من هذه البطاقات، يُرسَل `chatPrompt` المشوَّه (`'اعطني معلومات عن:  في لبنان'`) للذكاء الاصطناعي بلا موضوع فعلي.

**الإصلاح**: بما أنه لا يوجد أي مصدر بيانات حقيقي في المستودع لاستعادة العناوين/الملخصات المفقودة لهذه الـ8 سجلات (نفس المعيار المطبَّق في batch #446 — لا اختلاق بيانات)، أُعيدت تسمية المصفوفة الخام إلى `SERVICE_FAQ_RAW` (غير مصدَّرة)، وأصبح التصدير العام `SERVICE_FAQ` مرشَّحاً: `SERVICE_FAQ_RAW.filter(f => f.title)`. إصلاح واحد عند المصدر المشترك يُصلِح الاستهلاكات الأربعة كلها دفعة واحدة تلقائياً.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `lib/serviceFAQ.ts`.

### تحديث (batch #448) — `components/ProcedureLastUpdatedBadge.tsx`: شارة "آخر مراجعة" تعرض دائماً حالة "قديم/أحمر" لكل معاملة، بلا استثناء — بسبب تاريخ أساس ثابت في منطق افتراضي أصلاً ⚠️ (تنبيه سياسة: هذا المكوّن يعرض تاريخاً وهمياً غير حقيقي، وليس بيانات فعلية)

**تنبيه هام يتجاوز حجم هذه الدفعة**: هذا المكوّن (موجود في المشروع من مرحلة سابقة، ليس من إضافتي) يعرض تاريخ "آخر مراجعة" **مُختلَقاً بالكامل** — لا يُشتقّ من أي بيانات حقيقية، بل من دالة hash حتمية على كود الإجراء (`code`) لإنتاج رقم يوم عشوائي ثابت. هذا يتعارض مع قاعدة المشروع الصريحة "لا تستخدم Mock Data في النسخة النهائية". لا يوجد أي حقل `lastReviewed`/`updatedAt` حقيقي في `lib/enrichedProcedures.ts` (تحقّقتُ عبر `grep`) لاستبدال هذا المنطق ببيانات فعلية. **هذا القرار — إبقاء الشارة بمنطقها الوهمي الحالي، أو استبدالها بشارة "غير محدّد"، أو حذفها بالكامل — قرار منتج يخص المستخدم وليس شيئاً أستطيع حسمه بمفردي.**

**العطل التقني المُصلَح في هذه الدفعة (منفصل عن قرار المنتج أعلاه)**: كانت دالة `getReviewDate()` تُرسي تاريخها الوهمي على قاعدة ثابتة `new Date('2025-01-01')` + إزاحة عشوائية حتى 179 يوماً — أي أن أقصى تاريخ وهمي ممكن لأي إجراء هو 2025-06-29. ودالة `freshnessColor()` تقارن هذا التاريخ الثابت بـ`Date.now()` الحقيقي: بعد مرور 90 يوماً على 2025-06-29 (أي منذ أواخر سبتمبر 2025 تقريباً)، أصبح مسارا "أخضر" و"أصفر" في المنطق **يستحيل رياضياً** الوصول إليهما إلى الأبد — كل إجراء يعرض دائماً الحالة الحمراء "قديم"، بصرف النظر عن الكود.

**التحقق**: كتبتُ سكريبت Node مطابق للمنطق الفعلي وشغّلته على التاريخ الحالي (2026-07-25) — النتيجة: 71 من 71 إجراء حقيقي في `lib/enrichedProcedures.ts` يعرض اللون الأحمر (0 أخضر، 0 أصفر). أقصى تاريخ وهمي ممكن عبر كل الأكواد هو 2025-06-28 — أي أن مساري الأخضر/الأصفر كانا كوداً ميتاً رياضياً منذ نحو 10 أشهر.

**أثرها على المستخدم**: كل إجراء موسَّع على صفحة `/procedures` (الشارة حيّة، مستوردة ومعروضة فعلياً) يعرض تحذيراً أحمر دائماً بأن المحتوى "لم يُراجَع حديثاً" — تحذير كاذب ومضلِّل تماماً (ناتج عن تاريخ أساس مثبَّت، لا عن تقادم فعلي في المحتوى)، ما يقوّض ثقة المستخدم بمحتوى قد يكون دقيقاً وحديثاً فعلاً.

**الإصلاح المُطبَّق**: عولجت المشكلة التقنية الفورية (استحالة الوصول لأي حالة غير حمراء) بربط تاريخ الأساس بنافذة متحركة نسبةً لـ"الآن" (`new Date()` بدلاً من تاريخ ثابت) بدل تاريخ تقويمي ثابت — يحافظ هذا على نفس السلوك الحتمي لكل كود، لكنه يُبقي المسارات الثلاثة قابلة للوصول إلى الأبد. **هذا لا يحل المشكلة الجوهرية (كون التاريخ المعروض بالكامل وهمياً)** — فقط يمنع تدهورها الرياضي لحالة "أحمر دائم لكل شيء" إلى الأبد.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `components/ProcedureLastUpdatedBadge.tsx`. **يوصى بمراجعة المستخدم لقرار إبقاء/تعديل/حذف هذه الميزة بالكامل نظراً لطبيعتها غير الحقيقية.**

### تحديث (batch #449) — `components/GovHolidayAlert.tsx`: 4 أعياد هجرية (قمرية) مثبَّتة بتاريخ ميلادي ثابت سنوياً — تنجرف عن التاريخ الحقيقي كل سنة وستُطلق تنبيهاً في يوم خاطئ

**المشكلة**: مصفوفة `HOLIDAYS` تحتوي 15 عيداً، منها 4 أعياد هجرية (عيد الفطر، عيد الأضحى، رأس السنة الهجرية، المولد النبوي) مخزَّنة كزوج `{month, day}` ثابت تماماً كباقي الأعياد الميلادية الثابتة فعلياً. لكن التقويم الهجري ~354-355 يوماً في السنة، فكل عيد هجري حقيقي يتقدّم ~10-11 يوماً كل سنة ميلادية — القيم الأربع المخزَّنة تطابق فعلياً تواريخ 2025 الهجرية الحقيقية فقط (كما يوثّق تعليق في الملف الشقيق الميت `GovCalendar.tsx`: "Variable Islamic holidays (approximate 2025-2026 dates)")، لكن `GovHolidayAlert.tsx` نسخ نفس القيم بلا أي حقل سنة أو تنبيه انتهاء صلاحية، ودالة `checkAlert()` كانت تطابق `h.month === m && h.day === d` بلا أي فحص للسنة — أي تُعامَل كأعياد متكررة سنوياً إلى الأبد.

**أثرها على المستخدم (قابل للتحقق اليوم 2026-07-25)**: دورة هذه السنة انزاحت بالفعل — عيد الفطر/الأضحى/رأس السنة الهجرية الحقيقية لعام 2026 وقعت أبكر بـ~10-11 يوماً من التواريخ المثبَّتة (فاتت التنبيهات الصحيحة بصمت). العطل القادم القابل للتحقق: المولد النبوي الحقيقي لعام 2026 يقع أواخر أغسطس 2026، لكن المكوّن (بمنطقه القديم) كان سيُطلق تنبيه "غداً — المولد النبوي، الدوائر مغلقة" في 3 سبتمبر 2026 (ليوم 4 سبتمبر) — تنبيه خاطئ في يوم خاطئ تماماً، بلا أي تحذير في يوم الإغلاق الفعلي. هذا الانحراف يتفاقم كل سنة لاحقة. الشارة `role="alert"` حيّة على صفحة `/procedures` (مستوردة ومعروضة فعلياً).

**الإصلاح**: أُضيف حقل اختياري `year?: number` إلى واجهة `Holiday`، وضُبط `year: 2025` على الأعياد الهجرية الأربعة فقط (السنة الوحيدة المؤكَّدة لهذه القيم)، وعُدِّل شرط المطابقة في `checkAlert()` ليتجاهل أي إدخال مثبَّت بسنة خارج السنة الحالية الفعلية: `h.year === undefined || h.year === tomorrow.getFullYear()`. بهذا تتوقف الأعياد الهجرية عن المطابقة بصمت خارج سنتها المؤكَّدة (فشل نحو "لا تنبيه" بدل تنبيه خاطئ في تاريخ خاطئ — نفس مبدأ batch #448: لا افتراض بيانات غير مؤكَّدة). لم تُختلَق تحويلات هجرية-ميلادية جديدة لعام 2026 فصاعداً — هذا يتطلّب مصدراً فلكياً/API حقيقياً، خارج نطاق هذه الدفعة.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `components/GovHolidayAlert.tsx`.

### تحديث (batch #450) — `components/ProcedureDeadlineAlert.tsx`: زر "تأجيل ليوم واحد" لا يعمل فعلياً — التنبيه يعود للظهور فوراً بعد الضغط عليه، طوال اليوم تقريباً

**المشكلة**: الدالة `scanDeadlines()` (تبحث هل اليوم مؤجَّل) والدالة `snooze()` (تكتب مفتاح التأجيل) تستخدمان طريقتين مختلفتين لحساب "تاريخ اليوم" لبناء نفس مفتاح `localStorage`:
```ts
// scanDeadlines() — القراءة:
const today = new Date(); today.setHours(0,0,0,0)
const snoozeToday = today.toISOString().slice(0,10)   // منتصف الليل المحلي، ثم تحويله إلى UTC

// snooze() — الكتابة:
const today = new Date().toISOString().slice(0,10)    // اللحظة الحالية، محوَّلة مباشرة إلى UTC
```
`setHours(0,0,0,0)` يضبط منتصف الليل **بالتوقيت المحلي**، وتحويل هذه اللحظة إلى ISO بتوقيت UTC يُرجعها ليوم UTC **سابق** لأي منطقة زمنية بفارق موجب عن UTC — ولبنان بفارق +2/+3 (والتطبيق مخصَّص للبنان فقط). لذا فإن `snoozeToday` (مفتاح القراءة) يساوي التاريخ المحلي الحقيقي ناقص يوم واحد طوال اليوم تقريباً (باستثناء نافذة ~2-3 ساعات بعد منتصف الليل المحلي مباشرة)، بينما `snooze()` يكتب المفتاح بالتاريخ المحلي الصحيح لمعظم اليوم.

**التحقق**: شغّلتُ سكريبت Node بمنطقة زمنية `Asia/Beirut` (نفس منطقة التطبيق) في الوقت الحالي فعلياً — النتيجة: مفتاح الكتابة `2026-07-25`، مفتاح القراءة `2026-07-24` — عدم تطابق مؤكَّد ومُتكرِّر فعلياً الآن.

**أثرها على المستخدم**: زر "تأجيل ليوم واحد" (حيّ، ضمن `ProcedureDeadlineAlert` المعروض في `/procedures`) يكتب مفتاح تأجيل باسم `..._{اليوم}`، لكن `refresh()` التي تُستدعى فوراً بعد الضغط تبحث عن `..._{الأمس}` ولا تجده — فيعود التنبيه للظهور فوراً وكأن الزر لم يُضغَط، طوال اليوم تقريباً. الزر معطَّل فعلياً لجميع المستخدمين تقريباً طوال الوقت.

**الإصلاح**: تُوحيد طريقة حساب مفتاح القراءة في `scanDeadlines()` لتطابق طريقة الكتابة في `snooze()` — استخدام `new Date().toISOString().slice(0,10)` مباشرة بدل تمريرها عبر `setHours(0,0,0,0)` المحلي أولاً.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `components/ProcedureDeadlineAlert.tsx`.

### تحديث (batch #451) — حسم قرار batch #448: إزالة `ProcedureLastUpdatedBadge` نهائياً (بتفويض صريح من المستخدم لاختيار الحل الأصلح عالمياً)

طلب المستخدم صراحة اختيار "الأنسب والأصلح عالمياً" بخصوص شارة "آخر مراجعة" المُختلَقة (batch #448). المعيار العالمي القياسي في تصميم واجهات الثقة (trust signals) هو عدم عرض بيانات مُختلَقة كحقيقة واقعة للمستخدم أبداً — وقاعدة المشروع نفسها تنص صراحة على منع Mock Data في النسخة النهائية. لا يوجد بديل "يُصلح" هذا سوى الحذف: الإبقاء على التاريخ الوهمي يخالف كلا المعيارين، واستبداله بعبارة غامضة لا تزال مُختلَقة لا يحل المشكلة الجوهرية.

**الإصلاح**: أُزيل استيراد واستخدام `ProcedureLastUpdatedBadge` من `app/procedures/page.tsx` (كان مكاناً واحداً فقط)، وأُدرِج `components/ProcedureLastUpdatedBadge.tsx` في قائمة الحذف `rm -f` ضمن `push.sh` (لا يمكن حذف الملفات مباشرة من بيئة العمل الحالية). لم يتأثر أي عرض/وظيفة حقيقية أخرى.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `app/procedures/page.tsx` + حذف ملف واحد.

### تحديث (batch #452) — `components/NotificationBell.tsx`: تنبيهات "انتهاء صلاحية الوثائق" لا يمكن أن تظهر أبداً — تقرأ مفتاحاً لا يكتبه أي مكوّن حيّ

**المشكلة**: كان `NotificationBell` (حيّ، معروض عالمياً عبر `TopNav`/`MobileMenu` في كل صفحة) يقرأ مفتاح `localStorage` مسطّحاً واحداً `dalilak_doc_expiry` بصيغة JSON object، ويتوقّع مفاتيح ثابتة من قائمة تصنيف مكوّنة من 5 عناصر فقط (`passport`, `national_id`, `driving`, `residence`, `work`). لكن الكاتب الفعلي الوحيد والحيّ لهذا النوع من البيانات — `components/ProcedureDocumentStatus.tsx` (حيّ، الأداة الفعلية "+ تاريخ الانتهاء" تحت قائمة وثائق كل معاملة في `/procedures`) — يكتب بصيغة مختلفة كلياً: مفتاح منفصل لكل وثيقة `dalilak_doc_expiry_{code}_{index}` بقيمة تاريخ نصي مباشر، لأي وثيقة حقيقية من أي معاملة (لا تصنيف ثابت من 5 عناصر). الكاتب الوحيد للمفتاح المسطّح القديم كان `components/DocExpiryBanner.tsx` — تحقّقتُ عبر `grep` شامل أنه كود ميت تماماً (لا استيراد ولا استخدام JSX في أي مكان).

**أثرها على المستخدم**: عند ضبط تاريخ انتهاء صلاحية أي وثيقة حقيقية (مثلاً بطاقة الهوية) عبر `ProcedureDocumentStatus` في `/procedures`، تتحدّث الشارة المحلية بشكل صحيح، ويُطلَق حدث `dalilak_saved_change` الذي يستمع إليه `NotificationBell` — لكن منطق القراءة يفحص مفتاحاً لا يُكتَب أبداً، فتنبيه انتهاء الوثيقة في الجرس العالمي لا يمكن أن يظهر إطلاقاً مهما بلغت درجة إلحاحه، لأي مستخدم حقيقي. (تنبيهات المواعيد في نفس المكوّن كانت تعمل بشكل صحيح لأن `AppointmentReminder.tsx` الحيّ يكتب فعلاً المفتاح `dalilak_appointments` الذي يقرأه `NotificationBell`.)

**الإصلاح**: أُعيدت كتابة منطق فحص الوثائق في `loadItems()` ليمسح مفاتيح `localStorage` الحقيقية بصيغة `dalilak_doc_expiry_{code}_{index}` مباشرة، ويستخرج نص الوثيقة الفعلي عبر البحث في `ENRICHED_PROCEDURES` بنفس الكود والفهرس (نفس مصدر البيانات الذي يبني منه `app/procedures/page.tsx` مصفوفة `docs` الممرَّرة إلى `ProcedureDocumentStatus`)، بدل الاعتماد على قائمة التصنيف الثابتة الميتة. أُضيف أيضاً `components/DocExpiryBanner.tsx` (كود ميت مؤكَّد) إلى قائمة الحذف `rm -f`.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `components/NotificationBell.tsx` + حذف ملف ميت واحد.

### تحديث (batch #453) — `ProcedureProgressBadge` + فلتر "بدأت تحضيرها": نفس نمط عطل batch #452 — يقرآن مفتاحاً لا يكتبه أي مكوّن حيّ

**المشكلة**: `components/ProcedureProgressBadge.tsx` (حيّ، شارة "X/Y ✓" على كل بطاقة معاملة في `/procedures`) و `advFilters.started === 'yes'` في `app/procedures/page.tsx` (فلتر "بدأت تحضيرها" الحيّ ضمن `ProcedureFilterDrawer`) كانا يقرآن مفتاح `dalilak_checklist_{code}` — الكاتب الوحيد لهذا المفتاح هو `components/DocChecklistBuilder.tsx`، وتحقّقتُ عبر `grep` شامل أنه كود ميت تماماً (لا استيراد ولا استخدام JSX خارج ملفه). لذا كانت الشارة لا تظهر أبداً مهما تقدّم المستخدم فعلياً في تحضير وثائقه، وفلتر "بدأت تحضيرها" كان يُرجع دائماً قائمة فارغة.

**البيانات الحقيقية الحيّة الموازية**: المكوّن الحيّ الفعلي لتتبّع تجهيز الوثائق هو `components/ProcedureDocumentChecklist.tsx` (معروض فعلياً في `/procedures`)، الذي يكتب مفتاحاً منفصلاً لكل وثيقة `dalilak_doc_{code}_{index}` بقيمة `'1'`/`'0'` — وهو نفس المخطط الذي يقرأه بالفعل مكوّنان حيّان آخران (`ProcedureChecklistExport.tsx`، `ProcedureDocumentShare.tsx`)، ما يجعله المرجع الأكثر اعتماداً بين المكوّنات الحيّة. تأكّدتُ أيضاً من تسمية الفلتر نفسها ("بدأت تحضيرها" / "Started preparing") لأتحقّق أنه يقصد تقدّم قائمة الوثائق تحديداً، لا حالة "بدء المعاملة" المنفصلة التي يتتبّعها `ProcedureStartButton` بمفتاح مختلف كلياً (`dalilak_started_{code}`).

**الإصلاح**: أُعيدت كتابة `ProcedureProgressBadge` لتقرأ مفاتيح `dalilak_doc_{code}_{i}` الحقيقية (بدل المفتاح المسطّح الميت)، وحُدِّث فلتر "بدأت تحضيرها" في `app/procedures/page.tsx` ليطابق نفس المصدر. أُضيف أيضاً `components/DocChecklistBuilder.tsx` و`components/ProcedureProgressTracker.tsx` (كلاهما كود ميت مؤكَّد، الثاني يعتمد على نفس المفتاح الميت) إلى قائمة الحذف `rm -f`.

`tsc --noEmit` نظيف. إصلاح في ملفين (`components/ProcedureProgressBadge.tsx`, `app/procedures/page.tsx`) + حذف ملفين ميتين.

### تحديث (batch #454) — `ProcedureDocReadinessBar` و`ProcedureDocumentChecklist`: مكوّنان متجاوران يعرضان نفس قائمة الوثائق لكن بحالتين منفصلتين لا تتزامنان أبداً

**المشكلة**: تُعرَض `ProcedureDocReadinessBar` و`ProcedureDocumentChecklist` متتاليتين في نفس قسم "المستندات المطلوبة" لكل معاملة موسَّعة، بنفس قائمة الوثائق تماماً (`docs` بنفس المصدر والفهرسة) — لكن كل واحدة تحتفظ بحالة منفصلة في `localStorage`: `ProcedureDocReadinessBar` كانت تستخدم `dalilak_doc_ready_{code}_{index}`، بينما `ProcedureDocumentChecklist` تستخدم `dalilak_doc_{code}_{index}` — وهو نفس المخطط الذي تعتمد عليه بالفعل 3 مكوّنات حيّة أخرى (`ProcedureDocumentShare`، `ProcedureChecklistExport`) بالإضافة إلى إصلاحَي batch #453 (`ProcedureProgressBadge` وفلتر "بدأت تحضيرها"). تحقّقتُ عبر `grep` أن `dalilak_doc_ready_` لم يكن يُقرأ أو يُكتَب في أي مكان آخر خارج نفس الملف.

**أثرها على المستخدم**: تحديد وثيقة كـ"جاهزة" في `ProcedureDocReadinessBar` (الشريط الملوّن العلوي، مع رسالة "✅ جميع وثائقك جاهزة!") لا ينعكس إطلاقاً في مشاركة واتساب، أو تصدير PDF/نص، أو شارة التقدّم على البطاقة، أو فلتر "بدأت تحضيرها" — وكل هذه الأدوات تقرأ فقط من `ProcedureDocumentChecklist` المعروضة مباشرة أسفل الشريط. والعكس صحيح: تحديد مربّع في `ProcedureDocumentChecklist` لا يحدّث الشريط أعلاه أبداً. حالتان منفصلتان تماماً لنفس المهمة المفهومية، دون أن يدرك المستخدم ذلك.

**الإصلاح**: وُحِّد مفتاح `ProcedureDocReadinessBar` مع المخطط الحقيقي المتفق عليه بين 5 مكوّنات حيّة أخرى (`dalilak_doc_{code}_{index}`). عند التحقّق، اكتُشِف خلل إضافي كان سيظهر بعد التوحيد: منطق القراءة في `ProcedureDocReadinessBar` كان يستخدم فحص truthy بسيط (`!!localStorage.getItem(...)`) بدل `=== '1'` الصريح المستخدم في كل القرّاء الآخرين — بما أن `ProcedureDocumentChecklist` يكتب السلسلة الحرفية `'0'` للوثيقة غير المحدَّدة (بدل حذف المفتاح)، وهي سلسلة غير فارغة (truthy)، كان هذا سيُظهر أي وثيقة "غير جاهزة" كُتِبت مسبقاً عبر `ProcedureDocumentChecklist` على أنها "جاهزة" خطأً في الشريط. أُصلِح الفحص ليطابق `=== '1'` الصريح المستخدم في كل مكان آخر.

**ملاحظة**: مكوّن ثالث (`ReadinessChecker`، بمفتاح `dalilak_ready_enr-{code}` مختلف كلياً) معروض أيضاً في نفس القسم — تُرِك خارج نطاق هذا الإصلاح لأنه يبدو مكوّناً منفصلاً بتصميم مقصود (نفس البادئة `enr-` تُستخدَم أيضاً في `ProcedureTimeline` لتتبّع الخطوات)، وليس تكراراً عرَضياً — يُرصَد هنا للمراجعة المستقبلية إن رغب المستخدم بدمج/تبسيط قسم المستندات بالكامل.

`tsc --noEmit` نظيف. إصلاح محصور بالكامل داخل `components/ProcedureDocReadinessBar.tsx`.

### تحديث (batch #455) — نفس فئة الخلل، لكن لخطوات الإجراء بدل وثائقه: `ProcedureStepHighlight` و`ProcedureStepProgress`

**المشكلة**: يُعرَض `ProcedureStepHighlight` و`ProcedureStepProgress` في نفس قسم "الخطوات" لكل إجراء موسَّع، بنفس قائمة الخطوات تماماً (`steps`)، لكن كل مكوّن يحتفظ بحالة "تم إنجاز هذه الخطوة" في مفتاح `localStorage` مختلف: `ProcedureStepHighlight` كانت تستخدم `dalilak_step_done_{code}_{idx}`، بينما `ProcedureStepProgress` تستخدم `dalilak_step_check_{code}_{idx}`. تحقّقتُ عبر `grep` أن كل بادئة لا تُقرأ ولا تُكتَب إلا داخل ملفها الخاص — لا يوجد أي مستهلك مشترك بينهما.

**أثرها على المستخدم**: تحديد خطوة كمُنجزة في أحد المكوّنين لا ينعكس إطلاقاً في المكوّن الآخر المعروض مباشرة بجانبه/أسفله لنفس الإجراء — تماماً نفس النمط الذي أُصلِح للوثائق في batch #454 (`ProcedureDocReadinessBar` و`ProcedureDocumentChecklist`)، لكن لم يُلاحَظ حينها أنه يتكرر أيضاً في نظام "الخطوات" المنفصل.

**الإصلاح**: تم توحيد `ProcedureStepHighlight.tsx` على مخطط `dalilak_step_check_{code}_{idx}` (نفس مخطط `ProcedureStepProgress.tsx`)، مع تحديث التعليقات التوضيحية في كلا الملفين لتوثيق الترابط ومنع تكرار هذا الخطأ مستقبلاً. لا حاجة لإصلاح truthy-check إضافي هنا: كلا المكوّنين يستخدمان بالفعل `removeItem` (لا `'0'`) لحالة "غير منجز"، فالفحص `!!localStorage.getItem(...)` متوافق تماماً بين الاثنين.

**ملاحظة**: `ProcedureStepTimer.tsx` (بادئة `dalilak_step_timer_{code}_{i}`) معروض أيضاً في نفس المنطقة لكنه يتتبّع مفهوماً مختلفاً (وقت بدء العدّ التنازلي لكل خطوة، وليس حالة إنجاز)، فلم يُشمَل في هذا الإصلاح.

`tsc --noEmit` نظيف. إصلاح محصور بملفين: `components/ProcedureStepHighlight.tsx` (تغيير المخطط) و`components/ProcedureStepProgress.tsx` (تعليق توضيحي فقط، بلا تغيير منطقي).

### تحديث (batch #456) — ثالث ودّاة معطّلة في نفس قسم "المستندات المطلوبة": `ReadinessChecker` كانت تتبع مخططاً ثالثاً منفصلاً تماماً

**المشكلة**: عند فحص قسم "المستندات المطلوبة" للإجراءات الموسَّعة (`ENRICHED_PROCEDURES`) في `app/procedures/page.tsx` (الأسطر 743-785)، تبيّن أن هناك **ثلاثة** مكوّنات تتيح للمستخدم "تحديد كل وثيقة جهّزتها"، وليس اثنين فقط كما ظنَّت batch #454: `ReadinessChecker` (السطر 744، أول عنصر في القسم) و`ProcedureDocReadinessBar` (753) و`ProcedureDocumentChecklist` (759) — الثلاثة لنفس قائمة الوثائق (`proc.requiredDocuments`) ونفس الإجراء (`proc.code`). بعد إصلاح batch #454، أصبح آخر اثنين متزامنَين على مخطط `dalilak_doc_{code}_{index}` — لكن `ReadinessChecker` بقيت تكتب/تقرأ من مفتاح منفصل كلياً: `dalilak_ready_enr-{code}` بصيغة JSON array لكل الفهارس المحدَّدة معاً (`JSON.stringify(Array.from(checked))`)، وليس مفتاحاً لكل وثيقة.

**أثرها على المستخدم**: تحديد وثيقة كجاهزة في أول أداة (`ReadinessChecker`، وتعرض أيضاً "🎉 أنت جاهز تماماً!" عند الاكتمال) لا ينعكس إطلاقاً في الأداتين المعروضتين مباشرة أسفلها لنفس القائمة، ولا في زر مشاركة واتساب/تصدير PDF اللذين يقرآن من مخطط `dalilak_doc_`. ثلاث حالات "جاهزية" منفصلة معروضة في نفس البطاقة الموسَّعة لنفس المستخدم.

**الإصلاح**: لأن `ReadinessChecker` مُستخدَم أيضاً في مكانين آخرين (صفحة `/services` بمفتاح `svc-{slug}`، وقسم الإجراءات الأساسية `PROCEDURES_DATA` بمفتاح `proc-{slug}`) حيث لا يوجد أي تعارض أو ودّاة أخرى، لم يكن تغيير مخطط التخزين الداخلي للمكوّن بالكامل مناسباً (قد يفقد بيانات المستخدمين المحفوظة في تينك الحالتين دون داعٍ). بدلاً من ذلك، أُضيف للمكوّن prop اختياري جديد `docKeyPrefix?: string`: عند تمريره، يقرأ/يكتب المكوّن حالة كل وثيقة كمفتاح منفصل (`{docKeyPrefix}_{index}` → `'1'`/`'0'`) بدل المفتاح الواحد بصيغة JSON — وهو نفس مخطط `dalilak_doc_{code}_{index}` المتّفق عليه. عند التبديل، يُطلَق أيضاً حدث `dalilak_saved_change` ليتزامن مع بقية الأدوات فوراً. استُخدِم هذا الـ prop فقط في موقع استدعاء الإجراءات الموسَّعة (`docKeyPrefix={`dalilak_doc_${proc.code}`}`)، دون المساس بموقعَي الاستدعاء الآخرين.

`tsc --noEmit` نظيف. إصلاح في ملفين: `components/ReadinessChecker.tsx` (إضافة الخيار الاختياري) و`app/procedures/page.tsx` (تفعيله في موقع استدعاء واحد فقط).

### تحديث (batch #457) — تذكيرات محفوظة لكن لا تظهر أبداً: `SmartReminder` (العرض الوحيد للتذكيرات) غير مُستخدَم في أي مكان حي

**المشكلة**: مكوّنان حيّان — `ProcedureRemindMeLater.tsx` (يظهر في كل بطاقة إجراء موسَّعة) و`ProcedureReminderBell.tsx` (أيضاً في كل بطاقة) — يسمحان للمستخدم بإضافة تذكير يُحفَظ في `localStorage['dalilak_reminders']` (نفس المخزن الذي يستوردان منه `getReminders` من `SmartReminder.tsx`). لكن المكوّن الوحيد في التطبيق بأكمله الذي يقرأ هذا المخزن *ويعرض* التذكيرات المستحقة/المتأخرة (المكوّن الافتراضي `SmartReminder` نفسه) غير مستورَد في أي مكان تحت `app/` — تحقّقتُ عبر `grep -rn "SmartReminder" app --include="*.tsx"` الذي لم يُرجِع أي نتيجة. كان هذا مقصوداً: أُزيل استدعاء `SmartReminder` من الصفحة الرئيسية أثناء تنظيف مكدّس الودجات (موثَّق سابقاً في هذا الملف)، لكن الملف نفسه أُبقي عليه "لتصميم مستقبلي" — بينما ظلّ الكاتبان الحيّان يستوردان منه ويكتبان بياناتهما بلا أي قارئ حي.

**أثرها على المستخدم**: يضغط المستخدم "ذكّرني لاحقاً" أو يضبط تذكيراً عبر الجرس على أي إجراء، ويرى تأكيد الحفظ — لكن لا شيء في التطبيق الحي يتحقّق لاحقاً مما إذا كان التذكير مستحقاً أو متأخراً. البيانات تبقى JSON خامل في `localStorage` بلا أي إشعار فعلي على الإطلاق.

**الإصلاح**: بدل استعادة `SmartReminder` كواجهة منفصلة (ستكرّر تكديس الودجات الذي أُزيل عمداً سابقاً)، أُضيف مصدر تنبيه ثالث إلى `NotificationBell.tsx` — الجرس الحي الوحيد الذي يعرض فعلاً تنبيهات "انتهاء صلاحية المستندات" و"المواعيد القريبة" في نفس القائمة المنسدلة. أضفتُ استدعاء `getReminders()` (المُصدَّرة أصلاً من `SmartReminder.tsx`) داخل `loadItems()`، بنفس تعريف "مستحق" الذي يستخدمه `SmartReminder` (`!dismissed && date <= today`، بتوقيت بيروت)، ونوع عنصر جديد `'reminder'` مع تسمية "تذكير اليوم"/"تذكير متأخر" في القائمة. لم يتغيّر أي منطق قائم في `SmartReminder.tsx` أو الكاتبين — فقط أُضيف قارئ حي جديد لنفس البيانات.

`tsc --noEmit` نظيف. إصلاح محصور بـ`components/NotificationBell.tsx`.

### تحديث (batch #458) — عكس مشكلة batch #457: كتابة حية بلا أي قارئ على الإطلاق، لميزة "شوهدت مؤخراً" التي أُزيلت واجهتها عمداً سابقاً

**المشكلة**: `lib/savedItems.ts` يحتوي نظاماً فرعياً كاملاً لـ"شوهدت مؤخراً" (`trackView`/`loadRecentItems`، يكتب إلى `localStorage['dalilak_recently_viewed']`)، وكان يُستدعى فعلياً من مكانين حيّين: عند توسيع أي بطاقة إجراء في `app/procedures/page.tsx` وعند الضغط على أي بطاقة خدمة في `app/services/page.tsx`. لكن المكوّن الوحيد الذي كان يفترض أن يعرض هذه البيانات، `RecentlyViewedPanel.tsx`، كان قد أُزيل عمداً من قبل ضمن عملية تنظيف واجهة الصفحة الرئيسية من الودجات الزائدة (قائمة الملفات الميتة الكبيرة في batch #386 من `push.sh`). النتيجة: عكس مشكلة batch #457 تماماً — هنا كاتب حي بلا أي قارئ إطلاقاً (بدل قارئ حي بلا كاتب)، يكتب JSON بصمت في `localStorage` ويُطلِق حدثاً (`dalilak_recent_change`) لا يستمع إليه أحد، مع كل ضغطة من المستخدم.

**الفرق عن قرار batch #457**: في التذكيرات (batch #457)، اخترتُ إضافة قارئ جديد لأن الميزة (تنبيهات تذكير) لم تُحذف عمداً قط — بل أُبقي المكوّن "لتصميم مستقبلي". أما هنا، فإزالة `RecentlyViewedPanel` كانت قراراً مقصوداً موثَّقاً ضمن عملية تنظيف شاملة لواجهة المستخدم (إزالة ~49 ملفاً معتبَراً "ازدحاماً بصرياً"). إعادة إحياء الميزة بمكوّن جديد يتعارض مع قرار سابق مقصود، بينما استكمال حذف الكود اليتيم (المُنتِج بلا أي فائدة ظاهرة للمستخدم) هو الخيار الأصح عالمياً وينسجم مع القرار الأصلي.

**الإصلاح**: أُزيلت استدعاءات `trackView(...)` من موقعي الاستدعاء الحيّين (`app/procedures/page.tsx`، `app/services/page.tsx`) مع أسطر الاستيراد المرتبطة، وأُزيل الكود اليتيم بالكامل (`RECENT_KEY`, `RecentItem`, `loadRecentItems`, `trackView`) من `lib/savedItems.ts` مع تعليق توضيحي يشرح السبب والقرار.

`tsc --noEmit` نظيف. إصلاح في ثلاثة ملفات: `lib/savedItems.ts`، `app/procedures/page.tsx`، `app/services/page.tsx`. لا تأثير وظيفي على أي ميزة مرئية للمستخدم (لم يكن أي شيء يعرض هذه البيانات أصلاً).

### تحديث (batch #459) — زر "حفظ" حي على صفحتين رئيسيتين، لكن لا مكان يعرض ما حفظه المستخدم

**المشكلة**: `SaveButton` (زر المحفوظات) حيّ ويُستخدَم فعلياً في `app/procedures/page.tsx` و`app/services/page.tsx` — يكتب إلى `localStorage['dalilak_saved_items']` عبر `lib/savedItems.ts` (`saveItem`/`unsaveItem`/`isSaved`)، ويعرض بنفسه حالة "محفوظ" الصحيحة عند إعادة زيارة نفس البطاقة. لكن المكوّن الوحيد الذي يفترض أن يعرض *قائمة* كل ما حفظه المستخدم مجمَّعاً في مكان واحد، `SavedItemsPanel.tsx`، غير مثبَّت في أي مكان حي — تحقّقتُ عبر `grep -rn "SavedItemsPanel" app --include="*.tsx"` الذي لم يُرجِع أي نتيجة. الأرجح أن هذا فُقِد أثناء إعادة بناء الصفحة الرئيسية بتصميم v4.0 (chat-first)، إذ كانت لا تزال قيد التطوير والتحسين (إصلاحات نقاط اللمس) قبل عملية حذف كتلة الودجات الميتة في batch #317/#346 مباشرة.

**أثرها على المستخدم**: يضغط المستخدم "حفظ" على إجراء أو خدمة، ويرى تأكيد الحفظ ورمز الإشارة المرجعية يتحوّل للون الممتلئ — لكن لا توجد صفحة أو قائمة واحدة تجمع كل ما حفظه ليعود إليه لاحقاً. القيمة الوحيدة المتبقية هي أن نفس الزر على نفس البطاقة يتذكّر حالته إذا عاد المستخدم لنفس الصفحة تحديداً.

**القرار**: الصفحة الرئيسية الحالية (v4.0) مصمَّمة عمداً بحدّ أدنى من العناصر (شريط بحث + صفّان من الإجراءات السريعة + قائمة فئات) بميزانية ارتفاع محسوبة بدقة موثَّقة في التعليقات — إضافة `SavedItemsPanel` هناك تخاطر بمخالفة هذا التصميم المدروس. بدلاً من ذلك، رُكِّب المكوّن (كما هو، بلا أي تعديل) في `app/my-files/page.tsx` — الصفحة المخصَّصة أصلاً لـ"ملفاتي المحفوظة" — قبل قسم قائمة المعاملات المرتبطة بالخادم. المكوّن يعرض `null` تلقائياً إن لم يحفظ المستخدم أي شيء، فلا أثر إطلاقاً على من لم يستخدم زر الحفظ من قبل.

`tsc --noEmit` نظيف. إصلاح في ملف واحد (`app/my-files/page.tsx`) — استيراد وتركيب مكوّن موجود مسبقاً وجاهزاً بالكامل، بلا أي تعديل على منطقه.

### تحديث (batch #460) — زر "حفظ" على ردود المحادثة يعد بحفظ لا يتحقّق أبداً

**المشكلة**: `ChatMessageActions.tsx` (حيّ، تحت كل رد من الذكاء الاصطناعي في المحادثة الرئيسية) يحتوي زر "حفظ/Save" يكتب النص إلى `localStorage['dalilak_bookmarks']` عبر `addBookmark()`، ويعرض حالة "محفوظ/Saved" مؤقتة لثانيتين. لكن: (أ) لا يوجد أي مكان في التطبيق يقرأ `getBookmarks()` أو يستمع لحدث `dalilak_bookmarks_change` — تحقّقتُ عبر `grep` أن كل الاستخدامات محصورة داخل الملف نفسه؛ (ب) الأخطر: زر "حفظ" على الرسالة نفسها لا يتذكّر أنه محفوظ إن أعاد المستخدم فتح الرد لاحقاً — الحالة المؤقتة (`state === 'bookmarked'`) لا تُشتَقّ أبداً من `localStorage` عند إعادة العرض، خلافاً لـ`SaveButton` (الذي يستدعي `isSaved()` عند التركيب). فحتى القيمة الجزئية التي بقيت لمشكلة batch #459 (تذكّر الحالة على نفس البطاقة) غير موجودة هنا.

**أثرها على المستخدم**: يضغط المستخدم "حفظ" على رد مفيد، يرى "تم الحفظ" لثانيتين، ثم لا يبقى أي أثر مرئي على الإطلاق — لا قائمة، ولا حتى تذكّر الحالة على نفس الزر.

**الإصلاح**: بما أن هذه الميزة (خلافاً لحالة batch #458) لا يوجد أي دليل تاريخي على إزالتها عمداً — لم تُذكَر في أي قائمة تنظيف سابقة — عُومِلت وفق سابقة batch #457/#459: أُضيف قسم مدمج "الردود المحفوظة" في `app/my-files/page.tsx` (بعد قسم `SavedItemsPanel` المُضاف في batch #459 مباشرة)، يستدعي `getBookmarks()`/`removeBookmark()` المُصدَّرتين أصلاً من `ChatMessageActions.tsx`، ويعرض كل رد محفوظ (مقتطف 3 أسطر) مع زر إزالة، ويتزامن عبر `dalilak_bookmarks_change`. لم يُعدَّل أي منطق داخل `ChatMessageActions.tsx` نفسه.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بـ`app/my-files/page.tsx` (استيراد + state + قسم عرض جديد).

### تحديث (batch #461) — `ChatHistoryPanel`: تُحفَظ حتى 5 محادثات سابقة، لكن لا واجهة لاستعراضها أو استعادتها

**المشكلة**: `saveChatSession()` (من `components/ChatHistoryPanel.tsx`) تُستدعى فعلياً في ثلاثة مواضع حية في `app/page.tsx` (`onNewChat`، `onHomeClick`، `onHome`) — تحفظ حتى 5 محادثات سابقة (تاريخ، عدد رسائل، مقتطف، حتى 40 رسالة مختصرة) في `localStorage['dalilak_chat_sessions']`. لكن المكوّن الافتراضي `ChatHistoryPanel` (الذي يعرض قائمة "المحادثات السابقة" مع أزرار استعادة/حذف، ويقرأ عبر `getChatSessions()`) لم يكن مثبَّتاً في أي مكان — تحقّقتُ عبر `grep -rn "ChatHistoryPanel" app components --include="*.tsx"` الذي أظهر فقط الاستيراد المسمّى `{ saveChatSession }` في `app/page.tsx`، بلا أي استخدام JSX `<ChatHistoryPanel`. ملاحظة: هذا مختلف تماماً عن آلية أخرى موجودة فعلاً في نفس الملف (`CHAT_HISTORY_KEY = 'dalilak:chat:history'`) التي تستعيد تلقائياً *آخر* محادثة نشطة عند إعادة تحميل الصفحة — `ChatHistoryPanel` ميزة إضافية منفصلة: تصفّح واستعادة محادثات *سابقة متعددة* غادرها المستخدم عمداً.

**أثرها على المستخدم**: كل مرة يبدأ المستخدم محادثة جديدة أو يضغط "الرئيسية" من محادثة نشطة، تُحفَظ رسائله بصمت (حتى 40 رسالة) في `localStorage` — بيانات قد تكون حسّاسة لأنها محادثة حول معاملات حكومية شخصية — دون أي طريقة لرؤيتها أو استعادتها أو حتى حذفها لاحقاً عبر الواجهة.

**الإصلاح**: رُكِّب `<ChatHistoryPanel onRestore={...} />` في شاشة الترحيب (v4.0) مباشرة أسفل زرَي الإجراءات السريعة، مع ربط `onRestore` باستدعاء `setMessages()` لاستعادة المحادثة المختارة. المكوّن يعرض `null` تلقائياً عند عدم وجود محادثات محفوظة، فلا يخالف الحد الأدنى المتعمَّد لتصميم شاشة الترحيب لأي مستخدم لم يترك بعد محادثة من 3+ رسائل. لم يتغيّر أي منطق داخل `ChatHistoryPanel.tsx` نفسه أو مواضع استدعاء `saveChatSession`.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بـ`app/page.tsx` (استيراد المكوّن الافتراضي + تركيبه في موضع واحد).

### تحديث (batch #462) — `ChatDraftAutosave` تعمل على الحقل الخطأ في السيناريو الذي صُمِّمت له بالذات

**المشكلة**: `ChatDraftAutosave.tsx` (حيّ، مركَّب على حقل إدخال المحادثة النشطة عبر `input`/`setInput` في `app/page.tsx:2280`) موثَّقة صراحة بأنها "تحفظ ما يكتبه المستخدم بحيث ينجو من تحديث الصفحة أو إغلاق التبويب بالخطأ". لكن حالة `messages` في `app/page.tsx` غير محفوظة أبداً في أي تخزين (`useState<Message[]>([])` تبدأ فارغة دوماً)، فبعد أي تحديث للصفحة تُعرَض دائماً "شاشة الترحيب" (حين `messages.length === 0`) — وهذه الشاشة لها حالة إدخال منفصلة تماماً: `heroInput`/`setHeroInput`، غير مرتبطة بـ`ChatDraftAutosave` إطلاقاً. بما أن هذا المكوّن مركَّب فقط داخل عرض المحادثة النشطة (الذي لا يظهر أبداً مباشرة بعد تحديث الصفحة)، فإن منطق الاستعادة فيه لا يعمل أبداً في السيناريو الذي كُتِب من أجله بالضبط.

**أثرها على المستخدم**: يكتب المستخدم سؤالاً في شاشة الترحيب دون إرساله، يحدّث الصفحة أو يغلق التبويب بالخطأ ويعيد فتحه — النص المكتوب يُفقَد بصمت، رغم أن آلية الحفظ التلقائي موجودة وتعمل بشكل صحيح تقنياً (تكتب فعلياً إلى `localStorage['dalilak_chat_input_draft']`)، لكن لا شيء يقرأها في المكان الصحيح.

**الإصلاح**: بما أن شاشتي "الترحيب" و"المحادثة النشطة" لا تُعرَضان أبداً في آن واحد (تفرّع ternary واحد)، رُكِّب نفس المكوّن (بلا أي تعديل على منطقه) مرة ثانية في شاشة الترحيب، مربوطاً بـ`heroInput`/`setHeroInput` بدل `input`/`setInput` — كلا الموضعين يتشاركان نفس مفتاح `localStorage`، فتُستعاد المسودة أياً كانت الشاشة الظاهرة عند إعادة التحميل.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بـ`app/page.tsx` (تركيب إضافي لمكوّن موجود، بلا أي تعديل على `components/ChatDraftAutosave.tsx`).

### تحديث (batch #463) — مكوّنان جاهزان بالكامل لكن غير مُركَّبين (orphaned)، مع تعارض تصميمي حقيقي يمنع الحل الفوري

**الاكتشاف**: `components/ProcedureOfTheWeek.tsx` (183 سطر، "إجراء الأسبوع" — بطاقة مميزة تدور أسبوعياً على `ENRICHED_PROCEDURES`) و`components/SmartSuggestions.tsx` (223 سطر، صف رقاقات "اقتراحات ذكية" مبني على الشهر/يوم الأسبوع الحاليين) كلاهما مكوّن مكتمل تماماً، بلا أي TODO، يعتمد على بيانات حقيقية موجودة فعلاً في `lib/enrichedProcedures.ts` — لكن `grep -rn "ProcedureOfTheWeek\|SmartSuggestions" app components --include="*.tsx"` أظهر صفر نتائج خارج تعريف كل ملف بذاته؛ أي أنهما غير مستوردين ولا مُركَّبين في أي مكان يصل إليه المستخدم فعلياً. (`SmartInputSuggestions.tsx` مكوّن مختلف تماماً وحيّ فعلاً — لا علاقة له بهذا الاكتشاف.) كلاهما ظهر في قائمة "Task" قديمة كمُنجَز (بُني فعلياً في وقت سابق) ولم يظهر أي منهما في قائمة الحذف `rm -f` الخاصة بدفعة #386، فلم يُقرَّر حذفهما عمداً — بل يبدو أنهما فُقِدا بصمت أثناء إعادة بناء الصفحة الرئيسية بالكامل إلى تصميم v4.0 "Calm Government Digital Service" (دفعة #366).

**لماذا لم يُطبَّق إصلاح مباشر هذه المرة**: كل الحالات المشابهة السابقة (#457، #459، #461) كانت لمكوّنات "تُظهِر بيانات المستخدم الحقيقية أو لا تُظهر شيئاً" (self-guard إلى `null` تماماً عند عدم وجود بيانات)، فتركيبها كان بلا أي أثر مرئي على أي مستخدم لم يتفاعل من قبل. أما هذان المكوّنان فيعرضان دائماً محتوى مرئياً كاملاً (بطاقة/صف رقاقات) لكل زائر بلا استثناء — وهذا يتعارض مباشرة مع تعليق موثّق صراحة في `app/page.tsx` (السطور ~1679-1693) يصف ترتيب أقسام الصفحة الرئيسية v4.0 كعقد تصميم حرفي ونهائي: "Header → title → helper text → search → quick actions → popular → categories → one small trust line → simple footer → bottom nav — **explicitly no** hero banner, no 'how it works' explainer, no life-journey grid, no multi-card trust section على هذه الشاشة." إضافة أي قسم مرئي جديد، ولو صغيراً، تخالف هذا العقد الذي بُني بعد عشرات الدفعات من تبسيط الصفحة (#350، #354، #355 وغيرها قلّصت المحتوى عمداً، لم تُضِفه).

**الخيارات المطروحة (بلا حسم من طرف واحد لأنها قرار منتج/تصميم)**:
1. تركيب المكوّنين على الصفحة الرئيسية رغم مخالفة العقد الموثّق — يُعيد ميزتين مبنيتين لكنه يتعارض مع قرار تبسيط واضح ومتكرر.
2. حذف الملفين بوصفهما "ميزة فائضة عن تصميم لاحق" (بنفس منطق #458) — لكن بخلاف #458، لا يوجد أي دليل تاريخي على قرار إزالة متعمّد لهذين المكوّنين تحديداً (لم يردا في قائمة #386، ولا في أي ملاحظة تفكيك أخرى)، فحذفهما هنا افتراض غير مؤكَّد.
3. (الأقرب للتوفيق) دمج بياناتهما (المعاملة المميزة أسبوعياً، الاقتراحات الموسمية) داخل نقطة وصول موجودة أصلاً ومُتاحة فعلاً (كما فعل تعليق دفعة #366 نفسه مع `LIFE_JOURNEYS` عبر `GlobalSearch`/`MobileMenu`) — بدل إضافة قسم مرئي جديد على الصفحة الضيقة عمداً.

بلا تعديل على أي كود هذه الدفعة — تدقيق واكتشاف فقط، موثَّق هنا ليُحسَم لاحقاً بقرار واضح (مثل سوابق #420/#421/#435/#390) بدل فرض خيار قد يُعاكس قرار تصميم متعمَّد.

### تحديث (batch #464) — نصوص عربية ثابتة في نافذة "إنشاء محتوى جديد" ولوحة "تغيير الحالة" بصفحة إدارة المحتوى

**المشكلة**: `app/admin/content/page.tsx` صفحة حيّة وثنائية اللغة فعلياً (مربوطة من `app/admin/page.tsx:303`، وتستخدم `isAr` أكثر من 28 مرة للعناوين والأزرار وaria-label والتواريخ) — لكن 10 نصوص واجهة داخل لوحة "تغيير الحالة" ونافذة "إنشاء محتوى جديد" كانت عربية ثابتة بلا أي تفرّع `isAr`: عنوان "تغيير الحالة"، aria-label والـ placeholder لحقل ملاحظة التحويل، وفي النافذة: تسميات "العنوان"/"النوع"/"المحتوى"، الـ placeholders، aria-label نوع المحتوى، وخيارات القائمة المنسدلة الخمسة (تحديث إجراء / إجابة شائعة / معلومات جهة / دليل نموذج / إعلان). دفعة سابقة (#417) وثّقت أن هذه الصفحة "ثنائية اللغة بالكامل، فقط حقول البيانات title_ar/body_ar عربية بتصميم مقصود" — لكن هذا التحقق فاتته نصوص الواجهة (labels/placeholders/aria-labels/options) نفسها، وهي مختلفة عن حقول البيانات.

**الأثر**: مسؤول يبدّل التطبيق للإنجليزية (تبديل حيّ ومستخدم في بقية هذه الصفحة نفسها) يرى نافذة "Create new content" ولوحة "Change status" بالكامل بالعربية — كل تسمية وplaceholder وaria-label وخيار قائمة — بينما بقية عناصر الصفحة (الهيدر، بطاقات pipeline، القائمة، التواريخ) تتحوّل صحيحاً للإنجليزية.

**الإصلاح**: تفرّع كل النصوص العشرة على `isAr` بنفس نمط الاستخدام المتكرر 28 مرة في الملف نفسه (`{isAr ? '...' : '...'}`). لم تُمَس حقول البيانات الفعلية (`title_ar`/`body_ar`) لأنها محدودة عمداً للعربية حسب توثيق #417، وهذا ليس خللاً.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بملف واحد (`app/admin/content/page.tsx`).

### تحديث (batch #465) — JourneySheet نافذة حوار بلا فخّ تركيز (focus trap)، غابت عن تدقيق batch #360

**المشكلة**: `JourneySheet` (مُعرَّفة داخل `app/page.tsx`، وليست ملفاً مستقلاً) هي نافذة `role="dialog" aria-modal="true"` حقيقية وحيّة — تُعرَض من `TopNav` و`MobileMenu` عبر `onJourneySelect` → `getJourneyBySlug()` من `lib/lifeJourneys.ts` (8 رحلات حياتية حقيقية) → `setActiveJourney` → `{activeJourney && <JourneySheet .../>}`. لكن دفعة سابقة (#360/#361) طبّقت `useFocusTrap` على كل مكوّنات `role="dialog"` في التطبيق (13 مكوّناً موثّقة بالاسم في تعليق `lib/useFocusTrap.ts`) دون أن تشمل `JourneySheet` — على الأرجح لأنها معرّفة inline داخل `app/page.tsx` بدل ملف مكوّن مستقل، فتفلّتت من تدقيق قائم على أسماء الملفات. `grep -n "useFocusTrap" app/page.tsx` أظهر صفر نتائج قبل هذا الإصلاح.

**الأثر**: مستخدم لوحة مفاتيح يفتح نافذة رحلة حياتية (مثلاً من القائمة الجانبية) ويضغط Tab يخرج من النافذة المفتوحة إلى عناصر الصفحة المخفية خلف الطبقة الداكنة — بالضبط الفجوة في WCAG 2.4.3 التي أُصلحت في كل نوافذ الحوار الأخرى بالتطبيق.

**الإصلاح**: أُضيف `useRef<HTMLDivElement>` و`useFocusTrap(dialogRef, true)` إلى `JourneySheet` بنفس النمط المستخدم في `ServiceGroupSheet.tsx`/`MobileModeSheet.tsx` (النافذة تُركَّب فقط أثناء الفتح، لذا `active` تبقى `true` دائماً)، وربط `ref={dialogRef}` بعنصر الـ`div` صاحب `role="dialog"`.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بـ`app/page.tsx` (استيراد + مكوّن `JourneySheet` فقط، بلا تعديل على `lib/useFocusTrap.ts`).

### تحديث (batch #466) — نافذتا إدارة (تعديل مستخدم + إنشاء محتوى) بلا فخّ تركيز، نفس فجوة #465

**المشكلة**: مثل `JourneySheet` (#465)، نافذتان إداريتان حيّتان أخريان معرّفتان inline داخل ملفات صفحاتهما (لا ملف مكوّن مستقل) أفلتتا من تدقيق batch #360/#361 القائم على أسماء المكوّنات:
- `app/admin/page.tsx` — نافذة "تعديل مستخدم" (`{editUser && (...)}`، تُفتح من زر التعديل في جدول المستخدمين).
- `app/admin/content/page.tsx` — نافذة "إنشاء محتوى جديد" (`{showCreate && (...)}`).

كلاهما `role="dialog" aria-modal="true"` بمحتوى تفاعلي حقيقي (حقول نص، قوائم منسدلة، أزرار حفظ/إلغاء)، لكن `grep -n "useFocusTrap|dialogRef"` في كلا الملفين لم يُظهر أي نتيجة قبل هذا الإصلاح.

**الأثر**: مستخدم لوحة مفاتيح يفتح إحدى النافذتين ويضغط Tab يخرج من النافذة المفتوحة إلى عناصر الصفحة المخفية خلف الطبقة الداكنة (فجوة WCAG 2.4.3، مطابقة تماماً لما أُصلح في كل نافذة حوار أخرى بالتطبيق).

**الإصلاح**: بنفس نمط #465 تماماً — إضافة `useRef<HTMLDivElement>` و`useFocusTrap(ref, isOpenCondition)` في كلا الملفين، وربط `ref` بعنصر الـ`div` صاحب `role="dialog"`. لم تُمَس أي منطقة أخرى.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بملفين (`app/admin/page.tsx`, `app/admin/content/page.tsx`).

### تحديث (batch #467) — آخر نافذة بلا فخّ تركيز: ServiceSheet في صفحة /services

**المشكلة**: نفس فجوة الدفعتين #465/#466 بالضبط: `ServiceSheet` (مُعرَّفة inline داخل `app/services/page.tsx`، لا ملف مستقل) نافذة `role="dialog" aria-modal="true"` حيّة تُعرَض عند اختيار أي خدمة من قائمة `/services` (استدعاء `<ServiceSheet .../>` موجود في الملف نفسه)، لكن `grep -c useFocusTrap app/services/page.tsx` أعطى صفراً قبل الإصلاح.

**التحقق الشامل**: بعد إصلاح هذه الدفعة، أُجري مسح كامل على كل ملفات المستودع التي تحوي `role="dialog"` (16 ملفاً) للتأكد من عدم وجود أي فجوة متبقية — كل ملف الآن يحوي استدعاء `useFocusTrap` مطابقاً. سلسلة "نوافذ الحوار بلا فخّ تركيز" (batches #360/#361 + #465 + #466 + #467) مكتملة الآن على مستوى التطبيق بالكامل.

**الإصلاح**: نفس النمط المستخدم في #465/#466 — `useRef<HTMLDivElement>` + `useFocusTrap(dialogRef, true)`، مع الإبقاء على تأثير `closeRef.current?.focus()` الموجود مسبقاً (غير متعارض؛ `useFocusTrap` ينقل التركيز لأول عنصر قابل للتركيز عند الفتح، وهو نفسه زر الإغلاق هنا).

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بـ`app/services/page.tsx`.

### تحديث (batch #468) — خطأ نحوي + تناقض داخلي في مدة رحلتين حياتيتين (lib/lifeJourneys.ts)

**ملاحظة منهجية**: بحث أولي هذه الدفعة اقترح إصلاح رقم `formsCount` لوزارة الاقتصاد في `lib/allTransactions.ts` (571 بدل 572 محسوبة)، لكن `grep -rn "formsCount"` عبر كل ملفات `.tsx` في المشروع أظهر أن هذا الحقل **لا يُقرأ في أي مكان إطلاقاً** (لا في `app/forms/page.tsx` ولا `app/procedures/page.tsx`، اللذين يستوردان `TX_MINISTRIES` لأغراض أخرى فقط) — فتصحيحه ليس له أي أثر مرئي على أي مستخدم حقيقي. اسْتُبعِد هذا الإصلاح لعدم وجود أثر حيّ، والتُقِطت مشكلة أخرى بأثر مستخدم حقيقي بدلاً منها.

**المشكلة الفعلية**: في `lib/lifeJourneys.ts`، حقلا `totalEstAr` لرحلة "اشتريت سيارة" (`vehicle-registration`, السطر 604) و`estAr` لخطوة "تجديد بطاقة الهوية" ضمن رحلة "مغترب يريد تجديد وثائقه" (`residency-expat`, السطر 712) يستخدمان صيغة "١–٢ أسبوعان" — وهذا خطأ نحوي (المثنى "أسبوعان" حالة رفع، بينما موقعه بعد رقم/نطاق يستوجب حالة نصب/جر "أسبوعين"، وحتى الشكل الصحيح نحوياً غير مناسب هنا لأن العبارة نطاق "١–٢" وليست عدداً مضبوطاً "٢") **وتناقض داخلي حقيقي**: نفس رحلة "اشتريت سيارة" لديها `subtitleAr` (السطر 600) يصف **نفس المدة الزمنية بالضبط** بالصيغة الصحيحة "١–٢ أسابيع" (جمع)، بينما `totalEstAr` بجوارها مباشرة يصفها بصيغة خاطئة مختلفة — تناقض واضح بين حقلين لنفس القيمة في نفس الكائن.

**التحقق من الحيّوية**: كلا الحقلين يُعرضان فعلياً في `JourneySheet` بـ`app/page.tsx` — `totalEstAr` في السطر 383 (`⏱ {isAr ? journey.totalEstAr : journey.totalEstEn}`) و`estAr` على مستوى الخطوة في السطر 498 (`⏱ {isAr ? step.estAr : step.estEn}`)، وكلاهما يظهر لأي مستخدم يفتح رحلة "اشتريت سيارة" أو خطوة "تجديد بطاقة الهوية" في رحلة "مغترب".

**الإصلاح**: توحيد الصيغة إلى "١–٢ أسابيع" (جمع) في كلا الموضعين، مطابقةً للاتفاقية المستخدمة باستمرار في نفس الملف لكل نطاق زمني مشابه (مثل "٢–٦ أسابيع"، "٣–٨ أسابيع") وللحقل الشقيق `subtitleAr` ضمن نفس كائن الرحلة.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بملف واحد (`lib/lifeJourneys.ts`، تعديلان نصيّان فقط).

### تحديث (batch #469) — تناقض عددي حقيقي: رحلة "وُلد مولود" تقول للمستخدم مدتين مختلفتين لنفس الرحلة

**المشكلة**: رحلة "وُلد مولود" (`newborn`) في `lib/lifeJourneys.ts` لديها حقلان يصفان نفس المدة الزمنية الإجمالية للرحلة، لكنهما يتناقضان رقمياً: `subtitleAr`/`subtitleEn` (السطر 43-44) كانا يقولان "٣٠ يوماً"/"30 days"، بينما `totalEstAr`/`totalEstEn` (السطر 47-48) يقولان "٣–٤ أسابيع"/"3–4 weeks" — و٣-٤ أسابيع تساوي ٢١-٢٨ يوماً، أي أنها لا تساوي ٣٠ يوماً أبداً. تحقّق تقاطعي عبر كل الرحلات السبع الأخرى في نفس الملف (الزواج، فتح شركة، السفر، شراء عقار، الوفاة والإرث، شراء سيارة، تجديد وثائق المغترب) أكّد أن كل واحدة منها لديها `subtitleAr` يطابق `totalEstAr` تماماً — رحلة "وُلد مولود" هي الاستثناء الوحيد.

**التحقق من الحيّوية**: كلا الحقلين يُعرضان معاً في نفس لوحة رأس `JourneySheet` بـ`app/page.tsx` (`subtitleAr`/`subtitleEn` في السطر ~375، و`totalEstAr`/`totalEstEn` في السطر 383) — أي أن أي مستخدم يفتح بطاقة "وُلد مولود" يرى العبارتين المتناقضتين في نفس الشاشة مباشرة.

**الإصلاح**: توحيد `subtitleAr`/`subtitleEn` لتطابقا `totalEstAr`/`totalEstEn` ("٣–٤ أسابيع"/"3–4 weeks")، مطابقةً للاتفاقية المتّبعة في كل الرحلات الأخرى (استخدام صيغة الأسابيع في الحقلين معاً، لا الأيام).

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بملف واحد (`lib/lifeJourneys.ts`، تعديل نصّي واحد في كائن رحلة "وُلد مولود").

### تحديث (batch #470) — 4 بطاقات "جدول الرسوم" في /faq تعرض نص Python خام بدل الرسوم

**المشكلة**: `app/faq/page.tsx:293` لديه حارس (guard) هدفه إخفاء قيم `fees` الفاسدة (نصوص Python dict/list متبقية من تحويل JSON→TS قديم) — لكنه كان يتحقق فقط من `!item.fees.startsWith('{')`. دفعة سابقة (#411) وثّقت أنها تحققت من "9 حالات فاسدة" تبدأ بـ`{` وتركتها مخفية بنجاح، لكنها لم تفحص الحالات التي تبدأ بـ`[` (قائمة Python وليس قاموساً) — وهذه بالضبط حالة 4 سجلات: `fee_001` حتى `fee_004` (تصنيف "جدول الرسوم" 💰)، حيث `fees` يبدأ بـ`[{...}, {...}]`، فيمر الحارس الحالي (`!startsWith('{')` يُقيَّم `true`) ويُعرض النص الخام المقطوع بصيغته الغريبة (مثل `[{'service': 'جواز سفر عادي (5 سنوات)', 'fee_lbp': '4,900,000 ل.ل.'...`) مباشرة للمستخدم.

**لماذا هذا أسوأ من مجرد حافة نادرة**: هذه السجلات الأربعة من نوع `fee_table` وكل حقولها الأخرى فارغة عمداً (`summary:''`, `steps:[]`, `requiredDocuments:[]`, `authority:''`, `duration:''`) — أي أن `fees` هو المحتوى الوحيد الذي تعرضه البطاقة الموسّعة. وبما أن فئة "جدول الرسوم" بأكملها مخصصة لعرض الرسوم، فإن كل بطاقة فيها (4 من 4) تعرض نصاً غير قابل للقراءة بدل أي معلومة رسوم فعلية.

**التحقق من الحيّوية**: `SERVICE_FAQ` (التي تضم `fee_001-004` لأن `title` لديها غير فارغ) مستوردة ومعروضة في `app/faq/page.tsx`، وتغذّي `FAQ_CATEGORIES` التي تُظهر "جدول الرسوم" كفئة قابلة للنقر — أي أن البطاقات الأربع قابلة للوصول والتوسيع مباشرة من قبل أي مستخدم فعلي.

**الإصلاح**: توسيع الحارس ليرفض أي نص يبدأ بـ`{` أو `[`: `!/^[{[]/.test(item.fees.trim())` بدل `!item.fees.startsWith('{')`. لا تعديل على البيانات — فقط توسيع منطق الإخفاء الموجود مسبقاً ليغطي الحالة التي فاتته دفعة #411.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بسطر واحد في `app/faq/page.tsx`.

### تحديث (batch #471) — صفحة "دليل التنفيذ" (Playbook) تفشل في مطابقة البيانات المُثراة لمعظم الإجراءات

**المشكلة**: `app/procedures/[slug]/playbook/page.tsx:23` يبحث عن الإجراء داخل `ENRICHED_PROCEDURES` عبر `p.code === slug || p.code.includes(slug.replace(/-/g, ''))`. لكن `code` في `lib/enrichedProcedures.ts` (للسجلات ذات الصيغة المقروءة، مثل `interior-birth-certificate`) يبقى محتوياً على شرطات بين الكلمات، بينما `slug` القادم من `lib/procedures.ts` (مثل `birth-certificate`) كان يُجرَّد من شرطاته فقط هو قبل المقارنة — فتصبح `.includes('birthcertificate')` غير قابلة للتحقق أبداً داخل نص لا يزال يحوي `-` بين الكلمتين (`interior-birth-certificate`). النتيجة: المطابقة تعمل فقط للـ slug الوحيد أحادي الكلمة (`passport`)، وتفشل لكل slug متعدد الكلمات له `code` مطابق مفهومياً (تحقّق فعلي: `birth-certificate`، `building-permit`، `vehicle-registration`، `criminal-record`، `tax-registration` — كلها تفشل بهذا المنطق).

**الأثر**: عند فشل المطابقة، `proc` تصبح `undefined`، فـ: (أ) قسم "المستندات المطلوبة" في صفحة الـ Playbook يختفي بالكامل رغم ظهوره بنجاح في صفحة الإجراء الرئيسية لنفس الإجراء، و(ب) بيانات `flowchartSource` المُرسَلة لمولّد الخارطة بالذكاء الاصطناعي (الوزارة، الرسوم، مدة الإنجاز، المستندات، الوصف، الخطوات) تصبح جميعها `undefined`، فيفقد المولّد كل البيانات المنسَّقة يدوياً من دليلك ويعتمد فقط على استنتاج عام.

**التحقق من الحيّوية**: الزر الدائم الظهور "دليل التنفيذ — خارطة الإجراء" في `app/procedures/[slug]/ProcedureDetailClient.tsx:129` ينقل المستخدم مباشرة إلى `/procedures/${slug}/playbook` — أي أن هذه الصفحة قابلة للوصول من كل صفحة إجراء تقريباً.

**الإصلاح**: تجريد الشرطات من **الطرفين** (`code` و`slug` معاً) قبل المقارنة بدل طرف واحد فقط، عبر دالة `normSlug` صغيرة، فتُصبح المطابقة تعمل بشكل صحيح لكل الأكواد المقروءة المُقابلة (25 من أصل 71 سجلاً في `ENRICHED_PROCEDURES`).

`tsc --noEmit` نظيف (exit 0). إصلاح محصور بسطر منطق واحد في `app/procedures/[slug]/playbook/page.tsx`.

### تحديث (batch #472) — شارة الرسوم التقديرية تعرض رقم مرسوم/تاريخ بدل الرسم الفعلي

**المشكلة**: `components/ProcedureEstimatedFeeChip.tsx` (دالة `parseFeesAr`) تستخرج كل الأرقام من نص الرسوم الخام عبر `fees.match(/[\d.,]+/g)` وتأخذ أكبرها كتقدير للرسم، دون أي وعي بسياق النص. لكن نصوص الرسوم الحقيقية في `lib/enrichedProcedures.ts` كثيراً ما تتضمّن استشهاداً قانونياً مضمّناً (رقم مرسوم/قرار/قانون/تعميم، وأحياناً تاريخاً بصيغة يوم/شهر/سنة) لا علاقة له بقيمة الرسم، فيلتقطه المنطق القديم بالخطأ:
- `imu11-01` و`imu11-05` (خدمات أمنية مأجورة): النص يقول "حسب عدد العناصر ومدة التكليف استناداً إلى المرسوم 7847/1961" — أي لا رسم ثابت مذكور إطلاقاً، لكن الشارة كانت تعرض "رسوم منخفضة — حتى 7,847 ل.ل." (الرقم 7847 هو رقم المرسوم فقط).
- `mnb10-01` (رئاسة بيع بائع جملة): الرسم الفعلي 400 دولار سنوياً، لكن الشارة كانت تعرض "حتى 1,993 ل.ل." — الرقم 1993 مأخوذ من تاريخ الاستشهاد "18/6/1993" لا من الرسم.
- `mnb10-02` (إجازة بيع بالمفرق): النص يقول الرسوم تختلف حسب المنطقة (لا رقم فعلي)، لكن الشارة كانت تعرض "حتى 2,490 ل.ل." — الرقم مأخوذ من رقم القرار "2490".

تم التحقق من كل الحالات الـ 61 في `lib/enrichedProcedures.ts`؛ نحو 7 سجلات (~11%) متأثرة بهذا الخلل، مع التأكد أيضاً أن السجلات ذات الأرقام الحقيقية الكبيرة (مثل `mnb12-11`، `mnb12-12`، `mnl13-02`، `interior-id-card`) لا تتأثر بالإصلاح لأن صيغها لا تطابق نمط الاستشهاد القانوني أو التاريخ.

**الأثر**: الشارة ظاهرة دائماً (وليس فقط عند التوسيع) في رأس كل بطاقة إجراء مُثرى على صفحة `/procedures`، فالمستخدم يرى رقماً مضللاً كأنه الرسم الفعلي، بينما هو في الحقيقة رقم مرسوم أو سنة تاريخ.

**الإصلاح**: إضافة دالة `stripLegalCitations` تُزيل الإشارات إلى (مرسوم/قرار/قانون/تعميم + رقمها) والتواريخ بصيغة يوم/شهر/سنة من النص **قبل** استخراج الأرقام المرشّحة لتكون الرسم، بحيث لا تبقى إلا الأرقام التي تمثّل فعلاً قيمة مالية.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureEstimatedFeeChip.tsx` (دالة واحدة مضافة + سطر واحد معدّل).

### تحديث (batch #473) — شريحة "أسئلة ذات صلة" على صفحة الإجراءات تعرض فراغاً لـ 14 سؤالاً حقيقياً

**المشكلة**: `components/ProcedureFAQChips.tsx:101-103` يعرض عند التوسّع محتوى `faq.summary` فقط. لكن `lib/serviceFAQ.ts` يستخدم شكلين مختلفين للمحتوى: السجلات المرجعية تضع محتواها الفعلي في `summary`، بينما سجلات `type:'procedure'` تترك `summary:''` عمداً وتضع محتواها الحقيقي في `steps`/`requiredDocuments`/`fees`/`authority` بدلاً منه. صفحة `/faq` (`app/faq/page.tsx` أسطر 245-312) تتعامل مع هذا بشكل صحيح عبر سلسلة احتياطية كاملة (summary ← steps ← requiredDocuments ← fees المحروسة ← authority/duration)، لكن `ProcedureFAQChips.tsx` لم يحصل قط على هذه السلسلة الاحتياطية.

**الأثر**: المكوّن ظاهر ومُستخدَم في `app/procedures/page.tsx:940` ضمن قسم "اسأل دليلك" الدائم الظهور لكل بطاقة إجراء موسّعة. من أصل 52 سؤالاً حياً في `SERVICE_FAQ`، هناك 14 سؤالاً (`mun_001–mun_005`, `real_001`, `for_005`, `fee_001–fee_004`, `proc_001–proc_003`) لديها `summary:''` مع محتوى حقيقي (خطوات/مستندات) في حقول أخرى. مثال فعلي: إجراء "استخراج رخصة بناء" (`lib/enrichedProcedures.ts:1375`) يطابق السؤال `mun_001` ("رخصة البناء - الإجراءات والرسوم") الذي يحتوي 8 خطوات حقيقية تظهر بشكل صحيح على `/faq`، لكنها كانت تختفي بالكامل عند توسيع نفس السؤال من داخل بطاقة الإجراء.

**التحقق من عدم وجود أثر سلبي**: السجلات الأربعة من نوع "جدول رسوم" (`fee_001-004`) تحتوي فقط على بيانات `fees` تالفة (Python-repr)، وهي مخفية عمداً بنفس حارس `/^[{[]/` المستخدم في batch #470 — الإصلاح يحافظ على هذا السلوك (تبقى فارغة كما يجب). السجلات الـ 38 المتبقية التي تملك `summary` فعلياً غير متأثرة إطلاقاً.

**الإصلاح**: إضافة دالة `previewFor(faq)` تُطبّق نفس سلسلة الاحتياط المستخدمة في `/faq`: summary ← steps ← requiredDocuments ← fees (محروسة) ← authority/duration.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureFAQChips.tsx`.

### تحديث (batch #474) — تاريخ الإنجاز المتوقع يُحسب بنصف المدة الفعلية لإجراءين حقيقيين

**المشكلة**: `components/ProcedureEstimatedCompletion.tsx:61` يتحقق من الصيغة النصبية/الجرّية للمثنى "أسبوعين" لإرجاع 14 يوماً، لكنه لا يتحقق من الصيغة الرفعية للمثنى "أسبوعان" (نفس الكلمة، إعراب مختلف). بما أن "أسبوعان" تحتوي على "أسبوع" كنص فرعي (substring)، فإن السطر التالي (`أسبوع` → 7 أيام) كان يلتقطها بالخطأ ويُرجع 7 أيام بدل 14 — وهو تماماً نفس نوع الخلل الذي عالجه الملف نفسه سابقاً لصيغتي "شهرين"/"شهران" (المثنى للأشهر) في السطر الذي يليه مباشرة، لكن الصيغة الموازية للأسابيع لم تُضَف قط.

**الأثر**: المكوّن ظاهر ومُستخدَم في `app/procedures/page.tsx:874` ليحسب تاريخ الإنجاز المتوقع لأي إجراء بدأه المستخدم (بالاعتماد على `proc.processingTime` من `lib/enrichedProcedures.ts`). سجلّان حقيقيان يستخدمان "أسبوعان" فعلياً: `mnl12-14` ("استرجاع شهادة إيداع"، `processingTime:'أسبوعان.'`، والمقابل الإنجليزي `'Two weeks.'`) وإجراء "تصحيح الاسم في الأحوال الشخصية" (`processingTime:'أسبوعان لمحضر إداري بسيط — أشهر إذا تطلبت قرار قضاء'`، والمقابل الإنجليزي `'Two weeks for a simple administrative record...'`) — كلاهما كان يُحسب بـ 7 أيام بدل 14، فيُعرَض للمستخدم تاريخ إنجاز متوقع أقرب بأسبوع كامل من الحقيقة.

**التحقق من عدم وجود أثر سلبي**: لا يوجد أي سجل آخر في الملف يحتوي "أسبوعان"، والصيغة "أسبوعين"/"اسبوعين" غير متأثرة (نص مختلف)، فلا تراجع في أي حالة أخرى.

**الإصلاح**: إضافة `أسبوعان` إلى شرط الفحص الذي يُرجع 14 يوماً، بنفس أسلوب الإصلاح السابق لـ شهرين/شهران.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في سطر واحد في `components/ProcedureEstimatedCompletion.tsx`.

### تحديث (batch #475) — خدمتان إلكترونيتان من وزارة المالية مُصنَّفتان خطأً كغير متاحتين أونلاين

**المشكلة**: `lib/allServices.ts` — سجلّا `fin_gen_002` (تقديم شكوى لوزارة المالية عبر البوابة الإلكترونية) و`fin_gen_003` (دفع الرسوم القضائية إلكترونياً) يحملان `online_available: false`، بينما يصف نص كل سجل نفسه بوضوح كخدمة إلكترونية بالكامل: `fin_gen_002` عنوانه "...عبر البوابة الإلكترونية" ووصفه "قناة إلكترونية موحدة..."، و`fin_gen_003` عنوانه "دفع الرسوم القضائية إلكترونياً" ووصفه "خدمة إلكترونية تتيح تسديد... مباشرة عبر بوابة وزارة المالية الإلكترونية دون الحاجة لمراجعة الصندوق". السجلّان المجاوران في نفس المجموعة (`fin_gen_001` و`fin_gen_004`)، وهما أقل وضوحاً من ناحية الخدمة الذاتية الكاملة، مُصنَّفان بشكل صحيح `true` — ما يؤكد أن هذا خطأ في إدخال البيانات لا تصنيفاً مقصوداً.

**التحقق من الحيّوية**: حقل `online_available` مُستخدَم في 4 مواضع حقيقية: `app/services/page.tsx:137` و`:772` (إظهار/إخفاء شارة "متاح أونلاين" الخضراء على بطاقة الخدمة وصفحة التفاصيل)، `app/services/page.tsx:548` (إحصائية "خدمة أونلاين" على الصفحة)، و`app/authorities/page.tsx:58` (عدّاد الخدمات الأونلاين المنسوب لوزارة المالية في دليل الجهات). الخطأ يُخفي شارة "أونلاين" عن خدمتين إلكترونيتين حقيقيتين ويُقلّل عدّاد وزارة المالية بمقدار 2.

**التحقق من عدم وجود أثر سلبي**: فُحصت جميع 368 خدمة برمجياً؛ 8 سجلات أخرى بها `online_available: false` مع كلمات مرتبطة بالإلكترونية في النص لكنها تتطلب فعلاً حضوراً شخصياً (مثل التسجيل البيومتري لبطاقة الهوية، حجز موعد المفوضية) — هذه صحيحة ولم تُعدَّل.

**الإصلاح**: تغيير `online_available` من `false` إلى `true` للسجلّين `fin_gen_002` و`fin_gen_003` فقط.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في سطرين في `lib/allServices.ts`.

### تحديث (batch #476) — متتبّع دفع الرسوم يعرض "منذ 2 يوم" بدل "منذ يومين" (خطأ في مطابقة العدد والمعدود)

**المشكلة**: `components/ProcedureFeeHistory.tsx:90` كان يعرض دائماً الصيغة الثابتة `` `منذ ${days} يوم` `` بلا أي مطابقة نحوية للعدد والمعدود، وهي قاعدة أساسية في العربية: 1 → "يوم واحد"، 2 → "يومين" (مثنى)، 3-10 → "N أيام" (جمع)، 11+ → "N يوماً" (تمييز). النتيجة: "منذ 2 يوم" بدل "منذ يومين"، و"منذ 5 يوم" بدل "منذ 5 أيام" — أخطاء نحوية فعلية لا مجرد أسلوب. نفس القاعدة مطبّقة بشكل صحيح في مكوّنات أخرى من التطبيق (`components/ProcedureCountdownTimer.tsx`).

**الأثر**: المكوّن ظاهر ومُستخدَم في `app/procedures/page.tsx:773` (`<ProcedureFeeHistory code={proc.code} fees={displayFees} isAr={isAr} />`)، ويحسب `days` من تاريخ حقيقي مخزَّن في localStorage بعد أن يضغط المستخدم "سجّل الدفع الآن". بما أن نطاق "الأحيان الأخيرة" في المكوّن نفسه هو 30 يوماً (`days <= 30`)، فإن هذا الخلل يظهر بشكل شائع جداً — أي زيارة لاحقة للصفحة خلال 2-10 أيام من آخر دفعة مسجّلة تعرض صيغة خاطئة نحوياً.

**التحقق من عدم وجود أثر سلبي**: النسخة الإنجليزية (`${days} day${days !== 1 ? 's' : ''} ago`) لم تُلمَس وهي صحيحة أساساً.

**الإصلاح**: إضافة دالة `arDaysAgo(n)` تُطبّق قاعدة المطابقة الكاملة (مفرد/مثنى/جمع/تمييز)، بنفس الأسلوب المستخدم في `ProcedureCountdownTimer.tsx`.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureFeeHistory.tsx`.

### تحديث (batch #477) — متتبّع انتهاء صلاحية الوثائق يعرض "2 يوم" بدل "يومان" (نفس خلل المطابقة النحوية)

**المشكلة**: `components/ProcedureDocumentStatus.tsx:45` كان يعرض `` `${days} يوم` `` بلا مطابقة نحوية للعدد والمعدود — نفس نوع الخلل الذي عولج مرتين سابقاً في هذا التطبيق (`ProcedureFeeHistory.tsx` في batch #476، و`ProcedureEstimatedCompletion.tsx` في batch #474). النتيجة كانت صحيحة بالمصادفة فقط عند `days === 1`؛ لكل قيمة أخرى بين 0 و30 كانت خاطئة نحوياً: "2 يوم" بدل "يومان"، "5 يوم" بدل "5 أيام"، "15 يوم" بدل "15 يوماً". والملاحظ أن `components/NotificationBell.tsx` يطبّق المطابقة الصحيحة (`arDays()`) على نفس نوع بيانات `dalilak_doc_expiry_*` المخزَّنة في localStorage، لكن هذا المكوّن نفسه (`ProcedureDocumentStatus.tsx`) الذي يكتب هذه البيانات لم يُصلَح قط.

**الأثر**: المكوّن ظاهر ومُستخدَم في `app/procedures/page.tsx:760`. عند إدخال المستخدم تاريخ انتهاء صلاحية لوثيقة (مثل جواز سفر أو إقامة) يقع ضمن 30 يوماً، وعند توسيع قسم "تواريخ انتهاء صلاحية الوثائق"، تظهر الصيغة الخاطئة لـ 28 من أصل 31 قيمة ممكنة لعدد الأيام.

**التحقق من عدم وجود أثر سلبي**: النسخة الإنجليزية (`${days}d left`) والحالات الأخرى ("منتهية"/"صالحة") غير متأثرة.

**الإصلاح**: إضافة دالة `arDaysLeft(n)` بنفس قاعدة المطابقة الكاملة المستخدمة في الإصلاحين السابقين.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureDocumentStatus.tsx`.

**ملاحظة للمتابعة**: تم رصد خلل مماثل محتمل في `components/ProcedureDeadlineAlert.tsx:148` (`` `${item.daysLeft} أيام متبقية` ``، خاطئ عند daysLeft=2 أو ≥11) — مرشّح جيد لدفعة قادمة.

### تحديث (batch #478) — تنبيه الموعد النهائي يعرض "2 أيام متبقية" بدل "يومان متبقيان"

**المشكلة**: `components/ProcedureDeadlineAlert.tsx:148` (السطر المُرشَّح في batch #477 للمتابعة) يستخدم `` `${item.daysLeft} أيام متبقية` `` لكل القيم التي لا تساوي 0 أو 1. بفحص المنطق فعلياً: `scanDeadlines()` (سطر 78) يستثني أي `daysLeft > 7`، وحالتا 0 و1 معالجتان بشكل خاص أعلاه — فالقيم التي تصل فعلياً إلى هذا الفرع هي 2 إلى 7 فقط. من بين هذه القيم، صيغة الجمع "N أيام" صحيحة نحوياً لـ 3-7، لكنها خاطئة لـ 2 (يجب أن تكون "يومان متبقيان" بصيغة المثنى). أي أن نطاق الخلل الفعلي أضيق مما بدا أول الأمر (فقط القيمة 2)، لأن الفلترة عند >7 يوماً تمنع وصول أي قيمة ≥11 إلى هذا الفرع.

**الأثر**: المكوّن ظاهر ومُستخدَم في `app/procedures/page.tsx:275`. يظهر التنبيه لأي إجراء له موعد نهائي شخصي محدَّد (`dalilak_proc_deadline_{code}`) يقع ضمن أسبوع، فتظهر الصيغة الخاطئة تحديداً عندما يتبقى يومان بالضبط.

**التحقق من عدم وجود أثر سلبي**: القيم 3-7 (الصيغة الصحيحة أصلاً) لم تُلمَس؛ النسخة الإنجليزية غير متأثرة.

**الإصلاح**: إضافة حالة خاصة لـ `daysLeft === 2` تعرض "يومان متبقيان".

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureDeadlineAlert.tsx`.

### تحديث (batch #479) — شارة أيام التجربة المجانية في الهيدر تعرض "2 يوم" بدل "يومان" (نفس خلل المطابقة النحوية، رابع مرة)

**المشكلة**: `components/TopNav.tsx:241` كان يعرض `` `{currentUser.days_left}{isAr ? ' يوم' : 'd'}` `` بلا مطابقة نحوية — نفس فئة الخلل المُصلَحة ثلاث مرات سابقاً (`ProcedureFeeHistory.tsx` #476، `ProcedureDocumentStatus.tsx` #477، `ProcedureDeadlineAlert.tsx` #478). بخلاف تلك المكوّنات، لا يوجد هنا أي فلتر علوي يحدّ النطاق (`days_left` يتناقص عبر كامل مدة التجربة المجانية من الخلفية)، فكل قيمة من 0 حتى مدة التجربة تظهر فعلياً لمستخدمين حقيقيين — بما في ذلك نطاق 2-10 وما فوق 11، وهو بالضبط النطاق الذي يظهر فيه التنبيه بالألوان التحذيرية (كهرماني/أحمر) الأكثر وضوحاً للمستخدم.

**الأثر**: الشارة ظاهرة في الهيدر العلوي (`app/page.tsx:1347`) لكل مستخدم على خطة تجريبية (`currentUser.plan === 'trial'`)، وهي بيانات حقيقية من الخلفية (`lib/auth.ts`)، تظهر في كل صفحة تقريباً.

**التحقق من عدم وجود أثر سلبي**: النسخة الإنجليزية (`${n}d`) غير متأثرة.

**الإصلاح**: إضافة دالة `arTrialDays(n)` بنفس قاعدة المطابقة الكاملة (مفرد/مثنى/جمع/تمييز) المستخدمة في الإصلاحات الثلاثة السابقة.

**ملاحظة للمتابعة**: رُصد نفس النمط غير المُصلَح في `app/admin/page.tsx:386` (جدول المسؤول فقط، أثر أقل)، وفي `components/AppointmentReminder.tsx:89` — لكن تبيّن أن `dalilak_appointments` ليس لها أي كاتب حقيقي في الكود (لا واجهة تكتب إليها)، فهذا المسار غير قابل للوصول حالياً ولم يُفحص أكثر.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/TopNav.tsx`.

### تحديث (batch #480) — عمود "الأيام المتبقية" في لوحة المشرف يعرض نفس خلل المطابقة النحوية (خامس مرة)

**المشكلة**: `app/admin/page.tsx:386` (السطر المُرشَّح في batch #479 للمتابعة) كان يعرض `` `${u.days_left} يوم` `` بلا مطابقة نحوية — نفس الحقل بالضبط (`days_left`) الذي أُصلِح في `components/TopNav.tsx` (batch #479)، لكن في جدول مستخدمي لوحة المشرف بدل شارة الهيدر.

**الأثر**: ظاهر في جدول "المستخدمون" بلوحة تحكم المشرف (`app/admin/page.tsx`) لكل مستخدم على خطة تجريبية غير مدفوعة، وهو بيانات حقيقية من الخلفية. أثره محدود بكونه صفحة مشرفين فقط (وليس متاحاً لعموم المستخدمين)، ولذلك أُجِّل عن batch #479 وأُنجِز الآن كمتابعة.

**الإصلاح**: إضافة دالة `arDaysLeftAdmin(n)` بنفس قاعدة المطابقة الكاملة المستخدمة في `TopNav.tsx`.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/admin/page.tsx`.

### تحديث (batch #481) — تنبيه إغلاق الدوائر الحكومية يعيد الظهور بعد إغلاقه بسبب خلط بين توقيت بيروت وتوقيت UTC

**المشكلة**: `components/GovHolidayAlert.tsx` يحسب "غداً" بشكل صحيح باستخدام توقيت بيروت (`getBeyrouthTomorrow()`، أسطر 67-74)، لكن مفتاح الإخفاء المخزَّن في localStorage (`dalilak_holiday_alert_dismissed_{date}`) كان يُحسَب بشكل منفصل مرتين (عند التحميل وعند الإغلاق) باستخدام `new Date().toISOString().slice(0,10)` — وهو **توقيت UTC**، وليس توقيت بيروت (UTC+2 شتاءً أو UTC+3 صيفاً). هذا يخلق نافذة زمنية يومية مدتها 2-3 ساعات فور منتصف ليل بيروت (00:00-02:00/03:00 بتوقيت بيروت = 21:00/22:00-00:00 UTC لليوم السابق) يكون فيها تاريخ بيروت قد تقدّم فعلاً بينما تاريخ UTC لا يزال متأخراً يوماً كاملاً — وهي بالضبط اللحظة التي يظهر فيها تنبيه جديد لأول مرة ("غداً" ينقلب عند منتصف ليل بيروت). إذا أغلق المستخدم التنبيه خلال هذه النافذة، يُخزَّن تحت مفتاح اليوم السابق بتوقيت UTC؛ وما إن يلحق تاريخ UTC (نحو الساعة 3 صباحاً بتوقيت بيروت، وما زال نفس يوم بيروت)، يصبح المفتاح المحسوب مختلفاً عن المخزَّن، فيعيد التنبيه المُغلَق سابقاً الظهور لبقية ذلك اليوم.

**الأثر**: المكوّن ظاهر بلا شرط في `app/procedures/page.tsx:272`، ويتكرر هذا الخلل كل يوم جمعة أو سبت (عطلة نهاية الأسبوع الحكومية) أو أي من 15 عطلة رسمية مُدرَجة.

**التحقق من عدم وجود أثر سلبي**: محتوى التنبيه نفسه (تاريخ العطلة المعروض للمستخدم) يبقى صحيحاً دائماً لأنه يعتمد على `getBeyrouthTomorrow()` غير المتأثرة؛ الخلل محصور فقط في دوام حالة "تم الإغلاق".

**الإصلاح**: إضافة دالة `getBeyrouthTodayKey()` تحسب تاريخ اليوم بتوقيت بيروت (وليس UTC) بنفس أسلوب `getBeyrouthTomorrow()` الموجود مسبقاً، واستخدامها في كل من نقطتي حساب المفتاح (عند التحميل وعند الإغلاق) بدل `toISOString()`.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/GovHolidayAlert.tsx`.

### تحديث (batch #482) — شارة "محدَّث حديثاً" على بطاقات الإجراءات تعتمد على منطقة زمنية مختلفة عن النص المعروض

**المشكلة**: `components/ProcedureVersionTag.tsx` يصف نفسه في التعليق العلوي بأنه "حتمي" (deterministic) — أي أن نفس رمز الإجراء يجب أن يعطي نفس النتيجة دائماً. لكن `getUpdateBucket()` (سطر 34-35، قبل الإصلاح) كان يحسب "الفئة الزمنية" (هذا الشهر / الشهر الماضي / إلخ) باستخدام توقيت **جهاز الزائر المحلي** (`new Date().getFullYear()`/`getMonth()`)، بينما `getDateLabel()` المجاورة له مباشرة تعرض النص بتوقيت **بيروت** صراحة (`timeZone: 'Asia/Beirut'`). النتيجة: حول أي حدّ بين شهرين، أو لأي زائر يتصفح من منطقة زمنية مختلفة عن بيروت (جمهور حقيقي لتطبيق موجّه لمغتربين لبنانيين)، يمكن أن تختلف الفئة المحسوبة (وبالتالي ظهور الشارة أصلاً، إذ `bucket >= 4` يُخفيها، ولونها) عن الشهر الذي يدّعيه النص المعروض لنفس المستخدم في نفس اللحظة الحقيقية — تناقض مباشر مع التصميم "الحتمي" المُعلَن.

**الأثر**: المكوّن ظاهر ومُستخدَم في `app/procedures/page.tsx:647` على كل بطاقة إجراء مُثرى في صفحة `/procedures`.

**الفئة**: هذا خلل من نفس فئة batch #481 (خلط توقيت UTC/محلي مع منطقة بيروت الزمنية)، لكنه مثال مختلف لم يغطّه المسح السابق (الذي كان يخص فقط مفاتيح localStorage اليومية، لا حساب "الشهر"). تم التحقق من كل المكوّنات الأخرى التي تستخدم `Asia/Beirut` (`ProcedureFeeHistory`، `NotificationBell`، `ProcedureDeadlineAlert`، وغيرها) — جميعها تحسب "اليوم" بتوقيت بيروت باستمرار؛ `ProcedureVersionTag.tsx` هو الاستثناء الوحيد المتبقي.

**الإصلاح**: إضافة دالة `getBeirutYearMonth()` تحسب السنة والشهر الحاليين بتوقيت بيروت عبر `Intl.DateTimeFormat`، واستخدامها في `getUpdateBucket()` بدل التوقيت المحلي للجهاز — فتُصبح الفئة المحسوبة متوافقة دائماً مع الشهر المعروض فعلياً في النص.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureVersionTag.tsx`.

### تحديث (batch #483) — حاسبة التكاليف تعرض "2 وثيقة" بدل "وثيقتين" (خلل مطابقة نحوية جديد، لكلمة "وثيقة" هذه المرة)

**المشكلة**: `components/CostEstimator.tsx:127` كان يعرض `` `كاتب العدل / التوثيق (${docCount} وثيقة × $5)` `` بلا مطابقة نحوية بين العدد والمعدود — نفس فئة الخلل المُصلَحة ست مرات سابقاً لكلمة "يوم"، لكن هذه أول مرة تُكتشف لكلمة "وثيقة" (document). القاعدة الصحيحة: 1 → "وثيقة واحدة"، 2 → "وثيقتين" (مثنى)، 3-10 → "N وثائق" (جمع)، 11+ → "N وثيقة" (تمييز مفرد — وهذا فقط يصادف الشكل القديم).

**الأثر**: المكوّن ظاهر ومُستخدَم في `app/procedures/page.tsx:719` (`docCount={proc.requiredDocuments.length}`) لكل إجراء له رسوم، ضمن بند "كاتب العدل / التوثيق" الذي يظهر دائماً عندما `docCount > 0`.

**التحقق من النطاق الفعلي**: فحص جميع الإجراءات الـ 71 في `ENRICHED_PROCEDURES` حسب عدد المستندات المطلوبة: التوزيع الفعلي {1:3, 2:4, 3:6, 4:12, 5:19, 6:15, 7:2, 8:2, 9:2, 13:1, 14:1, 17:1, 18:1, 30:2}. أي أن **58 من أصل 71 إجراءً (82%)** يقعان ضمن نطاق 2-10 حيث كانت الصيغة القديمة خاطئة نحوياً؛ فقط 3 إجراءات (عدد=1) و8 إجراءات (عدد≥11) كانت تُعرَض بشكل صحيح بالمصادفة — والإصلاح يحافظ على صحتها.

**ملاحظة**: بند "ترجمة وثائق" المجاور (`ترجمة وثائق (${docCount} × $15)`) غير متأثر لأن الرقم فيه لا يسبق الاسم مباشرة، فلا تطبّق عليه قاعدة المطابقة بنفس الشكل.

**الإصلاح**: إضافة دالة `arDocsLabel(n)` بنفس قاعدة المطابقة الكاملة المستخدمة في الإصلاحات السابقة لكلمة "يوم".

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/CostEstimator.tsx`.

### تحديث (batch #484) — تتبّع تقدّم الخطوات يعرض "6 خطوة" بدل "6 خطوات" (خلل مطابقة نحوية ثالث، لكلمة "خطوة")

**المشكلة**: `components/ProcedureStepProgress.tsx:95` كان يعرض `` `${doneCount} من ${steps.length} خطوة` `` بلا مطابقة نحوية — ثالث كلمة تُكتشف بها هذا النوع من الخلل بعد "يوم" (6 مكوّنات) و"وثيقة" (`CostEstimator.tsx`، batch #483).

**الأثر**: المكوّن ظاهر ومُستخدَم في `app/procedures/page.tsx:811` ضمن لوحة الإجراء المُثرى الدائمة الظهور. فُحص طول مصفوفة `steps` لكل الإجراءات الـ 71 في `ENRICHED_PROCEDURES`: التوزيع {3:7, 4:21, 5:12, 6:22, 7:4, 8:1, 9:1, 10:2, 15:1} — أي أن **70 من أصل 71 إجراءً (99%)** يقع ضمن نطاق 3-10 (يتطلب صيغة الجمع "خطوات")، وسجل واحد فقط (15 خطوة) كان يُعرَض بشكل صحيح بالمصادفة.

**ملاحظة نحوية دقيقة**: بما أن الجملة الكاملة هي "X من Y خطوة" (X من أصل Y)، فحرف الجر "من" يقتضي حالة الجرّ للمعدود، لذا صيغة المثنى الصحيحة هنا هي "خطوتين" (منصوبة/مجرورة) وليس "خطوتان" (مرفوعة) المستخدمة سابقاً في سياقات أخرى قائمة بذاتها مثل "يومان متبقيان" (batch #478) — تم مراعاة هذا الفرق النحوي عند الإصلاح.

**الإصلاح**: إضافة دالة `stepPhraseAr(n)` بنفس قاعدة المطابقة الكاملة (مفرد/مثنى مجرور/جمع/تمييز).

**ملاحظة للمتابعة**: رُصد نفس النمط في `components/ProcedureFlowchart.tsx:150` (`${nodes.length} خطوة`) لكنه يعتمد على مصدر بيانات مختلف (خطوات مخطط انسيابي مُولَّدة ديناميكياً)، ولم يُفحص توزيعها الفعلي بعد — مرشّح لدفعة قادمة.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureStepProgress.tsx`.

### تحديث (batch #485) — خارطة الإجراء (Flowchart) تعرض نفس خلل "خطوة" في شريط التقدّم

**المشكلة**: `components/ProcedureFlowchart.tsx:150` (السطر المُرشَّح في batch #484 للمتابعة) كان يعرض `` `${nodes.length} خطوة` `` بلا مطابقة نحوية — نفس الخلل بالضبط المُصلَح في `ProcedureStepProgress.tsx` (batch #484) لنفس الكلمة، لكن لمصدر بيانات مختلف (عُقَد خارطة الإجراء المُولَّدة بدل مصفوفة `steps` الثابتة).

**التحقق من النطاق**: خرائط الإجراء الاحتياطية الثابتة الست في `lib/flowchartData.ts` تحتوي 6-8 عُقَد لكل منها؛ خرائط الذكاء الاصطناعي المُولَّدة تتبع نفس توزيع خطوات الإجراءات الحقيقية المُدقَّق في batch #484 (الغالبية العظمى ضمن نطاق 3-10 الذي يتطلّب صيغة الجمع).

**الأثر**: المكوّن ظاهر ومُستخدَم في ثلاث صفحات: `app/procedures/[slug]/playbook/page.tsx`، `app/forms/[slug]/FormDetailClient.tsx`، و`app/services/page.tsx` — بلا تمرير `compact`، فيظهر شريط التقدّم دائماً.

**الإصلاح**: إضافة دالة `flowStepPhraseAr(n)` بنفس قاعدة المطابقة المستخدمة في `ProcedureStepProgress.tsx` (بما في ذلك صيغة المثنى المجرورة "خطوتين" المناسبة لهذا السياق).

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureFlowchart.tsx`.

### تحديث (batch #486) — بحث "GuidedFlow" في وضع اللغة الإنجليزية معطّل بالكامل

**المشكلة**: `components/GuidedFlow.tsx` (المُعالج التفاعلي لاختيار الإجراء، `filtered` useMemo عند السطر ~95-101) كان يطابق نص البحث فقط ضد `p.ar` (والحقل الاختياري `p.authority`، وكلاهما عربي)، ولا يطابق أبداً ضد `p.en` — رغم أن المكوّن يعرض حقول بحث إنجليزية كاملة عند `isAr === false` (aria-label و placeholder إنجليزيان، و`dir='ltr'`، مؤكَّد بقراءة الأسطر 282-335).

**التحقق من النطاق**: فُحصت 368 قيمة `name_ar` فعلية في `lib/allServices.ts` — 313 منها (85%) عربية خالصة بلا أي حرف لاتيني، والـ 55 المتبقية تحتوي فقط اختصارات لاتينية بين قوسين (مثل CNSS، NPTP، SARL/LLC) وليست ترجمة كاملة. أي أن أي بحث بكلمة إنجليزية حقيقية (غير الاختصار بالمصادفة) لن يطابق `p.ar` إطلاقاً — البحث الإنجليزي معطّل عملياً لكل الكتالوج تقريباً.

**التحقق من الوصول الفعلي**: مؤكَّد عبر `grep -rn "GuidedFlow" app components --include="*.tsx"` أن المكوّن مستورد ديناميكياً في `app/page.tsx:15` (`dynamic(() => import('@/components/GuidedFlow'), { ssr: false })`) ومُستخدَم فعلياً في JSX عند `app/page.tsx:2516`.

**الإصلاح**: إضافة شرط `p.en.toLowerCase().includes(q) ||` إلى مصفوفة شروط الفلترة، إلى جانب الشروط العربية الموجودة (لا حذف، فقط إضافة).

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/GuidedFlow.tsx`.

### تحديث (batch #487) — قيمة "N/A" في حقل website تُعرَض كرابط "الموقع الرسمي" فعّال ومُعطَّل

**المشكلة**: في `lib/allServices.ts` سجلّان فقط من أصل 367 يستخدمان قيمة نصية غير فارغة كإشارة لعدم وجود موقع إلكتروني: السطر 3538 (`re_008` — إيجار المنازل): `website: 'N/A (لا توجد جهة إلكترونية مركزية)'`، والسطر 5191 (`misc_006` — شهادة الإقامة): `website: 'N/A'`. بينما بقية السجلات (10 سجلات مؤكَّدة) التي لا تملك موقعاً تستخدم بشكل صحيح `website: ''` (نص فارغ).

**الأثر**: كلا المستهلكَين لهذا الحقل يتحققان فقط من truthy بلا أي تحقق من صحة الرابط:
- `app/services/page.tsx:287` — `{service.website && <a href={service.website}>...الموقع الرسمي</a>}` — بما أن `'N/A'` نص truthy، يُعرَض رابط "الموقع الرسمي" فعلياً بـ href حرفي `"N/A"` (يُحلّ كمسار نسبي معطَّل)، لخدمتين تُصرِّحان في بياناتهما بعدم وجود جهة إلكترونية مركزية.
- `app/authorities/page.tsx:50/63/367` — نفس الخلل ينتقل عبر التجميع إلى صفحة الجهات: يُسجَّل `website` الجهة كـ `'N/A...'` عند أول ظهور (سطر 50)، ولأن القيمة truthy فلا يمكن تصحيحها لاحقاً حتى لو ظهرت خدمة أخرى بنفس الجهة بموقع حقيقي (سطر 63) — وعند العرض (سطر 367) يُتخطّى fallback الصحيح "بلا موقع" ويظهر رابط مضلّل بدلاً منه.

**التحقق**: تأكيد أن `re_008`/`misc_006` سجلّان حقيقيان وليسا كوداً ميتاً. تأكيد عبر `grep -n "website: '" lib/allServices.ts | grep -v "https\?://"` أن هذين السجلّين فقط (من أصل 367) يخالفان اصطلاح النص الفارغ المستخدم في 10 سجلات أخرى مؤكَّدة (لا إيجابيات كاذبة). تأكيد الاستهلاك الحي عبر grep لـ `service.website`/`auth.website` في الملفين.

**الإصلاح**: تغيير كلا السطرين إلى `website: ''` ليطابق اصطلاح الملف نفسه — يُصلح الخلل في كلا الصفحتين دون أي تعديل على منطق العرض.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `lib/allServices.ts` (سطرين).

### تحديث (batch #488) — نص وصفي غير رقمي في حقل phone يُعرَض كرابط اتصال tel: فعّال ومُعطَّل في /authorities

**المشكلة**: `app/authorities/page.tsx:344` كان يبني `href={\`tel:${auth.phone}\`}` بلا أي تحقق من أن القيمة رقم هاتف حقيقي. 18 سجلاً في `lib/allServices.ts` تحمل نصاً وصفياً بلا أي رقم فعلي في حقل `phone` (مثل `'يتفاوت بحسب البلدية'`، `'يختلف حسب المنطقة'`، `'—'`)، وهذا نفس فئة الخلل التي أُصلحت لحقل `website` في batch #487 (قيمة نائبة/placeholder تُعامَل كقيمة حقيقية قابلة للتفاعل).

**الأثر المؤكَّد**: سجلّان على الأقل هما المصدر الوحيد لمفتاح `authority_ar` الخاص بهما في دالة `deriveAuthorities()`، فلا يوجد سجل آخر يمكن أن يستبدل قيمة الهاتف الوصفية بقيمة حقيقية:
- `real_003` (`وزارة العدل — القضاء العدلي (قضايا الإيجارات)`, السطر 5417): `phone: '—'` → رابط اتصال بعنوان "—" و`href="tel:—"`.
- `mun_002` (`البلدية المختصة + المديرية العامة للتنظيم المدني`, السطر 3068): `phone: 'يتفاوت بحسب البلدية'` → رابط اتصال بعنوان "يتفاوت بحسب البلدية" (تتفاوت حسب البلدية) و`href` يحتوي نصاً عربياً بدل رقم.

**التحقق**: تأكيد أن كلا السجلّين هما التطابق الوحيد لمفتاح `authority_ar` الخاص بهما (`grep -c` = 1 لكل منهما)، وأن الصفحة `/authorities` مُتاحة عبر التنقل الرئيسي (`TopNav.tsx:30`, `MobileMenu.tsx:67`) وليست معزولة. فُحصت 18 قيمة `phone` غير رقمية عبر `grep` على النص العربي الوصفي الشائع (يتفاوت/يختلف/يُنشر/يُراجع/—).

**الإصلاح**: إضافة دالة `isDialablePhone(p)` تتحقق من وجود رقمين متتاليين على الأقل (`/\d{2,}/`) قبل عرض رابط `tel:`، وعرض نص عادي (span) بدلاً من رابط إذا لم تكن القيمة قابلة للاتصال — مع تنظيف الفواصل غير الرقمية من الأرقام الحقيقية قبل استخدامها في `href`. نفس النص الظاهر للمستخدم يبقى كما هو في الحالتين.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/authorities/page.tsx`.

### تحديث (batch #489) — CostEstimator يخترع رسماً حكومياً لإجراءات مجانية صراحةً

**المشكلة**: `components/CostEstimator.tsx:40` كان يتحقق من صيغة "مجاني" فقط لاعتبار الرسم صفراً؛ لم يكن يتعرّف على صيغ "لا رسوم"/"لا يتوجب أي رسم" الشائعة في `lib/enrichedProcedures.ts`. النتيجة: النصوص الخالية من أي رقم تسقط إلى fallback رقمي عام في السطر 59-64 أو إلى القيمة الافتراضية الثابتة `?? 20` (سطر 95)، وأحياناً تلتقط رقماً غير ذي صلة (كنسبة ضريبة القيمة المضافة) وتعرضه كرسم.

**التحقق من البيانات الحقيقية**: 3 سجلات فعلية في `lib/enrichedProcedures.ts` تحمل صيغة "لا رسوم" بلا كلمة "مجاني":
- السطر 185 (`imu11-04`): `'لا رسوم على تقديم البلاغ.'` — بلا أرقام إطلاقاً → كان يُعرَض $20 كرسم رسمي مُختلَق.
- السطر 1059 (`phe211-06`): `'لا يتوجب أي رسم'` — بلا أرقام → نفس النتيجة ($20).
- السطر 1404 (`finance-tax-registration`): `'لا رسوم للتسجيل — الضريبة تُدفع على المبيعات (11% VAT).'` — يحتوي رقم "11" غير ذي صلة (نسبة ضريبة) كان يُقرأ خطأً كرسم $11.

فُحصت كل سجلات "مجاني" الأخرى (`grep`) للتأكد أنها تحوي فعلاً كلمة "مجاني" وتُعامَل بشكل صحيح مسبقاً — لا خطر على أي سجل رسم حقيقي غير صفري.

**الأثر**: المكوّن مُستخدَم فعلياً في `app/procedures/page.tsx:719` (مُغذّى بـ `displayFees` غير الفارغ من `proc.fees`)، فيظهر لأي مستخدم يفتح حاسبة التكلفة لهذه الإجراءات الثلاثة رقم رسم مُختلَق يناقض نص البيانات نفسه الذي يقول صراحةً إن المعاملة مجانية.

**الإصلاح**: توسيع الفحص في `parseFeesUSD()` ليشمل نمط `/لا\s*(?:رسوم|يتوجب\s*أي\s*رسم)/i` إلى جانب "مجاني/free/gratis"، بحيث تُرجِع القيمة 0 قبل وصول المنطق إلى fallback الرقم العام.

**ملاحظة للمتابعة**: نفس الفجوة موجودة في `components/ProcedureEstimatedFeeChip.tsx:52` (فحص "مجاني/مجانا/بدون رسوم/دون رسوم" فقط، بلا "لا رسوم/لا يتوجب") — يعرض شارة "رسوم متغيرة" بدل "🆓 مجاني" لنفس السجلات الثلاثة. أثر أخف (تصنيف خاطئ فقط، لا رقم مُختلَق) — مرشّح لدفعة قادمة.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/CostEstimator.tsx`.

### تحديث (batch #490) — متابعة: نفس فجوة "لا رسوم" في ProcedureEstimatedFeeChip

**المشكلة**: `components/ProcedureEstimatedFeeChip.tsx:52` (المُرشَّح للمتابعة من batch #489) كان يتحقق فقط من "مجاني/مجانا/بدون رسوم/دون رسوم"، بلا صيغة "لا رسوم/لا يتوجب أي رسم" — نفس الفجوة بالضبط المُصلَحة في `CostEstimator.tsx` لنفس السجلات الثلاثة (imu11-04، phe211-06، finance-tax-registration).

**الأثر**: أقل حدّة من batch #489 (لا رقم مُختلَق، فقط تصنيف خاطئ) — الشارة الصغيرة على بطاقة الإجراء في `app/procedures/page.tsx` (وأيضاً `PrintProcedureModal.tsx`) كانت تعرض "رسوم متغيرة" بدل "🆓 مجاني" لإجراءات مجانية صراحةً.

**الإصلاح**: توسيع نفس الفحص في `parseFeesAr()` ليشمل `/لا\s*(?:رسوم|يتوجب\s*أي\s*رسم)/`.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureEstimatedFeeChip.tsx`.

### تحديث (batch #491) — تسرّب Python dict repr إلى حقل summary في /faq (ولم يكن الحارس القديم كافياً)

**المشكلة**: سجل `ref_004` (`قانون تأسيس الشركات في لبنان - أنواع الشركات`، فئة "مراجع قانونية") في `lib/serviceFAQ.ts:572` يحمل نص `summary` تالفاً هو تمثيل خام لقاموس Python: `"شركة_ذات_مسؤولية_محدودة: {'الاسم': 'SARL (Société à Responsabilité Limitée)', ...} | شركة_مساهمة: {'الاسم': 'SAL "` (مقتطَع أيضاً عند 300 حرف بالضبط). كان يُعرَض حرفياً في `app/faq/page.tsx:245-249` بلا أي حارس — نفس فئة الخلل الذي أُصلح سابقاً لحقل `fees`، لكن `summary` لم يُشمَل يومها.

**فجوة في الحارس القديم**: الحارس السابق `/^[{[]/` (يتحقق أن القيمة بأكملها تبدأ بـ `{`/`[`) لا يكتشف هذه الحالة لأن التلف هنا يظهر بعد نص عربي بادئ ("شركة_ذات_مسؤولية_محدودة: {...")، وليس في بداية السلسلة تماماً.

**التحقق**: بُني كاشف أعم `isCorruptDictRepr(s)` يتحقق من نمط `{'...':` في أي موضع بالسلسلة (توقيع مميز لقاموس Python المُحوَّل إلى نص). فُحص مقابل كل 60 قيمة `summary` وكل 60 قيمة `fees` الحقيقية في `lib/serviceFAQ.ts` (بعد محاكاة فك تشفير هروب الاقتباس كما يحدث فعلياً وقت التشغيل): طابق فقط `summary` سجل `ref_004` الفاسد و13 سجل `fees` المعروفة سابقاً — لا إيجابيات كاذبة على أي نص عربي حقيقي آخر.

**الأثر**: `app/faq/page.tsx` (صفحة `/faq` المباشرة) و`components/ProcedureFAQChips.tsx` (المُستخدَم في `previewFor()` عبر صفحات تفاصيل الإجراءات) كلاهما يعرضان `item.summary`/`faq.summary` بلا حارس.

**الإصلاح**: إضافة دالة `isCorruptDictRepr()` في كلا الملفين، واستخدامها لحراسة عرض `summary` (وترقية حارس `fees` القديم إلى نفس الكاشف الأعمّ في `app/faq/page.tsx` لضمان الاتساق، بلا تغيير في سلوكه على الحالات الـ13 المعروفة).

**ملاحظة للمتابعة (غير مُصلَحة الآن)**: فُحص أيضاً اقتطاع 16 سجل summary عند 300 حرف بالضبط في منتصف كلمة (بلا "…")، بما فيها معلومات قانونية/عمّالية حقيقية مقطوعة (مثل يوم الراحة الأسبوعي القانوني). أثر أخف (معلومة ناقصة، لا نص فاسد بصرياً) — مرشّح لدفعة قادمة إن رغب المستخدم بإصلاحه على مستوى البيانات.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/faq/page.tsx` و`components/ProcedureFAQChips.tsx`.

### تحديث (batch #492) — ProcedureEstimatedCompletion يخترع موعد إنجاز خاطئ لإجراء مدته "أسابيع إلى أشهر"

**المشكلة**: `components/ProcedureEstimatedCompletion.tsx:79` (آخر fallback في `parseProcessingDays()`) كان يتحقق من `lower.includes('شهر')` لإرجاع 30 يوماً. لكن صيغة الجمع الكسري "أشهر" (شهور) تحتوي كلمة "شهر" كجزء منها (أ-**ش-ه-ر**)، فأي نص يذكر "أشهر" بلا رقم وبلا صيغة مثنى (التي عولجت مسبقاً في batch سابق لـ"شهرين/شهران") يسقط في هذا الفخ ويُقرأ كـ"شهر واحد" (30 يوماً) رغم أن النص الأصلي غامض/غير رقمي عمداً. نفس الخلل موجود بالإنجليزية (`lower.includes('month')` يطابق داخل "months").

**التحقق من البيانات الحقيقية**: السجل `justice-personal-status-court` (تقديم دعوى أمام المحاكم الشرعية/الدينية — الزواج والطلاق والنفقة) في `lib/enrichedProcedures.ts:1290`: `processingTime:'يتفاوت حسب نوع القضية: أسابيع إلى أشهر'` — بلا أي رقم. الدالة كانت تُرجِع 30 يوماً بدل `null` (غير قابل للتحليل)، فيعرض المكوّن للمستخدم تاريخ إنجاز مُتوقَّع محدَّد وعدّاد "متبقٍ/متأخر" لقضية أحوال شخصية حساسة (زواج/طلاق/حضانة/نفقة) رغم أن البيانات نفسها تصرّح بأن المدة تتراوح من أسابيع إلى أشهر دون تحديد.

**فحص الإيجابيات الكاذبة**: فُحصت كل 7 حالات "أشهر" في `lib/enrichedProcedures.ts` — 6 منها تحتوي رقماً صريحاً قبل "أشهر" (مثل "6 أشهر"، "2–6 أشهر") وتُعالَج بشكل صحيح مسبقاً عبر regex الرقم+الوحدة قبل الوصول لهذا الـfallback؛ السجل السابع (`justice-personal-status-court`) هو الوحيد الخالي من أي رقم، وهو الوحيد الذي يُصيب الخلل.

**الإصلاح**: تعديل الشرط ليستثني حالة الجمع: `lower.includes('شهر') && !lower.includes('أشهر')` (وبالمثل للإنجليزية: `!lower.includes('months')`) — فتسقط الحالات الجمعية غير المرقّمة إلى `null` كما هو مقصود أصلاً حسب توثيق المكوّن نفسه.

**ملاحظة للمتابعة**: حالة نادرة واحدة (`fin121-01`، سطر 669) يحتوي نصها رقمي مزدوج ("مهلة 6 أشهر... يصدر خلال 30 يوماً") — لم تُعالَج لأن الدلالة هنا غامضة فعلاً (30 يوماً هي مهلة إصدار القرار بعد التقديم، لا مدة الإجراء الكلي)، والرقم الأصغر (30) هو المُرجَع حالياً — أُبقي كما هو، غير مؤكَّد أنه خطأ.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureEstimatedCompletion.tsx`.

### تحديث (batch #493) — خلل "وثيقة" لم يُصلَح إلا في ملف واحد، رغم إغلاقه سابقاً في السجلّ

**تصحيح مهم**: تبيّن أن قاعدة التطابق العددي لكلمة "وثيقة" (batch #483) طُبِّقت في `CostEstimator.tsx` فقط — وليس في "6 مكوّنات" كما أُشير خطأً في تعليقات لاحقة (بالمقارنة مع "يوم" الذي أُصلح فعلاً في 6 مكوّنات). نفس النمط المكشوف (اسم مفرد ثابت بجانب عدّاد متغيّر) بقي حياً وغير مُصلَح في 6 مكوّنات أخرى.

**السجلات المتأثرة**: فُحص التوزيع الفعلي لطول `requiredDocuments` عبر كل الـ71 سجلاً في `lib/enrichedProcedures.ts`: 3 سجلات بطول 1 (صحيح بالمصادفة)، 4 سجلات بطول 2 (تحتاج مثنى "وثيقتين")، 58 سجلاً بطول 3-10 (تحتاج جمعاً "وثائق")، 6 سجلات بطول 11+ (صحيح بالمصادفة) — أي **62 من 71 (87%)** يعرض نصاً مخالفاً للقواعد.

**المكوّنات المُصلَحة في هذه الدفعة** (كلها تُستخدَم فعلياً في `app/procedures/page.tsx` و/أو `app/services/page.tsx`، تحقّق عبر grep):
- `components/ProcedureDocReadinessBar.tsx:107` — `${doneCount} من ${docs.length} وثيقة جاهزة` (مفرد ثابت).
- `components/ProcedureRelatedSuggestions.tsx:135` — `{docCount} وثيقة` (مفرد ثابت).
- `components/ProcedureSearchModal.tsx:285` — `{docs.length} وثيقة` (مفرد ثابت).
- `components/ProcedureProgressBadge.tsx:96,135` — `${checked} من ${total} وثيقة محضّرة` و`${checked}/${total} وثيقة` (مفرد ثابت، حتى في وضع compact).
- `components/ProcedureChecklistExport.tsx:89` — `${doneCount} من ${docs.length} وثيقة جاهزة` (في نص القائمة القابلة للطباعة/التصدير).
- `components/ReadinessChecker.tsx:226` — **خلل معكوس**: `${done} من ${total} وثائق جاهزة` (جمع ثابت دائماً) — خطأ للسجلات الثلاثة ذات وثيقة واحدة.

**الإصلاح**: إضافة دالة `arDocsUnit(n)` (نفس قاعدة CostEstimator.tsx: مفرد/مثنى مجرور/جمع/تمييز) محلياً في كل ملف من الستة، واستبدال النص الثابت بها.

`tsc --noEmit` نظيف (exit 0) بعد تعديل الملفات الستة.

### تحديث (batch #494) — ProcedureDocumentStatus وNotificationBell يعرضان عدد أيام مختلفاً لنفس تاريخ انتهاء الوثيقة

**المشكلة**: `components/ProcedureDocumentStatus.tsx:56` كان يحسب الأيام المتبقية بطرح خام: `Math.ceil((new Date(dateStr).getTime() - Date.now()) / 86_400_000)`. بما أن `new Date('YYYY-MM-DD')` تُفسَّر حسب المواصفة كمنتصف ليل بتوقيت UTC، بينما `Date.now()` هو اللحظة الفعلية الحالية، فإن الفارق ليس "عدد أيام تقويمية" حقيقياً.

**التناقض المباشر**: `components/NotificationBell.tsx:49-52` تقرأ **نفس** مفاتيح `dalilak_doc_expiry_{code}_{i}` (بحسب تعليقها الخاص: "written by ProcedureDocumentStatus.tsx") لكنها تحسب الفارق بين منتصف ليل اليوم ومنتصف ليل تاريخ الانتهاء (حساب تقويمي حقيقي) — طريقة مختلفة جذرياً. بالنسبة لمستخدم بتوقيت بيروت، يُصبح "منتصف ليل UTC" لتاريخ الانتهاء متأخراً 3 ساعات عن "منتصف ليل بيروت" الحقيقي، فينتج فارقاً منهجياً يتكرر يومياً: تاريخ انتهاء مضبوط على "غداً" ويُفحص الساعة 1:00 صباحاً بتوقيت بيروت يُظهر "يوم واحد متبقٍ" في الجرس (`NotificationBell`) لكن "يومان" (٢) في `ProcedureDocumentStatus` — نفس التاريخ المخزَّن، رقمان وصيغتان نحويتان مختلفتان معروضتان في آن واحد.

**التحقق من الوصول الحي**: كلا المكوّنين يُعرَضان في آن واحد فعلياً — `NotificationBell` عام في `TopNav.tsx` (وأيضاً `MobileMenu.tsx`)، و`ProcedureDocumentStatus` يُعرَض في `app/procedures/page.tsx` — أي أن أي مستخدم يفتح صفحة الإجراءات سيرى الاثنين معاً.

**الإصلاح**: تغيير `getStatus()` لتستخدم نفس اصطلاح التوقيت المُعتمَد سابقاً في الملفات الأخرى (`Asia/Beirut`، كما في `GovHolidayAlert.tsx`/`ProcedureVersionTag.tsx`): حساب "اليوم" الحالي بتوقيت بيروت عبر `toLocaleDateString('en-CA', { timeZone: 'Asia/Beirut' })`، ثم فارق تقويمي حقيقي بين منتصفي الليل.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureDocumentStatus.tsx`.

**ملاحظة تحقق إضافية**: فُحص الاحتمال المطروح بأن حقلي `steps`/`requiredDocuments` في `lib/serviceFAQ.ts` قد يحملان نفس تلوّث Python dict repr المُكتشَف سابقاً في `summary` — لم يُعثر على أي حالة تلوّث في أي منهما (بحث شامل عبر كل القيم)، فهذا الاحتمال يُغلَق الآن دون الحاجة لإصلاح.

### تحديث (batch #495) — ProcedureStartButton وProcedureCompletionBadge يسجّلان تاريخاً خاطئاً (UTC بدل بيروت) عند البدء/الإنجاز

**المشكلة**: كلا المكوّنين كانا يحفظان التاريخ عبر `new Date().toISOString().slice(0, 10)` — وهذا يُعيد التاريخ التقويمي بتوقيت **UTC**، وليس بتوقيت بيروت. بما أن بيروت متقدّمة دائماً عن UTC (+2 أو +3 حسب التوقيت الصيفي)، فخلال نافذة الساعتين إلى الثلاث ساعات التي تلي منتصف ليل بيروت (قبل أن يصل UTC لمنتصف ليله)، يكون تاريخ UTC لا يزال "أمس" بينما تاريخ بيروت الفعلي هو "اليوم".

**الأثر المؤكَّد بالمحاكاة**: عند محاكاة لحظة الساعة 01:00 بتوقيت بيروت (`2026-07-26T22:00:00Z` = `2026-07-27T01:00+03:00`):
- الضغط على "بدأت هذه المعاملة" يُخزِّن `2026-07-26` (بينما التاريخ الفعلي ببيروت هو `2026-07-27`) — فتعرض `daysElapsed()` فوراً "بدأت منذ يوم واحد" بدل "بدأت اليوم".
- نفس الخلل بالضبط في `ProcedureCompletionBadge.tsx` عند "أنهيت هذه المعاملة" — يُسجَّل ويُعرَض تاريخ إنجاز أقدم بيوم واحد من التاريخ الفعلي، بشكل دائم (لا يُصحَّح لاحقاً).

**التحقق من الوصول الحي**: كلا المكوّنين مُستوردان ومُستخدَمان فعلياً في `app/procedures/page.tsx:871-872` على كل بطاقة إجراء مُثرى.

**نفس فئة الخلل** المُصلَحة سابقاً في `GovHolidayAlert.tsx` (batch #481)، `ProcedureVersionTag.tsx` (batch #482)، و`ProcedureDocumentStatus.tsx` (batch #494) — لكن هذين المكوّنين لم يُلمَسا من قبل رغم أن `formatDate()` في `ProcedureCompletionBadge.tsx` تفترض أصلاً توقيت بيروت (`timeZone: 'Asia/Beirut'`) للعرض، بينما التخزين نفسه كان يستخدم UTC.

**الإصلاح**: إضافة دالة مُصدَّرة `todayLb()` في `ProcedureStartButton.tsx` (تُرجِع تاريخ اليوم بتوقيت بيروت عبر `toLocaleDateString('en-CA', { timeZone: 'Asia/Beirut' })`)، واستخدامها في كلا موضعي `ProcedureStartButton.tsx` (السطرين 28 و94 سابقاً)، واستيرادها واستخدامها أيضاً في `ProcedureCompletionBadge.tsx` (السطرين 28 و78 سابقاً) بدل `toISOString().slice(0,10)`.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureStartButton.tsx` و`components/ProcedureCompletionBadge.tsx`.

**ملاحظة للمتابعة (منخفضة الأولوية، لم تُصلَح)**: 16 سجلاً في `lib/serviceFAQ.ts` يحمل حقل `summary` مقطوعاً عند 300 حرف بالضبط في منتصف كلمة بلا "…" — أثر تجميلي فقط (معلومة ناقصة، لا نص فاسد)، مرشّح لدفعة قادمة إن رغب المستخدم بإصلاحه.

### تحديث (batch #496) — آخر حالة من خلل UTC مقابل بيروت: حقل اختيار الموعد النهائي يسمح باختيار تاريخ منقضٍ فعلياً

**المشكلة**: `app/procedures/page.tsx:904` (خاصية `min` لحقل `<input type="date">` الخاص بتحديد موعد نهائي للإجراء) كان يستخدم `new Date().toISOString().slice(0, 10)` — تاريخ UTC وليس تاريخ بيروت.

**الأثر**: خلال نافذة الساعتين إلى الثلاث ساعات التي تلي منتصف ليل بيروت (قبل وصول UTC لمنتصف ليله)، يبقى `min` يساوي تاريخ الأمس بتوقيت UTC، فيسمح للمستخدم باختيار تاريخ "الأمس" الفعلي كموعد نهائي رغم أنه منقضٍ فعلاً. بما أن `components/ProcedureDeadlineAlert.tsx` (المستهلك الحي لهذه البيانات عبر `setDeadline()`) يحسب "الأيام المتبقية" بتوقيت منتصف الليل المحلي الصحيح، فإن أي موعد يُحفَظ بهذه الطريقة الخاطئة يُصنَّف فوراً كـ"موعد فائت" (🚨) بعد حفظه مباشرة — تجربة مستخدم متناقضة ومربكة.

**التحقق**: تأكيد أن هذا هو آخر مثال حي غير مُصلَح من نفس فئة الخلل المُصلَحة في 5 مكوّنات سابقة (`GovHolidayAlert.tsx`، `ProcedureVersionTag.tsx`، `ProcedureDocumentStatus.tsx`، `ProcedureStartButton.tsx`، `ProcedureCompletionBadge.tsx`) — بحث شامل عبر الشجرة الكاملة لم يظهر أي استخدام آخر حي وغير مُصلَح لنمط `toISOString().slice(0, 10)` الخام. مكوّنات مشابهة في نفس المستودع (`SmartReminder.tsx`، `ProcedureRemindMeLater.tsx`) تستخدم بالفعل الصيغة الصحيحة `toLocaleDateString('en-CA', { timeZone: 'Asia/Beirut' })`.

**تحقق إضافي (بلا إصلاح، بالتصميم)**: فُحص استخدام مشابه لـ`toISOString()` في `ProcedureDeadlineAlert.tsx:62` (مفتاح "snooze") — تبيّن أنه **متّسق ذاتياً عمداً** (تعليق الكود نفسه يشرح أن الكتابة والقراءة كلتاهما تستخدمان نفس تعبير UTC، وأن تغيير أحدهما فقط دون الآخر سيكسر خاصية "التأجيل" من جديد) — لا خلل حالياً، تُرِك كما هو.

**الإصلاح**: تغيير `min` إلى `new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Beirut' })`.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/procedures/page.tsx` (سطر واحد + تعليق توضيحي).

### تحديث (batch #497) — تسريب نصوص عربية خام (اسم الجهة والمدة) داخل صفحة الإجراء بوضع اللغة الإنجليزية

**المشكلة**: `app/procedures/[slug]/ProcedureDetailClient.tsx:182-185` — شرائح "الجهة" و"المدة" لكل خطوة (`step.authority` و`step.duration`) كانت تُعرض دون أي شرط `isAr`، على عكس كل حقل آخر في نفس المكوّن (العنوان والوصف يُفرَّعان دائماً عبر `isAr ? x_ar : x_en`).

**السبب الجذري**: نموذج البيانات نفسه في `lib/procedures.ts` (النوع `ProcedureStep` في `lib/types.ts:82-83`) لا يحتوي على حقلي `authority_en`/`duration_en` — بخلاف `title_ar`/`title_en` و`description_ar`/`description_en` الموجودين لكل خطوة. تحقّق فعلي عبر `grep` أظهر 38 قيمة `authority` و100 قيمة `duration` في `lib/procedures.ts`، وكلها نصوص عربية خالصة (مثل: `'الأمن العام'`، `'1–2 أسابيع'`، `'نفس اليوم أو يومان'`) بلا أي مقابل إنجليزي.

**الأثر**: مستخدم يتصفح صفحة إجراء (مثل جواز السفر `/procedures/passport`) بوضع اللغة الإنجليزية يرى نصاً عربياً خاماً بمنتصف جملة إنجليزية، مثال: "Go to the nearest General Security center and submit the file. 🏛 **الأمن العام** 🕐 **1 ساعة**" — لكل خطوة تحتوي هذين الحقلين، أي لغالبية الخطوات في معظم الإجراءات.

**التحقق**: تأكيد حي عبر `grep -n "ProcedureDetailClient" app/procedures/[slug]/page.tsx` (استيراد + استخدام JSX فعلي عبر `generateStaticParams` لكل الإجراءات)، و`isAr` مصدره `useLanguage()` من `lib/LanguageContext.tsx` — سياق لغة حقيقي وقابل للتبديل من المستخدم (وليس قيمة ثابتة أو ميتة).

**الإصلاح**: بما أنه لا توجد ترجمة إنجليزية حقيقية لهذين الحقلين في مصدر البيانات (إضافة `authority_en`/`duration_en` لكل الـ138 قيمة هي مهمة محتوى منفصلة، وليست إصلاح كود)، تم اختيار الحل الآمن الفوري: إخفاء هاتين الشريحتين في وضع اللغة الإنجليزية إلى حين توفر ترجمة حقيقية، بدل عرض نص عربي خاطئ أو اختراع ترجمة:
```tsx
{step.authority && isAr && <span ...>{step.authority}</span>}
{step.duration && isAr && <span ...>{step.duration}</span>}
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/procedures/[slug]/ProcedureDetailClient.tsx` (سطرين + تعليق توضيحي). ملاحظة للمستقبل: إتمام الترجمة الحقيقية لحقلي authority/duration عبر كل الإجراءات في `lib/procedures.ts` يبقى تحسيناً محتوى مرغوباً، لكنه خارج نطاق هذا الإصلاح البرمجي.

### تحديث (batch #498) — NotificationBell.tsx يحسب الأيام المتبقية بتوقيت الجهاز المحلي بدل بيروت، ما يناقض ProcedureDocumentStatus.tsx على نفس البيانات

**المشكلة**: `components/NotificationBell.tsx:49-53` — دالة `daysUntil()` كانت تستخدم `new Date(); now.setHours(0,0,0,0)` (منتصف ليل **الجهاز المحلي**) بدل منتصف ليل بيروت، بينما `ProcedureDocumentStatus.tsx` (المُصلَح سابقاً في batch #494) يقرأ **نفس** مفاتيح `dalilak_doc_expiry_{code}_{i}` لكنه يحسب الفرق بالاستناد إلى منتصف ليل بيروت (`toLocaleDateString('en-CA', { timeZone: 'Asia/Beirut' })`).

**الأثر**: لمستخدم لبناني مغترب (جمهور حقيقي لهذا التطبيق، كما ورد في تعليق `ProcedureVersionTag.tsx`) بجهاز مضبوط على منطقة زمنية غير بيروت، يظهر عدد أيام مختلف في جرس الإشعارات (`NotificationBell`، مثبّت في `TopNav.tsx` و`MobileMenu.tsx` — أي حاضر عملياً في كل صفحة) عن العدد الظاهر في `ProcedureDocumentStatus.tsx` لنفس تاريخ انتهاء الصلاحية المحفوظ — بالضبط التناقض الذي حذّر منه تعليق الكود في `ProcedureDocumentStatus.tsx` نفسه دون أن يُصلَح هنا.

**تحقق إضافي**: اكتُشف تناقض داخلي إضافي في نفس الملف — السطر ~127 يحسب `today` بالفعل بتوقيت بيروت الصحيح لتصفية التذكيرات المستحقة (`r.date > today`)، لكنه بعدها يمرر التاريخ إلى `daysUntil()` (بتوقيت الجهاز) لحساب عدد الأيام المعروض — أي أن نفس الدالة كانت تُستخدم بمعيارين مختلفين ضمن نفس الملف.

**التحقق من الحيوية**: `NotificationBell` مستورد ومُستخدم فعلياً في `components/TopNav.tsx:287` و`components/MobileMenu.tsx:366`. مسار المستندات (`'doc'`) له كاتب حي مؤكد (`ProcedureDocumentStatus.tsx`)؛ مسار المواعيد (`'appt'`) تبيّن أنه بلا كاتب حي حالياً (لا استخدام لـ`dalilak_appointments` كمفتاح كتابة في أي مكان بالمستودع) فلم يُحتسب كدليل إضافي.

**الإصلاح**:
```ts
function daysUntil(dateStr: string): number {
  const todayLb = new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Beirut' })
  const d = new Date(dateStr + 'T00:00:00')
  return Math.round((d.getTime() - new Date(todayLb + 'T00:00:00').getTime()) / 86_400_000)
}
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/NotificationBell.tsx` (دالة واحدة + تعليق توضيحي).

### تحديث (batch #499) — ProcedureFeeHistory.tsx يستخدم إزاحة UTC+3 ثابتة (صيفية فقط) لتوقيت بيروت، فيُخطئ حساب الأيام لمدة 5 أشهر في السنة

**المشكلة**: `components/ProcedureFeeHistory.tsx:27-29` — دالة `daysDiff()` كانت تحلّل التاريخ المحفوظ بإزاحة ثابتة `+03:00`:
```ts
function daysDiff(iso: string): number {
  const then = new Date(iso + 'T00:00:00+03:00').getTime()
  return Math.floor((Date.now() - then) / 86400000)
}
```
لكن بيروت تكون بتوقيت UTC+3 فقط خلال التوقيت الصيفي (أواخر آذار–أواخر تشرين الأول)؛ بقية السنة تكون UTC+2. تحقّق فعلي عبر بيانات ICU الحقيقية لمنطقة `Asia/Beirut`:
```
2026-01-15 → GMT+2
2026-03-01 → GMT+2
2026-07-26 → GMT+3
2026-11-01 → GMT+2
2026-12-31 → GMT+2
```

**الأثر**: خلال أشهر الشتاء، كانت الدالة تُحلّل التاريخ المحفوظ بفارق ساعة كاملة قبل منتصف ليل بيروت الفعلي. عملياً، خلال الساعة الأخيرة من كل يوم شتوي (23:00–00:00 بتوقيت بيروت)، كان عداد "الأيام منذ الدفع" يتقدم يوماً كاملاً قبل الأوان — فتظهر عبارة "منذ يوم واحد" بدل "دفعت اليوم ✓"، وحد الـ30 يوماً للتلوين (كهرماني/أخضر، الأسطر 97-98) ينزاح معها بيوم مبكراً.

**التحقق من الحيوية**: `ProcedureFeeHistory` مستورد ومُستخدم فعلياً في `app/procedures/page.tsx:773` — يظهر داخل كل بطاقة إجراء موسّعة عند الضغط على "تسجيل الدفع الآن".

**الإصلاح**: الاستغناء الكامل عن أي إزاحة UTC ثابتة، والاعتماد فقط على سلسلة التاريخ التقويمي المشتقة من `Asia/Beirut` (نفس نمط `NotificationBell.tsx`):
```ts
function daysDiff(iso: string): number {
  const todayLb = new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Beirut' })
  const then = new Date(iso + 'T00:00:00').getTime()
  const now  = new Date(todayLb + 'T00:00:00').getTime()
  return Math.round((now - then) / 86400000)
}
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureFeeHistory.tsx` (دالة واحدة + تعليق توضيحي).

### تحديث (batch #500) — 16 ملخّص سؤال وجواب مقطوع منتصف الجملة بلا علامة حذف (…)

**المشكلة**: 16 من أصل 60 سجلاً في `lib/serviceFAQ.ts` لديها حقل `summary` مقطوع عند 300 حرف بالضبط (تأكيد عبر سكريبت Node: أطول ملخّص في الملف كله هو 300 حرفاً بالضبط، و16 سجلاً يصلون لهذا الحد تماماً) — أثر واضح لخط أنابيب توليد آلي قطع النص دون أي علامة "…". مثال على نهاية ملخّص مقطوع: `"...بالتقسيط أو القرض المصرفي، تُستوفى رسوم "`.

**التحقق من الأثر**: `app/faq/page.tsx:258` يعرض `item.summary` كاملاً وخاماً دون أي تقصير إضافي عند التوسيع — بخلاف `components/ProcedureFAQChips.tsx`'s `previewFor()` الذي يُعيد اقتطاع المعاينة عند 160 حرفاً مع علامة "…" خاصة به، فلا يظهر هذا القطع هناك. لكن في صفحة `/faq` نفسها، يرى المستخدم فعلياً جملة تنتهي بشكل مفاجئ ومبتور.

**الإصلاح**: دالة عرض `displaySummary(s)` في `app/faq/page.tsx` تُضيف "…" فقط عندما يبلغ طول النص 300 حرفاً أو أكثر (أي عند حد القطع المؤكد فعلياً)، دون المساس بالمحتوى الحقيقي نفسه:
```ts
function displaySummary(s: string): string {
  return s.length >= 300 ? s + '…' : s
}
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/faq/page.tsx` (دالة واحدة + تعليق توضيحي). ملاحظة للمستقبل: الإصلاح الأمثل الكامل هو إعادة توليد/إكمال هذه الملخصات الـ16 في `lib/serviceFAQ.ts` نفسها — مهمة محتوى منفصلة خارج نطاق هذا الإصلاح البرمجي.

### تحديث (batch #501) — تذكيرات جرس الإجراء لا يمكن إخفاؤها أبداً بعد استحقاقها؛ تبقى في قائمة التنبيهات إلى الأبد

**المشكلة**: `components/ProcedureReminderBell.tsx` (الجرس الظاهر على كل بطاقة إجراء، مُستخدم فعلياً في `app/procedures/page.tsx:648`) يضيف تذكيرات إلى `dalilak_reminders` عبر `addReminder()`، لكن لا يحتوي على أي واجهة لإلغاء أو حذف تذكير — فقط أزرار إضافة. هذه التذكيرات تُقرأ لاحقاً في `components/NotificationBell.tsx` (مثبّت عالمياً في `TopNav.tsx` و`MobileMenu.tsx`، أي في كل صفحة)، وتظهر في قائمة التنبيهات المنسدلة دون أي زر لإخفائها هناك أيضاً — فقط زر "اضغط لاسأل دليلك".

**التحقق من عدم وجود أي مسار حي للإلغاء**: البحث الشامل عن كل موضع يضبط `dismissed: true` أو يحذف تذكيراً أظهر مسارين فقط:
- `SmartReminder.tsx` يحتوي على منطق `dismiss()`/`deleteReminder()` صحيح، لكن مكوّنه الافتراضي المُصدَّر غير مُركَّب في أي مكان حي بالتطبيق (فقط دوال مُصدَّرة منه مثل `getReminders()` تُستورد، لا `<SmartReminder />` نفسها) — واجهته معطّلة فعلياً.
- `ProcedureRemindMeLater.tsx` يستدعي `removeReminder('quick_' + code)` فقط — بمعرّف ثابت خاص بتذكيره هو، لا يمكنه أبداً مطابقة معرّف تذكير أُنشئ عبر `ProcedureReminderBell` (الذي يستخدم `Date.now().toString()`).

**الأثر**: أي تذكير يُضاف عبر جرس الإجراء يظهر في القائمة العالمية بمجرد استحقاقه، ويبقى ظاهراً كل يوم إلى ما لا نهاية، يضخّم عدّاد الشارة الحمراء، ولا توجد أي طريقة لإخفائه سوى مسح localStorage يدوياً من أدوات المطوّر.

**الإصلاح**: تصدير دالة `dismissReminderById(id)` من `components/SmartReminder.tsx` (تكرار للمنطق الصحيح الموجود أصلاً هناك، بلا إحياء لواجهته الميتة)، واستخدامها في `components/NotificationBell.tsx`: إضافة حقل `rawId` إلى `NotiItem`، وإعادة هيكلة عنصر القائمة من زر واحد بعرض كامل إلى صف يحتوي زرّ "اسأل دليلك" (كما كان) بجانب زر إخفاء "✕" منفصل يظهر فقط لعناصر النوع `reminder` (عناصر المستندات والمواعيد لها مسار حل آخر بالفعل — تعديل/مسح تاريخ الانتهاء).

`tsc --noEmit` نظيف (exit 0). الإصلاح محصور في `components/SmartReminder.tsx` (دالة مُصدَّرة جديدة) و`components/NotificationBell.tsx` (حقل جديد + إعادة هيكلة عنصر القائمة + زر إخفاء).

### تحديث (batch #502) — 61 سجلاً في lib/allTransactions.ts يعرضون نص إنجليزي خام (بقايا استخراج آلي) كعنوان عربي

**المشكلة**: `lib/allTransactions.ts:3407` — عند بناء `TX_ALL` من مصفوفة `_R` المستخرجة آلياً من dawlati.gov.lb، الحقل `title` (الذي تعامله كل المستهلكين كعنوان عربي) يساوي حرفياً `titleEn` في 61 من أصل 2484 سجلاً — تأكيد فعلي عبر سكريبت Node يحلّل مصفوفة `_R` مباشرة من الملف: 61 سجلاً بالضبط، منها 2 لديهما `hasForm=1`. أمثلة حقيقية: `'GENSEC0213'` (رمز خدمة خام من الأمن العام)، و`'Illegal Birth Registration (Lebanese citizen living abroad) - E-Services Detail'` (نص إنجليزي كامل مع لاحقة استخراج `" - E-Services Detail"`، من وزارة الداخلية والبلديات).

**التحقق من الحيوية**: `app/forms/page.tsx` يستورد `TX_ALL`/`filterTxAll`/`filterTxForms` ويعرض `{isAr ? tx.title : (tx.titleEn || tx.title)}` بلا أي حارس لجودة البيانات (بخلاف `isCorruptDictRepr()` المستخدم لمشكلة مشابهة في `serviceFAQ.ts`) — في السطرين 223 و272، وتبويب `forms-tx` (الذي يحوي هذه السجلات) هو التبويب الافتراضي عند فتح `/forms` (`useState<ViewTab>('forms-tx')`). حتى زر "اسأل دليلك" يُدرج النص الإنجليزي الخام داخل جملة عربية: `` `ما هي متطلبات وإجراءات معاملة: ${tx.title}؟` ``.

**الإصلاح**: في نقطة بناء `TX_ALL` نفسها — إزالة لاحقة الاستخراج `" - E-Services Detail"`، وللسجلات الـ61 التي لا تحتوي أي حرف عربي حقيقي، إضافة اسم الوزارة العربي (متوفر دائماً) كبادئة بدل عرض نص إنجليزي/رمز خام كعنوان عربي:
```ts
const hasArabicChars = (s: string) => /[؀-ۿ]/.test(s)
const stripScrapeSuffix = (s: string) => s.replace(/\s*-\s*E-Services Detail\s*$/i, '').trim()

export const TX_ALL:TxItem[]=_R.map(([title,mid,hf,titleEn],i)=>{
  const cleanTitle = stripScrapeSuffix(title)
  const cleanTitleEn = stripScrapeSuffix(titleEn || title)
  return {
    id:`tx-${i}`,
    title: hasArabicChars(cleanTitle) ? cleanTitle : `${_M[mid][0]} — ${cleanTitle}`,
    titleEn: cleanTitleEn,
    ministry:_M[mid][0],ministryEn:_M[mid][1],ministrySlug:_M[mid][2],icon:_M[mid][3],
    pdfUrl:_P[i]||'',hasForm:hf===1
  }
})
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `lib/allTransactions.ts` (نقطة بناء `TX_ALL` وحدها، لم تُعدَّل بيانات `_R` الخام نفسها ولا أي حقل آخر).

### تحديث (batch #503) — رمز QR لمشاركة الإجراء يشير إلى نطاق ميت غير موجود

**المشكلة**: `components/ProcedureQRShare.tsx:276` كان يبني الرابط المُرمَّز في QR باستخدام `https://dalilak.app/...` — نطاق مختلف تماماً عن التطبيق الفعلي. تحقّق فعلي: زيارة `https://dalilak.app` أدت لانتهاء المهلة (timeout) دون أي استجابة؛ بينما `https://dalilak-frontend.vercel.app` (النطاق الحقيقي) يعرض الصفحة الرئيسية الفعلية لدليلك.

**التحقق من التفرّد**: بحث شامل عبر الكود أظهر أن كل موضع آخر يبني رابطاً كاملاً (`app/layout.tsx` — `metadataBase`، `app/sitemap.ts`، `app/robots.ts`، `lib/breadcrumbJsonLd.ts`، `components/ProcedureShareViaEmail.tsx`، `components/ChatMessageActions.tsx`) يستخدم `https://dalilak-frontend.vercel.app` بشكل متسق — `ProcedureQRShare.tsx` هو الملف الوحيد الذي كان يشير لنطاق مختلف.

**التحقق من الحيوية**: المكوّن مستورد ومُستخدم فعلياً في `app/procedures/page.tsx:1050`، ويظهر كزر/شريحة "QR" على كل بطاقة إجراء موسّعة، بنافذة منبثقة نصّها "امسح الرمز بكاميرا هاتفك للوصول المباشر" — وعداً لا يتحقق أبداً لأن الرابط المُرمَّز كان يشير لنطاق ميت.

**الإصلاح**: تصحيح النطاق إلى `https://dalilak-frontend.vercel.app`، مطابقاً لكل الاستخدامات الأخرى في الكود:
```ts
const url = `https://dalilak-frontend.vercel.app/procedures?q=${encodeURIComponent(isAr ? titleAr : (titleEn || titleAr))}`
```

**ملاحظة للمتابعة**: حتى بعد هذا الإصلاح، الرابط لا يزال يستخدم بحثاً نصياً حراً بالعنوان (`?q=`) بدل معرّف الإجراء الثابت (`proc.code`، المُمرَّر للمكوّن لكنه غير مُستخدم فعلياً في بناء الرابط)، و`app/procedures/page.tsx` لا يقرأ حالياً أي `?q=` من رابط الصفحة عبر `useSearchParams()` ليطبّق فلترة فعلية عند الوصول — فالوعد الكامل بـ"الوصول المباشر" يتطلب تعديلاً أوسع خارج هذا المكوّن (قراءة معامل الرابط وتطبيق فلترة دقيقة بالمعرّف). هذا الإصلاح يعالج العطل الأساسي (النطاق الميت) فقط، وهو الإصلاح الآمن والفوري القابل للتحقق.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureQRShare.tsx` (سطر واحد + تعليق توضيحي).

### تحديث (batch #504) — مشاركة الإجراء بالبريد الإلكتروني تُدرج الخطوات بالعربية دائماً حتى في وضع اللغة الإنجليزية

**المشكلة**: `components/ProcedureShareViaEmail.tsx:29` (داخل `buildMailto()`) — حقل `steps` كان الحقل الوحيد بلا تفريع `isAr`:
```ts
const steps = proc.steps.map((s, i) => `${i + 1}. ${s}`)
```
بينما كل حقل آخر في نفس الدالة (`title`، `ministry`، `fee`، `time`، وحتى `docs`) يتفرّع بشكل صحيح على `isAr` مع الرجوع لنسخته الإنجليزية `_en` عند توفرها.

**التحقق من الحيوية**: `app/procedures/page.tsx:34/1045` يستورد ويعرض المكوّن فعلياً في قسم "مشاركة وطباعة" لكل بطاقة إجراء، مُمرِّراً `proc` و`isAr` الحقيقيين. تحقّق عبر Node أن `steps_en` مُعبَّأ فعلياً في جميع الإجراءات الـ71 بملف `lib/enrichedProcedures.ts` — أي أن الخلل يؤثر على كل إجراء، وليس حالة بيانات جزئية.

**دليل إضافي على أن هذا سهو لا تصميم مقصود**: زر "مشاركة عبر واتساب" المجاور (في `app/procedures/page.tsx:1011`) يطبّق المنطق الصحيح فعلاً: `isAr ? proc.steps : (proc.steps_en?.length ? proc.steps_en : proc.steps)` — ما يؤكد أن `ProcedureShareViaEmail.tsx` هو الموضع الوحيد الذي فاته هذا التفريع.

**الأثر**: مستخدم بوضع اللغة الإنجليزية يضغط "مشاركة بالبريد" يحصل على رسالة بعنوان وتحية وتسميات إنجليزية، لكن خطوات الإجراء نفسها بالعربية في منتصف الرسالة — غير مفهومة لمستلم لا يقرأ العربية، ومتناقضة مع زر واتساب المجاور في نفس الواجهة.

**الإصلاح**:
```ts
const steps = (isAr ? proc.steps : (proc.steps_en?.length ? proc.steps_en : proc.steps))
  .map((s, i) => `${i + 1}. ${s}`)
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureShareViaEmail.tsx` (سطر واحد + تعليق توضيحي).

### تحديث (batch #505) — سجل نشاط الإجراء لا يسجّل أبداً أحداث "بدأت"/"أكملت"، فقط "تم العرض"

**المشكلة**: `components/ProcedureHistoryLog.tsx` مبني بالكامل لدعم ثلاثة أحداث: `viewed`/`started`/`completed` (تسميات، أيقونات، منطق إزالة التكرار — كلها جاهزة في `EVENT_LABELS`)، لكن دالة `addHistoryEvent()` المُصدَّرة منه لها موضع استدعاء واحد فقط في كامل الكود (تأكيد عبر `grep -rn "addHistoryEvent("`): الاستدعاء الداخلي لحدث `'viewed'` في الملف نفسه. لا `ProcedureStartButton.tsx`'s `setStartDate()` ولا `ProcedureCompletionBadge.tsx`'s `markCompleted()` كانا يستدعيانها.

**التحقق من الحيوية**: الثلاثة مكوّنات (`ProcedureStartButton`، `ProcedureCompletionBadge`، `ProcedureHistoryLog`) مستوردة ومعروضة متجاورة في نفس بطاقة الإجراء الموسّعة (`app/procedures/page.tsx:871/872/880`).

**الأثر**: مستخدم يضغط "بدأت هذه المعاملة" ثم "أنهيت هذه المعاملة" — كلا الزرّين يُحدّثان شارتهما الخاصة بنجاح — لكن "سجل النشاط" أسفلهما مباشرة لا يعرض هذين الحدثين أبداً؛ يبقى عالقاً على "تم العرض" إلى الأبد مهما تقدّم المستخدم فعلياً في الإجراء. سطر "آخر حدث" في رأس السجل يبقى مضللاً بعرض "👁️ تم العرض" حتى بعد إكمال الإجراء كلياً.

**الإصلاح**: استدعاء `addHistoryEvent(code, 'started')` داخل `setStartDate()` و`addHistoryEvent(code, 'completed')` داخل `markCompleted()` — لا خطر من استيراد دائري (`ProcedureHistoryLog.tsx` لا يستورد من أي من الملفين):
```ts
// ProcedureStartButton.tsx
export function setStartDate(code: string) {
  const today = todayLb()
  try { localStorage.setItem(LS_PREFIX + code, today) } catch {}
  addHistoryEvent(code, 'started')
  ...
}
// ProcedureCompletionBadge.tsx
export function markCompleted(code: string) {
  const today = todayLb()
  try { localStorage.setItem(LS_COMPLETE + code, today) } catch {}
  addHistoryEvent(code, 'completed')
  ...
}
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ProcedureStartButton.tsx` و`components/ProcedureCompletionBadge.tsx` (استيراد + سطر واحد لكل منهما).

### تحديث (batch #506) — فلتر "سرعة الإنجاز" في البحث المتقدم يُقصي 30% من الإجراءات الحقيقية من كل تصنيف بسبب صيغة الجمع المكسّر

**المشكلة**: `app/procedures/page.tsx:216-224` — فلتر `advFilters.speed` (ضمن `filteredEnriched`) يتحقق من احتواء `processingTime` على الكلمة المفردة فقط:
```ts
if (advFilters.speed === 'fast')   return pt.includes('يوم') || pt.includes('day') || ...
if (advFilters.speed === 'normal') return pt.includes('أسبوع') || pt.includes('week') || ...
```
لكن الجمع المكسّر العربي لـ"يوم" هو "أيام" وليس "أياموم" أو ما يحتوي حرفياً على "يوم" كسلسلة فرعية — الهمزة في بداية "أيام" تكسر التطابق النصي. نفس الأمر لـ"أسبوع" وجمعها "أسابيع":
```
$ node -e "console.log('أيام'.includes('يوم')); console.log('أسابيع'.includes('أسبوع'));"
false
false
```

**التحقق من الحيوية**: `app/procedures/page.tsx:27/1173` يستورد ويعرض `ProcedureFilterDrawer` فعلياً كزر "فلترة متقدمة" على صفحة `/procedures`؛ `filteredEnriched` (الذي يطبّق هذا الفلتر) هو نفسه المصفوفة المعروضة كقائمة الإجراءات، و`totalResults={filteredEnriched.length + filteredGuided.length}` هو العدد المعروض على زر "عرض N نتيجة" في نفس الدرج.

**التحقق من البيانات الحقيقية**: فحص كل الإجراءات الـ71 في `lib/enrichedProcedures.ts`:
```
$ node -e "... يفحص processingTime لكل سجل ..."
{ total: 71, nonEmpty: 70, matchedNone: 21 }   // قبل الإصلاح: 21 من 70 سجلاً حقيقياً لا يُصنَّف تحت أي تصنيف سرعة
```
من الأمثلة: `'3–5 أيام عمل'` (إجراء وزارة العمل للموافقة المسبقة على عاملة/عامل خدمة منزلية — عملية من 3-5 أيام فقط، يجب أن تُصنَّف "سريع")، و`'4–8 أسابيع'` (يتكرر 4 مرات، يجب أن يُصنَّف "عادي"). فحص الإيجابيات الكاذبة: السجلات بصيغة المفرد/المثنى/التنوين (`'يوم واحد'`، `'يومان'`، `'15 يوماً'`، `'أسبوعان'`) كانت تُطابَق بشكل صحيح أصلاً، والسجلات بـ"أشهر" كانت تُطابَق بشكل صحيح أصلاً لأن "أشهر" تحتوي فعلياً على "شهر" كسلسلة فرعية — الخلل خاص بالجمع المكسّر لـ"يوم" و"أسبوع" فقط، وليس عطلاً عاماً.

**الأثر**: مستخدم يفتح "فلترة متقدمة" على `/procedures` ويختار أي خيار سرعة (سريع/عادي/طويل) يفقد 21 إجراءً حقيقياً من كل نتيجة — بينها إجراءات سريعة فعلاً (3-5 أيام) وإجراءات متوسطة فعلاً (4-8 أسابيع)، بغض النظر عن التصنيف المختار. عداد النتائج على الدرج نفسه يعكس هذا النقص أيضاً.

**الإصلاح**: إضافة صيغتي الجمع المكسّر لكل من "يوم" و"أسبوع":
```diff
-        if (advFilters.speed === 'fast')   return pt.includes('يوم') || pt.includes('day') || pt.includes('ساعة') || pt.includes('hour')
-        if (advFilters.speed === 'normal') return pt.includes('أسبوع') || pt.includes('week') || pt.includes('أسبوعين')
+        if (advFilters.speed === 'fast')   return pt.includes('يوم') || pt.includes('أيام') || pt.includes('day') || pt.includes('ساعة') || pt.includes('hour')
+        if (advFilters.speed === 'normal') return pt.includes('أسبوع') || pt.includes('أسابيع') || pt.includes('week') || pt.includes('أسبوعين')
```
بعد الإصلاح: `matchedNone` انخفض من 21 إلى 3 سجلات فقط — المتبقية هي `'فوراً.'` (نص حر بلا وحدة زمنية)، `'حسب جدول الامتحانات الرسمية'` (مرتبط بجدول خارجي لا بمدة ثابتة)، و`'3 ايام عمل'` (تهجئة نادرة بلا همزة على الألف، سجل واحد فقط) — هذه الحالات الثلاث نصوص حرة أو تهجئات شاذة لا يمكن إصلاحها بإضافة كلمة مفتاحية بسيطة دون توسيع نطاق التغيير، ويُترك توثيقها هنا كملاحظة للدفعة القادمة إن استدعى الأمر.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/procedures/page.tsx` (سطرين).

### تحديث (batch #507) — فلتر "الرسوم" في البحث المتقدم يُصنِّف 7 إجراءات بلا بيانات رسوم كـ"مدفوعة" افتراضياً، رغم أن أحدها بعنوانه "رسم انتقال"

**المشكلة**: `app/procedures/page.tsx:209-214` — فلتر `advFilters.feeType` يتحقق فقط من كون النص يحتوي على "مجان"/"free"/"0"؛ عندما يكون حقل `fees` فارغاً تماماً (`''`)، تفشل كل هذه الشروط فتُصبح `isFree = false`، فيُصنَّف الإجراء تلقائياً كـ"مدفوع" رغم عدم وجود أي بيانات رسوم فعلية له إطلاقاً:
```ts
const fees = p.fees || ''
const isFree = fees.includes('مجان') || fees.toLowerCase().includes('free') || fees === '0'
return advFilters.feeType === 'free' ? isFree : !isFree   // fees === '' ⇒ isFree=false ⇒ يُحسب "مدفوع"
```

**التحقق من الحيوية**: نفس مسار `advFilters` المُتحقَّق منه في batch #506 (`ProcedureFilterDrawer` مستورد ومعروض في `app/procedures/page.tsx:27/1173`؛ `filteredEnriched` هو المصفوفة المعروضة فعلياً).

**التحقق من البيانات الحقيقية**: فحص `lib/enrichedProcedures.ts` يُظهر 7 سجلات حقيقية بحقل `fees:''` فارغ تماماً:
```
$ grep -c "fees:''" lib/enrichedProcedures.ts
7
```
من بينها 6 إجراءات مساعدات/منح اجتماعية يُرجَّح أنها مجانية فعلاً (`'طلب منح مدرسية لأولاد الشهداء'`، `'طلب منح مدرسية لأبناء المعتقلين'`، `'طلب مساعدة إضافية'`، `'طلب الانتساب إلى سجل المرضى المصابين بالقصور الكلوي المزمن'`، وغيرها من مجلس الجنوب ووزارة الصحة) — **لكن** السجل السابع عنوانه حرفياً `'إستكمال المستندات وتحرير التركة وتحقيق رسم الإنتقال'` (رسم انتقال تركة)، وهو يشير بوضوح إلى وجود رسم فعلي رغم أن الحقل فارغ. هذا يمنع أي افتراض آمن بأن "الحقل الفارغ = مجاني" — البيانات الحقيقية متضاربة، وتخمين المحتوى الفعلي (المبلغ أو حتى "مجاني") يخالف قاعدة عدم استبدال المحتوى الحقيقي بتخمينات.

**الأثر**: مستخدم يفلتر بـ"مدفوعة" يرى ضمن النتائج 6 إجراءات مساعدات لا تحمل أي بيانات فعلية تؤكد أنها مدفوعة (تصنيف مضلِّل)؛ ومستخدم يفلتر بـ"مجانية" لا يرى أياً من هذه الـ7 رغم أن معظمها على الأرجح مجاني فعلاً.

**الإصلاح**: بدلاً من تخمين أن الحقل الفارغ يعني "مجاني" (وهو تخمين غير آمن نظراً لوجود سجل "رسم الانتقال")، تم استثناء السجلات بحقل رسوم فارغ من نتائج الفلتر بالكامل — بدل تصنيفها الخاطئ كـ"مدفوعة":
```diff
       list = list.filter(p => {
         const fees = p.fees || ''
+        if (!fees) return false // fee not recorded for this procedure — don't assert it's paid or free
         const isFree = fees.includes('مجان') || fees.toLowerCase().includes('free') || fees === '0'
         return advFilters.feeType === 'free' ? isFree : !isFree
       })
```
هذا يزيل التصنيف الخاطئ كـ"مدفوعة" دون افتراض عكسي غير مؤكَّد بأنها "مجانية" — سلوك فلتر "مجانية" يبقى كما هو (استبعاد هذه السجلات، لأنها غير مؤكَّدة)، وسلوك فلتر "مدفوعة" يصبح صحيحاً (استبعادها بدل تضمينها خطأً). ملء بيانات الرسوم الفعلية للسجلات السبعة يتطلّب مصدراً رسمياً موثوقاً ويُترك لدفعة مستقبلية بتوثيق البيانات الحقيقية (خاصة سجل "رسم الانتقال" الذي على الأرجح ليس مجانياً).

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/procedures/page.tsx` (سطر واحد).

### تحديث (batch #508) — قسم "الإجراءات المُرشدة" (27 إجراءً) يتجاهل فلاتر البحث المتقدم كلياً ويبقى ظاهراً بلا استثناء

**المشكلة**: `app/procedures/page.tsx:182-195` — القائمة `filteredGuided` (مصدرها `PROCEDURES_DATA`، وهو نظام بيانات منفصل عن `lib/enrichedProcedures.ts`) لا تستخدم `advFilters` إطلاقاً في منطقها ولا في مصفوفة الاعتماديات:
```tsx
const filteredGuided = useMemo(() => {
  let list = PROCEDURES_DATA.filter(p => p.status === 'active')
  ...
  return list
}, [search, ministryFilter])   // advFilters غائب تماماً
```
بينما `filteredEnriched` (السطر 197-238) وحدها تطبّق `advFilters.hasForm`/`feeType`/`speed`/`started`. الأهم: بيانات `PROCEDURES_DATA` لا تملك حقل `hasForm` إطلاقاً (`grep -c "hasForm" lib/procedures.ts` → 0)، لذا لا يمكن تطبيق هذا الفلتر عليها حتى من حيث المبدأ.

**التحقق من الحيوية**: `filteredGuided.map(...)` يُعرَض مباشرة في السطر 442؛ `totalResults={filteredEnriched.length + filteredGuided.length}` في السطر 1178 يُمرَّر إلى `ProcedureFilterDrawer` (مستورد ومعروض فعلياً في السطر 27/1173-1180) ويظهر على زر "عرض N نتيجة".

**التحقق من البيانات الحقيقية**: `grep -c "status: 'active'" lib/procedures.ts` → 27 إجراءً مُرشداً نشطاً يبقى معروضاً دون أي فلترة. مثال ملموس: `lib/procedures.ts:262` — `slug: 'property-transfer'` ("نقل ملكية العقار") يحمل رسوماً حقيقية (`'رسوم تسجيل نقل الملكية' — '5% من قيمة العقار المقدّرة'` و`'رسوم كاتب العدل' — '0.5–1% من قيمة العقار'`) — ومع ذلك يبقى ظاهراً حتى عند اختيار فلتر "الرسوم: مجانية" أو "السرعة: سريع" أو "نموذج رسمي: يملك نموذجاً".

**الأثر**: مستخدم يفتح "فلترة متقدمة" ويختار أي خيار (رسوم/سرعة/نموذج/بدأ الإجراء) ويضغط "عرض N نتيجة" — يرى الـ27 إجراءً المُرشداً كاملة دون استثناء، وبينها إجراءات تناقض الفلتر المختار صراحة (كإجراء نقل ملكية عقار مدفوع تحت فلتر "مجانية"). الفلتر يبدو معطلاً لثلث محتوى الصفحة تقريباً في كل استخدام.

**الإصلاح**: بما أن بيانات `PROCEDURES_DATA` لا تملك حقولاً مكافئة لتطبيق `advFilters` عليها، تم إخفاء هذا القسم كلياً عند تفعيل أي فلتر متقدم (باستخدام الدالة الموجودة أصلاً `hasActiveFilters` من `ProcedureFilterDrawer.tsx`)، بدل تجاهل الفلتر بصمت:
```diff
   const filteredGuided = useMemo(() => {
+    // advFilters (hasForm/feeType/speed/started) has no matching field on
+    // guided PROCEDURES_DATA — showing this section unfiltered while an
+    // advanced filter is active silently defeats the filter for it.
+    if (hasActiveFilters(advFilters)) return []
     let list = PROCEDURES_DATA.filter(p => p.status === 'active')
     ...
     return list
-  }, [search, ministryFilter])
+  }, [search, ministryFilter, advFilters])
```
`hasActiveFilters` كانت مستوردة أصلاً في السطر 27 دون استخدام لهذا الغرض تحديداً.

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/procedures/page.tsx` (4 أسطر).

### تحديث (batch #509) — استرجاع "المحادثات السابقة" يحذف بصمت أحدث الرسائل من أي محادثة تتجاوز 40 رسالة

**المشكلة**: `components/ChatHistoryPanel.tsx:45` — دالة `saveChatSession()` تحفظ `messages.slice(0, 40)` أي **أقدم** 40 رسالة (المصفوفة زمنية تصاعدية، الرسائل الجديدة تُضاف في النهاية عبر `setMessages(prev => [...prev, ...])`)، بينما `messageCount` المحفوظ يبقى العدد الحقيقي الكامل:
```ts
const session: ChatSession = {
  ...
  messageCount: messages.length,                 // العدد الحقيقي الكامل
  messages: messages.slice(0, 40).map(...),       // لكن المحفوظ فعلياً هو أقدم 40 رسالة فقط!
}
```

**التحقق من الحيوية**: `app/page.tsx:35` يستورد `saveChatSession`، وتُستدعى فعلياً عند "محادثة جديدة" (`onNewChat` السطر 1352) وعند الضغط على الصفحة الرئيسية (`onHomeClick` السطر 2507، `onHome` السطر 2590) بتمرير `messages` الحقيقية للمحادثة الحالية. `setMessages(prev => [...prev, newMsg])` مؤكَّد في مواضع متعددة (مثلاً السطور 1054، 1060) — أي أن المصفوفة زمنية تصاعدية فعلاً، وأحدث رسالة دوماً في النهاية. زر "استرجاع" في `ChatHistoryPanel.tsx` يمرر `s.messages` مباشرة إلى `onRestore` الذي يستدعي `setMessages(msgs)` في `app/page.tsx:1511`.

**الأثر**: مستخدم لديه محادثة تتجاوز 40 رسالة، يضغط "محادثة جديدة" أو الصفحة الرئيسية — الشارة المعروضة في اللوحة تقول العدد الحقيقي (مثلاً "52 رسالة")، لكن الضغط على "استرجاع" يعيد فقط أول 40 رسالة، أي يحذف بصمت آخر 12+ رسالة (عادة الأهم والأحدث) دون أي تنبيه بأن شيئاً فُقِد. التناقض بين الشارة والمحتوى الفعلي المسترجَع ملحوظ ومباشر.

**الإصلاح**: الاحتفاظ بأحدث 40 رسالة بدل أقدمها:
```diff
-      messages: messages.slice(0, 40).map(m => ({ role: m.role, content: m.content.slice(0, 2000) })),
+      messages: messages.slice(-40).map(m => ({ role: m.role, content: m.content.slice(0, 2000) })),
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/ChatHistoryPanel.tsx` (سطر واحد).

### تحديث (batch #510) — زر "حفظ في الملاحظات" أسفل ردود المحادثة يعرض علامة نجاح خضراء، لكن الملاحظة تختفي إلى الأبد ولا يمكن استرجاعها أبداً

**المشكلة**: `components/ChatSaveToNotes.tsx:14/30` يكتب النص المحفوظ إلى `localStorage['dalilak_notepad']`، ويعرض علامة "✓" خضراء توحي بالنجاح (السطر 54). لكن المكوّن الوحيد الآخر في الكود الذي يشير إلى نفس المفتاح النصي `'dalilak_notepad'` هو `components/QuickNotepad.tsx`، وله عيبان مزدوجان:
1. **لا يُستخدم في أي مكان بالتطبيق إطلاقاً** — لا استيراد ولا استخدام JSX له خارج ملفه (تأكيد بالبحث الشامل).
2. حتى لو كان مُركَّباً، فهو يقرأ من `sessionStorage` وليس `localStorage` (`components/QuickNotepad.tsx:13/25/35`)، أي واجهة تخزين مختلفة تماماً رغم تطابق اسم المفتاح النصي.

**التحقق من الحيوية**:
```
$ grep -rn "ChatSaveToNotes" app components --include="*.tsx"
app/page.tsx:32:import ChatSaveToNotes from '@/components/ChatSaveToNotes'
app/page.tsx:1974:  <ChatSaveToNotes text={msg.content} isAr={isAr} />
```
الزر مُركَّب فعلياً في صف إجراءات كل رد مساعد على الصفحة الرئيسية (بجانب زر "حفظ الرد" الذي أُصلِح مسبقاً في batch #460 لنفس فئة الخلل).
```
$ grep -rn "QuickNotepad" app components --include="*.tsx"
components/ChatSaveToNotes.tsx:4: * ... saves an AI message snippet to QuickNotepad.
components/QuickNotepad.tsx:15:export default function QuickNotepad() {
```
لا نتيجة JSX خارج ملف `QuickNotepad.tsx` نفسه — غير مُركَّب في أي صفحة.

**هذا نفس فئة الخلل التي أُصلِحت مرتين سابقاً** لمكوّنين شقيقين: `dalilak_bookmarks` (batch #460) و`dalilak_chat_sessions` (batch #461) — لكن `ChatSaveToNotes`/`QuickNotepad`، رغم وجودهما في نفس صف الإجراءات المجاورة تماماً، فاتا التدقيق السابق، وخللهما أخطر لوجود طبقة عطل إضافية (تعارض `localStorage`/`sessionStorage`).

**الأثر**: مستخدم يقرأ رد الذكاء الاصطناعي ويضغط "📝 حفظ في الملاحظات" — يرى علامة نجاح خضراء توحي أن الملاحظة حُفظت. النص فعلياً يُكتب في `localStorage['dalilak_notepad']`، لكن لا توجد صفحة أو لوحة في التطبيق الحي تقرأ هذا المفتاح إطلاقاً — الملاحظة تُفقد نهائياً وبصمت رغم أن الواجهة تؤكد للمستخدم أن الحفظ نجح.

**الإصلاح**: بنفس النمط المُتَّبع مسبقاً لـ`dalilak_bookmarks` (إضافة قسم عرض في `app/my-files/page.tsx`، الصفحة المخصصة أصلاً لـ"ملفاتك المحفوظة")، تمت إضافة قراءة مباشرة لـ`localStorage['dalilak_notepad']` (وليس `sessionStorage`، لأن الكاتب الحي الوحيد يستخدم `localStorage`) وعرضها في قسم "الملاحظات المحفوظة" مجاور لقسم "الردود المحفوظة" الموجود أصلاً، مع زر "مسح":
```ts
const NOTEPAD_LS_KEY = 'dalilak_notepad'
const [notepadText, setNotepadText] = useState('')
useEffect(() => {
  try { setNotepadText(localStorage.getItem(NOTEPAD_LS_KEY) || '') } catch {}
}, [])
const clearNotepad = () => {
  try { localStorage.removeItem(NOTEPAD_LS_KEY) } catch {}
  setNotepadText('')
}
```
لم يُلمَس `QuickNotepad.tsx` (يبقى مكوّناً منفصلاً غير مُركَّب، خارج نطاق هذا الإصلاح المحصور بمسار الكتابة الحي الفعلي).

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/my-files/page.tsx` (إضافة state + قسم عرض).

### تحديث (batch #511) — ملاحظة تدقيق فقط: تذكيرات المواعيد (`AppointmentReminder` + قسم في `NotificationBell`) بلا أي مصدر كتابة إطلاقاً في الكود

**الملاحظة**: `components/AppointmentReminder.tsx:25` و`components/NotificationBell.tsx:36` كلاهما يقرأ `localStorage['dalilak_appointments']` (توست أسفل الصفحة + قسم في جرس الإشعارات، كلاهما مُركَّب فعلياً وحي: `AppointmentReminder` في `app/page.tsx:25/2489`، و`NotificationBell` في الهيدر على كل الصفحات). لكن بحثاً شاملاً في `app/` و`components/` و`lib/` لا يُظهر أي موضع `setItem('dalilak_appointments', ...)` إطلاقاً — لا نموذج "إضافة موعد"، ولا أي مصدر بيانات آخر يكتب هذا المفتاح:
```
$ grep -rn "dalilak_appointments" app components lib --include="*.tsx" --include="*.ts"
components/NotificationBell.tsx:7 (تعليق فقط)
components/NotificationBell.tsx:36 (تعريف المفتاح)
components/AppointmentReminder.tsx:5 (تعليق فقط)
components/AppointmentReminder.tsx:25 (تعريف المفتاح)
```
لا توجد نتيجة `setItem` واحدة لهذا المفتاح في كامل الكود.

**لماذا لم يُصلَح**: هذا يختلف عن الأخطاء المُصلَحة سابقاً (مثل batch #460/#461/#510) حيث كان هناك زر حي يكتب البيانات لكن لا عارض لها — هنا العكس تماماً: العارضان حيّان وصحيحان، لكن لا يوجد أي مصدر كتابة على الإطلاق. الإصلاح الحقيقي يتطلب بناء ميزة "إضافة موعد" كاملة من الصفر (نموذج، تخزين، تحقق) — وهذا تصميم ميزة جديدة كاملة وليس إصلاح خلل بحد ذاته، ويتجاوز نطاق "إصلاح جراحي محصور" المتَّبع في هذا السجل. بما أن التذكير يفشل بصمت (يُرجع `null` فقط) دون أي رسالة مضللة للمستخدم، فالأثر الحالي محدود (ميزة خاملة لا ميزة تكذب)، ويُترك توثيقه هنا كملاحظة تدقيق لدفعة مستقبلية قد تتضمن تصميم نموذج إضافة المواعيد إن اتُّخذ قرار ببناء هذه الميزة فعلياً.

لا تعديل على أي ملف في هذه الدفعة — ملاحظة توثيقية فقط.

### تحديث (batch #512) — اقتراحات الإكمال التلقائي في صندوق المحادثة: التنقل بالسهمين لا يُظهر أي تحديد بصرياً، وضغط Enter قد يستبدل رسالة المستخدم بصمت بدل إرسالها

**المشكلة**: يوجد تطبيقان منفصلان تماماً لنفس ميزة اقتراحات الإكمال التلقائي في `components/SmartInputSuggestions.tsx`:
1. المكوّن الافتراضي `SmartInputSuggestions` (السطر 84 سابقاً) — يعرض القائمة المنسدلة فعلياً، ويملك متغير حالة `activeIdx` خاصاً به، لكن هذا المتغير **لا يُحدَّث إلا عبر `onMouseEnter`** (تمرير الفأرة)، وليس عبر لوحة المفاتيح.
2. الخطاف المُصدَّر `useSmartSuggestionsKeyDown` (السطر 191 سابقاً) — يملك متغير `activeIdx` **منفصلاً وثانياً بالكامل**، وهو المرتبط فعلياً بحدث `onKeyDown` في مربع النص.

في `app/page.tsx:604-606` يُستخدم الخطاف فقط لالتقاط ضغطات لوحة المفاتيح، بينما المكوّن `<SmartInputSuggestions>` (السطر 2322 سابقاً) **لا يتلقى `activeIdx` كخاصية (prop) إطلاقاً** — واجهته (`Props`) لم تكن تتضمنه أصلاً. كل مكوّن يدير نسخته الخاصة من "العنصر النشط" بمعزل عن الآخر.

**التحقق من الحيوية**:
```
$ grep -rn "SmartInputSuggestions\|useSmartSuggestionsKeyDown" app/page.tsx
43:  import SmartInputSuggestions, { useSmartSuggestionsKeyDown } from '@/components/SmartInputSuggestions'
604: const { suggestions: smartSuggestions, activeIdx: smartActiveIdx, handleKeyDown: smartKeyDown, setDismissed: setSmartDismissed } = useSmartSuggestionsKeyDown(...)
2322: <SmartInputSuggestions ... />   // بلا تمرير activeIdx
2368: onKeyDown={e => { smartKeyDown(e); if (!e.defaultPrevented) handleKeyDown(e) }}
```
`smartActiveIdx` كان يُستخرَج من الخطاف لكن لا يُستخدم في أي مكان آخر — لم يكن يُمرَّر للمكوّن أبداً. سطر `onKeyDown` يُظهر أن معالج الإرسال العادي (`handleKeyDown`، الذي يرسل الرسالة عند Enter) لا يعمل إلا إذا لم يُستدعَ `e.preventDefault()` مسبقاً من `smartKeyDown`.

**تتبّع السيناريو الفعلي (وليس افتراضياً)**:
1. المستخدم يكتب 3 أحرف أو أكثر في صندوق المحادثة الرئيسي → تظهر قائمة اقتراحات (حتى 3 اقتراحات من `SUGGESTIONS_AR`)، ونص الإرشاد أسفلها يقول حرفياً: `'↑↓ للتنقل · Enter للاختيار · Esc للإغلاق'`.
2. المستخدم يضغط `ArrowDown` (سلوك متوقَّع تماماً بناءً على النص الإرشادي المعروض) → `activeIdx` في **الخطاف** يتغيّر من -1 إلى 0، و`e.preventDefault()` يُنفَّذ. لكن بصرياً: لا شيء يتغيّر — `activeIdx` في **المكوّن المعروض** يبقى -1 (لأنه لا يتحدّث إلا بتمرير الفأرة)، فلا يظهر أي تظليل لأي اقتراح.
3. المستخدم يضغط `Enter` معتقداً أنه سيرسل رسالته الأصلية → داخل `smartKeyDown`، الشرط `e.key === 'Enter' && activeIdx >= 0` أصبح صحيحاً (activeIdx الخطاف = 0)، فيُستدعى `onSelect(suggestions[0])` الذي يستبدل نص المستخدم المكتوب بالكامل باقتراح جاهز غير متعلق بالضرورة بما كتبه، و`e.preventDefault()` يمنع معالج الإرسال الحقيقي من العمل — **الرسالة لا تُرسَل إطلاقاً**.

**الأثر**: في صندوق المحادثة الرئيسي (نقطة التفاعل الأساسية في التطبيق)، أي مستخدم يضغط سهم لوحة المفاتيح أثناء ظهور قائمة الاقتراحات (وهو بالضبط ما يرشده النص المعروض على الشاشة للقيام به) لا يرى أي تحديد بصري، ثم يفاجأ بأن ضغط Enter لم يرسل رسالته بل استبدلها بصمت باقتراح آخر. هذا خلل حتمي وقابل للتكرار دوماً، وليس حالة نادرة، ويتناقض مباشرة مع تعليمات الاستخدام المعروضة على الشاشة لنفس الميزة.

**الإصلاح**: توحيد مصدر الحقيقة — تمرير `activeIdx` و`setActiveIdx` من الخطاف كخاصيتين (`activeIdx`، `onHover`) للمكوّن المعروض بدل احتفاظه بنسخته الخاصة، وحذف معالج لوحة المفاتيح الميت داخل المكوّن (كان مُعلَّقاً بتعليق `void handleKeyDown` يعترف أن الأب "يجب أن" يستخدمه، لكن هذا لم يحدث فعلياً قط):
```diff
 interface Props {
   input: string
   onSelect: (suggestion: string) => void
   isAr: boolean
+  activeIdx: number
+  onHover: (i: number) => void
 }

-export default function SmartInputSuggestions({ input, onSelect, isAr }: Props) {
+export default function SmartInputSuggestions({ input, onSelect, isAr, activeIdx, onHover }: Props) {
   const [suggestions, setSuggestions] = useState<string[]>([])
-  const [activeIdx, setActiveIdx] = useState(-1)
-  const [dismissed, setDismissed] = useState(false)
   ...
-  // معالج لوحة مفاتيح ميت لم يُستخدَم قط من الأب
-  const handleKeyDown = useCallback(...)
-  if (!suggestions.length || dismissed) return null
+  if (!suggestions.length) return null
   ...
-  onMouseEnter={() => setActiveIdx(i)}
+  onMouseEnter={() => onHover(i)}
```
```diff
-  return { suggestions: dismissed ? [] : suggestions, activeIdx, handleKeyDown, setDismissed }
+  return { suggestions: dismissed ? [] : suggestions, activeIdx, setActiveIdx, handleKeyDown, setDismissed }
```
وفي `app/page.tsx`:
```diff
-  const { suggestions: smartSuggestions, activeIdx: smartActiveIdx, handleKeyDown: smartKeyDown, setDismissed: setSmartDismissed } = useSmartSuggestionsKeyDown(...)
+  const { suggestions: smartSuggestions, activeIdx: smartActiveIdx, setActiveIdx: setSmartActiveIdx, handleKeyDown: smartKeyDown, setDismissed: setSmartDismissed } = useSmartSuggestionsKeyDown(...)
   ...
   <SmartInputSuggestions
     input={input}
     onSelect={s => { setInput(s); textareaRef.current?.focus(); setSmartDismissed(true) }}
     isAr={isAr}
+    activeIdx={smartActiveIdx}
+    onHover={setSmartActiveIdx}
   />
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/SmartInputSuggestions.tsx` و`app/page.tsx`.

### تحديث (batch #513) — شارة تحذير الحصة اليومية المتبقية مُثبَّتة على RTL دائماً حتى في وضع اللغة الإنجليزية

**المشكلة**: `app/page.tsx:2068-2073` — حاوية شارة "الحصة المتبقية" (تظهر عندما `quotaRemaining <= 10`) مُثبَّتة على `direction: 'rtl', textAlign: 'right'` بلا أي تفريع على `isAr`، رغم أن نص الشارة نفسه يتفرّع بشكل صحيح على `isAr` (`'Daily quota exhausted'` مقابل `'استنفذت حصتك اليومية'`):
```tsx
<div style={{
  maxWidth: 'var(--container-md)', margin: '0 auto', padding: '0 12px 2px',
  direction: 'rtl', textAlign: 'right',   // ثابت بلا تفريع
}}>
```
بالمقارنة، الشريحة الشقيقة المجاورة مباشرة فوقها في نفس الملف ("إعادة المحاولة عند خطأ الاتصال"، `app/page.tsx:2047`) تُصلِحت سابقاً (batch #424) لتتفرّع بشكل صحيح: `direction: isAr ? 'rtl' : 'ltr'` — لكن شارة الحصة المتبقية لم تحصل على نفس المعالجة قط.

**التحقق من الحيوية**: `app/page.tsx` هي صفحة المحادثة الرئيسية الحية (`/`)؛ `quotaRemaining` (السطر 580) يُملأ فعلياً من استجابة الخادم الحقيقية عبر SSE:
```
$ grep -n "metaRemaining|setQuotaRemaining" app/page.tsx
580:  const [quotaRemaining, setQuotaRemaining] = useState<number | null>(null)
1173:      let metaRemaining: number | null = null
1206:              if (typeof p.remaining === 'number') metaRemaining = p.remaining
1237:      if (metaRemaining !== null) setQuotaRemaining(metaRemaining)
```
لا يوجد مسار بيانات وهمي — أي مستخدم حقيقي تنخفض حصته اليومية إلى 10 أسئلة أو أقل يرى هذه الشارة في الإنتاج.

**الأثر**: مستخدم بوضع اللغة الإنجليزية توشك حصته اليومية على النفاد (10 أسئلة أو أقل، وتصبح حمراء عند 3 أو أقل) يرى الشارة التحذيرية معروضة داخل حاوية RTL بمحاذاة يمين، بينما بقية تخطيط الصفحة بوضع اللغة الإنجليزية LTR بمحاذاة يسار — بالضبط نفس النمط الذي أُصلِح مسبقاً للشريحة المجاورة. هذا يظهر بشكل غير متسق ومقلوب بصرياً في لحظة مهمة (تحذير المستخدم من اقتراب انتهاء حصته).

**الإصلاح**: تفريع `direction`/`textAlign` على `isAr` بنفس نمط الشريحة الشقيقة المجاورة:
```diff
           <div style={{
             maxWidth: 'var(--container-md)', margin: '0 auto', padding: '0 12px 2px',
-            direction: 'rtl', textAlign: 'right',
+            direction: isAr ? 'rtl' : 'ltr', textAlign: isAr ? 'right' : 'left',
           }}>
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/page.tsx` (سطر واحد).

### تحديث (batch #514) — حقول "استوديو الصياغة" النصية الطويلة (textarea) مُثبَّتة على RTL دائماً حتى في وضع اللغة الإنجليزية، بعكس حقول الإدخال المجاورة

**المشكلة**: `components/DraftingStudio.tsx:327` — نمط `<textarea>` (المُستخدَم لكل الحقول من نوع `'textarea'` عبر القوالب الثمانية، 14 حقلاً مثل "سبب الإخلاء"، "وصف الحالة"، "ملاحظات") يحمل `direction: 'rtl'` ثابتاً بلا أي تفريع على `isAr`:
```tsx
style={{ ..., direction: 'rtl', ... }}
```
بينما نمط `<input>` المجاور مباشرة أسفله (السطر 332-348، لنفس الحقول من نوع نصي عادي) **لا يحمل أي تجاوز لـ`direction` إطلاقاً** — يرث الاتجاه الصحيح تلقائياً من `dir` الصفحة الأم (`isAr ? 'rtl' : 'ltr'`).

**التحقق من الحيوية**: `app/drafting-studio/page.tsx:5/93` يستورد ويعرض `<DraftingStudio isAr={isAr} .../>` فعلياً على مسار حي `/drafting-studio`، مع تمرير حالة اللغة الحقيقية للتطبيق.
```
$ grep -c "type: 'textarea'" components/DraftingStudio.tsx
14
$ grep -n "direction: 'rtl'" components/DraftingStudio.tsx
327: ... direction: 'rtl', ...   ← نمط واحد مشترك لكل الحقول الـ14
```

**الأثر**: مستخدم بوضع اللغة الإنجليزية يملأ نموذج "استوديو الصياغة" (مثل "Reason for Eviction" أو "Case Description") يجد حقل النص الطويل مفروضاً عليه اتجاه RTL رغم كتابته بالإنجليزية، بينما حقل الاسم النصي القصير المجاور مباشرة على نفس النموذج يبقى LTR بشكل صحيح — تناقض بصري مباشر ومربك على نفس الشاشة، مطابق تماماً لخلل الشارة المُصلَح للتو في batch #513 لكن في مكوّن مختلف تماماً.

**الإصلاح**: تفريع `direction` على `isAr` بنفس نمط حقل `<input>` المجاور:
```diff
-                    style={{ ..., direction: 'rtl', ... }}
+                    style={{ ..., direction: isAr ? 'rtl' : 'ltr', ... }}
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/DraftingStudio.tsx` (سطر واحد).

### تحديث (batch #515) — معاينة الملف المرفق في صندوق المحادثة مُثبَّتة على محاذاة يمين حتى في وضع اللغة الإنجليزية

**المشكلة**: `app/page.tsx:2142` — حاوية نص معاينة الملف المرفق (اسم الملف + الحجم + "جاهز للتحليل") مُثبَّتة على `textAlign: 'right'` بلا أي تفريع على `isAr`، رغم أن `isAr` في النطاق فعلياً ويُستخدَم بشكل صحيح سطرين لاحقاً لنص الحالة نفسه (`isAr ? 'جاهز للتحليل' : 'Ready to analyze'`):
```tsx
<div style={{ flex: 1, minWidth: 0, textAlign: 'right' }}>   // ثابت بلا تفريع
  <div>{attachedFile.name}</div>
  <div>{formatSize(attachedFile.size)}</div>
  <div>{isAr ? 'جاهز للتحليل' : 'Ready to analyze'}</div>   // هذا السطر يتفرّع بشكل صحيح
</div>
```
هذه نفس فئة الخلل المُصلَحة للتو في batch #513 (شارة الحصة المتبقية) وbatch #514 (حقول Drafting Studio) — نمط تكرر للمرة الثالثة في مكوّن مختلف تماماً.

**التحقق من الحيوية**: `app/page.tsx:554/556/566` — `Home()` هي صفحة المحادثة الرئيسية على `/`، و`isAr` في النطاق (`const { lang, isAr, toggleLang } = useLanguage()`)، و`attachedFile` حالة حقيقية (`useState<AttachedFile | null>(null)`) تُملأ عند إرفاق أي ملف فعلي عبر زر الإرفاق في شريط الإدخال — هذا الكتلة تُعرَض مباشرة عند `attachedFile !== null`، أي في كل مرة يرفق فيها مستخدم حقيقي ملفاً. الغلاف الأعلى للصفحة (`app/page.tsx:1376`) يضبط `direction: isAr ? 'rtl' : 'ltr'` بشكل صحيح، أي أن هذه الكتلة تُعرَض ضمن سياق LTR فعلي بوضع اللغة الإنجليزية، لكن محاذاة نصها الداخلي مُثبَّتة يمين رغم ذلك.

**الأثر**: مستخدم بوضع اللغة الإنجليزية يرفق ملفاً (صورة أو مستند) لرسالة محادثة يرى اسم الملف وحجمه ونص "Ready to analyze" مُلتصقة بالحافة اليمنى لبطاقة المعاينة بدل أن تتبع أيقونة نوع الملف على اليسار بشكل طبيعي — عكس ترتيب القراءة الطبيعي لـLTR، ومتناقض مع بقية عناصر الشاشة الإنجليزية.

**الإصلاح**: تفريع `textAlign` على `isAr`:
```diff
-                <div style={{ flex: 1, minWidth: 0, textAlign: 'right' }}>
+                <div style={{ flex: 1, minWidth: 0, textAlign: isAr ? 'right' : 'left' }}>
```

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `app/page.tsx` (سطر واحد).

### تحديث (batch #516) — عناوين الأقسام القابلة للطي في "تحليل المستند الذكي" (7 أقسام) مُثبَّتة على محاذاة يمين دائماً، حتى في وضع اللغة الإنجليزية

**المشكلة**: `components/DocumentIntelligenceView.tsx:51-57` — دالة تنسيق `sectionHeader` المشتركة لكل عناوين الأقسام القابلة للطي (`textAlign: 'right'` ثابت) لا تتفرّع على `isAr` إطلاقاً، ومكوّن `CollapsibleSection` (السطر 763-766) **لا يتلقى `isAr` كخاصية أصلاً** رغم استخدامه 7 مرات لأقسام تُعرَض عناوينها بالإنجليزية فعلياً في وضع اللغة الإنجليزية ("Extracted Data"، "Related Procedures"، "Missing Requirements"، "Potential Risks"، "Suggested Templates"، "Next Steps"، "Sources & Confidence").

**التحقق من الحيوية**: `components/ChatMessage.tsx:14/292-294` يستورد المكوّن ديناميكياً (`dynamic(() => import('./DocumentIntelligenceView'))`) ويعرضه فعلياً مع `isAr={ar}` الحقيقي عند وجود `msg.documentAnalysis` (أي بعد أي تحليل مستند مرفوع). الغلاف الجذري للمكوّن نفسه (`components/DocumentIntelligenceView.tsx:798`) يضبط `direction: isAr ? 'rtl' : 'ltr'` بشكل صحيح — يؤكد أن الإغفال في `sectionHeader` سهو وليس تصميماً مقصوداً.

**الأثر**: مستخدم بوضع اللغة الإنجليزية يرفع مستنداً للتحليل الذكي يرى عناوين الأقسام السبعة كلها (مثل "Extracted Data"، "Potential Risks") مدفوعة لحافة يمين شريط العنوان، ملتصقة بسهم الطي، بدل أن تتبع أيقونة القسم على اليسار بشكل طبيعي — عكس ترتيب القراءة LTR في كل الأقسام السبعة دفعة واحدة.

**الإصلاح**: تمرير `isAr` كخاصية عبر `CollapsibleSection` إلى `sectionHeader`، وتحديث كل مواضع الاستدعاء السبعة:
```diff
-const sectionHeader = (expanded: boolean): React.CSSProperties => ({
+const sectionHeader = (expanded: boolean, isAr: boolean): React.CSSProperties => ({
   ...
-  fontFamily: 'inherit', textAlign: 'right' as const,
+  fontFamily: 'inherit', textAlign: isAr ? 'right' as const : 'left' as const,
   ...
 })

 function CollapsibleSection({
-  title, icon, children, defaultOpen = true,
-}: { title: string; icon: React.ReactNode; children: React.ReactNode; defaultOpen?: boolean }) {
+  title, icon, children, defaultOpen = true, isAr,
+}: { title: string; icon: React.ReactNode; children: React.ReactNode; defaultOpen?: boolean; isAr: boolean }) {
   ...
-  <button ... style={sectionHeader(open)} ...>
+  <button ... style={sectionHeader(open, isAr)} ...>
```
مع إضافة `isAr={isAr}` لكل من مواضع الاستدعاء السبعة (`<CollapsibleSection isAr={isAr} title={...}>`).

`tsc --noEmit` نظيف (exit 0). إصلاح محصور في `components/DocumentIntelligenceView.tsx`.

هذا الملف سيُحدَّث مع كل دفعة تالية — لا يُعاد كتابته من الصفر، بل تُضاف/تُحدَّث بنوده.

---

### مشروع أرشيف الوثائق الرسمية (خارج ترقيم batch # المعتاد أعلاه)

**المصدر:** أرشيف خارجي من 6504 ملف عبر ثلاثة مجلدات
(`05_Administrative_Transactions`، `06_Official_Forms`، `07_Legal_Forms`)
بناءً على طلب المستخدم صراحة لتحليل ورفع "آلاف النماذج القانونية والإدارية
اللبنانية".

**خط المعالجة الكامل** (سكريبتات في `forms_pipeline/` بجذر مشروع dalilak):
1. `extract_text.py` — استخراج نص فعلي عبر `pdftotext` + `openpyxl` لكل
   الـ6504 ملف (لا OCR في هذه المرحلة، لسرعة التغطية الكاملة أولاً).
2. `ocr_pass.py` — تمريرة OCR منفصلة (PyMuPDF + pytesseract عربي/إنكليزي)
   للملفات التي فشل فيها `pdftotext` (نص أقل من 40 حرفاً) — رتّبت حسب حجم
   الملف تصاعدياً لزيادة عدد الملفات المُنجَزة ضمن كل تمريرة محدودة زمنياً.
3. `merge_classify.py` — دمج النتائج + تصنيف الجهة الحكومية عبر مطابقة
   الاسم الحرفي الموجود أصلاً في اسم الملف (لا حاجة لـ regex هش) + استبعاد:
   - **353 ملف** امتداده فعلياً استعلام URL محفوظ خطأً كامتداد (`aspx?source=...`)
     — كلها صفحات إعادة توجيه/تسجيل دخول ميتة من الزحف، تأكّد ذلك بأن نص
     الاستخراج فارغ (0 حرف) للـ351 منها بلا استثناء.
   - **122 ملف** داخلي لجامعة AUB (نماذج إدارية جامعية أو صفحات AUB ميتة) —
     ليست محتوى حكومياً لبنانياً بحكم التعريف، فاستُبعدت كاملةً من "أرشيف
     الوثائق الرسمية" (لم تُحذف من القرص، فقط لم تُدرَج).
   - **1474 ملف** تعذّر استخراج نص حقيقي منه حتى بعد OCR (أقل من 30 حرفاً).
4. `build_dataset.py` — إزالة **177 نسخة مكررة حرفياً** (نفس أول 1000 حرف من
   النص)، تطبيع Unicode NFKC (يصحّح Arabic Presentation Forms التي ينتجها
   `pdftotext` أحياناً في ملفات PDF عربية قديمة)، واشتقاق عنوان لكل وثيقة
   من أول سطر متماسك فعلي في النص المستخرج (وليس نصاً مُختلَقاً) — مع بديل
   احتياطي (اسم الجهة + رمز الملف المنظّف) حين يكون النص المستخرج غير
   متماسك (شائع في نتائج OCR الرديئة).

**الناتج النهائي: 4378 وثيقة حكومية لبنانية حقيقية** موزّعة على 18 جهة
(وزارة الزراعة 1315، وزارة المالية 728، مصرف لبنان 569، وزارة الداخلية 361،
مجلس النواب 312، ...). **اكتشاف مهم غيّر شكل الميزة الأصلي المخطَّط له:**
غالبية المحتوى ليست "نماذج تعبئة" بل أرشيف وثائق حكومية متنوّع (تعاميم
مصرف لبنان، تعديلات جمركية، تحقيقات وزارية، تقارير بيئية/إحصائية، مناهج
تربوية، لوائح انتخابية، محاضر) — عُرض هذا على المستخدم صراحةً عبر سؤالين
توضيحيين قبل المتابعة، وأكّد المستخدم صفحة أرشيف منفصلة بدل حشو
`allTransactions.ts`.

**الوجهتان المتفق عليهما (سؤال توضيحي، إجابة المستخدم):**
- **Qdrant** (الأولوية، منجزة بالكامل): كل الـ4378 وثيقة مرفوعة إلى نفس
  الـ collection الحالي `dalilak_ai_v2` (لا collection جديد، لا تغيير على
  الـ Backend API) عبر `upload_archive_to_qdrant.py` — بنفس نموذج
  `text-embedding-3-large` المعتمد أصلاً في `upload_to_qdrant.py`، بحقل
  `domain=official_documents_archive` مفهرَس ككلمة مفتاحية للفلترة.
  تحقّق: `qdrant.count(filter domain=official_documents_archive)` = 4378.
- **Frontend**: صفحة جديدة `/archive` ("أرشيف الوثائق الرسمية") — بحث +
  تصفية حسب الجهة (14 جهة الأكثر تكراراً كأزرار + "الكل") + بطاقات بنفس
  نمط `/forms` (`MobileHeader` + `SearchInput` + `StatsRow` + بطاقة). بيانات
  الصفحة في `lib/archiveDocuments.ts` (JSON مُضمَّن كسلسلة نصية مُفسَّرة عبر
  `JSON.parse` بدل مصفوفة TS حرفية — تفادياً لخطأ `tsc` "TS2590: union type
  too complex" الذي ظهر فعلياً عند تجربة 4378 عنصر ككائنات TS حرفية).
  رابط تنقّل جديد أُضيف في `components/MobileMenu.tsx` (لا يوجد مكان في
  `BottomNav` — أربعة عناصر فقط حسب قرار "Calm Government Digital Service"
  الموثّق أعلاه، فاتُّبع نفس نمط `/services`/`/authorities` بالظهور في
  القائمة الجانبية فقط).

**قيد معروف يحتاج إجراءً من المستخدم:** حجم ملفات PDF الأصلية 1.8GB —
كبير جداً لإدراجه داخل مستودع `dalilak-frontend`. بالاتفاق مع المستخدم
(سؤال توضيحي)، جُهِّز مستودع GitHub منفصل `dalilak-forms-archive` (نفس
حساب `wfklawfirm`) في `forms_pipeline/archive_staging/` مع `push_archive.sh`
جاهز — **يتطلب من المستخدم إنشاء المستودع الفارغ على GitHub أولاً** (لا
يمكن لأي أداة هنا إنشاء حساب/مستودع GitHub نيابة عن المستخدم). حتى يتم
هذا الرفع، روابط "عرض الملف الأصلي" في `/archive` ستُرجع 404 — الصفحة
ونصوص البحث تعمل بالكامل بغضّ النظر (البيانات النصية والفلترة والبحث لا
تعتمد على استضافة الملفات).

`tsc --noEmit` نظيف (exit 0) بعد إضافة `lib/archiveDocuments.ts` +
`app/archive/page.tsx` + `app/archive/layout.tsx` + تعديل `MobileMenu.tsx`.

### تصحيح لاحق — استكمال OCR واكتشاف/معالجة خطأين في التنفيذ (2026-07-26)

المستخدم سأل: "المجلد الذي حددته يحتوي على فوق ٤٠ جب، لماذا رفعت فقط
١.٨؟" — سؤال دقيق كشف مشكلتين حقيقيتين:

1. **خطأ Sandbox (تم تصحيحه)**: كامل `forms_pipeline/` (بما فيها مجلد
   `archive_staging/` بحجم 1.8GB و4378 ملف) كانت مبنية فقط داخل بيئة
   عمل مؤقتة غير مرئية للمستخدم، وليس داخل مجلد المشروع الحقيقي المتصل.
   نُقلت بالكامل إلى `forms_pipeline/` الحقيقي داخل مجلد المشروع
   (نسخ مجزّأ resumable لتجاوز حد 45 ثانية لكل استدعاء).

2. **نقص حقيقي في المحتوى (تم تصحيحه)**: الفرق بين حجم المصدر الحقيقي
   (05+06+07 = 8.75GB، ليس 40GB — الـ40GB+ كان يشمل مجلد `_archived_zips`
   وهو نسخة احتياطية لجهاز غير متعلقة بالنماذج إطلاقاً) وحجم الأرشيف
   المرفوع (1.8GB) تبيّن أنه ناتج عن معالجة OCR غير مكتملة: من أصل 1598
   ملف ممسوح ضوئياً كان يحتاج OCR، لم يكن قد اكتمل إلا 255 فقط (قرار سابق
   بإيقاف OCR لصالح أولويات أخرى، مع تحذير صريح في المراجعة السابقة بأنه
   "غير مكتمل عمداً"). هذا أدى لاستبعاد ~950 وثيقة حكومية حقيقية (وزارة
   العمل، الزراعة، التربية، الداخلية، الطاقة...) كـ"غير قابلة للاستخراج"
   بينما هي فعلياً مستندات ممسوحة صالحة لم تُعطَ فرصة OCR كاملة.

   **الإصلاح**: أُكمل OCR على كل الـ1598 ملف (كان الجزء الأصعب زمنياً —
   عشرات الدفعات المتتالية بسبب حد 45 ثانية لكل استدعاء). أُعيد تشغيل
   خط المعالجة كاملاً: `merge_classify.py` → `build_dataset.py` →
   `generate_archive_ts.py` → إعادة `stage_copy.py` (925 ملف جديد) →
   إعادة رفع الفرق لـ Qdrant (625 سجل جديد، نفس الـcollection
   `dalilak_ai_v2`، بدون تعديل Backend). النتيجة: **4378 → 5303 وثيقة**،
   **18 → 19 جهة رسمية**، حجم الأرشيف المستضاف 1.8GB → 3.14GB. تم تحديث
   الأرقام في `page.tsx` (تعليق الترويسة + النص التعريفي) و`layout.tsx`
   (SEO metadata).

3. **قيد بنية جديد اكتُشف (غير محلول من طرفي — يتطلب المستخدم)**: عند
   محاولة `git add`/`git commit` في مستودعي `frontend/` و
   `forms_pipeline/archive_staging/` من داخل بيئة العمل الحالية، فشلت
   العملية بخطأ `fatal: Unable to create '.git/index.lock': File exists`
   — ملف `index.lock` قديم موجود لا يمكن حذفه من هذه البيئة (`rm` يفشل
   بـ"Operation not permitted" حتى على ملف أملكه أنا). هذا قيد على مستوى
   نظام الملفات المُوصَّل (mount)، ليس قراراً مني. **الحل**: يحتاج
   المستخدم لتشغيل الأوامر التالية بنفسه من Terminal على جهازه:
   - `cd frontend && bash push.sh` (سيضيف ويرفع تعديلات هذه الدفعة)
   - `cd forms_pipeline/archive_staging && bash push_archive.sh` (لرفع
     925 ملف جديد لمستودع `dalilak-forms-archive` — المستودع والدفعة
     الأولى مرفوعان مسبقاً بنجاح من محاولة سابقة للمستخدم؛ `git add -A`
     سيكتشف الملفات الجديدة تلقائياً).
   - إذا ظهر نفس خطأ `index.lock` عند المستخدم أيضاً (احتمال ضعيف لأنه
     سيعمل من نظام ملفاته الحقيقي مباشرة، لا من خلال هذا الـmount):
     `rm frontend/.git/index.lock` ثم إعادة تشغيل `push.sh`.

### تصحيح لاحق — تحسين جودة عناوين الأرشيف واستبعاد صفحات حجب WAF (2026-07-26)

بطلب المستخدم: "يوجد عناوين غير مقروءة حسّنها" — ضمن طلب أوسع من 4 نقاط
(تحسين العناوين، بحث بالذكاء الاصطناعي، دمج النماذج الرسمية في المعاملات،
واستفادة "اسأل دليلك" من الأرشيف).

1. **عناوين غير مقروءة (تم تصحيحها)**: فحص عيّنة عشوائية من 25 وثيقة كشف
   4 أنماط عناوين تالفة فعلية:
   - عناوين OCR بحروف لاتينية عشوائية بلا معنى ("Aya ye Lalla ce eWiges").
   - تشفير خط عربي تالف (font-encoding mojibake) يظهر كرموز رياضية/سهمية/
     رمزية من نطاقات يونيكود لا تظهر أبداً في نص حقيقي.
   - عناوين احتياطية (fallback) تكرر اسم الجهة نفسها بلا أي معلومة إضافية
     (لأن اسم الجهة كان أيضاً جزءاً من اسم الملف نفسه).
   - شذرات قصيرة بلا معنى (كلمة أو رقم منفرد).

   **الإصلاح**: أُعيدت كتابة `looks_coherent()`/`derive_title()` في
   `forms_pipeline/build_dataset.py`:
   - رفض فوري لأي سطر يحتوي حرفاً من نطاقات يونيكود رياضية/رمزية/سهمية
     (`GARBAGE_RANGE_RE`) — هذه لا تظهر أبداً في نص وثيقة حكومية حقيقي.
   - للعربية: اشتراط 3+ كلمات عربية حقيقية (لا مجرد علامات ترقيم) بدل
     الاعتماد على نسبة الأحرف العربية فقط.
   - للاتينية: اشتراط 4+ كلمات مع وجود كلمة وقف إنجليزية شائعة (4+ أحرف)
     — عدّل مرتين بعد اختبار على عيّنات حقيقية، لأن كلمات الوقف القصيرة
     ("by", "all", "a") كانت تمر بالخطأ عبر ضجيج OCR عشوائي.
   - للعنوان الاحتياطي: إزالة اسم الجهة من التسمية (label) قبل تكوين
     العنوان النهائي، وإسقاط التسمية كلياً إن لم يبقَ منها محتوى حقيقي
     (3+ أحرف عربية/لاتينية متتالية)، بدل عرض عنوان مكرر لا فائدة منه.

2. **69 وثيقة كانت في الحقيقة صفحة حجب من جدار حماية (تم استبعادها)**:
   اكتُشف أثناء فحص جودة النصوص أن 69 سجلاً محفوظاً كـ"وثيقة" هو في
   الحقيقة صفحة رفض من WAF/CDN ("The requested URL was rejected. Please
   consult with your administrator...") — أي أن التحميل الأصلي اصطدم
   بحاجز حماية بدل جلب الملف الحقيقي. تم التحقق من النمط على كامل
   المجموعة (69 سجلاً يطابقونه بدقة، صفر تطابقات لأنماط حجب أخرى محتملة
   كـ"Access Denied"/"403 Forbidden"/"Page Not Found" — لتفادي استبعاد
   محتوى حقيقي بالخطأ) قبل إضافته كفلتر استبعاد في `merge_classify.py`.

3. **تنظيف Qdrant المطابق**: حُذفت الـ69 نقطة الخاصة بصفحات الحجب من
   collection `dalilak_ai_v2` (باستخدام point IDs الحتمية المحسوبة مسبقاً
   من `doc_id`)، وحُدّث حقل `title` لكل الوثائق الـ5234 المتبقية عبر
   `batch_update_points`/`SetPayloadOperation` (تحديث payload فقط، بلا
   إعادة تضمين/embedding) — اكتمل التحديث كاملاً في تمريرة واحدة (~33
   ثانية).

**النتيجة النهائية**: الأرشيف صار **5234 وثيقة** (بدل 5303)، لا يزال من
**19 جهة رسمية**. تم تحديث `page.tsx` (تعليق الترويسة) و`layout.tsx`
(SEO metadata) لتعكس "أكثر من 5200 وثيقة". تم التحقق من `npx tsc --noEmit`
نظيف بعد كل التعديلات.

### تصحيح لاحق — بحث بالذكاء الاصطناعي في /archive بدل بحث نصي بسيط (2026-07-26)

بطلب المستخدم: "اجعل البحث عبر الذكاء الاصطناعي و ليس محرك بحث عادة" —
الجزء الثاني من نفس الطلب الرباعي.

**الحل المعماري**: لا يوجد أي endpoint بحث مخصّص على الباك-إند، وممنوع
إضافة/تعديل أي Backend API. لكن `/chat/stream` الموجود مسبقاً يقبل أصلاً
معامل `domain: Optional[str]` (لم يُستخدم من قبل من الفرونتند لهذا الغرض)
و`search_qdrant(vec, domain=...)` يبني فلتر Qdrant `must` صارماً يُرجع
فقط النقاط التي حقل `domain` فيها يطابق القيمة المُرسَلة — أي أن تمرير
`domain: "official_documents_archive"` يُقيّد الاسترجاع بالكامل على وثائق
الأرشيف فقط، بلا أي تغيير على كود الباك-إند.

**التنفيذ**:
- `components/ArchiveAISearch.tsx` (ملف جديد): يستدعي `POST /chat/stream`
  بنفس نمط الاستدعاء الموجود في `app/page.tsx` (نفس `authHeaders()`،
  نفس تحليل بروتوكول SSE سطراً بسطر `data: {...}` وحتى `[DONE]`، ونفس
  التمييز بين أحداث `meta`/`token`/`error`) مع إضافة `domain:
  "official_documents_archive"` فقط في نص الطلب.
- مطابقة عناوين المصادر المُعادة (`sources: [{title, ministry, score}]`)
  مع `lib/archiveDocuments.ts` محلياً (تطابق تام أولاً، ثم تطابق جزئي
  احتياطي) لعرض رابط PDF حقيقي لكل مصدر — بلا حاجة لتعديل الباك-إند
  لإضافة `pdf_url` لقائمة المصادر (كان موجوداً في payload الأصلي لـQdrant
  لكن `search_qdrant` لا يمرّره حالياً؛ عدم لمس ذلك الكود يحافظ على قيد
  "لا تغيّر Backend APIs" بأدق تفسير له).
- `app/archive/page.tsx`: مبدّل وضعين أعلى الصفحة — "بحث بالذكاء
  الاصطناعي" و"بحث نصي سريع". بما أن `/chat/stream` يتطلب تسجيل دخول
  أصلاً (نفس شرط "اسأل دليلك" الرئيسي في `app/page.tsx`)، لم نجعل وضع
  الذكاء الاصطناعي إجبارياً لكل الزوار: **المستخدم المسجّل** يبدأ مباشرة
  بوضع الذكاء الاصطناعي (تحقيق حرفي لطلب "اجعل البحث... ذكاء اصطناعي")،
  و**الزائر غير المسجّل** يبدأ بوضع البحث النصي السريع الموجود أصلاً بلا
  أي قيد جديد — قرار متعمّد لتفادي مخالفة "لا تحذف الوظائف" (لم تكن /archive
  تتطلب تسجيل دخول من قبل لأي شكل من البحث). كلا الوضعين متاحان للتبديل
  دائماً لأي مستخدم.
- الوضع النصي السريع (كل الكود القديم للبحث/الفلاتر/القائمة) لم يُحذف أو
  يُعدَّل — فقط أُحيط بشرط عرض جديد `searchMode === 'quick'`.

تم التحقق من `npx tsc --noEmit` نظيف بعد إضافة المكوّن الجديد ودمجه.

### تصحيح لاحق — دمج وثائق الأرشيف الرسمية داخل صفحات المعاملات (2026-07-26)

بطلب المستخدم: "ادمج النماذج التي تكون معملة إدارية أو رسمية في
المعاملات" — الجزء الثالث من نفس الطلب الرباعي.

**تحدٍ معماري**: `Procedure` (النوع الذي تعرضه `app/procedures/[slug]/
page.tsx` عبر `getProcedureBySlug`) لا يملك أي حقل مطابقة مباشر مع
`ArchiveDoc`، وتوجد فعلياً 4 مصادر بيانات معاملات/نماذج منفصلة في
المشروع (`PROCEDURES_DATA`، `FORMS_DATA`، `ENRICHED_PROCEDURES`،
`TX_ALL`) بحقول جهة مختلفة الأسماء (`authority.ministry_en`،
`ministry_en`، `ministryEn`...)، وصفحة `/playbook` تقرأ من مصدر مختلف
تماماً (`ENRICHED_PROCEDURES`) عن صفحة التفاصيل الرئيسية
(`PROCEDURES_DATA`). قرار النطاق: التعديل يغطي فقط صفحة تفاصيل المعاملة
الرئيسية (`app/procedures/[slug]/page.tsx` → `ProcedureDetailClient.tsx`،
وهي الصفحة التي يزورها المستخدم فعلياً لكل معاملة) بدل توسيع التغيير
لأربع ملفات بيانات مختلفة بلا ضرورة — يحافظ هذا على قيد "لا تغيّر مسارات
الصفحات بلا ضرورة" ويقلّل مساحة الخطر.

**الحل**: مطابقة نصية ديناميكية عند العرض (لا بيانات مُخزَّنة مسبقاً ولا
تخمين) في `ProcedureDetailClient.tsx`: نأخذ
`proc.authority?.ministry_en`/`proc.authority?.name_en` الحقيقيين لكل
معاملة، ونتحقق إن كانت تحتوي (substring، غير حسّاس لحالة الأحرف) أي من
19 اسم جهة موجودة فعلاً في `ArchiveDoc.institution` — إن لم يوجد أي
تطابق، لا يُعرض القسم إطلاقاً (لا افتراض علاقة غير مؤكدة). عند وجود
تطابق، تُعرض حتى 5 وثائق من تلك الجهة، مع ترتيب الوثائق التي يحتوي
عنوانها كلمة دالة على نموذج/طلب فعلي (نموذج/طلب/استمارة/form/application)
أولاً — دون اختراع تصنيف غير موجود في البيانات (حقل تصنيف "نموذج رسمي"
الأصلي كان موجوداً في اسم الملف الخام لكنه أُزيل أثناء تنظيف الاسم في
مرحلة سابقة من المشروع، فلم يبقَ حقل بنيوي صريح لذلك).

**تحقّق فعلي قبل الاعتماد**: سكريبت Python مستقل (يوازي منطق TypeScript
حرفياً) طابق كل قيم `authority.ministry_en`/`name_en` الحقيقية في
`lib/procedures.ts` (134 قيمة فريدة) مقابل جهات الأرشيف الفعلية — 13
قيمة طابقت (وزارة الداخلية، العدل، المالية، العمل، الصناعة، الخارجية...)
أي أن القسم الجديد سيظهر فعلياً على معاملات حقيقية بعد النشر، وليس ميزة
صامتة لا تُفعَّل أبداً. باقي الوزارات (كالأشغال العامة والشؤون
الاجتماعية) لا تملك وثائق مطابقة في الأرشيف حالياً، فلن يظهر القسم لها —
سلوك صحيح ومتوقّع بدل عرض نتائج غير دقيقة.

تم التحقق من `npx tsc --noEmit` نظيف بعد هذا التعديل.

### تصحيح لاحق — التحقق من استفادة "اسأل دليلك" الفعلية من الأرشيف (2026-07-26)

بطلب المستخدم: "اجعل البحث عبر اسأل دليلك يبحث ايضاً في الأرشيف" — الجزء
الرابع والأخير من الطلب الرباعي. هذا تحقّق (verification) لا ميزة جديدة:
فحصت `backend/main.py` و`backend/system_prompt.txt` (عبر subagent قراءة
فقط) لأتأكد أن المحادثة الرئيسية تسترجع فعلاً محتوى الأرشيف وتستشهد به
بشكل جيد.

**اكتُشف خلل حقيقي غير متعلّق بالأرشيف تحديداً لكنه يؤثر عليه**:
`backend/system_prompt.txt` كان يحتوي على علامات تعارض Git غير محلولة
حرفياً (`<<<<<<< Updated upstream` … `=======` … `>>>>>>> Stashed
changes`) — أي أن هذا النص الحرفي (بما فيه رموز `<<<<<<<` نفسها) كان
يُرسَل كجزء من System Prompt لكل استدعاء `/chat`، `/chat/stream`،
`/chat/structured` منذ آخر عملية merge لم تُحل بشكل كامل. لم يكن هذا خللاً
في منطق الاسترجاع (retrieval) — الاسترجاع كان يعمل بشكل سليم أصلاً (لا
فلتر `domain` افتراضياً في `search_qdrant`، أي أن كل استعلام يخلط نتائج
كل النطاقات بما فيها `official_documents_archive`) — بل خلل في جودة النص
المُرسَل فعلياً للنموذج، يؤثر على كل رد بلا استثناء، لا الأرشيف فقط.

**الإصلاح**: أعدت دمج الجزءين (كانا محتوى غير متداخل: جملة الترحيب
وقواعد الأمان من جهة، وهيكل الإجابة والعطل والمراجع والطوارئ والنماذج
والقواعد الصارمة وسياق RAG من جهة أخرى) بترتيب منطقي، مع حذف أسطر علامات
التعارض فقط — لم يُحذف أي محتوى حقيقي من الجهتين. هذا تعديل نصي بحت على
ملف نصي وليس على أي API أو منطق Backend، فلا يخالف قيد "لا تغيّر Backend
APIs".

**تحسين إضافي (نصي بحت أيضاً)**: أضفت قسماً جديداً "أرشيف الوثائق الرسمية
— كيف تستخدمه في إجاباتك" يوجّه النموذج لكيفية الاستشهاد بمقاطع الأرشيف
حين تصله ضمن السياق (يتعرّف عليها من عنوان المقطع الذي يتضمّن اسم الجهة
وسنة الوثيقة، وهو موجود فعلاً في البيانات المُرسَلة له حالياً بلا أي
تعديل على `context_str()` أو `search_qdrant()`): ذكر العنوان/الجهة/السنة
حرفياً، توضيح أنها وثيقة أرشيفية حقيقية لا اجتهاد، وتوجيه المستخدم لصفحة
`/archive` للحصول على الملف الكامل (بدون اختراع رابط تحميل مباشر، لأن
النموذج لا يستلم `pdf_url` ضمن السياق حالياً — لم أُعدّل ذلك تحديداً
تفادياً للمس منطق `search_qdrant`/`context_str` في الـBackend).

**قرار نطاق متعمّد**: `search_qdrant()` يجلب فعلياً حقل `pdf_url` من
Qdrant لكن `context_str()` لا يمرّره للنموذج — تمريره كان يتيح للنموذج
اقتراح رابط تحميل مباشر حقيقي، لكن هذا يتطلب تعديل كود Python في
`backend/main.py` (لا مجرد ملف نصي)، فاخترتُ عدم لمسه حفاظاً على أضيق
تفسير لقيد "لا تغيّر Backend APIs" ولأنه لا يمكن اختباره قبل نشره فعلياً
على Render. البديل الآمن المعتمد: توجيه المستخدم لصفحة `/archive` نفسها
حيث يمكنه البحث عن الوثيقة (بالبحث الذكي أو النصي) وتحميلها مباشرة من
هناك.

**النشر**: هذا التعديل في `backend/` (لا `frontend/`)، فلا ينطبق عليه
`push.sh` — أُنشئ `push_backend_v7.sh` في جذر المشروع لنشره بشكل منفصل.

### تصحيح لاحق — إظهار مستند/نموذج مرتبط في رد "اسأل دليلك" مع رابط تحميل (2026-07-26)

بطلب المستخدم: "اذا سأل أي سؤال ولديك نموذج أو مستند مرتبط يظهر في
الإجابة ويمكن تحميله". فحص `AgentResponseRenderer.tsx` كشف أن بطاقة
"المصادر المستخدمة" (`TrustBadge`) موجودة أصلاً ومصمَّمة لعرض `src.url`
كرابط تحميل إن وجد — الفجوة لم تكن في العرض، بل في أن `AgentSource` القادم
من `/chat/stream` لا يحمل `url` أبداً (فقط title/ministry/score)، فلا
تُطابَق المصادر أبداً مع ملف حقيقي قابل للتحميل.

**الحل (frontend بالكامل، صفر تعديل Backend)**:
- `lib/archiveDocuments.ts`: إضافة `findArchiveDocByTitle(title)` —
  مطابقة تامة أولاً، ثم مطابقة جزئية (prefix) كخيار احتياطي، بلا أي تخمين.
- `app/page.tsx`: بعد اكتمال أي رد يحتوي مصادر، يُستورَد
  `lib/archiveDocuments` عبر **dynamic import** (`await import(...)`)
  لا استيراد ثابت في أعلى الملف. السبب: هذا الملف حجمه ~4MB خاماً بسبب
  حجم الأرشيف (5,234 وثيقة)، واستيراده بشكل ثابت في الصفحة الرئيسية كان
  سيضخّم حزمة الصفحة الأكثر زيارة في التطبيق فوراً — بالضبط النوع من
  الخلل الذي أُصلح سابقاً في Batch #354 ("remove 1.7MB of dead data
  imports from homepage"). الـdynamic import يجعل التحميل يحدث فقط عند
  الحاجة الفعلية (وجود مصادر لفحصها)، ويُخزَّن (cache) في المتصفح بعد أول
  استدعاء فلا يتكرر التحميل.
- `components/ChatMessage.tsx`: يمرّر `defaultExpandSources` (prop كان
  موجوداً أصلاً في `AgentResponseRenderer`/`TrustBadge` منذ مرحلة سابقة
  من المشروع لكنه لم يُستخدَم من أي مستدعٍ) — يُفتح تلقائياً فقط عند وجود
  رابط ملف حقيقي ضمن المصادر، بدل إبقاء البطاقة مطويّة دائماً.

**لماذا هذا الحل بالذات**: لم يُنشأ أي مكوّن أو زر جديد — أُعيد استخدام
آلية موجودة أصلاً (بطاقة المصادر) وأُغني بيانياتها فقط، وهو الاتجاه
الصحيح مع الطلب الثاني للمستخدم بتقليل العجقة، لا زيادتها.

---

## Batch #498 — تدقيق شامل لتقليل العجقة والأزرار في كل الصفحات (2026-07-26)

### الطلب
"بعدها حاول بإحترافية في الصفحات كلها تقليل العجقة و الازرار للحد
الادنى بإحترافية و بعملية و بذكاء حاد" — مع القيد الثابت الدائم: **لا
تحذف الوظائف**. أي تنفيذ هنا هو دمج/إزالة تكرار فعلي فقط، لا حذف قدرة
كان المستخدم يصل إليها.

### المنهجية
1. تشغيل Explore agent (نموذج opus، صلاحية قراءة فقط) على كل الصفحات
   الرئيسية (الرئيسية، المعاملات، تفصيل معاملة، الخدمات، المحترفين،
   القائمة الجانبية، BottomNav، MobileHeader، الأرشيف، ملفاتي، الأسئلة
   الشائعة، الجهات الرسمية، الإعدادات، الإدارة) بتوجيه صريح بالتشكك
   وعدم اقتراح أي حذف وظيفي.
2. أعاد التدقيق **15 نتيجة** موثّقة بملف:سطر بمستويات ثقة مختلفة.
3. **كل نتيجة تحقّقتُ منها بنفسي بقراءة الكود الفعلي قبل التنفيذ** —
   لم يُنفَّذ شيء بالاعتماد على تقرير الوكيل وحده. هذا كشف أن بعض
   النتائج المقترحة كانت في الواقع قرارات تصميم مقصودة ومُوثَّقة بتعليق
   سابق في الكود نفسه (رُفض تنفيذها):
   - صفّ "الإجراءات السريعة" بزرَّين فقط في الصفحة الرئيسية — موثَّق
     بتعليق "exactly 2 quick actions... per spec" من batch #342.
   - زر "Accessibility" في `MobileMenu.tsx` — موثَّق بتعليق يصفه
     كمدخل مباشر مقصود بدل دفنه داخل قائمة الإعدادات العامة.

### النتائج المنفَّذة (4، بثقة عالية فقط، بلا أي حذف وظيفي)

| الملف | التكرار المُزال | الوجهة/الوظيفة المتبقية |
|---|---|---|
| `app/page.tsx` | عدّاد أحرف مزدوج في صف الكتابة، أحدهما بخطأ كمون (كان يُظهر "طويل جداً" عند 600 حرف رغم أن الحد الفعلي 4000) | `ChatInputCharCounter` وحيد الآن مع `maxLength={MAX_INPUT}` الصحيح |
| `app/procedures/[slug]/ProcedureDetailClient.tsx` | زر "الرئيسية" الثانوي في صف الأزرار | نفس الوجهة (`/`) متوفرة عبر تبويب "الرئيسية" في `BottomNav` أسفل الشاشة |
| `app/services/page.tsx` | زر نصي "إغلاق" في أسفل الشيت | زر X في الرأس (مرئي دائماً) + النقر خارج الشيت + Esc |
| `components/MobileMenu.tsx` | بند "المساعدة" (يؤدي إلى `/faq`) | بند "أسئلة شائعة" الموجود في القائمة نفسه، نفس الوجهة تماماً |
| `app/professional/page.tsx` | تبويبا "الملفات" و"المسودات" (6→4 تبويبات) — كل منهما كان يعرض فقرة + زر واحد فقط | نفس الوجهتين (`/my-files`, `/drafting-studio`) متوفرتان بضغطة من "الإجراءات السريعة" في تبويب "نظرة عامة" لنفس الصفحة |

### نتائج مؤجَّلة عمداً (خطر تراجع أعلى من الفائدة في هذه الدفعة — لم تُنفَّذ، ليست منسية)
- دمج 4 مؤشرات جاهزية وثائق متداخلة في `/procedures`
  (`ReadinessChecker`, `ProcedureDocReadinessBar`,
  `ProcedureDocumentChecklist`, `ProcedureDocumentStatus`) — أعلى خطر
  تراجع، يحتاج جولة منفصلة مركّزة.
- دمج أزواج النسخ/المشاركة (`ProcedureDocumentShare` /
  `ProcedureChecklistExport`, `ProcedureCopyableSteps` /
  `ProcedureCopySummaryLine`) في `/procedures`.
- دمج أيقونتَي "بحث متقدم" و"فلاتر متقدمة" في `/procedures`.
- تقليص تبويبات `/forms` من 3 إلى 2 (ثقة التدقيق نفسه "متوسطة" فقط).
- إزالة زر "عن دليلك" من `MobileMenu` (نفس وجهة "الإعدادات") — أقل
  وضوحاً كتكرار من زر "Accessibility"، تُرك دون تغيير هذه الجولة.

### نتائج فُحصت واعتُبرت **مغلقة** (لا حاجة لعمل، ليست TODO)
- `ModeSelector` + `ChatResponseLength`: يتحكمان بأمرين مختلفين فعلياً
  (وضع الاسترجاع/الخادم مقابل تلميح طول النص من جهة العميل) — تم ضغطهما
  عمداً في batch #358 لأسباب ازدحام الموبايل، ليس تكراراً وظيفياً.
- `ChatKeyboardSendHint`: يُخفي نفسه تلقائياً على الأجهزة اللمسية عبر
  `window.matchMedia('(pointer: coarse)')` — القلق الذي رفعه التدقيق
  معالَج فعلاً في الكود.

### التحقق
`npx tsc --noEmit` — بلا أخطاء بعد كل التعديلات الخمسة.

### النشر
مضاف إلى `push.sh` تحت تعليق "Batch #498" — ينفّذه المستخدم عبر
`bash push.sh` من `frontend/`.

---

## Batch #499-501 — تدقيق وظيفي + ملخص الخدمة + عناوين ## ملوّنة (2026-07-26)

### الطلب
"تأكد من عمل كل الأزرار والميزات (الإشعارات مثلاً)، واجعل لكل سؤال عن
معاملة أو مستند ملخص AI يشرح ما هي هذه الخدمة ولماذا تُستخدم، واجعل شكل
الجواب أكثر ترتيباً وعملية."

### #499 — تدقيق وظيفي شامل
شُغِّل Explore agent (opus) بتوجيه صريح: البحث عن أي زر/ميزة تبدو تعمل
لكنها في الواقع Stub أو بيانات مزيّفة أو مفتاح localStorage مكسور. النتائج
الكاملة والمُتحقَّق منها بقراءة الكود فعلياً:

- **NotificationBell** (المثال الذي ذكره المستخدم بالاسم): فُحص بالكامل —
  حقيقي 100%. يقرأ مواعيد انتهاء الوثائق (`dalilak_doc_expiry_*`)
  والمواعيد (`dalilak_appointments`) والتذكيرات (`dalilak_reminders`) من
  localStorage فعلياً، يحسب الأيام المتبقية بتوقيت بيروت، والنقر عليه يفتح
  محادثة حقيقية بالسياق الصحيح. لا حاجة لأي تعديل.
- **`components/FeedbackWidget.tsx`** (خطأ حقيقي مؤكد): تقييم 1-5 نجوم +
  تعليق اختياري كان يُخزَّن في `localStorage['dalilak_site_feedback']` فقط
  ويعرض "🙏 شكراً على تقييمك" — لكن هذا المفتاح غير مُستخدَم في أي مكان
  آخر بالتطبيق ولا توجد نقطة Backend تقرأه؛ التقييم يُحذف عملياً على جهاز
  المستخدم نفسه دون أن يصل لأي إنسان. بالمقابل، تقييم 👍/👎 على كل رد في
  `AgentResponseRenderer.tsx` يُرسِل فعلياً لـ `/feedback` ويظهر في
  `/admin/feedback` الحقيقية. **الحل**: `handleSubmit` أصبح يرسل نفس
  البيانات إلى `/feedback` (بإسقاط تقييم النجوم على حقل `rating` بصيغة
  up/down الذي يقبله الـ Backend فعلاً، ووضع النجوم والتعليق في حقل
  `answer`) — بلا أي تعديل على شكل الـ API نفسه.
- **`app/professional/page.tsx`** (خطأ حقيقي مؤكد، أقل حدّة): 4 بطاقات
  إحصائية في أعلى "نظرة عامة" (موكّلون نشطون / ملفات مفتوحة / مسودات
  محفوظة / طلبات مراجعة) كانت جميعها القيمة الحرفية `"0"` بلا أي استعلام
  — تبدو كلوحة بيانات حيّة رغم أنها رقم مكتوب يدوياً دائماً. **الحل**:
  "ملفات مفتوحة" أصبحت تُحسَب فعلياً من `listTransactions()` (نفس نقطة
  `/transactions` التي تعرضها صفحة "ملفاتي" الحقيقية)؛ الثلاث الأخرى لا
  يوجد لها أي تخزين حقيقي في المشروع حتى الآن (لا `clients`، لا حفظ
  مسودات، لا طلبات مراجعة) فأصبحت تعرض شارة "قريباً" رمادية بصراحة، بدل
  رقم "0" مُضلِّل يوهم المستخدم بتتبّع لا يحدث فعلياً.
- بقية النقاط المفحوصة (صوت/TTS، رفع ملفات، بحث، طباعة/تصدير، مشاركة QR،
  لوحة الإدارة) تأكّد أنها حقيقية بالكامل — لا حاجة لأي إصلاح.

### #500 — ملخص "ما هي هذه الخدمة؟ ولماذا تحتاجها؟"
`backend/system_prompt.txt`: أُضيف قسم إلزامي جديد في مطلع أي رد عن
معاملة/خدمة/مستند محدد — جملتان إلى ثلاث بلغة مبسّطة بلا مصطلحات قانونية،
تشرح طبيعة المعاملة والغاية العملية منها، **قبل** أي تفصيل إجرائي (جهة/
وثائق/خطوات). هذا يلبي طلب المستخدم مباشرة: "ملخص يشرح ما هي هذه الخدمة
ولماذا تستخدم".

### #501 — تنظيم شكل الردود (اكتشاف مهم: محرك تنسيق موجود لم يكن يُستخدَم)
عند فحص `components/AgentResponseRenderer.tsx` تبيّن أن الفرونتند يملك
فعلاً محرك تنسيق متقدّم (`SECTION_MAP` + `AgentSectionCard`) يحوّل أي عنوان
Markdown حقيقي (`## عنوان`) إلى بطاقة ملوّنة بأيقونة مخصّصة (جهة رسمية 🏛️،
وثائق 📄، خطوات مرقّمة ⚙️، رسوم 💰، تنبيهات ⚠️...) — بُنِي هذا في مرحلة
سابقة من المشروع (Phase 2) لكنه **لم يُستخدَم فعلياً أبداً**، لأن قالب
"هيكل الإجابة المثالي" في `system_prompt.txt` كان يستخدم نصاً عريضاً
`**عنوان**` لا عناوين `##` — والمُحلّل (`parseSections`) لا يكتشف الأقسام
إلا من عناوين `#`/`##`/`###` حرفية. نتيجة ذلك: كل رد كان يُعرض كنص متدفّق
واحد بمحاذاة عريضة متفرقة، لا بطاقات منظّمة، رغم أن الأداة اللازمة لذلك
جاهزة منذ البداية.

**الحل**: `system_prompt.txt` — القالب بالكامل حُوِّل من `**عنوان**` إلى
`## عنوان` حقيقي لكل قسم (الجهة المختصة، الوثائق المطلوبة، الخطوات
المطلوبة، الرسوم، مدة الإجراء، تنبيه العطل، النموذج المتوفر، تنبيهات
مهمة، الخدمات المرتبطة)، مع تعليمة صريحة بحذف أي قسم لا ينطبق بدل كتابة
"لا يوجد" (لتبقى الإجابة عملية لا حشوة). `AgentResponseRenderer.tsx`:
أُضيف مفتاحان جديدان إلى `SECTION_MAP` ('ما هي هذه الخدمة' و'الخدمات
المرتبطة') لتغطية القسمين الجديدين بنفس أسلوب الأيقونات الملوّنة. لا حاجة
لأي منطق عرض جديد — فقط ربط القالب النصي بالمحرك الموجود أصلاً.

### التحقق
`npx tsc --noEmit` — بلا أخطاء بعد كل التعديلات (FeedbackWidget،
professional/page.tsx، AgentResponseRenderer.tsx). `system_prompt.txt`
تعديل نصي بحت، لا كود يُترجَم.

### النشر
- Frontend: مضاف إلى `push.sh` تحت تعليق "Batch #499-501".
- Backend: `push_backend_v8.sh` (سكريبت جديد بجذر المشروع، منفصل عن
  `frontend/push.sh` لأن `system_prompt.txt` في `backend/`).

---

## Batch #503 — بنك الخدمات الحقيقي (368) أصبح قابلاً للتصفّح ضمن /procedures (2026-07-26)

### الطلب
المستخدم أرسل لقطة شاشة من `/procedures` على البيئة الحية تُظهر شريط
إحصائيات "+24 جهة مختصة / 368 خدمة متاحة / 98 إجراء موثّق"، مع قائمة
بحث أسفله نصّها الفعلي "ابحث في 98 إجراء..." — أي أن الـ 368 الموعودة في
الرأس غير موجودة في القائمة نفسها. طلب: "قم بالتدقيق و إضافة كل الاجراءات
الموثقة المتاحة".

### التدقيق (بقراءة الكود الفعلي، لا بالتخمين)
- `98` = `PROCEDURES_DATA` النشطة (27، `lib/procedures.ts`) +
  `ENRICHED_PROCEDURES` (71، `lib/enrichedProcedures.ts`) — إجراءات كاملة
  بخطوات مرقّمة حقيقية مصدرها ملفات PDF فعلية من dawlati.gov.lb.
- `368` = `ALL_SERVICES` (`lib/allServices.ts`) — **بيانات حقيقية موثّقة
  بالكامل**: كل خدمة فيها فعلاً وصف + رسوم + مدة إنجاز + مستندات مطلوبة +
  هاتف + موقع + ساعات عمل + نماذج + ملاحظات مهمة، لا نصوص تجريبية ولا
  حقول فارغة مُخترَعة. لكنها كانت **مُستخدَمة فقط لحساب رقم الإحصائية**
  ("368 خدمة متاحة") ولحساب فئات صفحة `/services` المنفصلة — لم تكن
  تظهر إطلاقاً ضمن قائمة البحث/الفلترة القابلة للتصفّح في `/procedures`
  نفسها، التي كانت تعرض الـ 98 فقط. الرابط الوحيد لها كان بطاقة صغيرة في
  أسفل الصفحة تُحوِّل المستخدم لصفحة `/services` مختلفة تماماً.
- لا يوجد أي حقل "verified"/"موثّق" في أي من `TxItem` (2,484 صفاً في
  `allTransactions.ts`، بلا خطوات/رسوم — لا يصلح كمصدر إجراءات موثّقة
  بالتحويل المباشر) أو `ServiceItem` أو `EnrichedProcedure` — 98 هو عدد
  ثابت لما كُتِب يدوياً بالتفصيل الكامل، لا نتيجة فلتر جودة.
- **الخلاصة**: لا حاجة لاختراع أي محتوى جديد لتوسيع "98" — الحل هو عرض
  بيانات حقيقية موجودة أصلاً (368 خدمة) في المكان الذي وعد به رأس الصفحة
  نفسه، بدل تركها محصورة في صفحة أخرى.

### الحل (frontend بالكامل، صفر بيانات جديدة أو مُخترَعة)
`app/procedures/page.tsx`:
- `filteredServices` (`useMemo`) جديد يُطبّق نفس منطق البحث + فلتر
  الوزارة (بمطابقة نصية لاسم الوزارة الحقيقي `authority_ar` مقابل تسمية
  رقاقة الفلتر) + الفلاتر المتقدمة (نموذج/رسوم/سرعة) على `ALL_SERVICES` —
  تماماً بنفس النمط المستخدم أصلاً لـ `filteredEnriched`.
- قسم ثالث "خدمات إضافية" يُعرَض ضمن نفس قائمة النتائج (بعد الإجراءات
  المرشَدة والموثّقة، وليس في صفحة منفصلة) — بطاقة قابلة للتوسيع لكل خدمة
  تعرض: الوصف، الرسوم، مدة الإنجاز، الوثائق المطلوبة، الهاتف والموقع
  وساعات العمل، النماذج المطلوبة إن وُجدت، الملاحظات المهمة إن وُجدت،
  وزر "اسأل دليلك عن هذه الخدمة" (يستخدم `chatPrompt_ar` الحقيقي المُعَدّ
  أصلاً لكل خدمة في `allServices.ts`). كل حقل معروض هو حقل حقيقي موجود
  أصلاً في البيانات — لم يُضَف أي نص أو حقل جديد.
- `PROCEDURES_TOTAL` أصبح `98 + 368 = 466` بدل `98` فقط — فرأس الصفحة
  الآن يَعِد المستخدم بعدد يطابق فعلاً ما هو قابل للتصفّح والبحث في نفس
  الشاشة، ونص "ابحث في X إجراء" أصبح يعكس الرقم الصحيح.
- فلتر "بدأت هذه المعاملة بالفعل" (من الفلاتر المتقدمة) يُصرَّح بصراحة
  بإرجاع صفر نتائج لهذه الخدمات، لأنه لا توجد آلية تتبّع "بدء" حقيقية
  لها (بخلاف الإجراءات المرشَدة) — بدل اختراع حالة غير موجودة فعلاً.

### لماذا هذا الحل بالذات
لم تُنشأ صفحة جديدة ولم تُكرَّر بيانات `/services` — أُعيد استخدام مصدر
البيانات الحقيقي الموجود أصلاً (`ALL_SERVICES`) وعُرِض في المكان الذي كان
شريط الإحصائيات يَعِد به المستخدم من البداية. هذا يلتزم حرفياً بالقيد
الدائم "لا تستخدم Mock Data في النسخة النهائية" ولا يضيف أي زر أو عنصر
تحكم غير ضروري (نفس بنية البطاقة القابلة للتوسيع المستخدمة أصلاً في هذه
الصفحة، لا تصميم جديد).

### التحقق
`npx tsc --noEmit` — بلا أخطاء بعد التعديل.

### النشر
مضاف إلى `push.sh` تحت تعليق "Batch #503".

---

## Batch #504 — إدراج المعاملات الناقصة من بنك الأرشيف الخام (2,484) (2026-07-26)

### الطلب
متابعة مباشرة لـ batch #503: "دقق اقصد و ادرج المعاملات الناقصة و
الموجودة في الارشيف" — المستخدم يريد نفس المعالجة (عرض حقيقي بدل عدّاد
إحصائي فقط) للمصدر الثالث المتبقي: بنك المعاملات الخام.

### التدقيق
`lib/allTransactions.ts` يُصدِّر `TX_ALL` — 2,484 معاملة حقيقية
("AUTO-GENERATED from transactions_master.json ... dawlati.gov.lb
archive") بحقول: عنوان عربي/إنجليزي، وزارة، رابط PDF رسمي (عند وجوده)،
وعلم "له نموذج". هذا المصدر **كان مقروءاً فعلياً** — لكن فقط داخل تبويب
"معاملات (2,484)" في صفحة `/forms` المنفصلة (`app/forms/page.tsx:85`) —
ولا يظهر إطلاقاً في `/procedures`، تماماً كما كانت حال `ALL_SERVICES`
قبل batch #503.

الفرق الجوهري هذه المرة: هذا المصدر **يتقاطع جزئياً** مع الـ 466 معاملة
الموثّقة أصلاً (98+368) — لأن `enrichedProcedures.ts` و`allServices.ts`
أُنشِئا يدوياً من مصادر متعددة (بعضها نفس بوابة dawlati.gov.lb) بعناوين
مطابقة أو قريبة جداً من عناوين هذا الأرشيف الخام. عرض الـ 2,484 كاملة بلا
تصفية كان سيُكرِّر معظم الـ 98 الموثّقة بخطوات كاملة تحت عنوان مختصر
بلا تفاصيل — إرباك لا إضافة قيمة.

### الحل
`app/procedures/page.tsx`:
- **فهرس مطابقة العناوين** (`COVERED_TITLES` + `isTxAlreadyDocumented`):
  يُبنى مرة واحدة من عناوين `PROCEDURES_DATA` + `ENRICHED_PROCEDURES` +
  `ALL_SERVICES` (عربي وإنجليزي)، بحد أدنى 6 أحرف لتجنّب تطابقات عشوائية
  قصيرة. كل معاملة خام تُطابَق (احتواءً جزئياً، بالطريقة نفسها المستخدمة
  في `findArchiveDocByTitle` من batch #497) تُستبعَد — فلا تكرار.
- **`TX_ALL` يُحمَّل بـ dynamic import** بعد التركيب (لا استيراد ثابت في
  الأعلى) — حجمه الخام ~400KB، ولا يجوز تحميله ضمن حزمة الصفحة الأولى
  لكل زائر (نفس نمط batch #354/#497).
- **الأداء**: المطابقة الثقيلة (2,484 × ~460 مقارنة نصية) تُحسَب في
  `useMemo` يعتمد فقط على تحميل البيانات (`[txAll]`) — لا يُعاد حسابها مع
  كل ضغطة مفتاح في البحث؛ تصفية البحث/الوزارة بعدها منفصلة وخفيفة.
- **قسم رابع جديد** "من أرشيف المعاملات الرسمي (غير مُصنَّفة بعد)" —
  عنوانه الصريح يوضّح أن هذا مستوى توثيق أخفّ (عنوان + وزارة + رابط
  الوثيقة الرسمية فقط، بلا وصف/رسوم/خطوات)، بصفوف مبسّطة بدل بطاقات
  معقّدة، مع "عرض المزيد" (20 في كل مرة) بدل رسم آلاف الصفوف دفعة واحدة.
  كل صف: أيقونة الوزارة، العنوان، رابط فتح الوثيقة الرسمية الحقيقية (إن
  وُجد)، وزر "اسأل دليلك".
- فلتر "بدأتها بالفعل" يُخفي هذا القسم بالكامل (نفس مبدأ batch #503 —
  لا آلية تتبّع بدء حقيقية لهذا المصدر، فالتصريح الصريح بغياب النتيجة
  أفضل من نتيجة مضلِّلة).

### لماذا هذا الحل بالذات
لا تكرار مع المحتوى الموثّق أصلاً (فهرس المطابقة يضمن ذلك)، ولا وهم بأن
هذه الصفوف المبسّطة "موثّقة" بنفس مستوى الـ 466 (العنوان الصريح للقسم
ينفي ذلك بوضوح)، ولا إبطاء لتحميل الصفحة (dynamic import + حساب مطابقة
مرة واحدة فقط). كل بيانات معروضة هي بيانات حقيقية من الأرشيف — لا حرف
مُخترَع.

### التحقق
`npx tsc --noEmit` — بلا أخطاء.

### النشر
مضاف إلى `push.sh` تحت تعليق "Batch #504".

## Batch #505 — تصنيف وتحسين عناوين أرشيف الوثائق (5,234) عبر AI (2026-07-26)

### طلب المستخدم
"قم بتصنيفها و تحسينها و تاكد من تحسين الكتابة في عناوين الارشيف كلها كونه
يوجد العديد من الملفات غير منظمة و غير مقروؤة و اجعل الذكاء الاصطناعي اذا
اضطر الامر يعيد تسميتها و تصنيفها" — متابعة مباشرة لتوسيع أرشيف الوثائق
(`lib/archiveDocuments.ts`، 5,234 وثيقة حقيقية من dawlati.gov.lb/PortableSSD،
انظر batch سابق "مشروع أرشيف النماذج القانونية").

### التدقيق
استخراج JSON الكامل (5,234 سجلاً — الرقم الفعلي، لا 4,378 كما كان مكتوباً
خطأً في تعليق قديم بالملف) وتحليل جودة حقل `title`:
- **~2,342 وثيقة (45%)** عنوانها بلا معنى: إما نمط `"Institution (year)"`
  فارغ من أي وصف، أو نص لاتيني مُشوَّه ناتج عن فشل OCR على مسح قديم رديء
  (مثال: `"ged shall Sl ese Eh a Ahead ate"`).
- **~234 وثيقة** عنوانها اسم ملف خام غير مُترجَم لم يُعالَج قط (مثال:
  `"Ministry of Economy and Trade — metrology-1952 (1952)"`).
- حتى الغالبية "الجيدة" ظاهرياً (~2,657) كانت فعلياً مجرّد أول سطر مُستخرَج
  آلياً من نص OCR وقت البناء — غالباً جملة مقتطعة أو اسم قسم لا عنوان
  حقيقي (مثال: `"جانب وزارة الزراعة"`)، وليست عناوين حقيقية اختارها أحد.
- لا يوجد أي حقل تصنيف (نوع الوثيقة) في البنية القديمة إطلاقاً.

### الحل
مرّت الوثائق الـ5,234 كلها عبر نموذج AI حقيقي (`gpt-4o-mini` عبر مفتاح
OpenAI الموجود أصلاً في `backend/.env` — لا مفتاح جديد، ولا تعديل باك-إند)
على دفعات (15 وثيقة/طلب، تزامن 12 طلباً)، بتعليمات صارمة: منع اختراع أي
تفصيل غير مذكور فعلياً في المقتطف (`snippet`)/الجهة/السنة؛ تحسين الصياغة
فقط إن كان النص المصدر مقروءاً؛ وإن كان تشويش OCR غير مفهوم كلياً، عنوان
صادق عام "وثيقة رسمية من {الجهة} ({السنة})" بدل الاختراع. مُرِّرت 85 وثيقة
متبقّية غير مُرضية (غالباً بيانات جمركية برمز تصنيف أو تقاويم سندات خزينة
بالإنكليزية) بمرحلة تنقيح ثانية لترجمة/تفسير الرمز ضمن جملة عربية كاملة
(مثال: `"class0476"` ضمن أرشيف جمركي → `"بيان/تعريفة جمركية - تصنيف 0476"`).
النتيجة النهائية: **0 عنوان غير مقروء** من أصل 5,234 (تحقّق آلي عبر فحص
وجود نص عربي حقيقي وغياب أي بقايا slug/كود إنكليزي غير مُفسَّر).

كل وثيقة صُنِّفت أيضاً ضمن 10 أنواع ثابتة: قانون (74)، مرسوم (33)، قرار
(65)، تعميم (431)، نموذج/استمارة (262)، تقرير/محضر (1,763)، بيان/إعلان
(365)، دراسة (256)، اتفاقية (9)، أخرى (1,976 — غالباً وثائق OCR فاشل
حصلت على العنوان الصادق العام فقط، مصنَّفة "أخرى" بصدق بدل تصنيف مُخمَّن).

### نقطة أمان مهمة: حقل `title` الأصلي لم يُمَس
`s.title` العائد من الباك-إند/RAG كمصدر استشهاد (نفس `title` الذي بُني منه
Qdrant الأصلي) يبقى يطابق `ArchiveDoc.title` حرفياً بلا أي تغيير — لو
استُبدل لكسِرت ميزة "رابط تحميل حقيقي للمستند" في ردود اسأل دليلك (batch
#495-497) بلا أي طريقة لإصلاحها بدون إعادة بناء Qdrant بالكامل. لذا أُضيف
حقل **جديد** `displayTitle` (العنوان المُحسَّن) بدل استبدال `title`، و`category`
(نوع الوثيقة). `findArchiveDocByTitle` (المطابقة الوحيدة المُستخدَمة فعلياً
من app/page.tsx وArchiveAISearch.tsx) يستمر بمطابقة `title` الأصلي حصراً —
لا كسر لأي وظيفة موجودة. العرض المرئي فقط تحوّل إلى `displayTitle` في:
`app/archive/page.tsx` (بطاقات القائمة)، `ProcedureDetailClient.tsx` (قسم
"نماذج ووثائق رسمية ذات صلة")، و`ArchiveAISearch.tsx` (نتائج "من الأرشيف").

### تحديثات الواجهة
- `lib/archiveDocuments.ts`: `ArchiveDoc` احتوى الآن `displayTitle: string`
  و`category: string`؛ أُضيف `ARCHIVE_CATEGORIES` و`ARCHIVE_CATEGORY_LABELS`
  (عربي/إنكليزي لكل نوع).
- `/archive`: فلتر ثانوي جديد بشرائح التصنيف (تحت فلتر الجهة الموجود أصلاً،
  بحجم أصغر تعمّداً لتجنّب عجقة شريطين متساويين) + شارة تصنيف على كل بطاقة
  + البحث النصي يشمل الآن `displayTitle` أيضاً.
- كل مكان كان يعرض `d.title` مباشرة للمستخدم (بطاقات /archive، القسم
  المرتبط في صفحة المعاملة، نتائج البحث بالذكاء الاصطناعي) تحوّل لعرض
  `d.displayTitle`.

### التحقق
- سكريبت Python تحقّق آلياً: 0/5,234 عنوان متبقٍّ بلا نص عربي حقيقي أو
  فيه بقايا رمز إنكليزي غير مُفسَّر.
- `npx tsc --noEmit` بعد إعادة توليد الملف (7.9MB) وبعد تعديلات الواجهة —
  بلا أخطاء (تحقُّق منفصل مرتين).
- تأكيد يدوي أن `findArchiveDocByTitle` ما زالت تطابق حصراً `title` الأصلي
  غير المُعدَّل (قراءة الكود مباشرة، لا افتراض).

### النشر
مضاف إلى `push.sh` تحت تعليق "Batch #505".
